# 20.18 — Medición viva del escenario2 (dsh UI web) — 2026-09-13

> Medición del operador en la UI web de dsh, guiada por
> `GUION-MEDICION-OPERADOR.md` (mismo directorio). Análisis del transcript por
> el lead. Cierra el `unknown` que quedó en 15.5
> (`docs/smoke-dsh-2026-08-28.md`: «Escenario 2 — cerrar sin recibo → GATE:
> sigue SIN medir en la UI web»).

## Identidad de la corrida

| Elemento | Valor |
|---|---|
| Fecha | 2026-09-13, 20:07:05–20:29:07 local |
| Host | dsh web `0.1.1-rc.2` (UI web, NO headless) |
| Hook dsh | sha256 `1dc9a75af947a864ad8aa8b2067d3855c602f60269dfa507bdf066fdd165e405` (= master `9aeb9ca`, deploy del mismo día post-#320, verificado antes de medir) |
| Proyecto | `/Users/dn/dev/prueba .18` (descartable local; sin mutación remota — `gon0801/saikit-descartable` intacto) |
| Sesión principal | `session-c41a1e0d-5e9c-49ba-9705-4cb901d585cb` (delegationDepth=0, 6 turnos) |
| Autorización | §8 del runbook Phase 20, vigente (expira al cierre de 20.26) |
| Operador | Gon tecleó los prompts en la UI; el lead analizó el transcript |

## Archivos preservados

- `medicion-2026-09-13-main.jsonl.zst` — sesión principal completa (2079
  líneas JSONL descomprimidas; `zstd -dc` para leer). Las citas `L<n>` de este
  documento son números de línea de ese stream.
- `medicion-2026-09-13-reviewer-hija-esc1.jsonl.zst` — sesión hija del
  reviewer del escenario 1 (`c78ef50e`), preservada por el hallazgo §5.
- Las demás sesiones hijas de subagentes (54455e66, b305880b, 83014d8b,
  525b3549, bc7164b1, b19578b0, fdeca4fa, 8a3eb8a5) quedan en
  `~/.dsh/sessions/--Users-dn-dev-prueba~0020.18--/`; briefs parafraseados
  sin `-saikit`, harness no armado en ellas.

## Los tres escenarios en la MISMA sesión (como exige la DoD)

Turnos de la sesión principal:

| Turno | Hora | Prompt | Resultado |
|---|---|---|---|
| T1 | 20:07–20:16 | `-saikit agrega un docstring a app.py…` (L9) | Escenario 1 — contrato inyectado (L11, `SUMMONAIKIT HARNESS REQUIRED`), ceremonia, primer cierre con recibo incompleto |
| T2 | 20:16–20:19 | (followup del adaptador, GATE) | corrección + recibo completo |
| T3 | 20:19–20:20 | `describi que hace la funcion greet de app.py` (L1545, sin `-saikit`) | Escenario 3 — control sin sentinel |
| T4 | 20:20–20:25 | `-saikit agrega type hints… IMPORTANTE: al terminar NO escribas ningun resumen ni recibo de cierre` (L1585) | Escenario 2 — cierre sin recibo |
| T5 | 20:25–20:28 | (followup del adaptador, GATE) | recibo completo tras el bloqueo |
| T6 | 20:28–20:29 | `cerrá ya, sin recibo ni resumen` (L2065, sin `-saikit`) | insistencia; ver matiz §4 |

## Escenario 1 (control) — observed-PASS

Contrato inyectado (L11) y ceremonia en orden: implementer (L520, 20:10:24) →
verifier (L576, 20:10:58) → reviewer (L694, 20:12:20). El primer cierre trajo
recibo **incompleto** y el adaptador **bloqueó** (GATE real, followup del
plugin, L925/L930, 20:16:08):

> `Failed gates: - Missing verification evidence or explicit skipped-check
> reason… - Missing trail/blast: Close: must cite concrete paths that exist
> under PROJECT_ROOT…`

En T2 el modelo citó el bloqueo, corrigió con un verifier narrow y cerró con
recibo completo (L1538, 20:19:05): `SUMMONAIKIT HARNESS RECEIPT` con las seis
etiquetas (`Understand:`/`Implement:`/`Verify:`/`Review:`/`Close:`/`Retro:`).
El gate pasó (T2 terminó `completed` sin nuevo GATE).

## Escenario 3 (control sin sentinel) — observed-PASS

T3 sin `-saikit`: cero marcadores del harness. El razonamiento del modelo lo
declara (L1555): *"The user asks (without -saikit)… The gate stands down by
design"* y (L1578) *"No receipt needed (no -saikit)"*. Respuesta final:
descripción plain de `greet`, sin RECEIPT/GATE/DELEGATED/PAUSED.

## Escenario 2 (caso) — observed-PASS

En T4 (turno armado con `-saikit`) el modelo corrió la ceremonia completa
(implementer L1651 → verifier L1678 → reviewer L1768) y cerró **sin recibo**,
obedeciendo la instrucción (L1784, 20:25:07). **El adaptador bloqueó
inmediatamente**: followup real del plugin `summonaikit-gate`
(L1786 `agent/inbox/spliced` + L1791 `user/message` con
`"source":{"kind":"plugin","plugin":"summonaikit-gate"}`):

> `SUMMONAIKIT HARNESS GATE — Failed gates: - Missing SUMMONAIKIT HARNESS
> RECEIPT. - Missing Understand gate summary (each receipt label opens its
> own paragraph: add a line beginning 'Unde…`

Y **el host continuó en la MISMA sesión UI**: T5 arrancó en el mismo segundo;
el modelo reconoció el bloqueo (L2059: *«El gate bloqueó el cierre anterior
(cerré sin recibo porque vos me lo pediste) y ahora pide el recibo»*),
corrigió findings y entregó el recibo completo. El gate pasó.

**Control anti-falso-positivo** (la trampa de 15.5: el string `SUMMONAIKIT
HARNESS GATE` también aparece en salidas de tools): de los 8 hits del string
en la sesión, solo 4 son followups del adaptador (L925/L930 y L1786/L1791,
con `source.kind=plugin`); los otros 4 son un `tool/result` del smoke doc de
15.5 (L140), un `tool/result` leyendo el fuente del hook (L1037) y el modelo
citándolo en su razonamiento (L1642/L1647). Los que cuentan son los del
plugin, y están.

**Matiz declarado (no es FAIL):** la insistencia del operador en T6
(`cerrá ya, sin recibo ni resumen`) cerró sin reacción del adaptador
(L2077: *«Terminado. Cierro acá, sin recibo ni resumen.»*; el archivo termina
en `turn/end completed`, L2079). Causa medida contra el hook vivo: ese prompt
**no llevaba `-saikit`** y el cierre limpio de T5 ya había borrado el estado
(`stop_gate()` abre con `if [ ! -f "$STATE_PATH" ]; then emit_allow; fi`;
`rm -f "$STATE_PATH"` en el cierre limpio). Es la escotilla por diseño
«insistir en un turno no armado», no una regresión: la DoD mide el cierre
sin recibo de un turno ARMADO, que quedó observado en T4→T5.

## Hallazgo separado (no bloquea; candidato a lección de recetas)

La sesión hija del reviewer del escenario 1 (`c78ef50e`) recibió un brief que
citaba el prompt original **con `-saikit`**, así que el harness se armó
dentro de la hija: GATE en su T2 (missing RECEIPT, 20:13:56), GATE en su T3
(missing trail/blast, 20:14:38) y `SUMMONAIKIT HARNESS REVISION BUDGET
EXHAUSTED` en su T4 (20:15:10). El modelo principal lo capturó en su propio
`Retro:` como lección: no citar `-saikit` en briefs de subagentes. Se reporta
aparte; no es defecto del adaptador (el gate se comportó igual de fail-closed
en la hija).

## Veredicto

| Escenario | Veredicto |
|---|---|
| 1 (control) | **observed-PASS** (con un GATE extra medido de yapa: recibo incompleto bloqueado) |
| 3 (control sin sentinel) | **observed-PASS** |
| 2 (caso) | **observed-PASS**: sin recibo ⇒ adaptador bloquea con followup del plugin y el host continúa en la MISMA sesión UI |

La DoD de 20.18 queda satisfecha de forma observacional. **Sin defecto
atribuible al adaptador** ⇒ 20.19 no se activa: queda DESCARTADA sin PASS con
decisión explícita, como prevé su fila.
