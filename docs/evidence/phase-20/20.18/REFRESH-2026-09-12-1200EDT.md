# 20.18 / 20.19 — Refresh 2026-09-12 12:00 EDT (post-encuentro de ttys001)

> Anexo sobre `PREFLIGHT-2026-09-12.md` (commit `d5fd7f1`).
>
> NO reemplaza al commit `d5fd7f1` — lo complementa. El preflight original
> queda archivado por trazabilidad. Este refresh registra los hechos
> verificados durante el repreflight (12:00 EDT) y corrige **sólo** la
> sección §4/§6 del preflight. Las gates §§1-3, §5, §7-8 NO se vuelven a
> validar (ya pasaron en `d5fd7f1`) — summa-gate rule 1.

## R1. Mapa win → tab → tty → title → cwd (verificado por `lsof -a -p $pid -d cwd -Fn`)

Comando (recortado por brevedad; ver log completo en
`docs/evidence/phase-20/20.18/REFRESH-2026-09-12-1200EDT.log`):

```
$ /usr/bin/osascript -e 'tell application "Terminal" to repeat with w in windows …' \
    para enumerar WINDOW id / tabs / tty / title
$ ps -t <tty> -o pid,ppid,user,comm         # foreground PID por tty
$ /usr/sbin/lsof -a -p <pid> -d cwd -Fn     # cwd verificable, no asumir por título
```

Resultado (12:00 EDT, 2026-09-12, Mac `Davids-MacBook-Pro`, usuario `dn`):

| tty        | título                              | cwd verificable               | proc         | PID    | nota               |
|------------|-------------------------------------|-------------------------------|--------------|--------|--------------------|
| ttys000    | goncloud-Orbit                      | /Users/dn/dev/goncloud-Orbit  | muse         | 74009  | Orbit sesión       |
| **ttys001**| **✳ Revisar pendientes**            | **/Users/dn/dev/summonaikit-claude** | **claude**   | **95447** | **sesión habitual David** |
| ttys003    | ✳ A.5 implementation review         | /Users/dn/dev/goncloud-Orbit  | claude       | 66171  | corrida A.5 Orbit  |
| ttys005    | revisa a.2b y a.3                   | /Users/dn/dev/goncloud-Orbit  | kimi-code    | 27311  | Orbit kimi         |
| ttys006    | [OpenClaw-director] Cross-review    | /Users/dn/dev/summonaikit-claude | kimi-code  | 28907  | director (kimi)    |
| ttys007    | OpenCode                            | /Users/dn/dev/goncloud-Orbit  | opencode     | 28248  | Orbit opencode     |
| ttys008    | summonaikit-claude                  | /Users/dn/dev/summonaikit-claude | muse       | 71993  | título dice summo  |
| ttys010    | goncloud-openclaw                   | /Users/dn/dev/goncloud-openclaw | muse       | 7261   | openclaw sesión    |
| ttys012    | ✳ Claw acceso navegador clave       | /Users/dn/dev/goncloud-openclaw | claude     | 94329  | openclaw claude    |
| ttys013    | revisa si se puede agregar el mo…    | /Users/dn/dev/goncloud-openclaw | kimi-code  | 13757  | openclaw kimi      |
| ttys014    | ✳ Orden 2000018393906916 no reg…   | /Users/dn/dev/goncloud-bridge-in-out | claude | 34598  | bridge-in-out      |

> **Conclusión R1**: la sesión que David tenía abierta y que el preflight
> previo marcó como `host impediment` **sí existe** y es ttys001
> (`✳ Revisar pendientes`, claude PID 95447, cwd real
> `/Users/dn/dev/summonaikit-claude`). El preflight previo (commit
> `d5fd7f1`, sección §4) reportó "ningún `claude` con cwd =
> `/Users/dn/dev/summonaikit-claude`" — eso fue un muestreo incompleto
> (`pgrep -f claude | xargs -I{} lsof` no incluía `claude-mem MCP` y/o
> ttys001 no llegó al filtro). El listado correcto, etapa por etapa
> (osascript → ps -t → lsof -a -p -d cwd), sí la incluye.

## R2. Por qué la medición de 20.18 sigue **bloqueada** a pesar de ttys001 encontrado

Hechos (no atribuibles a implementación, sí a la DoD de 20.18):

1. **DoD explícita de 20.18** (`docs/plans-archivo.md:166`,
   `docs/phase20-mediciones-runbook.md §2 concepto "Escape de escenario
   2"`):
   > *Sin recibo => adaptador bloquea/encola y host continúa en la MISMA
   > sesión UI; no headless sustituto; captura literal y control sin
   > sentinel; falta de observación sigue abierta.*

   La sustitución headless (vía `claude -p`) está **explícitamente
   excluida** desde el cierre de 15.5: el ensayo headless de 15.5 obtuvo
   `DELEGATED`, no el `GATE` exacto que requiere escenario 2. Repetir
   headless = false positivo por construcción.

2. **El agente `ingenieria`** (este agente, OpenClaw) opera desde el
   gateway Windows, sin tooling nativo para tipear dentro de la sesión
   interactiva `ttys001`. La pilots dado por el brief —
   `mac-terminal-control` + `mac-agent-transcript` — **no existen** en:

   - `~/.claude/skills/` (catálogo de skills del cliente Mac: 31
     instaladas, ninguna con esos nombres — verificado con
     `ls -la ~/.claude/skills/`)
   - `~/.claude/plugins/marketplaces/*` (5 marketplaces registradas:
     claude-code-harness, claude-plugins-official, openai-codex,
     superpowers-marketplace, thedotmack — `grep -rL mac-terminal` 0 hits)
   - `~/.claude/plugins/cache/claude-code-harness-marketplace/
     claude-code-harness/5.15.0/{bin,scripts}` (sin scripts con esos
     nombres — verificado)
   - `~/{,.local}/{bin,sbin}` y `/opt/homebrew/bin` (PATH del servicio
     del nodo y PATH del operador: sin matches — verificado con
     `find /Users -maxdepth 5 -name "mac-terminal*"`)

   El único skill workspace-side para este agente es
   `mac-node-ops` (`~/.openclaw/agents/ingenieria/agent/
   workshop-skills/mac-node-ops/SKILL.md`), que cubre **exec, file
   transfer, screenshots, UI verification** del nodo Mac — pero NO typing
   en sesiones de terminal ajenas, ni captura continua de transcripts.

3. **Por tanto, el techo de observabilidad del agente está por debajo
   de la DoD de 20.18**. El agente sí puede:
   - Listar ttys y cwds (R1) ✅
   - Leer hooks / cordis.patch.yml on-disk (verificación de
     `~/.claude/settings.json` y de la envoltura `/opt/homebrew/bin/bash`
     que invoca el hook) ✅
   - Leer artefactos post-facto (transcripts JSONL / screenshots) que
     David capture en ttys001, por `view_image` + `read` vía transfer
     Mac→gateway (skill mac-node-ops §3) — **post-medición**.

   El agente NO puede:
   - Tipear `-saikit` prompts dentro de la sesión interactiva de Claude.
   - Capturar literales frame-a-frame del TUI de Claude Code.
   - Reaccionar a eventos del adaptador sin esa captura intermedia.

## R3. Estado propuesto (post refresh)

| Fila    | Estado actual        | Motivo                                                          |
|---------|----------------------|-----------------------------------------------------------------|
| 20.18   | `cc:TODO`            | host impediment, ahora con motivo "agent observability ceiling" |
| 20.19   | `cc:TODO`            | gated por 20.18 (no hay repro FAIL atribuible al adaptador)     |

`work/20.18-20.19-preflight` queda con este refresh para que el próximo
`harness-work --parallel 1` arranque aquí sin volver a validar gates.

## R4. Bloqueos que el operador (David) puede destrabar

1. **Habilitar mediciones desde la sesión ttys001**:
   - El adaptador / hook / cordis.patch.yml YA están en disco
     (verificado este refresh). Sólo falta que David interactúe.
2. **Decidir sobre tooling faltante** (operador / aprobación):
   - Si la fase "revisión Claude hasta aprobar" del row 20.19 quiere
     automatizarse con `mac-terminal-control` + `mac-agent-transcript`,
     esos skills aún no están en el catálogo. Posibilidades:
     - Instalar skills (`/plugin install …` desde marketplace).
     - Definirlos como comandos del plugin harness en un PR upstream.
     - Mantener la fase de revisión manual: el agente asiste, David
       aprueba dentro de ttys001.
3. **Si David quiere que el agente escriba los seeds del escenario 2**:
   - Abrir la sesión claude en ttys001 y tipear
     `-saikit 20.18-escenario2 re-medición` (sentinel literalmente en
     frontera). El adaptador dsh ramificará el contrato y bloqueará sin
     recibo (escenario 2). El transcript queda en
     `~/.claude/transcripts/<sessionId>.jsonl`.
4. **Captura de evidencia**:
   - El agente toma el transcript literal post-medición y arma
     `docs/evidence/phase-20/20.18/medicion-<ts>.jsonl` con observación
     literal + control 1/3 + escenario 2 + breadcrumb verbatim. La fila
     cierra `cc:完了` sólo si el cierre es observacional.

## R5. Coste / guardia

- 0 commits de código (no hay fix que aplicar — sin repro)
- 0 push (PR queda en `work/20.18-20.19-preflight` para reuso del
  operador)
- 0 mutación remota (descartable intacto: el preflight d5fd7f1 ya
  verificó topic, marcador, §8, PR count 11/15 — summa-gate rule 1)
- 1 commit local de documentación de refresh (este archivo +
  accompanying log)
