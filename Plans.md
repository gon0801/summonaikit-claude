# summonaikit-claude — Plans.md

Creado: 2026-08-08
Alcance: hacerse cargo del hook que gatea cada turno y llevar la misma fuente a
los hosts aprobados — Claude Code, zcode, Codex y Grok Build— con pruebas
propias, instalación controlada y cierre de los agujeros que hoy lo vuelven un
control decorativo. Contrato de producto y premisas medidas:
`docs/spec/00-project-spec.md`.

**Contrato de datos:** `not_observed != absent`. Lo que no se pudo observar se
marca `unknown`, nunca se afirma como ausente.

---

Fases 0-14 cerradas: ver docs/plans-archivo.md — movidas 2026-08-25 (0-12) y 2026-08-28 (13-14), higiene del tope de 200 lineas. La Clasificacion del alcance y la Validacion del plan original tambien viven alla.

## Phase 15 — summonaikit en dsh (2026-08-27) — CERRADA, archivada

Las 6 filas (15.1-15.6) y el propósito viven en `docs/plans-archivo.md`,
movidas 2026-08-30 al cerrar la Phase 16 (higiene del tope de 200 líneas).
Resumen: el gate corre en `dsh` por un adaptador JS que traduce sus eventos
a los payloads del hook; una sola fuente de verdad, el port es delgado. **Con su
límite, que el resumen anterior omitía** (grok, cross-review 2026-08-30): el
alcance medido es la UI web, y el GATE en esa UI quedó `unknown` en el escenario
2 de la 15.5. La fila entera está en el archivo.

---

## Phase 16 — El recetario (2026-08-28)

**Propósito:** encima del gate — que sigue midiendo lo mismo — un recetario por
tipo de tarea que el **líder elige y declara en el recibo**, con principios por
rol en los perfiles y la voz para un usuario que no lee código ni inglés.
Origen: pstack (MIT), triage de 69 piezas. Diseño y decisiones D0–D23 (v2,
validada por 3 revisores): `docs/phase-16-18-recetario-autopilot-design.md`; plan paso a paso para
workers: `docs/phase-16-recetario-plan.md` (implementa DeepSeek; el lead
revisa, cierra y despliega). Host de la primera ola: Claude Code. **El carril
lo fija el sentinel, nunca la receta** (D3, medido). Lint bash baseline:
`tests/lib/check_syntax.sh`.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 16.1 | `[Diseño]` `[lane:fast]` `[tdd:skip:docs]` **Diseño + spec delta.** `docs/phase-16-18-recetario-autopilot-design.md` (D0–D23 + Apéndices A–C) y § "Ampliación — Phases 16–18" del spec; Phases 13–14 archivadas; validación de equipo con 3 revisores (43 hallazgos integrados) | Cada decisión cita su medición o `unknown`; spec y diseño coherentes; `test_plans_ledger` verde; `Plans.md` ≤200 líneas | — | cc:完了 [PR #95, merge `dd37809`] — **CIERRE**: diseño v3 (D0–D24, Apéndices A–C) + spec § Ampliación (8 reglas + límites + Non-Goals); Phases 13–14 archivadas; validación interna de 3 revisores (43 hallazgos) + Greptile P1 + cross-review externo **codex y grok en paralelo** (22) + CodeRabbit (9): 75 hallazgos, todos integrados o rechazados con razón (Apéndice C). Incidente del PR: `test_adversary_artifact_contract` se puso rojo en CI al archivar la fila 14.3 (ancla a `Plans.md`); corregido en `b5716c1`/`1d455f3` (el ancla sigue al ledger archivado y valida SOLO la fila 14.3, con 4 casos sintéticos). 事前確認 aprobado por el operador el 2026-08-28 y registrado en `.claude/state/plan-preapprovals.json`. Deploy no-op (docs-only), ver `docs/deploy-log.md` |
| 16.2 | `[Recetario]` `[lane:fast]` `[tdd:required]` **`recetas/` fuente**: `00-lider.md` (4 principios del líder, brief de 8 campos con "Lo que ves", 5 secciones del PR, regla de preguntar en dos ramas, "rastro previo autoritativo") + 6 recetas en español ≤80 líneas con frontmatter YAML (`saikit_owned`, D2): `bug`, `funcion`, `refactor`, `lento`, `investigar` (fast, con voz `teach` y epistemología de `why`), `boceto` (fast); `tools/gen-recetas-manifest.sh` → `recetas/MANIFEST.sha256` (campos separados por TAB: `sha256`, `tipo` `receta`\|`lider`, `nombre`, `carril`, `titulo` al final; título sin TAB ni salto) y que corre el linter antes de incluir; `.gitattributes`: `recetas/** text eol=lf` (hash igual en Windows y Linux); linter `tests/test_recetas.sh` escrito ANTES que las recetas | Linter rojo con fixture rota / con término prohibido (`Graphite`, `Bugbot`, `gt` seguido de espacio, `AskQuestion`, `/loop` de Cursor) / con manifiesto desactualizado / con `00-lider` marcado `receta` / con TAB en el título; verde con 6 recetas + lider + manifiesto, incluido un título de varias palabras que se lee entero; tolera CRLF en lectura; cada receta trae Pasos / Qué le dices al usuario / Recibo; el hash del manifiesto coincide en CI Linux y en local Windows | 16.1 | cc:完了 [PR #98, merge `89ff713`; implementó DeepSeek; gate final: CI `Quality`/`suite` verde en el head `eacfc2d` (run 33219941839) y en master tras el merge (run 33225605701)] — **CIERRE**: 7 archivos en `recetas/` (≤80 líneas, español; `00-lider` `tipo: lider`), `MANIFEST.sha256` TSV con hash normalizado a LF, `tests/lib/recetas_lint.sh` + `tests/test_recetas.sh` (29 casos en sandbox cuando se cerró esta fila; hoy son 30: forma, términos prohibidos con frontera de token y `Cursor` sensible a caja, 81 líneas sin salto, `nombre == archivo`, links con espacios, título plegado, CRLF, manifiesto ausente/stale vía `--dir`), `tools/gen-recetas-manifest.sh` (`--check` ⇒ 1 si difiere o falta, `--dir`, exit 5 en IO, nombres únicos), `.gitattributes` `recetas/** eol=lf`. Revisión del lead en 2 vueltas con 13 sondas y 2 mutaciones del linter (quitar `gt`; volver a `wc -l`) que ponen el test en ROJO; cross-review real grok (7; #2 artefacto de base vieja); Greptile+CodeRabbit 9 hilos cerrados. Desviación aceptada: el borde de `gt` excluye `-` (no caza el operador `-gt`). Nit para 16.5: `--dir` sin valor y args desconocidos sin `uso()`. Proceso limpio (sin merge/push/deploy del worker; `AGENTS.md` modificado quedó fuera). Deploy no-op: el hook no cambió |
| 16.3 | `[Perfiles]` `[lane:fast]` `[tdd:skip:prompt-only]` **Principios por rol en `agents/*.md`** (D5): implementer (9 + 3 reglas de `architect`), reviewer (4 + "comentarios y supresiones" + 4 cubos de adjudicación), verifier (prove-it-works complemento + "mejor ningún test que uno malo, declarado" + regla de redacción antes de escribir), adversary (idempotencia); `boundary-discipline` consolidado en UN lugar | `grep -c '^Cuándo:'` en cada perfil = número de principios asignados; frontmatter/`saikit_owned` intactos; el encabezado `## Boundary Discipline` existe en exactamente 1 perfil (la palabra `boundary` sigue en el ataque "trust boundary" del adversary); ningún perfil promete lo que el hook no mide | 16.1 | cc:完了 [PR #100, merge `0cd1b3d`, CI run `33231759486` verde sobre `b4f4b15`] — **CIERRE**: los cuatro perfiles (`agents/{implementer,reviewer,verifier,adversary}.md`) con sus principios y `## Boundary Discipline` en UNO solo. Hubo revisión del lead (tuteo, idempotencia en 3 escenarios, authorization aparte) y cross-review externo (codex+grok). **La fila quedó sin cerrar desde el 2026-08-29 03:44 hasta el cierre de 16.4/16.6**: el trabajo estaba mergeado y el ledger decía `cc:TODO`. El candado de estructura de `test_plans_ledger.sh` no lo atrapa — mira la FORMA de la fila, no si su estado corresponde a la realidad |
| 16.4 | `[Contrato]` `[lane:gate]` `[tdd:required]` **Menú de recetas en el contrato** (D1/D4): `RECETAS_DIR` con override `SAIKIT_RECETAS_DIR`; menú desde `MANIFEST.sha256` con verificación de hash por receta (mismatch/ausente ⇒ omitida + aviso; sin manifiesto ⇒ línea fija); **sin ruta absoluta en el contrato**; regla "elige/lee/copia/declara `Receta:`/`skip: razón`"; edición del bloque de preguntar (D7); `Close:` con "nunca inventes link/cita/comando" (D8) | Rojo medido; `caso_g1_contrato_nombra_recetario` (planta `$LAB/hooks/recetas` vía `SAIKIT_RECETAS_DIR`) + `caso_g1_sin_recetario_contrato_igual` (byte-idéntico salvo la línea fija) + `caso_g1_receta_hash_distinto_se_omite`; mutación `menu_recetas_apagado`; `test_hook_source` 131 verde; golden regrabada con `--record` y diff auditado (1 de 5 declarados); el Stop gate sin cambios | 16.2 | cc:完了 [PR #105, merge `8ca9bab`, CI run `33270136308` verde sobre `df13ca9`] — **CIERRE**: el menú se arma desde `MANIFEST.sha256` verificando el sha de CADA archivo, y el título/carril que entran al contrato se leen del **archivo autenticado**, no de las columnas del manifiesto (dos correcciones del lead sobre el PR: quien pudiera editar el manifiesto escribiría el contrato de cada turno armado, y el awk que lee esos campos se acotó al bloque de frontmatter). Poder discriminante medido por el lead con corrida propia: `mut_menu_sin_corte_frontmatter` queda acreditada a UN solo caso. Golden auditado: 2258 líneas de estructura idénticas, 54 escenarios en el mismo orden, ningún `exit` ni estado cambió. **Límite declarado:** el corte de frontmatter es preventivo — hoy ninguna receta tiene `titulo:`/`carril:` fuera del frontmatter, el caso lo planta sintéticamente |
| 16.5 | `[Instalador]` `[lane:gate]` `[tdd:required]` **`install-hook.sh` planta `<hookdir>/recetas/` + `MANIFEST.sha256`** (host claude) reutilizando `agente_estado_con_vendor` por archivo (marca `saikit_owned` en frontmatter; ajeno ⇒ `DESCONOCIDO`, no se toca), `--dry-run` que no escribe, y `~/.claude/skills/sencillo/SKILL.md` (D8) con la misma marca; `check-hook-registration.sh` reporta recetario/manifiesto ausente y ACE ajenos (advisory, exit 0); `test_hook_acl.sh` y `hook-acl.ps1` extendidos a `hooks/recetas/` y a la LISTA explícita `skills/{sencillo,saikit-verificar-app,saikit-setup-autopilot}` (un glob `saikit-*` deja fuera a `sencillo`) | Casos: limpio / dos corridas no reescriben / archivo ajeno intacto y reportado / dry-run sin escritura / quitar; clasifica todo antes de publicar (precedente 12.9); **Windows-bound**: casos corridos en local y citados en el cierre (CI los salta con `SAIKIT_CI_LINUX=1`) | 16.2 | cc:完了 [PR #108, merge `aa9c0ac`, CI run `33301267516` verde sobre `463f61c`] — **CIERRE**: implementó DeepSeek (3 rondas de cross-review propias); la 4ª la pidió el operador y la corrió el lead. **Lo que costó el PR y no estaba en el plan:** una fuga que se salía del árbol gestionado — con `recetas/` como symlink a un directorio externo, el glob expandía hacia afuera y `rm -f` borraba el archivo ajeno. Reproducida en Linux por el lead, cerrada con guard `-L` a nivel archivo Y directorio, en instalar y en quitar. Poder discriminante medido quitando cada guard: los casos se ponen rojos nombrando el bug. **`tests/test_recetas_symlink.sh` es archivo propio a propósito**: `test_install_hook.sh` es Windows-bound y el CI Linux lo saltea entero, así que los casos de symlink no corrían en ningún gate; el archivo nuevo NO está en la lista de saltos y sale `unknown` (exit 3), no SKIP, donde el host no crea enlaces reales. **Falso positivo medido, no supuesto:** el junction de Windows — se creó uno con `mklink /J` y `[ -L ]` da VERDADERO en MSYS2; el guard los atrapa. **Límite declarado (no se finge cerrado):** la carrera TOCTOU entre el chequeo `-L` y el borrado. Quien la explota ya tiene escritura en `~/.claude/hooks/` — nivel que cubren la Phase 0 y `hook-acl.ps1`, no el instalador — y el arreglo propuesto no se expresa en shell portable; mismo criterio que la fila 0.2 con A7-bis: *ese vector se detecta, no se previene*. **Hueco del PLAN, no del trabajo:** `hook-acl.ps1 -RutasExtra` nació sin caller de producción — el paso 5 pedía un parámetro, no un caller. **Levantado en 16.12** (16.9 no lo descargó): la auditoría cubre `skills` por defecto cuando `-Path` es el perfil de Claude; `recetas/` siempre estuvo cubierto por la recursión (premisa corregida). Corregirla con `-Fix` sigue exigiendo el flag, a propósito. Windows-bound corridos en local: `test_install_hook`, `test_hook_registration`, `test_hook_acl`, los tres OK |
| 16.6 | `[Carril]` `[lane:gate]` `[tdd:required]` **Alias en español del carril fast que NOMBRAN su receta** (D3, **Required**): `-saikit:pregunta` ⇒ `lane=fast` + `receta_alias=investigar`; `-saikit:boceto` ⇒ `lane=fast` + `receta_alias=boceto`; el contrato lo dice ("carril fast por `-saikit:boceto`: la receta es `boceto`, no toques código de producción"); match exacto con frontera; cualquier otro sufijo ⇒ full sin alias (límite declarado, como 10.1) | Rojo medido; caso por alias (lane + estado + línea del contrato) + `caso_g1_sufijo_desconocido_arma_full` sigue verde; mutación `alias_pregunta_apagado` y `alias_sin_receta`; golden regrabada con diff auditado (2 de 5) | 16.4 | cc:完了 [PR #105, merge `8ca9bab`, CI run `33270136308` verde sobre `df13ca9`] — **CIERRE**: los dos alias bajan el carril y nombran su receta, con guard ESTRICTO (corrección del lead): si la receta no existe en el recetario con su hash verificado, el alias NO baja el carril y no nombra nada — `caso_g1_alias_sin_receta_no_baja_el_carril` y `caso_g1_alias_sin_recetario_queda_full`. `podar_dir_sesion` limpia `receta_alias` (mutación `alias_no_se_limpia`). El carril lo fija el sentinel, nunca la receta ni el recibo |
| 16.7 | `[Ruteo]` `[lane:gate]` `[tdd:required]` **`model-routing.sh --task <receta>`** ⇒ tier existente, nunca modelo (D9, Optional) | `test_model_routing.sh`: cada receta resuelve a `standard`\|`verify`\|`review`; el candado "IDs solo en el router" sigue verde | 16.2 | **cc:NO EJECUTADA** (decisión del lead en el cierre 16.9, con el operador) — es `Optional`. **CORRECCIÓN (cross-review grok, 2026-08-30):** la versión anterior de esta fila decía que «ninguna fila la lista en `Depends`», y **eso era falso** — 16.9 lista `16.2–16.8`, rango que la incluye. El error fue mío: busqué `16.7` literal y no leí la notación de rango. Lo que sí es cierto y sostiene la decisión: 16.9 **se cerró sin ella** y ninguna fila quedó bloqueada; la Phase 17 no la menciona. Lo que compra es enrutar por receta a un tier existente, que no cambia **las palabras** que el operador ve — aunque enrutar a otro tier sí podría cambiar costo, latencia o calidad, y eso no se midió (grok, misma ronda) — y que hoy no tiene demanda medida: las 6 recetas ya se eligen y se siguen sin ella (16.8). No se borra: queda acá con su DoD intacto para cuando exista una razón medida para hacerla — por ejemplo, si el ruteo por tier empieza a costar plata o latencia de forma observable. **Esto es una decisión declarada, no un olvido** |
| 16.8 | `[Medición]` `[lane:release]` `[tdd:skip:medición]` **Un turno vivo por receta** (D10; hasta 3 por sesión en el mismo repo descartable) con `tools/stage-override.sh`: el líder eligió la receta correcta, la declaró en `Understand:`, copió los pasos, y el recibo cerró; `docs/smoke-recetas-<fecha>.md` con cita del transcript por receta | 6 de 6 con evidencia literal o `unknown` declarado; receta sin turno vivo NO entra al manifiesto (queda en `recetas/pendientes/`) | 16.4, 16.5, 16.6 | cc:完了 [PR #110, merge `28d55ad`, CI run `33339398780` verde sobre `d4ce8da`] — **CIERRE**: los 6 turnos los tecleó el operador en `C:/dev/saikit-recetas-lab` (quien escribió las recetas no puede medirlas: 16.2 la implementó DeepSeek). Evidencia en `docs/smoke-recetas-2026-08-30.md`. **Resultado:** las 6 recetas se eligieron, se declararon y cerraron con recibo COMPLETO (los 6 bloques traen las seis etiquetas, medido por el lead, 0 parciales); `SUMMONAIKIT HARNESS GATE` aparece 0 veces en los dos transcripts. **VEREDICTO DEL LEAD: las 6 se promueven, NINGUNA a `recetas/pendientes/`** — y es una desviación deliberada de la letra de esta fila, explicada en 16.10. **La (c) falló en los 6 turnos**: 0 `TodoWrite` en las dos sesiones; nadie copió los pasos al todolist, que es lo que el menú pide literalmente. **La (a) es `unknown` por turno**: el menú se inyecta una vez por SESIÓN, y darlo por presente en cada turno porque el agente nombró una receta es circular — lo hallaron codex y grok por separado. Cuatro rondas de revisión sobre el informe; el lead refutó 4 hallazgos MIDIENDO sobre los `.jsonl` (entre ellos una supuesta contaminación de la causa: `STATS` aparece 91 veces y `KeyError` 18 en el turno 1) |
| 16.10 | `[Contrato]` `[lane:gate]` `[tdd:required]` **El menú pide un todolist que nadie usa** (medido en 16.8: 0 `TodoWrite` en 6 de 6 turnos). El menú dice *"copy its steps into your todolist before reasoning"* y ningún líder lo hizo; siguieron los pasos narrándolos. Seis de seis fallando idéntico NO es evidencia de seis recetas malas — por eso las 6 se promovieron — sino de una instrucción del contrato que no se cumple. **Decidir y ejecutar una de dos:** (a) hacerla cumplible y medible, o (b) sacarla del menú y describir lo que el líder sí hace. Lo que no se puede es dejarla pidiendo algo que 6 de 6 ignoran: una instrucción que nadie obedece entrena a ignorar el resto del contrato | Rojo medido sobre el texto del menú; caso que ate la forma elegida; `test_hook_source` y golden regrabada con diff auditado | 16.8 | cc:完了 [PR #113, merge `0fd64db`, CI run `33342983052` verde sobre `03f64c8`] — **CIERRE**: se eligió **(b)**, y el operador aprobó la recomendación. **El `unknown` se midió antes de decidir**: no hay ninguna restricción de tools ni en el lab ni en el perfil global, y `TodoWrite` es herramienta por defecto de Claude Code — estaba disponible y nadie la usó, **incluido el lead con 0 invocaciones en una sesión propia de horas**. Así que no era imposible: era un ritual que ningún líder considera útil. **Al medir apareció un diseño mejor que el propuesto:** de las tres cosas que pedía el menú, la única cumplida (`Receta:`, 11 y 12 apariciones sobre 2 inyecciones) es la que aterriza en el RECIBO; el todolist y los `skip:` dieron cero — los 2 `skip:` por sesión eran el texto del propio contrato, no uso. **El contrato se obedece exactamente donde el gate mira**, así que la instrucción no se borró: se movió a esa superficie (`follow its steps IN ORDER` + los saltos nombrados en el recibo), cumplible en todos los hosts. Mutación `mut_menu_pide_todolist` acreditada a UN solo caso; golden `--check` 54 escenarios iguales; `test_gate_behavior` OK en local. **CodeRabbit encontró el agujero peor puesto**: el candado rechazaba `todolist` pero no `TodoWrite`, el nombre real, en un caso cuyo punto entero es prohibir esa herramienta; cubre las dos grafías desde `03f64c8`. **Nada fijaba esta instrucción antes** — por eso pidió durante toda la fase algo que nadie hacía |
| 16.11 | `[Receta]` `[lane:fast]` `[tdd:skip:prompt-only]` **`funcion` no contiene el alcance** (medido en 16.8, turno 2). Se pidió una lista de tareas con crear y borrar; el turno además arregló el 500, cambió el esquema de ids, agregó `GET /tasks`, validación y tope de `Content-Length`, y corrió una revisión adversarial con 10 hallazgos. El operador lo marcó en el momento: *"te inventaste esa decision no es algo que yo tenga q decidir"*. La receta se eligió y se siguió — por eso no va a `pendientes/` — pero le falta un paso que acote: lo que aparece de más se NOMBRA y se propone aparte, no se hace | `grep` del paso nuevo en `recetas/funcion.md`; manifiesto regenerado; el linter de recetas sigue verde | 16.8 | cc:完了 [PR #115, merge `5128410`, CI run `33347396488` verde sobre `101e368`] — **CIERRE**: implementó DeepSeek. El paso nuevo es el 3, **antes de delegar**: *«Nombra y propón aparte lo que aparece de más (un bug, una mejora no pedida, deuda); no lo hagas en este turno. La propuesta va en el recibo»*, más una línea nueva del recibo `Fuera de alcance (propuesto aparte): <…; si no hubo, ninguno>`. **Dos aciertos del autor, no pedidos:** el paso va temprano —la decisión de no hacerlo se toma cuando la cosa aparece, no al final, cuando el trabajo de más ya está hecho— y el `si no hubo, ninguno` hace OBSERVABLE la ausencia: sin esa coletilla, un recibo sin la línea no distingue «no apareció nada» de «no lo miré». Es `not_observed != absent` aplicado a una receta. Verificado por el lead: sha del archivo == línea del manifiesto (`240d5f35e638…`), manifiesto regenerado con `gen-recetas-manifest.sh` y byte-idéntico al del PR, 28 líneas (tope 80), frontmatter intacto. **Dos hallazgos de CodeRabbit NO se aplicaron, con razón escrita:** (1) hacer incondicional la «rebanada completa» apartaría la receta del contrato, que dice *«When a gap is found, the change is a complete slice»* — la regla YA es condicional; (2) enumerar los ocho campos del brief duplicaría en un archivo de 80 líneas lo que `00-lider.md` define en un solo lugar. Los dos son texto PREEXISTENTE que este PR solo renumeró |
| 16.9 | `[Release]` `[lane:release]` `[tdd:skip:docs]` **Cierre**: **actualizar la guía de usuario** (artifact `b734d453-27a8-428a-835e-67ba0c692ada`, fuente en `docs/guia-usuario.html`) con lo que la Phase 16 dejó vivo — los sentinels de 16.6 pasan de "en camino" a "ya funciona", `/sencillo` igual si 16.5 lo plantó, y las recetas que 16.8 midió; spec § final con límites medidos; README con el puntero a la guía (no la duplica: el README es para quien lee código, la guía para el operador); deploy vía `install-hook.sh` anotado en `docs/deploy-log.md` (lo hace el lead); filas cerradas con sha/PR; archivar Phase 15 | `tests/test_recetas.sh` + `python tools/check_context_docs.py . --sweep` sin hallazgos nuevos; deploy-log con el sha; `Plans.md` ≤200 líneas; **la guía no deja ninguna pieza de la Phase 16 marcada "en camino" si su fila quedó `cc:完了`, y ningún sentinel listado que el hook no reconozca** (contrastar contra `hooks/summonaikit-harness.sh`) | 16.2–16.8 | cc:完了 [PR de cierre, ver `docs/deploy-log.md` 2026-08-30] — **CIERRE DE LA PHASE 16**. **Guía de usuario:** `-saikit:pregunta` y `-saikit:boceto` pasan de «en camino» a «ya funciona» (medidos en vivo en los turnos 5 y 6 de 16.8); `/sencillo` queda como instalada (16.5 la plantó y el harness la levantó en el acto); `funcion` gana la frase de que lo que aparece de más se propone aparte (16.11); y una nota nueva declara lo medido **y lo no medido**: las 6 recetas se probaron con seis pedidos reales, pero que funcionen en otros programas que no sean Claude Code es `unknown`. El candado `tests/test_guia_usuario.sh` sigue verde tras cada edición. **Spec:** sección «Límites MEDIDOS de la Phase 16», que difiere de los declarados en un punto que no estaba previsto — en esos seis turnos lo único cumplido fue lo que el recibo exigía (la regla general queda como HIPÓTESIS en el spec, no como ley), y una instrucción que no aterriza en el recibo no se cumple (evidencia: todolist y `skip:` en 0 de 6, `Receta:` en 11 y 12). **README:** puntero a la guía, con la razón de que no se dupliquen — si las dos explican lo mismo, una envejece mal. **Archivo:** Phase 15 movida a `docs/plans-archivo.md` (Plans.md de 200 a 179 líneas). **16.7 declarada NO EJECUTADA**, con su razón en la fila. **CORRECCIÓN tras el cross-review codex+grok (2026-08-30):** este cierre se dio por hecho sin cumplir **cuatro** de los cinco puntos de su propia DoD (el único cumplido era `Plans.md ≤200 líneas`), y se completaron después — `tests/test_recetas.sh` (OK, **30 casos**), `check_context_docs.py . --sweep` (limpio) el contraste de sentinels contra el hook —que **encontró un incumplimiento real**: la guía listaba `-saikit:autopilot`, que el hook NO reconoce, así que escribirlo hoy da un turno normal sin avisar. Corregido en la guía— y esta misma entrada del deploy-log, que también faltaba. **La primera versión de esta corrección decía «tres» y contó mal** (cross-review kimi): un incidente sobre no declarar lo no corrido, contando mal lo no corrido. Además, la 16.5 asignaba a esta fila el hueco de `-RutasExtra` sin caller de producción y no se descargó: se levanta como **16.12**, en vez de darlo por cerrado. |
| 16.12 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **`hook-acl.ps1 -RutasExtra` sin caller de producción** (heredado de 16.5, que lo asignó a 16.9; el cierre de 16.9 no lo descargó y el cross-review lo encontró). El parámetro existe y audita lo que se le pase — medido: 2 objetos sin él, 5 con una ruta extra de 2 archivos — pero **nadie lo pasa desde el repo**: `grep` no encuentra ningún invocador de `hook-acl.ps1` fuera de su propio archivo, los tests y la documentación. La cobertura ACL de `hooks/recetas/` y `skills/*` que la 16.5 prometía solo aplica si el operador escribe el flag a mano. **Decidir:** que el guardrail lo pase por defecto cuando audita el perfil de Claude, o declarar que la cobertura es manual y decirlo donde se promete | Caso que falle si se audita el perfil sin cubrir `hooks/recetas/`; `test_hook_acl.sh` verde; el DoD de 16.5 deja de prometer cobertura que no ocurre | 16.5 | cc:完了 [PR #138, merge `d203b864`, CI 9/9] — **CIERRE**: decisión (a) con alcance recortado por el reviewer: el default deriva `<perfil>\.claude\skills` SOLO cuando `-Path` tiene la forma exacta del perfil, y **solo AUDITA** — corregirla exige `-RutasExtra` explícito (el radio destructivo de `-Fix` sin flags queda idéntico al de antes). **Premisa de la fila corregida y MEDIDA**: `hooks/recetas` ya estaba cubierto por la recursión de `Get-HookAclTarget` — el caso que esta DoD pedía sobre `recetas/` no puede ponerse rojo tal como se escribió, y el que vale (skills) existe y discrimina. TRES ciclos internos (adversary 9 hallazgos/3 altas; reviewer NO PUBLICABLE dos veces: derivación fuera de `.claude` con `-Path ~/.claude` — la DoD de la 0.2 —, junctions, y la segunda puerta `Repair-StaleInherited` que mutaba sin respaldo, atrapada midiendo un HIJO y no el directorio) + cross-review externo pedido por el operador: codex 3/5 reales (ruta anclada, exit 1 fijado, siembras verificadas) y **grok con el hallazgo de la ronda** — `-Fix` con `-Path` limpio mutaba hooks por hallazgos ajenos de la raíz derivada; reproducido en vivo y cerrado con no-op por radio mutable. Suite: 24 casos, 0 FAIL, cada mecanismo con mutante rojo. Diferidos declarados: TOCTOU con flag explícito, corchetes/`-LiteralPath` (task propia), acceso-denegado en la derivada (Noted). Deploy NO-OP (el guardrail vive en el repo) |

**Fuera de alcance, declarado:** recetas, `/sencillo` y menú en zcode/codex/
grok/kimi/dsh (entran después por la vía de los perfiles); las 10 piezas NO y
las 10 DESPUÉS del Apéndice A; que el líder cambie el carril; shellcheck.

---

## Phase 17 — Verificación real y rastro (2026-08-28)

**Propósito:** que el verifier pruebe **la app como usuario**, no solo tests y
typecheck; que cada task deje un **rastro de decisiones en español** legible
sin abrir código; y que el verifier nombre **el hecho único** por el que el
cambio es seguro y lo corra (blast radius, nivel 1–5). Es lo que hace posible
un autopilot que no mergee a ciegas. Decisiones D11–D14 del diseño.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 17.1 | `[Skill]` `[lane:gate]` `[tdd:required]` **`saikit-verificar-app`** (D11): skill a nivel usuario que genera `verify/` en el repo del usuario (`LEEME.md` con mapa de 3–5 funciones en español + Launch/Doctor/Drive/Evidence/Cleanup); **Drive = e2e bajo `verify/` que ejercita la superficie de usuario, en el framework que el repo ya tiene, invocado por un comando que `TEST_RUNNER_RE` acredita** (`node --test` se envuelve en `npm test`; la suite unitaria del repo NO cuenta como Drive); framework nuevo solo con sí explícito del usuario, pinneado, nunca `npx @latest`; sin sí ⇒ Drive "manual, pendiente" y `verify_app: n/a`; se corre UNA vez antes de entregar. **El mapa se sella con la fecha y el sha del commit en que se generó** (`generado: <fecha> · <sha>` en el frontmatter de `LEEME.md`) y el verifier reporta su antigüedad en el recibo: un mapa viejo describe una app que ya no existe y da confianza falsa — que es peor que no tener mapa, y en autopilot autorizaría un merge por comprobar funciones que ya no están. El sello vuelve esa deriva OBSERVABLE sin necesitar la skill de mantenimiento | `tests/test_verificar_app.sh` (portable, corre en CI) contra 2 repos fixture (node con `node --test`, python+pytest) en sandbox: archivos esperados bajo `verify/` + el comando de Drive incluye `verify/` y matchea `TEST_RUNNER_RE` LEÍDO del hook con `grep -o`; repo sin framework ⇒ propone y espera el sí, no instala; **el `LEEME.md` generado trae el sello con el sha real del repo fixture (no uno inventado), y con el sello ausente el verifier lo declara `unknown`, nunca "al día"** | 16.1 | cc:完了 [PR #120, merge `7dd2ba9`, CI run `33365962146` verde sobre `dd25be0`] — **CIERRE**: implementó DeepSeek, 1 ronda de corrección. **CodeRabbit encontró tres Major, los tres con la MISMA forma — acreditar algo que no se comprobó:** (1) un `"test": "echo ok"` pasaba como «Drive acreditable» por un comando que solo corría `echo` — era la falla exacta que el brief advirtió; ahora se exige un runner ADMITIDO (`NODE_TEST_RUNNER_RE`) o el Drive queda «manual, pendiente» con `verify_app: n/a`; (2) el Drive generado usaba `require`/`__dirname` y en un proyecto ESM (`type: module`) fallaba antes de correr — ahora genera `drive.test.cjs`; (3) un sello con fecha ilegible y sha correcto salía `al_dia` — ahora `unknown`. **Verificado por el lead con reproducción propia de los tres escenarios** (incluida la distinción ESM entre fallo de carga y aserción del fixture), y el rojo medido está pegado en cada hilo con su línea de FAIL literal. El test LEE `TEST_RUNNER_RE` del hook con `grep -o`, no una copia. **Alcance declarado:** esta task NO instala la skill en el perfil — eso es de 17.6; el deploy tras el merge fue no-op del hook |
| 17.2 | `[Perfil]` `[lane:fast]` `[tdd:skip:prompt-only]` **`agents/verifier.md`, lane de app real**: si existe `verify/` lo corre y su salida es evidencia; si no existe, lo propone (no lo inventa en el turno); "inconcluso o superficie equivocada no es PASS" | `grep` de las 3 frases en el perfil; consistente con 16.3; el perfil no promete nada que el hook no acredite | 17.1, 16.3 | cc:完了 [PR #131 junto con 17.4, merge `4e17f07e`, CI 9/9 verde sobre `c1107b0`] — **CIERRE**: las 3 frases están en el perfil en el formato Cuándo/Regla de 16.3, y cada promesa tiene check que la mide — `TEST_RUNNER_RE` y el candado 13.4 verificados en el hook por el lead con grep, no de palabra. Implementó DeepSeek en un solo toque a `agents/verifier.md` (por eso fue junto con 17.4) |
| 17.3 | `[Rastro]` `[lane:gate]` `[tdd:required]` **`.saikit/decisiones/<task>.tsv`** (D12): columnas `cuando\|etapa\|decision\|por_que\|evidencia\|resultado`; `tools/saikit-decision.sh` append seguro; **`tools/lib/redactar.sh` compartido** (patrones del hook + `ghp_`/`github_pat_`/`gho_`/`sk-`/`AKIA`/`xox[bp]-`) usado por rastro y blast, y **la familia de patrones del hook se extiende** con esas formas (rojo/verde + mutación, escaneo de 13.4); contrato: `Close:` cita la ruta + sección **Atención**; **se commitea antes de despachar al reviewer**; adversary lee el tsv | `tests/test_saikit_decision.sh`: append crea/agrega sin partir filas; cada forma de token sale `[REDACTED]` en el helper Y en el escaneo del hook (caso de gate + mutación `redaccion_sin_ghp`); tsv malformado se reporta, no se repara; golden regrabada con diff auditado (3 de 5) | 16.4 | cc:完了 [PR #126, merge `aecae38`, CI run `33424788197` verde sobre `d164316`; el trabajo lo implementó DeepSeek en el PR #122] — **CIERRE**: el rastro, el append seguro y `redactar.sh` compartido entraron intactos; lo que costó TRES rondas fue la **higiene de los fixtures frente a gitleaks**, que escanea el historial completo: (1) fixtures con forma de secreto real (#122, DeepSeek); (2) prefijos slack contiguos en los format strings — `slack-legacy-token` matchea `xox[bp]-` con UN solo segmento (#124, error del lead); (3) un COMENTARIO con `flag=-sk-proj` de ejemplo — `generic-api-key` salta con palabra-clave + forma de asignación incluso en comentarios (#126, error del lead; identificado con `-v` en la rama desechable #125). Cada rama descartada se borró y sus commits quedaron inalcanzables — el force-push está prohibido por el guard, y la vía rama-nueva+borrado es equivalente. **La lección, tres veces pagada:** el verificador es gitleaks, no un grep propio; el criterio de cierre es su job `secrets` en verde, que es como cerró. Verificado por el lead tras cada corrección: batería OK en corrida propia, mutación `redaccion_sin_ghp` acreditada a UN solo caso, golden solo-cabecera. Deploy REAL: el hook vivo trae la familia nueva |
| 17.7 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **El candado local de secretos es más laxo que el CI** (medido en 17.3): `tools/check-secrets.sh` (pre-commit) dejó pasar TRES veces lo que el gitleaks del CI rechazó — fixtures con forma real, prefijos slack contiguos y el comentario con forma de asignación. Un candado local que dice PASS entrena a confiar y el rojo aparece recién en CI, con el literal YA commiteado en la historia de la rama — que es exactamente lo caro. **Decidir y ejecutar:** instalar gitleaks pinneado como capa local (mismo binario y versión que el CI) con fallback declarado donde no esté, o documentar en el propio check-secrets.sh que su PASS no es el veredicto y que el juez es el job `secrets` | Caso que reproduzca uno de los tres hallazgos medidos y confirme que la capa local lo ATRAPA (o lo declare fuera de su alcance por texto); `test_check_secrets.sh` verde | 17.3 | cc:完了 [PR #133 junto con 17.8, merge `ecee3b4e`, CI 9/9] — **CIERRE**: DeepSeek eligió la opción (a): gitleaks pinneado 8.30.1 (mismo binario/versión que el job `secrets`) con AVISO ante versión distinta, y fallback grep que DECLARA que su PASS no es el veredicto. El hallazgo #3 (comentario keyword+asignación) reproducido: el fallback lo deja pasar (la brecha medida) y el gitleaks pinneado lo atrapa — corrido DE VERDAD en la máquina del lead, no skip. CodeRabbit encontró un Major real: `SAIKIT_GITLEAKS` pisado por el PATH (la remediación documentada era mentira); cerrado por el lead con rojo medido (2 FAIL) y shims que registran quién corrió. Mutación: fallback sin AVISO ⇒ 1 FAIL |
| 17.8 | `[Protocolo]` `[lane:gate]` `[tdd:required]` **El candado y el append del rastro no aguantan concurrencia hostil** (cross-review codex+grok sobre 17.3, hallazgos ALTA convergentes; los baratos ya cerraron en el PR #128 con rojo medido — esto es lo ESTRUCTURAL): (a) carrera ABA en la reclamación del lock — un proceso que vio un pid muerto puede borrar el candado que OTRO ya readquirió, y el `trap EXIT` de uno le borra el lock al otro (los dos terminan con `candado_propio=1`); (b) `printf` en append no es atómico ante SIGKILL/ENOSPC — una fila truncada sin salto final se PEGA a la siguiente y el check la da por buena; (c) el pid se escribe tragando el fallo (lock sin pid), y `--check` corre SIN candado (puede leer una fila a medio escribir y reportar malformado falso); (d) portabilidad del reclamo: `stat -c` es GNU y el fallback deja edad 0 — hoy eso significa «nunca reclamar», que es fail-safe, pero hay que DECLARARLO en el código. **Nota para el blast de la Phase 18, anotada acá para no perderla:** `redactar` procesa por líneas; el TSV está a salvo por el veto de NL, pero el blast recibirá texto multilínea y debe APLANAR antes de redactar (**cerrado en 17.4**: `saikit-blast.sh` aplana antes de redactar, medido con mutación — a esta fila le queda solo candado y append) | Casos que reproduzcan (a) y (b) de forma determinista (inyección de pausas o kill dirigido, como el precedente de la 0.4); el diseño elegido para el append atómico se DECLARA con su tradeoff; `test_saikit_decision.sh` sigue verde entero | 17.3 | cc:完了 [PR #133 junto con 17.7, merge `ecee3b4e`, CI 9/9] — **CIERRE**: token dueño `pid:nonce` por adquisición (el trap suelta SOLO el candado propio), re-verificación + `mv` atómico al reclamar huérfanos, guard de fila truncada (la última fila debe terminar en salto) + verificación del append por delta de BYTES (detecta y falla cerrado, no repara — tradeoff declarado), `--check` bajo el mismo candado, límite GNU de `stat` declarado en el código. Casos DETERMINISTAS con pausa inyectable (`test_knee`, no-op en producción). Auditoría del lead: mutaciones medidas y atrapadas (trap sin comparar token ⇒ 2 FAIL; sin guard de truncada ⇒ 3 FAIL). Noted: microventana SIGKILL entre el `mv` y el `rmdir` al reclamar puede dejar el lock clavado — límite declarado en el PR |
| 17.4 | `[Blast]` `[lane:gate]` `[tdd:required]` **El hecho único** (D13): paso del verifier + esquema `.saikit/findings/blast-<task>.json` (`hecho`,`comando`,`salida` recortada y redactada,`nivel` 1–5) + adjudicación en `reviewer.md`; anotar que el candado del adversary no aplica al verifier (medido) | `tests/test_blast_artifact_contract.sh` (molde `test_adversary_artifact_contract.sh`): válido / nivel ≥4 sin `comando` ⇒ inválido / nivel fuera de rango ⇒ inválido; caso de gate: blast con secreto en `salida` NO dispara el escaneo de 13.4 (llega `[REDACTED]`) | 17.2 | cc:完了 [PR #131 junto con 17.2, merge `4e17f07e`, CI 9/9 verde sobre `c1107b0`, run `33449646189`] — **CIERRE**: implementó DeepSeek (`tools/saikit-blast.sh` + test + adjudicación en `reviewer.md`); orden aplana→redacta→recorta→re-redacta; contrato de `--task` byte-idéntico al de `saikit-decision.sh` (diff vacío); el caso de gate trae referencia VIVA (el archivo crudo SÍ dispara el escaneo). **Auditoría del lead con 3 mutaciones medidas**: sin re-redacción y sin trim de comando, atrapadas; con el orden roto (recortar antes de redactar) el caso de la credencial quedaba VERDE — vacuo, el padding 990 dejaba el corte en `https://FA` — corregido por el lead en `c1107b0` (padding 975 = el corte cae justo tras la clave; rojo/verde re-medidos). DeepSeek corrió su propio cross-review codex+glm y un adversary, hallazgos reales cerrados. CodeRabbit NO revisó (rate limit semanal), declarado. Deploy: hook no-op; perfiles verifier y reviewer REPARADOS byte a byte con `--host claude`. La deuda del blast anotada en 17.8 (aplanar antes de redactar) quedó cerrada aquí |
| 17.5 | `[Medición]` `[lane:release]` `[tdd:skip:medición]` **Dos turnos vivos** (app node, app python) con `verify/` generado, rastro y blast; `docs/smoke-verificacion-<fecha>.md` | Evidencia literal: el hook acreditó la corrida de Drive como verificación (evento de tool o prosa); el tsv y el blast existen y el reviewer los adjudicó; lo no observado `unknown` | 17.1–17.4 | cc:完了 [medición viva 2026-08-31, sesiones `d2959d1d` (node) y `07fdabb0` (python), `docs/smoke-verificacion-2026-08-31.md`] — **CIERRE**: la lane de app real funciona de punta a punta en vivo — receta `funcion`, tres roles despachados, `verify/` con sello REAL en ambos labs, Drive acreditable corrido (node `npm test -- verify/drive.test.cjs` 8/8 + curl a la app viva; python `pytest verify/` 6 passed) con evidencia en el recibo, y el sello se reporta (python: `desactualizado`, 0 días). **El punto «tsv y blast existen y el reviewer los adjudicó» NO se cumplió — ausencia MEDIDA, no unknown**: 0 invocaciones de los tools en los 2 transcripts (la única mención es un listado de archivos) y `.saikit/` no existe en los labs; nada del contrato los EXIGE (el hook es advisory) y el instalador no planta los tools (los del lab fueron copiado manual, declarado). Decisión del operador (A): el hallazgo se declara aquí y el cierre del gap es la fila 18.12 |
| 17.6 | `[Release]` `[lane:release]` `[tdd:skip:docs]` **Cierre**: spec § límites medidos (qué runners acredita, qué no), **guía de usuario actualizada** (`verificar-app` y el mapa de funciones pasan a "ya funciona"; se explica en palabras simples qué es el mapa y que el operador lo corrige), README, instalador planta `saikit-verificar-app` (Windows-bound), deploy-log, filas cerradas | `test_recetas.sh` + sweep sin hallazgos nuevos; casos del instalador citados; deploy-log con el sha; **la guía describe el mapa de funciones sin jerga y ninguna pieza de la Phase 17 con fila `cc:完了` sigue marcada "en camino"** | 17.5, 16.5 | cc:完了 [PR #136, merge `8504e585`, CI 9/9 run `33518589146`] — **CIERRE**: spec § **Límites MEDIDOS de la Phase 17** (qué runners acredita — `npm test -- verify/drive.test.cjs` y `pytest verify/`, los dos medidos en vivo — y qué no: script `test` sin runner admitido, la suite unitaria, y `node --test verify/` pelado); guía de usuario: `verificar-app` pasa a **ya funciona** con el mapa explicado sin jerga y **quién lo corrige** (el operador), incluido el límite del sello (avisa que envejeció, no comprueba que siga siendo cierto); README § recetario y skills; instalador planta `saikit-verificar-app` (los DOS archivos). **Hallazgo del lead al cerrar, con rojo medido:** la marca `saikit_owned` se lee del PRIMER bloque `---`, y en `verificar.sh` ese bloque era el heredoc que escribe el frontmatter del `LEEME.md` — el generador que el kit mismo planta se clasificaba DESCONOCIDO y **no se actualizaba nunca**, en silencio; cerrado con la marca en un bloque no-op al inicio + 5 casos en `test_install_hook.sh` (plantado byte a byte, reparación, ajeno intacto, dry-run, quitar). Límite declarado: esa cobertura es **Windows-bound** (el CI Linux saltea el archivo entero), así que la instalación de la skill en Linux queda `unknown`. La guía NO anuncia rastro ni blast: 17.5 midió que no se disparan solos, y prometerlos sería la deshonestidad que el candado de la guía existe para impedir |

**Fuera de alcance, declarado:** `maintain-verification-skill` — la auditoría
periódica del mapa contra la app. Disparador OBSERVABLE, no "cuando alguien
note que rotó" (eso dependía de que alguien mirara, justo la falla silenciosa
que este repo evita): entra cuando el sello de 17.1 muestre un mapa con más de
~30 días o varios commits de deriva sobre las rutas que nombra, o cuando pase
de 5 funciones. Hasta entonces el sello y el reporte de antigüedad en el recibo
son la cobertura, y su límite queda declarado: **avisan que el mapa envejeció,
no comprueban que siga describiendo la app.** Además: leer transcripts para
auditar (A6);
runners fuera de `TEST_RUNNER_RE` (se agregan con TDD si un repo lo pide).

---

## Phase 18 — El autopilot (2026-08-28)

> **DECISION DEL OPERADOR, 2026-08-30 - el autopilot NO mergea solo.** Prepara
> todo y **para antes de publicar**: abre el PR, espera el CI, adjudica lo que
> dicen los bots, arregla lo valido, contesta los hilos, vuelve a esperar verde,
> y ahi **pregunta**. Recien con el si explicito hace el merge, el deploy, la
> bitacora y el cierre de la fila - que es tramite sin criterio.
>
> **Por que, y no es prudencia generica.** El operador pidio expresamente que no
> fuera "full autopilot". Y el dato lo respalda: el 2026-08-30, sobre ~15 PRs de
> un dia, los bots dejaron una veintena de hallazgos; varios eran validos y
> **otros los refuto el lead midiendo** (una supuesta contaminacion de evidencia
> que era falsa; un junction de Windows que el guard si atrapaba). Un veredicto
> automatico que mergeara sobre esos hallazgos habria acertado a veces.
>
> **Aclaracion que corrige una premisa:** "como pstack" seria MAS autonomo, no
> menos - pstack autoriza con un *full-autonomy grant* del operador dado una vez
> (Apendice B del diseno). Aca el autopilot se arma **por turno** con
> `-saikit:autopilot`, nunca de forma permanente, y ademas para antes de mergear.
>
> **Lo que esta decision cuesta, dicho y no escondido:** el revert automatico
> deja de tener sentido - si nadie mergea sin que el operador diga que si, no hay
> merge desatendido que deshacer. La 18.5 se reduce; ver su fila.

**Propósito (reescrito por la decisión de arriba; el original prometía permiso
general y revert sin humano):** que el autopilot deje el PR **listo para
publicar** — veredicto atado al SHA exacto, CI verde, hilos de bots adjudicados
— y **pare a preguntar**. El merge solo ocurre con el sí explícito del operador,
y al confirmarse se **revalidan todas las condiciones**, no solo el sha. **El
script de merge es fail-closed y acotado a repo/PR/rama/head** (D18); la
protección de rama de GitHub no está disponible (403 medido). El merge corre
DENTRO del turno armado (cruce con el estado del hook); el post-merge — reducido
a aviso — en un turno desarmado. En serie. Decisiones D15–D23, leídas bajo la
decisión del 2026-08-30.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 18.1 | `[Diseño]` `[lane:gate]` `[tdd:skip:diseño-y-medición]` **Diseño medido + plan paso a paso** (`docs/phase-18-autopilot-plan.md`): formas reales de `gh pr checks` (concluido / pendiente / **sin checks**), `gh pr view --json mergeable` (frecuencia real de `UNKNOWN` diferido) y `gh run list --commit`; `gh pr merge --match-head-commit --body "Saikit-Merge: …"` **sin `--delete-branch`** y si pasa el runtime floor sin prompt; flujo de revert de un squash medido; las 5 preguntas del setup; esquemas del veredicto (D16) y de `autopilot.json` (D15); crea el repo descartable `gon0801/<nombre>` con topic `saikit-descartable` + marcador | Cada precondición de D18 con su forma observada o `unknown`; el descartable creado, nombrado y documentado | 17.6 | cc:完了 [PR #140, merge `15af87a`, CI 9/9 en el head `0e798b7` (run 33660631758)] — **CIERRE**: `docs/phase-18-autopilot-plan.md` con las formas de `gh` MEDIDAS contra Actions reales en el descartable `gon0801/saikit-descartable`, cada precondicion de D18 con su forma OBSERVADA o `unknown` declarado, los esquemas de D15/D16 y las 5 preguntas del setup. El limite se declara y no se disfraza: `mergeable: UNKNOWN` NO se observo en 2 PRs, y el costo por PR y el comportamiento con diffs grandes quedan para la 18.9 |
| 18.2 | `[Receta]` `[lane:fast]` `[tdd:required]` **`recetas/cuidar-pr.md`** (D17): modos revisar/cuidar/solo-hilos; orden conflictos→hilos→CI; clasificación del CI rojo (flake 1 build / base vieja merge de master / real fix); rúbrica fix/dismiss/ask + `.saikit/triage-patrones.md`; esperas sin bloquear; tope 2 rondas; descubrimientos se reportan; **cuidar nunca mergea** | Pasa `tests/test_recetas.sh` incluida la regla de términos prohibidos; manifiesto regenerado | 16.2, 18.1 | cc:完了 [PR #140, merge `15af87a`] — **CIERRE**: `recetas/cuidar-pr.md` (35 de 80 lineas) con los 3 modos, el orden fijo conflictos->hilos->CI, la clasificacion del CI rojo (flake / base vieja / real), la rubrica fix/dismiss/ask + `.saikit/triage-patrones.md`, esperas sin bloquear, tope 2 rondas, descubrimientos se reportan y «cuidar nunca mergea»; manifiesto regenerado y `test_recetas.sh` verde. **Corregido en la revision del lead**: los pasos NUMERADOS contradecian el orden fijo que la propia receta declara (Conflictos 3, CI 4, Hilos 5), asi que un modelo que sigue la numeracion invertia el orden — arreglado, con regresion dedicada en `test_recetas.sh` que ubica los tres pasos por numero de linea y exige conflictos < hilos < CI (rojo medido: «hilos (linea 19) no va antes que CI (linea 15)») |
| 18.3 | `[Veredicto]` `[lane:gate]` `[tdd:required]` **`.saikit/veredictos/<sha>.json` sellado por el hook** (D16): el líder commitea (incluido el rastro) ANTES de despachar al reviewer; el reviewer graba `sha = git rev-parse HEAD` + campos (`verify_app` con `comando`); **el hook registra `veredicto_sha256` del `tool_input.content` en el `Write` atribuido al reviewer sobre `veredictos/`** (registro de estado, sin check nuevo en el Stop); esquema + test de contrato; `.gitignore` de `veredictos/` creado por el hook; contrato: `Close:` cita sha y ruta | Rojo medido; `tests/test_veredicto_contract.sh`: válido / `sha` ≠ HEAD ⇒ inválido / campo faltante ⇒ inválido; casos de gate: `Write` del reviewer registra el hash / `Write` de otro rol NO lo registra / `Edit` posterior deja el archivo con hash distinto al registrado; mutación `sello_veredicto_apagado`; golden regrabada con diff auditado (4 de 5) | 17.4, 18.1 | cc:完了 [PR #140, merge `15af87a`] — **CIERRE PARCIAL; lo que falta es la fila 18.13**: el hook SELLA (`veredicto_sha256` = sha256 del `tool_input.content` en el `Write` atribuido al reviewer sobre `veredictos/`, registro de estado sin check nuevo en el Stop), `tools/lib/veredicto_contract.sh` valida el esquema, `tests/test_veredicto_contract.sh` cubre contrato y gate + mutacion `sello_veredicto_apagado`, golden regrabada (escenario 56). Verificado por el lead con medicion propia: el hash sellado es IGUAL al sha256 del contenido decodificado (237 bytes, sin salto final), y la tool `Write` materializa byte a byte sin agregar salto y preservando UTF-8 crudo — que es el supuesto del que depende la 18.4. **Corregido en la revision del lead**: el sello NO TENIA PRODUCTOR — ningun perfil, receta ni contrato mandaba escribir el veredicto (0 menciones en `agents/`, `skills/`, `recetas/`, `hosts/`, `tools/`), o sea la leccion de 17.5 otra vez; `agents/reviewer.md` gana `## El veredicto sellado` con el esquema, el sha de `git rev-parse HEAD`, la exigencia de la tool `Write` (Edit/Bash dejan el archivo y NO sellan) y la prohibicion de reescribirlo, atado por `productor_el_perfil_del_reviewer_pide_el_veredicto` (rojo medido: 16 aserciones). **Cross-review**: CodeRabbit («clave como valor») y codex («repo fresco sin .saikit») refutados MIDIENDO, cada uno con su regresion; el hilo de CodeRabbit contestado y resuelto. **NO ENTRA y pasa a la 18.13**: la linea `Close:` del contrato y el «el lider commitea ANTES de despachar», porque las dos tocan el TEXTO DEL CONTRATO y eso exige regrabar la golden desde la maquina que grabo la actual |
| 18.4 | `[Merge]` `[lane:gate]` `[tdd:required]` **`tools/saikit-merge.sh` fail-closed y acotado** (D18). **AJUSTADO POR LA DECISION DEL 2026-08-30:** el script hace todas sus comprobaciones y, con todas en verde, **NO mergea: reporta que esta listo y termina**. El merge ocurre en una invocacion aparte que solo existe si el operador dijo que si (`--confirmado`). **El sí confirma la INTENCIÓN, no las condiciones** (CodeRabbit, PR #119): entre el «listo» y el «sí» la base puede moverse, el CI re-correrse o el head cambiar, así que `--confirmado` **repite el gate completo** — mismo sha Y base al día Y CI verde Y veredicto sellado — y si algo cambió, vuelve a «listo» y avisa en vez de mergear. Las comprobaciones NO se aflojan por esto: siguen siendo la condicion para siquiera ofrecer el merge. Contrato original: repo del cwd, PR de la rama actual, `baseRefName == config.rama`, `headRefOid == sha`, autor del PR = cuenta de gh; `git fetch origin <rama>` + rama al día con la base (`merge-base --is-ancestor`); config leída de `origin/<rama>`; `mergeable: UNKNOWN` ⇒ un reintento; `verify_app: PASS` con comando bajo `verify/` o `n/a` solo con `sin_verify_app: true`; cruce con el estado del hook (`reviewer` en `agents_seen`, **`veredicto_sha256` == sha256 del archivo actual**, comando del blast en `harness-evidence.log` con éxito); commits del rango solo del `user.email` local o de la cuenta de gh; `gh pr merge --squash --match-head-commit <sha> --body "Saikit-Merge: <sha>"` sin `--delete-branch`; borrado remoto aparte; registra `merge_commit` en `veredictos/<sha>.merge` (el veredicto sellado no se toca); `--dry-run`; **modo `--revert-de <merge_commit>`** (D19): sin estado del hook y sin confiar en JSON local — exige que sea la punta de `origin/<rama>` tras fetch + trailer `Saikit-Merge:` + **igualdad exacta de árboles** (`<head>^{tree}` == `<merge_commit>^^{tree}`; `patch-id` solo como extra) + ningún otro commit + CI verde + `--match-head-commit` | `tests/test_saikit_merge.sh` con `gh` falso y git real en sandbox: todo ok ⇒ merge con trailer y el veredicto sellado queda byte-idéntico; config con `rama: main` ⇒ funciona igual; CI rojo / sin checks / pendiente / `mergeable` UNKNOWN dos veces / base avanzada / veredicto de otro sha / **veredicto reescrito tras el sello** / commits después del veredicto / `blast.nivel<4` / `verifier: FAIL` / `verify_app: n/a` sin `sin_verify_app` / Drive fuera de `verify/` / config ausente o `unknown` / PR que toca `autopilot.json` / PR de otra rama base / repo distinto / autor del PR ≠ cuenta / commit de otro email / sin estado del hook ⇒ NO mergea y nombra la razón; merge ok + borrado remoto falla ⇒ reporta sin reintentar; `--revert-de`: inverso exacto de la punta con trailer ⇒ merge; árbol distinto (incluido un cambio extra solo de whitespace, que `patch-id` no ve) / commit extra / sin trailer / no es la punta ⇒ NO; nunca `--admin` | 18.3 | cc:完了 [PR #142, merge `f1063af`, CI 9/9 en el head `2c58ea1` (run 33699818088); implemento GLM, reviso el lead] — **CIERRE**: `tools/saikit-merge.sh` fail-closed y acotado. Por defecto NO mergea: corre el gate entero y reporta `LISTO:`; `--confirmado` REPITE el gate completo (la decision del 2026-08-30: el si confirma la INTENCION, no las condiciones) y vuelve a `NO-MERGE` si algo se movio; `--dry-run`; `--revert-de` con punta de `origin/<rama>`, trailer, igualdad EXACTA de arboles (`patch-id` solo como extra) y un unico commit. Merge por `gh pr merge --squash --match-head-commit` con trailer, sin `--delete-branch` y **nunca `--admin`**; borrado remoto aparte y sin reintentar. **Endurecimiento del contrato del veredicto** (lo unico existente que la fila autorizaba tocar): se cerro el limite POC con un parser JSON real en awk, **sin `jq`**, estricto RFC 8259. **Punto de diseño resuelto y DECLARADO**: el `veredicto_sha256` vive en el estado de SESION y el script no conoce su `session_id`; se buscan los `harness-state.env` del proyecto (clave = `cksum` de la raiz fisica) que lo traigan y se toma el de mtime mas reciente — fail-closed, porque el sello de una sesion equivocada no calza con el sha256 del archivo actual. Override: `SAIKIT_ESTADO_ROOT`. **Verificacion independiente del lead** (no se tomo por bueno el PR): suite corrida entera (34 casos OK) y **las 18 mutaciones del banco atrapadas** (18/18, cero sobrevivientes, CONTADAS sobre el banco; la tabla del cuerpo del PR listaba 15 y estaba incompleta — el juez es el banco, que falla si alguna sobrevive); el parser fuzzeado contra el modulo `json` de Python con 33 casos adversarios ⇒ **cero discrepancias**. **Tres hallazgos cerrados por el lead sobre la entrega**: (a) en `--revert-de` el registro del merge escribia en un directorio que puede NO existir (`.saikit/veredictos/` lo crea el hook y su `.gitignore` lleva `*`, asi que no viaja en un clon fresco) y, sin `set -e`, `MERGE-OK` anunciaba un registro inexistente — ademas NO habia ningun caso feliz de `--revert-de`, asi que esa rama no se ejercitaba nunca; (b) una clave con escapes `\uXXXX` burlaba las DOS guardas del parser (duplicada disfrazada y clave que decodifica a `.`, el separador del flat), y la cabecera AFIRMABA rechazarlas — se rechaza toda clave con escape, fail-closed; (c) un hallazgo del bot descartado MIDIENDO (el caso del arbol acredita por la razon correcta: la punta se mira en `origin/<rama>`, que el push rechazado de la rama de revert no toca). **Residual DECLARADO, no arreglado**: `correr_mutacion` no comprueba que el caso este VERDE con el script SANO antes de mutar; hoy no enmascara nada (medido con una sonda: ningun caso esta rojo en sano), pero un caso siempre-rojo acreditaria su mutacion en falso. Guarda de ~6 lineas para cuando se toque ese banco. **Nota de proceso**: se toco `tests/test_adversary_lock.sh` (fuera del brief) por un flake medido en dos corridas de CI, en commit aparte y replicando el patron del sub-caso vecino — declarado, no escondido |
| 18.5 | `[Post-merge]` `[lane:gate]` `[tdd:required]` **`tools/saikit-postmerge.sh`** (D19), turno desarmado. **REDUCIDA POR LA DECISION DEL 2026-08-30:** sin merge desatendido no hay nada que revertir sin aviso, asi que el revert automatico SE CAE. Lo que queda es el aviso: tras un merge confirmado por el operador, mirar el run y la salud y **decirle** si algo se rompio, con el comando de revert listo para copiar — sin ejecutarlo. Se declara lo que se pierde: si el operador no mira el aviso, nada deshace el cambio solo. Contrato original, para cuando se quiera reabrir: run por `gh run list --commit <merge_commit>`; sin run ⇒ `unknown` con heartbeat acotado, timeout ⇒ `unknown` sin revert; `GET salud_url` solo http(s), sin redirects, `--max-time`, redactada; rojo ⇒ `git revert <merge_commit>` (squash) SOLO si es la punta de `origin/<rama>` con trailer `Saikit-Merge:`, PR de revert mergeado con `saikit-merge.sh --revert-de`, una vez; si algo aterrizó después o el revert no puede mergearse ⇒ avisa que la rama está roja y cómo revertir a mano; mensaje en español desde el `Close:` redactado (≤4096); `telegram-send` solo con `telegram: true` | `tests/test_saikit_postmerge.sh` (DoD reescrita por la decisión del 2026-08-30): verde ⇒ mensaje; rojo o salud caída ⇒ **AVISA con el comando de revert listo para copiar, y el caso verifica que NO lo ejecutó** (el árbol queda intacto); sin run aún ⇒ `unknown`; merge_commit sin trailer o que no es la punta ⇒ el comando propuesto lo dice y no se ofrece revert | 18.4 | cc:完了 [PR #161, merge `7b6dc3a`, CI 9/9 en el head `282cc5f` (run 33836630687); implemento GLM, reviso y cerro el lead] — **el AVISO, sin revert automatico** (reduccion del 2026-08-30). Mira el run y la salud y entrega el comando de revert listo para copiar, y el caso verifica que NO lo ejecuto mirando que el arbol quedo intacto, no una variable. Sin run ⇒ `unknown`; merge_commit sin trailer `Saikit-Merge:` o que ya no es la punta ⇒ lo dice y no ofrece revert. **Hallazgo del lead cerrado**: la `salud_url` con esquema roto se imprimia CRUDA por una rama que no pasa por `aviso()` — la unica que aplica `redactar` — con lo que un token en el query string llegaba al terminal y a cualquier log. **Hallazgo estructural cerrado**: la guarda anti-sed-obsoleto de las dos baterias estaba muerta (`mut_sed` reescribia `HERE=` siempre, asi que el `cmp -s` contra el original nunca podia dar iguales y una mutacion que dejara de aplicar pasaba en falso); ahora baselinea contra `BASE`. Las 14 mutaciones se verificaron aplicando de verdad, una por una |
| 18.6 | `[Sentinel+Contrato]` `[lane:gate]` `[tdd:required]` **`-saikit:autopilot`** ⇒ full + `autopilot=1` en estado (D15) y párrafo del contrato. **AJUSTADO 2026-08-30:** el párrafo ya no puede decir que mergea solo. Dice que prepara todo, que **para y pregunta antes de publicar**, y que el merge solo ocurre con el si del operador. El sentinel sigue siendo POR TURNO: no hay permiso permanente, y esa es la diferencia con el *full-autonomy grant* de pstack. Contrato original: (D21: qué hará solo, qué nunca; merge antes del recibo y SOLO por `tools/saikit-merge.sh`, nunca `gh pr merge` a pelo; `Close:` con sha mergeado o razón de no-merge) + la excepción escrita en la regla de preguntar (D7: lo autorizado en el setup no se vuelve a preguntar); **sin checks nuevos en el Stop** | Rojo medido; caso arma-autopilot / sufijo desconocido ⇒ full sin flag; mutación; golden regrabada con diff auditado (5 de 5) | 16.4, 18.4 | cc:完了 [PR #162, merge `f62a540`, CI 9/9 en el head `45cc4e1` (run 33841920056); implemento Cursor, reviso y cerro el lead] — el sentinel `-saikit:autopilot` arma FULL y marca `autopilot=1`, con el parrafo del contrato emitido por los TRES emisores (harness_context, build_gate_feedback y el del presupuesto agotado, este ultimo hallado por su review adversarial). El parrafo dice que PREPARA y PARA A PREGUNTAR antes de publicar: el merge solo con el si del operador y solo por `saikit-merge.sh` (decision del 2026-08-30). La golden se movio solo en la cabecera de identidad: el parrafo es condicional y ningun escenario lo arma, asi que los 55 quedan byte a byte. **Cuatro hallazgos cerrados por el lead** (tres suyos, el cuarto del cross-review de kimi): el parrafo salia DUPLICADO en grok (atado con un caso que CUENTA ocurrencias, no que busca la cadena); faltaba el caso de precedencia contra el carril alias, mas su mutacion `autopilot_no_pisa_alias`; el tercer emisor quedaba sin mutacion; y **el flag no estaba protegido en los rewrites de mitad de turno** — hallazgo del cross-review de kimi sobre las conclusiones del lead, medido: quitar el flag del `write_state` de `mark_evidence` dejaba la bateria en OK con 0 fallas. Cerrado con `caso_g1_autopilot_sobrevive_mark_evidence` y `..._record_agent`, que exigen ADEMAS que `verified` pase a 1 (sin esa segunda asercion el caso pasaria sin ejercitar el rewrite); con ellos la misma mutacion ahora muere. **Residual declarado**: `verdict_registrar_sello` y `adv_reescribir_estado` siguen sin caso — son los dos rewrites menos transitados |
| 18.7 | `[Setup]` `[lane:gate]` `[tdd:required]` **`saikit-setup-autopilot`**: 5 preguntas en español ⇒ `.saikit/autopilot.json` (esquema + test; `merge_despliega` nace `unknown`; `sin_verify_app` y `telegram` nacen `false`); lock en `$(git rev-parse --git-common-dir)/saikit-autopilot.lock` (D20): `mkdir` atómico con `pid`/`host`/`started_at`/`pr`, `rmdir` en `trap EXIT`, lock viejo se reporta y bloquea (nunca se borra solo), `--liberar-lock` explícito; `.gitignore` idempotente de `veredictos/`; el instalador planta la skill (Windows-bound) | `tests/test_autopilot_config.sh`: JSON válido/inválido; defaults; contención real con dos worktrees del mismo repo ⇒ el segundo espera o reporta, nunca dos merges; lock viejo (pid muerto) ⇒ reporta y bloquea; `--liberar-lock` lo quita | 18.1 | cc:完了 [PR #161, merge `7b6dc3a`, CI 9/9 en el head `282cc5f` (run 33836630687); implemento GLM, reviso y cerro el lead] — asistente de 5 preguntas ⇒ `.saikit/autopilot.json`, con `merge_despliega` naciendo `unknown` (no `false`: «nadie contesto» no es «contesto que no»). Lock en `$(git rev-parse --git-common-dir)/saikit-autopilot.lock` con `mkdir` atomico, `rmdir` en `trap EXIT`, y **contencion real medida con dos worktrees del mismo repo**, no un mock. El lock viejo se REPORTA y BLOQUEA: solo `--liberar-lock` lo quita. **Hallazgos del lead cerrados**: la escritura de `autopilot.json` era destructiva (sobrescribia una config valida ANTES de validar la nueva) — ahora `mktemp` en el mismo dir, validar, `mv -f`, el patron que ya usaba `install-hook.sh:312`; y veto de `.saikit` y `veredictos/` como enlace simbolico. El `.gitignore` de `veredictos/` YA existia anidado, se declaro en vez de duplicarlo. La skill se planta y se verifico plantada en el perfil vivo de macOS |
| 18.8 | `[CI]` `[lane:gate]` `[tdd:required]` **CI mínimo cuando no hay** (D22, Required): el setup detecta ausencia de workflows y ofrece en español un workflow de Actions que corre el test del repo + `verify/` (acciones pinneadas, sin secretos); sin CI y sin aceptar ⇒ el autopilot lo dice y no mergea | Test del generador en `tests/test_ci_minimo.sh`: repo sin workflows ⇒ propone; con «si» ⇒ YAML valido que el candado `check-yaml` parsea. **DoD AMPLIADA el 2026-09-04 tras el adversario: la primera solo cubria el GENERADOR, y un implementador conforme al 100% podia entregar un generador huerfano que nadie llama y un autopilot que mergea igual — el patron exacto que obligo a abrir 17.5 y 18.12.** (1) **Se exige el CABLEADO, no se describe**: un caso prueba que `saikit-setup-autopilot.sh` INVOCA al generador cuando detecta ausencia de workflows; sin ese caso la fila no cierra. (2) **Se exige el camino de ACEPTACION completo** (cross-review kimi): el caso no puede terminar en «propone»; tiene que cubrir que el operador acepta y que el workflow queda ESCRITO donde el repo lo va a correr. (3) **Se exige el VETO**: sin CI y sin aceptar ⇒ el autopilot lo dice y NO mergea, con caso propio sobre el camino de merge, no solo sobre el texto. (3) `check-yaml` **solo parsea**: un workflow con `name: ci` y el comando de test en un comentario cumple los literales y no corre nada — el caso afirma que el workflow ejecuta el test del repo y `verify/`. (4) **«Sin secretos» se EXIGE, no se describe** (cross-review CodeRabbit): el alcance lo pide y ninguna asercion lo cubria — un generador podia emitir `${{ secrets.X }}` y pasar todos los casos. Caso: el workflow generado no referencia ningun secreto y corre sin configuracion secreta. (5) **«Acciones pinneadas» queda DEFINIDA: pin = sha completo.** Se cierra la ambiguedad a proposito, porque dejarla abierta es lo que deja elegir la opcion floja: un tag no fija la revision que se ejecuta, y este workflow se planta en repos ajenos. Caso: cualquier referencia que no sea sha completo pone el caso rojo. **Limite declarado**: el CI de este repo NO cumple esa regla hoy (`.github/workflows/quality.yml` usa `actions/checkout@v7` y otros tags moviles); arreglarlo NO es alcance de esta fila, pero queda anotado como deuda. Mutacion por cada guarda | 18.7 | cc:完了 [PR #185, merge `4051e69`, CI 9/9 en el head `32e3682` (run 33984924089); implemento Cursor, reviso y cerro el lead en dos rondas] — `tools/saikit-ci-minimo.sh` genera un workflow de Actions que corre `tests/run.sh` y `verify/`, con acciones pinneadas a sha completo, `permissions: contents: read`, sin secretos y sin guardar el token del checkout; `saikit-setup-autopilot.sh` lo INVOCA tras el JSON cuando no hay workflows (sin TTY asume «no» y avisa); el veto vive en el camino de merge de `saikit-merge.sh` («sin checks NO es verde»), con caso propio. Las cinco exigencias de la DoD estan y DISCRIMINAN: ~27 mutaciones auto-verificadas (`no_invoca_desde_setup`, `mete_secrets`, `escribe_tag_no_sha`, `default_no_escribe_igual`, `ci_rojo_es_verde`, `sin_checks_n_opcional`...). **Cuatro hallazgos de CodeRabbit, verificados y cerrados**: el awk de `scripts.test` no cerraba el alcance del objeto (un `"test":` en `jest` fuera de `scripts` disparaba `npm test`; reproducido y ya no); sin paso de instalacion un checkout fresco no encontraba el runner (ahora `npm ci` con lockfile); sin minimo privilegio; y una carrera entre la comprobacion de ausencia y la publicacion (ahora rechequeo + `mv -n`). Rastro `.saikit/decisiones/18.8.tsv` presente. **Limite declarado**: el CI de este repo usa tags moviles y no cumple la regla de pin que impone a los repos ajenos; queda como deuda, no como alcance |
| 18.9 | `[Medición]` `[lane:release]` `[tdd:skip:medición]` **Tres escenarios en vivo** (D23) en el descartable con Actions reales: verde ⇒ merge; CI rojo ⇒ no merge con razón; post-merge rojo ⇒ revert; costo por PR (tokens/tiempo) vs ceremonia actual; `docs/smoke-autopilot-<fecha>.md` | 3 de 3 con evidencia literal (sha, run, PR de revert); costo medido, no estimado | 18.2–18.8 | cc:完了 [medición viva 2026-09-05, `docs/smoke-autopilot-2026-09-05.md`, repo `gon0801/saikit-descartable`, sesiones grok headless; midió el lead (sesión kimi), PR de docs por cerrar] — **CIERRE**: esc2 (CI rojo ⇒ no merge con razón: PR #5 cerrado sin mergear, razón literal) y esc3 (post-merge rojo ⇒ aviso ROJO exit 1 sin ejecutar + revert por `saikit-merge.sh --revert-de`: MERGE-OK `48aee6f`, PR #7) observados con evidencia literal. **esc1 (verde ⇒ merge) NO observado — se cierra declarándolo, no en verde**: el gate fail-closed cortó en cadena con 4 razones nombradas (parse de `gh` bajo el terminal del agente, `sin_verify_app`, base avanzada, veredicto viejo) y debajo quedó el gap que lo hace imposible en grok hoy: el sello no registra `veredicto_sha256`. El comportamiento del agente fue exactamente el pedido (ceremonia completa en orden, parar y preguntar, merge SOLO por la vía autorizada, NO-MERGE literal, sin atajos). Costo medido: ~120-230k tokens y 8-18 min de pared por PR. Los huecos medidos abren 18.25 (parse gh), 18.26 (sello grok) y 18.27 (recibo headless); el happy path en claude queda pendiente de la quota del operador, declarado |
| 18.10 | `[Release]` `[lane:release]` `[tdd:skip:docs]` **Cierre**: spec § límites medidos, **guía de usuario al día y COMPLETA** (autopilot y `cuidar-pr` a "ya funciona"; qué hace solo, qué NUNCA hace, y cómo deshacerlo — la sección que el operador va a leer cuando algo salga mal), README, deploy-log, retro de la fase; el descartable se borra SOLO con topic + marcador verificados (o a mano por el operador, recomendado) | Coherencia; deploy-log con el sha; descartable borrado y anotado, o entregado al operador; **ninguna pieza de las tres fases sigue marcada "en camino" en la guía, y la sección de deshacer nombra el paso exacto** | 18.9 | cc:TODO |
| 18.11 | `[Guardia]` `[lane:gate]` `[tdd:required]` **`PreToolUse` que niega el merge a pelo** (D24, **Recommended**). **SIGUE VIGENTE tras la decision del 2026-08-30, y con mas razon:** si el merge exige el si del operador, un `gh pr merge` a pelo es exactamente la via para saltarselo. La guardia deja de proteger un automatismo y pasa a proteger el punto donde el humano decide. Contrato original: medir primero que un `PreToolUse` de Claude Code con `decision: deny` detiene un `Bash` cuyo comando matchea `gh pr merge` / `gh api … /merge` / `git push … <rama>`; la vía permitida es por **forma canónica del comando** (invoca `tools/saikit-merge.sh` y el archivo tiene el sha256 del manifiesto del kit), NUNCA una variable de entorno (el `PreToolUse` corre antes del comando; la marca no existe en el evento); límite declarado: match de texto, un `gh` dentro de otro archivo no es visible; si confirma: registrar la fase (checker pasa de 3 a 4 fases, advisory), casos + mutación + golden; si no confirma: límite declarado y cierre con `unknown` | Medición citada; con deny confirmado: rojo medido, caso que niega el directo + caso que deja pasar al script con hash correcto + caso que niega al script con hash distinto + mutación + golden 0 divergencias; `check-hook-registration.sh` reporta la 4ª fase | 18.4 | cc:完了 [PR #198, merge `7009b74`, CI 8/8 con `gate` verde en el head `cf7f4a0` (run 34001332338); implemento Cursor, reviso y cerro el lead en 2 rondas (sesion kimi)] — fase `pretool` nueva en el hook: deny por `permissionDecision` a `gh pr merge` / `gh api …/merge` / `git push … master\|main` (incl. force, delete, `-C`, `+`, `:`); la via permitida es FORMA CANONICA por partida doble (token con basename exacto `saikit-merge.sh` + sha256 contra `tools/MANIFEST.sha256`), fail-closed sin manifesto o con hash distinto, sin bypass por env. R1 del lead: 1 HIGH medido (bypass del hatch por sufijo `.bak`: hasheaba el script real y ejecutaba el troyanizado) — cerrado con basename exacto + guarda anti-spoof y mutacion `pretool_hatch_spoof_apagado`; driver `test_pretool_merge.sh` 21/21; golden 55 escenarios, 0 flips, identidad byte a byte. Checker reporta la 4ta fase (advisory, fuera de ESPERADAS). **Residual del lead: el deny vivo queda `unknown`** — el registro de `PreToolUse` en `~/.claude/settings.json` es operator-owned y quedo INERTE hasta que el operador lo registre (el checker lo grita: «MERGE A PELO: PreToolUse no esta registrado»). Limites declarados: match de texto (un `gh` dentro de otro archivo no es visible; `gh 'pr' merge` pasa), matcher solo Bash (script+manifiesto reescritos con Write excede la fila), `git push` sin dest explicito pasa |
| 18.12 | `[Gate]` `[lane:gate]` `[tdd:required]` **El hecho único se EXIGE, no se describe** (hallazgo de 17.5, ausencia medida): en carril full el contrato pide el rastro (`.saikit/decisiones/<task>.tsv`) y el blast (`.saikit/findings/blast-<task>.json`) o su skip NOMBRADO con razón en el recibo; y `install-hook.sh` planta los tools (`saikit-decision.sh`, `saikit-blast.sh`, `lib/redactar.sh`) donde el repo del usuario los pueda correr — en 17.5 los perfiles los describían y NADA los jaló (0 invocaciones en 2 turnos vivos), y sin copiado manual ni existían. Sin esto el autopilot no tiene el hecho único (D13) que su adjudicación necesita | Caso de gate: turno full sin tsv/blast y sin skip declarado NO cierra limpio (rojo/verde + mutación acreditada + golden); test del instalador: tras instalar en un repo fresco, el comando que enseña `verifier.md` existe y corre | 17.3, 17.4, 16.5 | cc:完了 [PR #204, merge `70f3a94`, CI 8/8 con `gate` verde en el head `1881f87` (run 34008635230); implementó Cursor, revisó y cerró el lead (sesión kimi) en 2 rondas] — **CIERRE**: en carril full el Stop exige el rastro `.saikit/decisiones/<task>.tsv` y el blast `.saikit/findings/blast-<task>.json` o un `TRAIL SKIP: <razón>` NOMBRADO en el recibo; sin uno ni otro el turno NO cierra limpio (gate nuevo en `hooks/summonaikit-harness.sh`, casos en `tests/lib/trail_gate_cases.sh`, driver `tests/test_trail_gate.sh`, fixtures full-lane 57/58). `install-hook.sh` planta los 3 tools (`saikit-decision.sh`, `saikit-blast.sh`, `lib/redactar.sh`) de forma atómica e idempotente en `~/.claude/saikit-tools` (tests `test_trail_install.sh` + `test_trail_install_mutations.sh`). La ronda 1 del lead encontró rojos reales en el head anterior — `test_adversary_lock` y `test_gate_behavior` fallaban y la línea «Commit BEFORE dispatching the reviewer» de la 18.13(b) había quedado borrada de los fixtures; Cursor los reparó en `1881f87` y el lead re-midió en local con `SAIKIT_HOOK_VIVO` al hook del PR: ambos RC=0, línea restaurada en :1304 y en `trail_parrafo`. Mutaciones G8 nuevas acreditadas (`trail_skip_preambulo`, `trail_close_preambulo`). Rastro `.saikit/decisiones/18.12.tsv` y findings adversary/blast viajan en el merge. Deploy REAL post-merge 2026-09-05 (el hook cambió): las 4 copias REPARADO con backup + tools publicados en claude, `check-hook-registration.sh` RC=0 — deploy-log 2026-09-05 |
| 18.13 | `[Contrato]` `[lane:gate]` `[tdd:required]` **Lo que la 18.3 no pudo cerrar sin regrabar la golden** (revision del lead sobre el PR #140). Las tres piezas comparten UNA SOLA regrabacion, asi que es un solo viaje a la maquina que grabo la linea base: **(a)** el contrato pide que la linea `Close:` del recibo CITE el sha y la ruta del veredicto sellado — alcance original de la 18.3, documentado en `docs/phase-18-autopilot-plan.md` §4.1 y NO entregado; **(b)** el contrato instruye al LIDER a commitear (incluido el rastro) ANTES de despachar al reviewer — hoy `agents/reviewer.md` lo DA POR HECHO («el lider ya commiteo antes de despacharte») y NADIE lo instruye: 0 menciones en `recetas/00-lider.md` y en el perfil, medido; **(c)** el `Write` del veredicto se acredita como edicion de codigo y como `implemented` — `rn_is_noncode_path` no excluye `.saikit/veredictos/*.json`, asi que un turno de SOLO revision reporta trabajo que no existio (la golden del escenario 56 lo muestra: `implemented=1` y `last_code_edit=1`). Hoy NO dispara la falsa alarma de «codigo tocado despues del review» porque `rn_mark_review` y `rn_mark_code_edit` caen en el MISMO contador y el check compara con `-gt`; eso se declara y se ata, no se deja a la suerte. **Ademas, medido y sin arreglar**: la cabecera de `tests/golden/baseline.txt` declara `hook_bytes: 187898` / `hook_lineas: 3116`, pero el hook de `master` Y el DESPLEGADO son 187373 / 3114 (sha `4127f79a`) — la linea base se grabo desde un hook que ya no existe en ninguna forma, ni en el repo ni en el perfil vivo. El `--check` pasa porque compara COMPORTAMIENTO (55/55) pero avisa «el hook cambio de identidad», asi que la nota le salta a todo el mundo. **Nota de metodo, pagada cara en esta revision**: `resolver_hook_bajo_prueba` prefiere el hook VIVO sobre la fuente del repo, asi que una bateria corrida con un vivo DESACTUALIZADO ejercita otro hook y sus rojos se leen como «brecha de plataforma». Se perdio media revision en ese diagnostico falso; el deploy tras merge (AGENTS.md) es justo lo que lo evita, y `SAIKIT_HOOK_VIVO` es la salida cuando el vivo no se puede deployar | Rojo medido en (a), (b) y (c); golden regrabada con **diff auditado** que toca SOLO el texto del contrato, la cabecera y los escenarios que el cambio de (c) mueva; `test_veredicto_contract.sh`, `test_recetas.sh` y la bateria del gate siguen verdes; la identidad de la cabecera queda IGUAL al hook de `master`, o se declara en la fila por que no puede | 18.3 | cc:完了 [PR #154, merge `59d45b4`, CI 9/9 en el head `57e32de` (run 33728651684); implemento qwen, reviso el lead] — **CIERRE, las tres piezas en UNA regrabacion**: (a) el `Close:` sustantivo cita el sha y la ruta del veredicto sellado; (b) bullet en la Delegation rule: «Commit BEFORE dispatching the reviewer — the code AND the trail», cerrando el supuesto que `agents/reviewer.md` daba por hecho; (c) el `Write` del veredicto deja de acreditarse como trabajo. **Diagnostico de la fila CORREGIDO por qwen**: (c) tenia DOS mitades y la fila atribuia todo a `rn_is_noncode_path` — `last_code_edit` sale de ahi, pero `implemented` nace del GREP LAXO del PostToolUse. Fix en las dos: exclusion de `.saikit/veredictos/` (absoluta y relativa) + flag `vd_write_reviewer` que guarda el grep y que el bloque del SELLO reusa — una sola fuente de verdad, misma condicion. **Verificacion independiente del lead**: suite OK con la fuente de la rama; 5 mutaciones atrapadas; y la **discriminacion cruzada de (c) medida a mano**: quitar la exclusion pone rojo SOLO `verdict_write_no_marca_code_edit`, quitar la guardia pone rojo SOLO `verdict_write_no_acredita_implemented` — un caso que mirara una sola cosa habria dado verde con la otra mitad rota. **Los dos bloques de recibo (la trampa)**: la clausula va SOLO en el Close sustantivo; las formas desnudas son lo que el gate chequea (etiqueta+dos puntos, nunca contenido) y ponerla en una sola habria creado la inconsistencia que la fila advertia. **El detalle atado**: `verdict_turno_solo_revision_sin_falso_aviso` fija que un turno de solo revision no dispara «codigo tocado despues del review», por cualquiera de los dos mecanismos. **Golden**: 64 hunks, todos explicados hunk por hunk (cabecera, propagacion del contrato, grok con contrato adosado, y SOLO el escenario 56 por (c)). **La deriva fantasma que esta fila documentaba quedo CURADA**: tras el deploy, hook vivo y cabecera coinciden byte a byte (`36b436ae`, 188880 bytes, 3134 lineas) y el `--check` pasa por primera vez SIN el aviso de identidad. **CodeRabbit (`sha256sum` sin coreutils) descartado con medicion**: macOS lo trae de fabrica en `/sbin` (binario de Apple) y las llamadas son preexistentes de la 18.3/18.4; residual declarado solo para macOS antiguo. **No entra**: `test_recetas` sigue con 5 FAIL preexistentes por `sed -i` GNU en BSD sed — territorio de la 18.16, verificado que este cambio no los mueve |
| 18.14 | `[Instalador]` `[lane:gate]` `[tdd:required]` **`agente_traducido` tiene DOS incompatibilidades con el awk BSD, no una** (medido el 2026-09-02, awk BSD version 20200816; la segunda la encontro kimi en la ronda 2 del cross-review y se le habia pasado a codex, a grok y al lead). **(a) La regex**: `local omitir='a^'` (`:930`) es un idiom de GNU awk; en BSD es error de sintaxis (`awk: syntax error in regular expression a^`). **(b) El `-v` MULTILINEA**: la llamada `awk -v omitir=... -v inyectar=...` (`:940`) recibe un `inyectar` de DOS lineas (`model:` + `effort:`) cuando el router da las dos claves, y BSD awk lo rechaza con `awk: newline in string ... at source line 1` **ANTES** de evaluar ninguna regex. Comprobado aparte: `awk -v x=$'a\nb' 'BEGIN{print}'` falla igual. **Radio MEDIDO por host, y aca esta lo que importa**: `--host kimi` muere por **(a)** (su `inyectar` va vacio); `--host claude` muere por **(b)** (`awk: newline in string model: claude-sonnet...`, exit 2) y **NUNCA llega a (a)**; `grok` tiene `inyectar` multilinea (`model: grok-4.6` + `effort: medium`), asi que sus llamadas (`:1045`, `:1109`) tambien moririan por **(b)** — hoy no se ve porque muere antes en el preflight de `bash.exe`, pero **grok TAMBIEN necesita esta fila**, al reves de lo que decia la version anterior; `zcode` tiene `inyectar` vacio, asi que le toca **(a)**, tras salvar sus bloqueos previos (18.15). `dsh` y `codex` no llaman a esta funcion (grep, 0 resultados). **Consecuencia dura**: un fix que arregle SOLO la regex deja rotos a claude y a grok, que la propia fila declara en su radio | Rojo medido de **las dos** incompatibilidades ANTES del fix, con su mensaje literal distinto cada una. El DoD tiene que discriminarlas por separado: un caso con `inyectar` VACIO (forma kimi) y otro con `inyectar` MULTILINEA (forma claude/grok) — con solo el primero, arreglar la regex pasa el gate y (b) sobrevive. Ademas un FIXTURE con linea vacia DENTRO del frontmatter, porque `omitir` solo aplica con `n == 1` (`:947`) y los perfiles reales no tienen lineas vacias ahi: sin ese fixture, cambiar `a^` por `^$` pasaria igual. **Donde corre la acreditacion se DECIDE aca y se declara**: hoy `test_install_hook` se saltea en CI Linux (`tests/run.sh:126`). Que el awk de Windows sea GNU y acepte AMBAS formas es PREMISA NO MEDIDA (kimi, VERIFICAR): medirla es parte de la fila, no suponerla | — | cc:完了 [PR #150, merge `802611d`, CI 9/9; implemento qwen, reviso el lead] — **CIERRE**: `agente_traducido` deja de depender de una regex «que no matchea nada» (`omitir=''` mas una guarda `omitir != ""`, en vez de buscar otro idiom portable) y los valores viajan por **ENVIRON** en lugar de `awk -v`, que es lo que el awk BSD rechazaba cuando `inyectar` traia dos lineas. Las dos soluciones son portables de raiz, no parches. **Verificacion independiente del lead**, medida y no leida: `--host kimi` y `--host claude` pasan de morir con su error de awk a salir exit 0 sin error; `zcode` pasa a morir en su bloqueo previo (territorio de la 18.15). Delta del test: **230 FAIL en master a 61**, y los de awk de **45 a 2**. **Las TRES mutaciones atrapadas, cada una SOLO por su caso**: restaurar `a^` rompe el caso de kimi; volver a `-v` rompe **unicamente** el multilinea (el de kimi queda VERDE, que es lo que demuestra que los dos casos son independientemente necesarios — justo lo que kimi predijo en la ronda 2 y lo que un DoD flojo habria dejado pasar); y el fix ingenuo `^$` rompe el fixture de linea vacia. **Acreditacion DECLARADA**: corre en macOS con awk BSD; en CI Linux estos casos se saltean, y que gawk acepte las formas viejas queda como premisa NO medida. **Descubierto al implementar y derivado a la 18.16**: una CUARTA causa preexistente de fallo en macOS, `mtime_de` con `stat -c` de GNU |
| 18.15 | `[Instalador]` `[lane:gate]` `[tdd:required]` **Formas de comando de macOS para grok, dsh, codex y zcode** (medido el 2026-09-02). El cableado de esos hosts esta escrito SOLO en formas de Windows. **Bloqueos MEDIDOS, y NO son lista exhaustiva** (al implementar se enumeran del codigo, no de aca): **grok** fail-closed, exit 2 — exige `jq` (`:998`) y despues `bash.exe` para la forma PowerShell de 7.1 (`:1000-1002`), mas un JSON propio clasificable; **dsh** fail-closed — el chequeo que corta esta en `dsh_preflight` (`:1606-1611`), no en el override (kimi y qwen corrigieron esta cita); **zcode** cadena larga — `hooks.enabled` en `false`, mas `jq`, config existente y JSON valido, DEST `NUESTRO_IDENTICO` con el aislamiento de 5.3 (`:539-568`), despues `bash.exe`, y despues el awk de la 18.14; **codex NO es fail-closed, y ese es su defecto real**: `--host codex --dry-run` sale **exit 0**. `:171-177` solo DECIDE el DEST (la copia es del flujo generico) y la comprobacion registro→wrapper→hook es advisory (`avisar_registro` se traga el rc, `:259-264`). En macOS **instalaria con exito y no funcionaria**, porque el registro nombra un wrapper `.ps1` inexistente. Es fail-OPEN — **con aviso, no en silencio**: imprime `WRAPPER DE CODEX: no existe`, y llamarlo silencioso era mas ancho que la evidencia (kimi). **Trampa declarada**: `SAIKIT_ZCODE_BASH_WIN` (`:300`), `SAIKIT_GROK_BASH_WIN` (`:767`) y `SAIKIT_DSH_BASH_WIN` (`:1313`) son para MEDIR en los tests, no una via de produccion; `GROK_BASH_WIN` (`:748`) es interna, no un override | Las formas de macOS se MIDEN contra los CLIs reales y se declaran con su evidencia (comando y salida), como 7.1 hizo con las de Windows; ninguna se infiere de la otra, y lo que no se pueda medir se cierra `unknown`. **El DoD no se conforma con «el dry-run pasa»** (eso lo cumple un parche que en Darwin saltee la busqueda de `bash.exe` sin emitir NINGUNA forma): cada caso inspecciona la CONFIGURACION GENERADA y exige la forma medida. Para `zcode` se declara si `hooks.enabled` es precondicion del fixture o cambio de criterio. Para `codex` el criterio es OTRO: pasa a fail-closed cuando falta el wrapper, o se declara el limite. Un caso fija las formas de Windows byte a byte para que este cambio no las mueva. **grok y zcode no se pueden acreditar sin la 18.14**: al habilitar su forma de macOS chocan con `agente_traducido`. **Hallazgo para quien la retome (lead, 2026-09-03)**: el caso `18.15 zcode: sin 18.14 esperaba bloqueo a^` queda obsoleto tras la 18.14, pero el arreglo NO es borrarlo: su guarda `awk_omite_a_caret_ok()` pregunta lo equivocado. Sondea si el awk de la PLATAFORMA acepta `a^` — que en macOS sigue siendo que no — en vez de si el INSTALADOR todavia usa ese idiom. Debe sondear el instalador. **CONFIRMADO EN VIVO**: una implementacion WIP de esta fila (2026-09-02) midio que la rama POSIX del resolvedor funciona — las menciones de `bash.exe` en `test_install_hook` caen de 19 a 3 — pero el total de FAIL apenas baja de **230 a 229**, y el test NO llega a verde porque lo que queda son **18 fallos del bug de awk de la 18.14** (`agente_traducido fallo para grok/implementer`). Es lo que kimi predijo en la ronda 2. **La 18.14 va ANTES que esta fila, no despues**; el lead recomendo el orden inverso y estaba equivocado | 18.14 | cc:完了 [PR #153, merge `b945da4`, CI 9/9 en el head `a5d5511` (run 33728994135); implemento Cursor, midio y reviso el lead] — **CIERRE**: el resolvedor de bash gana la rama POSIX (destraba zcode y dsh de una), `grok_hook_cmd` deja el `&` de PowerShell fuera de Darwin, y codex planta un wrap POSIX que fija `SUMMONAIKIT_HOOK_TARGET=codex` y **falla cerrado** si la cadena no puede armarse (antes: exit 0 con el wrapper ausente). Las formas de Windows quedan fijadas byte a byte. Los casos inspeccionan la CONFIGURACION GENERADA, no el exit del dry-run. **Medicion en vivo, 4 de 4, el corazon de la fila**: codex lo midio Cursor con `CODEX_HOME` aislado (hooks disparan, marcador `FIRED tag=codex`). zcode, dsh y grok los dejo `unknown` porque exigian el perfil real y el brief lo prohibia — correcto — y **los midio el lead con el perfil real del operador** (2026-09-03, respaldos en el escritorio): **zcode** dispara de punta a punta (recibo completo de seis lineas y sintaxis literal de `ROLE FALLBACK`); **dsh** dispara y CORRE la ceremonia (estado durable bajo `~/.dsh/hooks/state/`, `lane=full`, `agents_seen=implementer`); **grok** dispara (recibo completo, reconoce el carril rapido). Evidencia literal en dos comentarios del PR. **Verificacion independiente del lead** con HOME aislado: codex planta hook y wrap con el TARGET correcto; zcode con el hook predesplegado cablea 4 fases con la forma POSIX exacta. **Corregido en la revision**: (1) la guarda `awk_omite_a_caret_ok` sondeaba la PLATAFORMA y no el instalador; ahora `instalador_usa_omitir_a_caret` grepea `$tool` — Cursor SI habia anticipado la 18.14, solo preguntaba lo equivocado; (2) hallazgo de CodeRabbit, confirmado: el wrap se escribia con `cat >` directo y una escritura a medias truncaba el previo sin que el rollback lo restaurara; ahora temporal EN EL MISMO dir + validar + `mv -f`, con caso que fuerza el fallo y mutacion medida por el lead (volver al `cat >` directo ⇒ ROJO). **Aprendizajes para el resto de la fase**: `zcode` exige `--prompt=` con el `=`, o toma el `-saikit` inicial como bandera; el estado del hook se escribe RELATIVO a la ubicacion del hook que corrio (dsh lo deja en `~/.dsh/hooks/state/`, no bajo `~/.claude`), asi que buscar solo ahi da un falso «no disparo»; un turno armado con `-saikit` corre la ceremonia ENTERA y tarda minutos — para medir el disparo, `-saikit:fast`. **Error del lead, declarado**: acuso a Cursor de revertir `Plans.md` por usar `git diff` con dos puntos en vez de tres; era falso, la rama era anterior a un merge y no tocaba el archivo. Retirado y disculpado en el PR |
| 18.16 | `[Tests]` `[lane:gate]` `[tdd:required]` **Cuatro tests no estan VERDES en NINGUN entorno si el proyecto se muda a macOS** (medido el 2026-09-02, tras decidir el operador que deja Windows). `tests/run.sh:124-131` saltea `test_capture_payloads`, `test_install_hook`, `test_probe_zcode_output` y `test_hook_acl` cuando `SAIKIT_CI_LINUX=1`, con su razon escrita y correcta. **Ese skip es una PROMESA de que alguien mas los corre** — y esa promesa era Windows. Sin Windows, nadie los corre. **Medido, los cuatro CORREN y FALLAN en macOS** — exit 1, o sea **FAIL, nunca `unknown`** (correccion del lead: la primera version decia «no corren» y es falso; `test_install_hook` sobre master ejecuta 144 casos y da 230 FAIL). Que corran y fallen es justo la distincion que el DoD de abajo exige, asi que la fila no podia describirla mal. Y NO fallan por la misma causa: `test_install_hook`, `test_capture_payloads` y `test_probe_zcode_output` mueren los tres por el mismo motivo que la 18.15 (`no encontre un bash.exe de Windows` para armar el command del registro), asi que son **aguas abajo de esa fila**, no trabajo nuevo; **Defecto de portabilidad propio y medido, y el PEOR de los dos porque no hace ruido** (hallado al implementar la 18.14, PR #150, y agravado por el lead al verificarlo): `mtime_de` (`tests/test_install_hook.sh:90`) usa `stat -c '%y'` de GNU con `2>/dev/null`, asi que en macOS devuelve **cadena VACIA** y las aserciones de `:123` y `:127` comparan "" contra "" y **PASAN EN FALSO** — no fallan, dejan de discriminar en silencio. El repo YA hallo y arreglo ese patron exacto en el cross-review de la 16.5 (el comentario vive en `:2331`: «daba cadena VACIA ante cualquier fallo y el caso comparaba "" contra "", verde sin haber medido nada»), pero lo cerro en UN solo sitio y este quedo vivo. **Otro defecto propio y medido**: `tests/test_install_hook.sh` invoca `python` (`:353`, `:400`, `:428`) y en macOS solo hay `python3`, asi que esos casos mueren con `python: command not found` aunque todo lo demas se arregle. **RESUELTO Y MEDIDO por el lead (2026-09-03), pendiente de entrar CON esta fila**: el arreglo NO es cambiar `python` por `python3` a ciegas — se resuelve `python3` con caida a `python`, y si NO hay ninguno se GRITA y se sale `unknown` (exit 3), jamas se sigue callando: un caso que no puede inspeccionar el JSON no puede afirmar nada sobre el. Son 14 invocaciones, incluidas 3 que la rama de la 18.15 ya habia resuelto a mano con `python3` fijo. **Cadena medida de `test_install_hook` en macOS**: 230 FAIL antes de todo ⇒ **61** con la 18.14 ⇒ **18** sumando la rama de la 18.15 ⇒ **1** con este arreglo ⇒ **0, `test_install_hook: OK`** al neutralizar un unico caso obsoleto. O sea: **las tres filas juntas ponen VERDE en macOS el test que hoy no lo esta en ningun entorno**, y este arreglo es el ultimo tramo. `test_hook_acl` es el distinto y el unico genuinamente de Windows: falla dentro de `tools/hook-acl.ps1:96` y su objeto son ACLs y SIDs (`S-1-5-18`), que en macOS no existen como concepto. **Consecuencia inmediata y concreta**: la acreditacion que exige la fila 18.14 vive en `test_install_hook`, asi que hoy **se EJECUTA pero no puede ACREDITAR nada**: da 230 FAIL, y una acreditacion necesita una linea base verde que ese test no tiene. Ejecucion y acreditacion no son lo mismo, y esta fila no puede confundirlas dos veces — el hallazgo que grok levanto en la ronda 1 deja de ser incidental y pasa a ser estructural. Y otros tres tools se quedan sin guarda sin que nada lo grite. **Alcance de esta fila**: NO re-implementar lo de la 18.15. Lo propio de aca es (a) que el skip deje de afirmar una cobertura que ya no existe, y (b) decidir y DECLARAR que pasa con `test_hook_acl` en un proyecto sin Windows | Decision POR TEST, escrita en la fila con su razon, sin dejar ninguno en limbo. Para los tres aguas abajo de la 18.15: caso que demuestre que con la forma de macOS puesta CORREN y pasan en esta plataforma. **Los tres estados NO se confunden** (hallazgo de CodeRabbit, y es la disciplina que `run.sh:150` ya declara — exit 3 es «no fallo, pero tampoco verifico nada»): un test que ARRANCA y termina en `exit 1` es **FAIL**, jamas `unknown`; `unknown` (exit 3) es **no haber VERIFICADO nada**, que NO es lo mismo que no haber ejecutado — `tests/run.sh:160-169` lo define asi: «el test no fallo, pero tampoco verifico nada». El caso del resolvedor de python de mas abajo es exactamente eso: el test SI corre, pero sin interprete no puede inspeccionar el JSON, asi que no observa nada y sale 3. La linea divisoria es si hubo OBSERVACION, no si hubo ejecucion. Confundirlos esconderia una regresion observada como falta de cobertura, que es exactamente la falla silenciosa que el conteo de `unknown` existe para evitar. Para `test_hook_acl`: se porta a la nocion de permisos de macOS, se reemplaza por una comprobacion equivalente, o se RETIRA con su razon — las tres son validas, dejarlo saltandose para siempre no lo es. **El mecanismo de skip cambia**: hoy dice SKIP y sigue; tiene que distinguir «lo corre otro» de «no lo corre NADIE» y, en el segundo caso, reportarlo como `unknown` en el resumen, que es la disciplina que `run.sh` ya aplica al resto (no cierra OK si todo fue unknown). **Dos casos del propio resumen**: uno con un test que CORRIO y salio `exit 1` ⇒ el resumen dice FAIL y el runner NO cierra OK; otro con un test SIN ejecutor ⇒ el resumen dice `unknown` y lo cuenta como tal. Rojo medido de los dos antes del fix | 18.15 | cc:完了 [PR #157, merge `ffd90a0`, CI 9/9 (run 33820109339); implemento Cursor, midio y reviso el lead] — **CIERRE, decision POR TEST como pedia la DoD**: los tres aguas abajo de la 18.15 (`test_install_hook`, `test_capture_payloads`, `test_probe_zcode_output`) CORREN y pasan en macOS — verificado por el lead sobre master, exit 0 los tres; los dos tools registradores ganaron la rama POSIX y grok perdio el call operator de PowerShell fuera de Windows. `test_hook_acl` **RETIRADO** (626 lineas) con su razon: su objeto son ACLs y SIDs de Windows, que en macOS no existen como concepto; portarlo seria inventar cobertura de una herramienta muerta. **El skip cambia de semantica**: ya no promete cobertura ajena. `SAIKIT_TESTS_SIN_EJECUTOR` declara los tests que NADIE corre, cuentan como `unknown` y el cierre en exit 3 se lee de `pasaron==0`. La lista queda VACIA hoy y el mecanismo queda atado con casos sinteticos en `test_runner_guards`. **Los dos defectos de portabilidad, cerrados**: `SAIKIT_PY` (python3 con caida a python; sin interprete GRITA y sale unknown) reemplaza 16 invocaciones; y `mtime_de` pasa a GNU con caida a BSD **exigiendo medicion NO vacia** — en macOS devolvia cadena vacia y las comparaciones pasaban EN FALSO, el patron que la 16.5 cerro en un solo sitio. **Mutacion medida por el lead**: el fix ingenuo (`stat -f` sin exigir no-vacio) deja 4 aserciones rojas, incluida «salio 0 con medicion vacia — seguiria comparando '' == '' en falso». **Corregido en la revision del lead**: el PR commiteaba un veredicto sellado (`3219000`), contra el diseño de la 18.3 — el `.gitignore` de `veredictos/` existe para impedirlo, y de fondo un veredicto es artefacto de SESION sellado contra su estado vivo; commiteado es un archivo muerto que aparenta ser prueba. Sacado en `9d92a0a`; el rastro `.saikit/decisiones/18.16.tsv` SI queda versionado, que es el registro durable que la fila pide. **RESIDUAL DECLARADO, no arreglado** (hallazgo de CodeRabbit, adjudicado ASK y con el hilo dejado ABIERTO a proposito): `zcode_hook_cmd`, `zcode_probe_cmd` y `grok_hook_cmd` interpolan rutas entre comillas sin escapar la sintaxis del shell, y esas cadenas las ejecuta el host — una ruta con `$()`, backtick o comilla rompe el entrecomillado. **Es PREEXISTENTE** (verificado: el mismo `printf` esta en `origin/master`), esta rama solo añadio la forma POSIX al lado. Cerrarlo pide escaping en las tres funciones con casos de ruta hostil (`$(id)`, comilla, backtick) que exijan que el comando generado no la interprete. **Ademas, hallado aqui y derivado a la 18.17**: el veredicto que el reviewer escribio no VALIDA contra su propio contrato (`blast: "n/a"` ⇒ «falta el campo blast.nivel») — primera corrida real de la maquinaria de la 18.3, y destapo un hueco del esquema |
| 18.17 | `[Veredicto]` `[lane:gate]` `[tdd:required]` **Un turno sin blast no puede escribir un veredicto valido** (hallado el 2026-09-03 al revisar el PR #157, la PRIMERA vez que la maquinaria de la 18.3 corrio de verdad de punta a punta; error del lead al redactar el esquema). El reviewer escribio `"blast": "n/a"` — la unica forma sensata de decir «este turno no corrio blast» — y su propio validador lo **RECHAZA**: `veredicto_validar` exige `blast.nivel`, `blast.hecho` y `blast.comando` como hojas obligatorias (`tools/lib/veredicto_contract.sh`), asi que un veredicto asi es invalido y el gate de la 18.4 lo rechazaria. **El hueco esta en las TRES capas y hay que cerrarlo en las tres**: `agents/reviewer.md` (el perfil que el lead escribio en la 18.3) da `blast` SIEMPRE como objeto y solo admite `"n/a"` para `adversary`; `docs/phase-18-autopilot-plan.md` §4.1 dice lo mismo; y el validador implementa esa lectura. **Ninguna de las tres contempla el caso real**: un turno de carril rapido, o uno de solo revision, o uno cuyo verifier no produjo blast. **Evidencia literal**: el veredicto del PR #157 (`blast: "n/a"`, `reviewer: "clean"`) da `INVALIDO: falta el campo blast.nivel`. **Cuidado con el arreglo facil**: aceptar `"n/a"` a secas convierte «no hubo blast» en indistinguible de «hubo blast y no lo declaro», que es justo el hecho unico (D13) que el gate existe para exigir. La salida tiene que dejar RASTRO de por que no hubo blast, no solo permitir la cadena | Rojo medido con el veredicto real del PR #157 (hoy `INVALIDO: falta el campo blast.nivel`). Se decide y se DECLARA la forma: o `blast` admite `"n/a"` con una razon adjunta, o el esquema gana un campo hermano que nombre por que no hubo blast — la eleccion se justifica contra D13 (el hecho unico), no por comodidad. Las TRES capas quedan consistentes: perfil, plan y validador; un caso lo ata comparando el esquema del perfil contra los campos que el validador exige, para que no vuelvan a divergir. Casos: turno CON blast sigue valido; turno sin blast con la forma nueva es valido; `"n/a"` sin la razon adjunta es INVALIDO. Mutacion: quitar la exigencia de la razon ⇒ rojo | 18.13 | cc:完了 [PR #165, merge `6b70fb2`, CI 9/9 en el head `47aca8d` (run 33834372821); implemento qwen, reviso y cerro el lead] — **`blast` paso a ser un XOR**: la triada `{nivel,hecho,comando}` cuando corrio, o `{"omitido": "<razon no vacia>"}` cuando no. Se rechaza el escalar `"n/a"` pelado (con guia), la razon vacia/`null`/`"n/a"`/bool/numero, y la mezcla de `omitido` con cualquier hoja de la triada. La eleccion se justifico contra D13: aceptar `"n/a"` a secas volvia «no hubo blast» indistinguible de «hubo blast y no lo declare». **Hueco extra cerrado, no anticipado por el brief**: `omitido` valida el contrato pero el merge (D18) sigue exigiendo la triada con `nivel >= 4`, asi que un turno sin blast NO mergea — el omitido cierra la sesion con rastro, no abre el autopilot. Las tres capas (perfil, plan §4.1, validador) quedan atadas por un caso que compara el esquema del perfil contra las hojas que el validador exige. Mutaciones: `omitido_sin_razon`, `blast_escalar_aceptado`, `blast_mezcla_aceptada`. **Nota del lead**: los commits originales usaban `fix(18.17):` como scope, que habria envenenado `audita-ledger.sh` de forma permanente; se reescribieron a `fix(veredicto):` con cherry-pick, verificado arbol identico |
| 18.18 | `[Verificacion]` `[lane:gate]` `[tdd:required]` **En un host ciego, ningun comando de verificacion REAL de este repo puede acreditar por `VERIFIED BY SUBAGENT`** (fila corregida por SEGUNDA vez el 2026-09-03; las dos redacciones anteriores del lead estaban mal y la segunda desmentia injustamente a dos implementadores). **La causa, medida**: el carril del label se juzga con `SAIKIT_VERIFIED_CMD_RE` (`:310`), cuyo vocabulario es `TEST_RUNNER_RE` (`:241`) mas {py_compile, compileall, dotnet build, bash -n, sh -n, node --check, git diff --check}. **`run.sh` NO esta en ese vocabulario.** Medido contra las constantes reales del hook: `bash tests/test_gate_behavior.sh` NO acredita, `bash tests/test_autopilot_config.sh` NO acredita, y **`bash tests/run.sh` TAMPOCO** — el runner propio del repo; en cambio `pytest` y `npm test` si. El carril de EVENTO usa otro regex, `TEST_RUNNER_CMD_RE` (`:272`), que si acredita `bash tests/run.sh`. **Los dos carriles tienen vocabularios distintos y el runner de este repo solo esta en uno.** Consecuencia: en zcode, donde el evento no se ve, no existe comando que sea la verificacion real del repo Y satisfaga el label — el mecanismo es inusable en su propio repo. El diseno 14.1 §4.3 declara que un comando fuera del vocabulario «cae al camino normal», pero en host ciego ese camino no existe, asi que el limite declarado como recuperable no lo es. **Dos ocurrencias medidas** (turno zcode cerrando 18.6; GLM cerrando 18.7/18.5): los dos declararon el label, los dos fueron rechazados con el mismo `Missing verification evidence`, los dos concluyeron que el harness estaba roto, **y los dos tenian razon**. Hallado por el cross-review de kimi sobre las conclusiones del lead, y confirmado midiendo | Rojo medido: el span literal de los dos recibos contra `SAIKIT_VERIFIED_CMD_RE`. Se decide y se DECLARA la salida: unificar los vocabularios de los dos carriles, o agregar la forma del runner del repo al del label — la eleccion se justifica contra D13. Casos: un label que nombra el runner real del repo con exito ⇒ acredita; el MISMO comando con resultado declarado fallido ⇒ NO acredita jamas; un comando fuera del vocabulario ⇒ no acredita y el mensaje NOMBRA que el comando no esta en el vocabulario, en vez del generico de hoy. **Mutacion con poder discriminante REAL** (hallazgo de CodeRabbit sobre la primera DoD): el veto global de fallos corre ANTES de la guarda del resultado exitoso, asi que un caso con resultado FALLIDO sigue rechazado aunque se quite esa guarda — esa mutacion sobrevive. El caso que discrimina es **comando permitido, SIN resultado exitoso y SIN senal de fallo** ⇒ no acredita. Sumar tambien: comando y resultado en spans SEPARADOS ⇒ no acredita (la implementacion exige el mismo span), y exito en un span mas fallo en otro ⇒ no acredita (el veto es global) | 18.17 | cc:完了 [PR #174, merge `762dcdd`, CI 9/9 en el head `f538a4b` (run 33852389037); implemento Cursor, reviso y cerro el lead] — `SAIKIT_VERIFIED_RUNNER_PROPIO_RE` agrega al vocabulario del label **las mismas formas** del runner propio que ya aceptaba el carril de evento, interpoladas al grupo central. Medido tras el fix: `bash tests/run.sh`, `tests/run.sh` y `./tests/run.sh` acreditan; `bash tests/test_gate_behavior.sh` y demas tests sueltos NO — el vocabulario sigue CERRADO, que es lo que preserva D13. El mensaje de falta pasa a nombrar la condicion incumplida via `saikit_verif_motivo` (verifier ausente / fallo declarado / sin resultado en el mismo span / fuera de vocabulario), en vez del generico que hizo que dos turnos seguidos quemaran el presupuesto sin saber que fallaba. **Limite declarado a proposito**: un verbo delante del runner dentro del span (`echo bash tests/run.sh`) pasa la frontera, porque NO se embeben las anclas de posicion del carril de evento — el span ES la declaracion del modelo, no un comando que el hook observa, y `pytest exit 0` ya acreditaba siendo igual de fabricable. **Mutacion**: la advertencia de la DoD se cumplio — el discriminante es comando permitido SIN resultado y SIN fallo, porque el veto global corre antes de la guarda de exito y un caso con fallo habria sobrevivido. **Dos rondas de review del lead**: la primera pidio afirmar el TEXTO especifico de cada razon (los casos solo exigian el generico, asi que las cuatro ramas nuevas podian regresar sin poner roja la bateria); la segunda, ejercitar la rama DIRECTA del regex (las 27 fixtures usaban todas `bash` delante, asi que borrarla pasaba en verde). Las dos cerradas |
| 18.19 |  `[Tests]` `[lane:gate]` `[tdd:required]` **La bateria del gate no corre limpia en macOS y lo reporta como FAIL, no como `unknown`** (hallado el 2026-09-03 midiendo el cross-review de la 18.6). Dos defectos en el mismo sitio. **(a) Dependencia no declarada**: `tests/lib/gate_cases.sh:705` usa `touch -d '15 days ago'`, que es GNU; el `touch` de BSD responde `out of range or illegal time specification` y la batería cierra en FAIL con 2 fallas. Con `PATH=/opt/homebrew/opt/coreutils/libexec/gnubin:$PATH` da `test_gate_behavior: OK` con 0 fallas — **medido las dos veces**. Esa dependencia no esta escrita en `AGENTS.md`, ni en `CLAUDE.md`, ni en `tests/run.sh`: el operador quedo con la unica maquina que tiene sin poder correr la bateria en verde, y nada se lo dice. **(b) El estado esta mal clasificado**: cuando el `touch` falla, el caso hace `_mal` (FAIL). Por la disciplina del propio repo (`tests/run.sh:160-169`) eso es `unknown` — corrio y no pudo observar nada. Marcarlo FAIL confunde «la proteccion se rompio» con «no pude medirla», que es exactamente lo que la 18.16 se dedico a separar. **Cuidado con el arreglo facil**: poner gnubin en el PATH desde el test lo esconde en vez de declararlo, y un `\|\| true` lo convierte en verde silencioso; la salida tiene que DECIR que no pudo medir y por que | Rojo medido con las dos corridas del 2026-09-03 (sin gnubin: FAIL con 2 fallas del `touch`; con gnubin: OK con 0). **DoD REESCRITA el 2026-09-04 tras el adversario: la primera pedia una mutacion que habria sobrevivido y nombraba un remedio que no alcanza.** (1) **La mutacion no discrimina sin costura**: los 6 jobs de CI son `ubuntu-latest`, donde `touch -d` funciona, asi que la rama «sin la herramienta» nunca se toma y la mutacion «hacer que la ausencia pase como verde» sobrevive en verde. La entrega necesita una costura explicita para SIMULAR la ausencia (override de entorno o PATH acotado al caso), y la mutacion se mide contra ella. (2) **El remedio nombrado no cubre la dependencia**: MEDIDO, `gnubin` trae `stat` y `touch` pero NO `sed` (GNU sed es paquete aparte), y con `gnubin` en el PATH `sed` sigue siendo `/usr/bin/sed`. Hay 10 usos de `sed -i` sin sufijo BSD en `tests/`, mas `stat -c` en `test_restore_vendor.sh`. La fila declara el alcance: o cubre las tres familias (`touch -d`, `sed -i`, `stat -c`) o acota a `touch -d` y NOMBRA las otras como pendientes. (3) **El `unknown` por archivo choca con el runner**: `tests/run.sh` clasifica por exit code del ARCHIVO, asi que un `exit 3` pierde las demas aserciones del archivo y un rojo real se publicaria como `unknown`. La entrega decide y declara la forma alcanzable: un skip NOMBRADO contado en categoria APARTE — ni PASS, ni FAIL, ni el `unknown` por `exit 3` del runner actual, que es un canal que hoy NO existe y la entrega tiene que crear y nombrar (cross-review kimi); un caso afirma ese conteo. **El canal se define como CONTRATO EJECUTABLE, no como palabra** (cross-review CodeRabbit): hay que fijar el marcador, quien lo emite, que exit code produce, y como lo agrega el runner. La trampa concreta: imprimir `SKIP` y devolver 0 hace que `run.sh` publique **PASS**, y devolver 3 esconde las aserciones posteriores del mismo archivo. Casos obligatorios: **skip seguido de un fallo real** (el fallo tiene que salir a la luz, no quedar tapado por el skip) y **skip sin ejecutor** (se cuenta como skip, no como PASS).  (4) Declarar el precedente: `test_adversary_lock.sh:210` ya resuelve el mismo caso con SKIP + `return 0` y el runner publica PASS — ese es el verde silencioso que esta fila condena, y hay que decidir si se unifica o se declara la diferencia **Tercera familia, hallada el 2026-09-05 revisando la 18.21**: `tests/test_golden_harness.sh` FALLA en macOS sin `TMPDIR=/private/tmp` («el registro NO es reproducible entre corridas», `--check` reporta DIVERGENCIA contra el mismo hook) y pasa con la variable — medido identico en master y en una rama ajena, o sea entorno, no regresion. El repo conoce esa dependencia para regrabar la golden (esta en los briefs y en el deploy-log) pero ningun test la declara ni la exige: el mismo patron que `touch -d`, con otra herramienta. Entra en esta fila. **(5) El requisito de `TMPDIR` se hace EJECUTABLE, no documental** (hallazgo de CodeRabbit sobre esta ampliacion): `tests/run.sh:155-156` PISA `TMPDIR`/`TMP`/`TEMP` con `$caja/tmp` para cada test, asi que un `TMPDIR=/private/tmp` externo no llega bajo el runner — solo corriendo el archivo directo. Causa raiz MEDIDA en macOS: `mktemp -d` da `/var/folders/...` y `pwd -P` da `/private/var/folders/...`; la ruta logica y la fisica del mismo sandbox divergen, y el registro de la golden deja de ser reproducible. Se decide y declara: o el arnes normaliza rutas (fisica en los dos lados) y deja de depender de donde viva el tmp, o el runner fija en Darwin un `caja` bajo `/private/tmp`. En cualquiera de las dos, el caso corre BAJO `tests/run.sh` (no el archivo suelto) y verifica el valor que el runner propaga; mutacion: quitar la normalizacion o el pin ⇒ rojo bajo el runner en macOS | 18.16 | cc:完了 [PR #186, merge `02fa21b`, CI 9/9 en el head `b9b6a41` (run 33952631797); implemento qwen, reviso y cerro el lead] — canal de SKIP por caso como contrato ejecutable (`tests/lib/skip_caso.sh` appendea un marcador al archivo `SAIKIT_SKIPS` de la caja; `run.sh` lo cuenta en categoria aparte, ni PASS ni FAIL ni `unknown`, y JAMAS altera exit codes; cuatro casos en `test_runner_guards.sh`, incluidos «skip seguido de fallo real» y «skip sin ejecutor no es PASS»); costura `SAIKIT_FINGIR_SIN=touch` que simula la ausencia, sin la cual la mutacion sobrevivia en CI ubuntu; las TRES familias: `touch -t` portable en `gate_cases.sh`, helper `sed_i` (temporal + mv) con 8 usos, y `mtime_de` en `test_restore_vendor.sh` exigiendo medicion no vacia (el bug de la 18.16 que seguia vivo); golden: el arnes normaliza CUATRO formas de la ruta del work y colapsa `/private`, con caso BAJO el runner; el precedente `test_adversary_lock.sh:210` unificado al canal. **Medido en la Mac del operador, particion rapida SIN GNU coreutils**: `test_gate_behavior` pasa de FAIL (master) a PASS (rama); `golden_harness`, `restore_vendor`, `recetas`, `runner_guards`, `install_hook`, `install_provenance` PASS; 12 casos contados como skip aparte. **Cero regresiones**: los diez tests que aun fallan en macOS fallan IDENTICOS en master (misma primera asercion, uno por uno). **Declaracion incompleta, corregida por el lead**: qwen nombro 2 de esos 10 como preexistentes; los otros 8 van a la fila 18.22. Asi que el titulo de esta fila sigue siendo VERDAD tras el merge — el runner cierra FAIL en macOS — pero por causas que esta fila no cubria y ahora estan medidas y nombradas |
| 18.22 | `[Tests]` `[lane:gate]` `[tdd:required]` **Los ocho tests que siguen rojos en macOS tras la 18.19: preexistentes, medidos, sin declarar** (hallados por el lead el 2026-09-05 corriendo la particion rapida de `tests/run.sh` en la Mac del operador SIN GNU coreutils, sobre la rama de la 18.19 y sobre master: **fallan identicos en los dos**, misma primera asercion). La 18.19 declaro 2 (`test_hook_registration` exige binario `python`; `test_golden_baseline` por `wc -l` con padding y `date -u -d` del hook). Los otros ocho, con su primera asercion medida: `test_sandbox` «mktemp -d cayo fuera del sandbox: /var/folders/...» (MISMA causa raiz que la golden en 18.19: ruta logica `/var` vs fisica `/private/var`); `test_stage_override` «python: command not found» (misma familia que hook_registration); `test_saikit_decision` «un candado sin pid pero viejo no se reclamo» (edad por mtime: familia stat/date GNU); `test_verificar_app` «estado del mapa node no es al_dia»; `test_blast_artifact_contract` «el reviewer no juzga el hecho sin re-correrlo»; `test_check_secrets` «el fallback dejo pasar trampa.txt»; `test_hook_source` «la fuente del repo NO se comporta como la linea base (exit 1)»; `test_recetas_symlink` «se reemplazo el symlink que no era nuestro». Los cuatro ultimos NO tienen causa raiz medida todavia: se diagnostican en esta fila, no se suponen. **Regla que esta fila fija**: en macOS, un test que no puede observar por falta de herramienta usa el canal de skip de la 18.19 y lo DICE; un test que falla por logica se arregla; ninguno se «declara» sin su primera asercion pegada | Rojo medido por test (la primera asercion, pegada) en macOS sin gnubin; control en master para probar que es preexistente. Por cada uno se decide y declara: causa raiz + arreglo portable, o skip por el canal de la 18.19 con la herramienta que falta NOMBRADA. Al cierre, `SAIKIT_PARTICION=rapidos bash tests/run.sh` en macOS sin gnubin cierra con `tests/run.sh: OK` (skips contados aparte), y se declara el estado de la particion lenta. Mutacion por cada arreglo de logica; para los skips, la costura de ausencia de la 18.19 | 18.19 | cc:完了 [PR #200, merge `229887a`, CI 8/8 con `gate` verde (run 34003475153); implemento glm, reviso y cerro el lead (sesion kimi)] — los 10 rojos de macOS diagnosticados con causa raiz medida cada uno: DOS defectos del HOOK arreglados (H1 `adv_canon_path` ancla el ancestro existente en forma fisica — cerraba la divergencia `/var` vs `/private/var` que inventaba violaciones y perdia sellos cuando el dir del blanco no existia; H2 `adv_epoca_a_epoch` GNU→BSD con round-trip al formato origen, molde `fecha_epoch` del PR #196), DOS de tools (`_edad_dir` portable en `saikit-decision.sh`; `cp -p -R` en `install-hook.sh` — BSD `-r` SIGUE symlinks), cinco de instrumento de test (python3→python, `wc -l \| tr -d`, grep sin rama vacia, `fb()` resolviendo bash antes de acotar PATH) y UN skip por el canal de la 18.19 con herramienta nombrada (`sandbox-mktemp-bare`: BSD mktemp pelado ignora TMPDIR, no observable). `test_verificar_app` fuera de alcance declarado (ya verde por #196). Golden regrabada SOLO identidad (diff = 3 lineas). `tests/run.sh` excluye `.DS_Store` del manifiesto (LEAK falso medido, con caso en `test_runner_guards`). **DoD re-medida por el lead en la Mac del operador sin gnubin**: `SAIKIT_PARTICION=rapidos` ⇒ `tests/run.sh: OK (37 tests)`, 13 skips contados aparte; y las mutaciones de H1/H2 quedaron al rojo MEDIDAS en esa misma maquina (la primera corrida del lead midio sin querer el hook viejo: `advlock_falla_infra_fail_open` FAIL «violacion inventada» y divergencia dorada del warning de epoca — exactamente donde la fila decia). Particion lenta: OK segun el implementer, no re-medida por el lead (declarado). Nota de metodo: la 1ª corrida del lead fallo por no fijar `SAIKIT_HOOK_VIVO` (midio el vivo de master contra la baseline nueva) — la trampa de la regla 1 de AGENTS.md, declarada |
| 18.23 | `[Contrato]` `[lane:gate]` `[tdd:required]` **El recibo se entrega un parrafo por etiqueta, y el gate lo exige** (regla del operador del 2026-09-03, que hoy vive solo en `CLAUDE.md` — archivo que leen las sesiones de Claude y NADIE mas; los recibos de qwen, GLM, grok y Cursor siguen el CONTRATO que inyecta el hook, que no dice nada de parrafos). **Medido**: `has_receipt_label` (`hooks/summonaikit-harness.sh`, Task 8.3/9.3) acepta la etiqueta en CUALQUIER posicion — la regex es `(^\|[^[:alpha:]'"])Label(\*\*\|__)?[[:space:]]*:` sin ancla de linea — asi que un recibo con las seis etiquetas corridas en un solo parrafo pasa el gate; el mensaje de falta dice «add a line beginning 'Understand:'» pero el codigo no lo exige. La frase de forma del contrato (`:1348`, «Receipt line shape») solo pide el COLON. Ejemplo real: el recibo de la 18.19 llego con Understand/Implement/Verify/Review/Close/Retro pegados en un bloque unico e ilegible. **Alcance**: (i) el contrato, en sus DOS bloques (`:1349-1354` y `:2615-2620`, la trampa de la 18.13), dice la regla como la escribio el operador: cada etiqueta abre su propio parrafo, con linea en blanco entre parrafos, sin mezclar puntos ni dejar etiquetas sin parrafo; las lineas `ADVERSARY:` / `ROLE FALLBACK:` / `VERIFIED BY SUBAGENT:` van con el mismo trato; (ii) el gate EXIGE que cada etiqueta este al INICIO de su linea (bullet o negrita permitidos, como hoy), y cuando no lo esta, el mensaje de falta NOMBRA la regla («cada etiqueta en su propio parrafo») en vez del generico — fail-closed con guia, como la 18.18; (iii) la separacion en parrafos se afirma en el contrato y en el caso de forma, no se parsea (los hosts colapsan espacios en blanco de forma distinta; exigir la linea en blanco seria fragil y se declara como limite) | Rojo medido: el recibo literal de la 18.19 (un solo parrafo) pasa hoy el gate. Casos: recibo un-parrafo-por-etiqueta ⇒ cierra limpio; recibo con seis etiquetas en un solo parrafo ⇒ BLOQUEA y el mensaje nombra la regla; recibo con `- **Understand**:` al inicio de linea ⇒ sigue contando (no romper C7); la etiqueta citada en prosa (`'Understand:'`) sigue sin contar (no romper C11). `caso_g1_contrato_muestra_forma_recibo` gana la aserción de la regla en el texto. **Mutacion con poder discriminante**: quitar el ancla de inicio de linea ⇒ el caso del recibo en un parrafo queda verde ⇒ rojo; OJO: el caso tiene que traer las seis etiquetas presentes para que la UNICA causa de bloqueo sea la posicion. Golden regrabada DESPUES del ultimo cambio al hook, diff auditado: solo los escenarios que imprimen el contrato | 18.13 | cc:完了 [PR #191, merge `3843b7a`, CI 8/8 con `gate` verde en el head `4863983` (run 33990766492); implemento glm, reviso y cerro el lead (sesion kimi)] — `has_receipt_label` con ancla de linea real `(^\|\n literal)[[:space:]]*([-,*,+] )?(negrita)?`: cada etiqueta del recibo abre su propio parrafo o el gate bloquea nombrando la regla en los 7 mensajes; la cita en prosa sigue sin contar y `- **Understand**:` sigue contando; el `\n` literal de codex cuenta como frontera (arreglo r1 del ALTO del adversary); contrato en sus DOS bloques. Rojo medido contra master reproducido por el reviewer del lead (las 3 lineas FAIL exactas del body); mutacion load-bearing `mut_ancla_de_linea_quitada` medida roja sobre los 3 casos esperados; golden regrabada post-ultimo-cambio con 227 pasos auditados, cero flips, sha del header == hook del head. Ceremonia del implementador: verifier (PASS 5/5, blast nivel 5, re-check post-rebase), adversary (3 hallazgos, el ALTO del falso bloqueo codex confirmado por el lead), reviewer 2 rondas, veredicto sellado `clean`. Hallazgo BAJO del lead (comentario stale en `gate_cases.sh:3221`) corregido en `4863983` antes del merge. Deploy real a las 4 copias (grok/dsh/codex REPARADO; claude ya al dia) — deploy-log 2026-09-05 |
| 18.24 | `[CI]` `[lane:gate]` `[tdd:required]` **El CI minimo generado nace rojo o verde-sin-correr en tres formas de repo** (cross-review de grok sobre la 18.8 mergeada, 2026-09-05; las tres REPRODUCIDAS por el lead con `--ofrecer --root <tmp> --ci-minimo si`). **(a) pytest sin toolchain**: en un repo con `pytest.ini` el YAML lleva `run: python -m pytest` SIN `actions/setup-python` ni `pip install` — el runner de GitHub trae Python, no pytest: el job nace rojo. El `install_step` de la 18.8 existe solo para `npm test`. **(b) `verify/` en verde sin ejecutar nada**: la rama de fallback de `VERIFY_CMD` (`saikit-ci-minimo.sh:34`) es `find verify -type f -print -quit \| grep -q .` — con CUALQUIER archivo en `verify/` sale 0; la skill `saikit-verificar-app` siempre deja `LEEME.md`, asi que el mapa del propio kit enciende ese verde. Medido: rc=0 sin ejecutar nada. **La DoD (4) de la 18.8 era satisfacible por coincidencia de cadena** (el test afirma que el `run:` CONTIENE `find verify`, no que ejecute) — error de redaccion del lead, el mismo patron de 17.5/18.12. **(c) sin runner detectado igual escribe**: repo vacio, `Makefile`, `Cargo.toml` o `package.json` sin `scripts.test` ⇒ `run: bash tests/run.sh` aunque el archivo no exista ⇒ 127 en cada push, y el veto «sin checks no es verde» bloquea TODO merge para siempre: un CI minimo que no puede cumplir la promesa con la que el operador dijo que si. **Medias de grok, verificadas por lectura**: el detector de `verify/` es distinto del de test y apunta a la carpeta; el pin a sha no tiene proceso de refresh (un comentario `# v4.2.2` no es proceso); `runs-on: ubuntu-latest` flotante, sin `timeout-minutes` ni `concurrency`, y yarn/pnpm no detectados; el veto de merge no decide `skipped`/`neutral` ni workflows solo-`push`. **Limites declarados (bajas)**: el YAML se escribe y no se commitea (documentado); el generador invocado fuera del setup no exige git; sin TTY asume «no» y hay aceptacion no interactiva. **Lo que esta bien y no se toca**: `on: pull_request`, `permissions: contents: read`, sin secretos, sin `\|\| true`, no pisa workflows ajenos, no auto-commitea | Rojo medido de cada forma con el generador real. **(1) Ejecutor en caja**: el test corre los `run:` del YAML generado dentro de un repo temporal (no compara cadenas) y exige: repo pytest ⇒ el paso de test instala el toolchain antes de correr (setup-python pinneado a sha completo + `pip install`), o el generador se niega; repo con `verify/` que solo tiene markdown y sin runner ⇒ el paso de verify sale DISTINTO de 0 o el generador no lo emite y lo dice — jamas verde por `find`; `verify/` reutiliza el `test_cmd` detectado, no un detector aparte ni la carpeta. **(2) Sin runner no hay YAML**: vacio / `Makefile` / `Cargo.toml` / `package.json` sin test ⇒ exit 2 con mensaje que nombra lo que no se detecto, y la oferta al operador CITA el comando que va a correr. **(3) Job amarrado**: imagen pinneada (`ubuntu-24.04`), `timeout-minutes`, `concurrency` con `cancel-in-progress`; yarn/pnpm detectados o no se emite `npm`. **(4) Veto**: casos `conclusion: skipped` y solo-`push` en `test_saikit_merge.sh`, con la decision declarada. **(5) Refresh del pin**: se declara la deuda con su camino, o se implementa. **Mutacion por guarda, con la trampa de siempre**: para (b), el caso tiene que tener el test del repo en verde para que la UNICA causa de rojo sea `verify/`; si no, «volver al find» sobrevive | 18.8 | cc:完了 [PR #192, merge `69f5893`, CI 8/8 con `gate` verde en el head `aaec0b6` (run 33992318298); implemento Cursor, reviso y cerro el lead (sesion kimi)] — el generador falla cerrado: sin runner (vacio/Makefile/Cargo/package.json sin scripts.test) exit 2 nombrando lo no detectado y sin YAML; pytest con setup-python@sha40 (v5.6.0, verificado contra la API) + pip install; verify/ solo-markdown omite el paso Y lo dice, jamas verde por `find`; job amarrado (ubuntu-24.04, timeout 15, concurrency cancel-in-progress); yarn/pnpm detectados; veto: `skipped` es rojo y solo-push sigue verde, decision declarada. Cross-review adversary de grok (artefacto `adversary-20260905T210102Z.json`, viaja en el branch): 6 hallazgos confirmados por el reviewer — 5 Act on cerrados en `aaec0b6` (F1 ALTO pnpm sin toolchain: setup-node@sha40 v4.4.0 verificado + corepack enable; F2 requirements-sin-pytest instala ambos; F3 rama TIENE con caso real e inbox que la corre; F4 setup degrada con aviso en vez de morir a mitad con config persistida; F5 `-iname`); 1 Dismissed con razon (F6: la DoD pedia casos+decision, no cambio de conducta). Mutaciones nuevas atrapadas en la corrida del reviewer (`sin_setup_node_pnpm`, `pip_sin_pytest_extra`, `tiene_omite_verify`, `setup_propaga_exit2`, `leeme_md_case_sensitive`). Residual declarado `unknown`: corepack sin campo `packageManager` baja el pnpm latest-known; no se midio si algun corepack viejo pide confirmacion interactiva en CI. Deuda declarada con camino: refresh de pins (`tools/bump-ci-pins.sh` o dependabot). Deploy no-op del hook (no tocado) — deploy-log 2026-09-05 |
| 18.25 | `[Gate]` `[lane:gate]` `[tdd:required]` **El gate de merge depende de la forma de la salida de `gh`, que el terminal del agente cambia** (medido en vivo en la 18.9, esc1): bajo grok headless `gh repo view --json nameWithOwner` llego en una forma que `saikit_json_get` no parseo y `saikit-merge.sh` cerro con «devolvio algo inesperado» teniendo el repo CORRECTO. Re-medido por el lead: el parser sin jq acepta pretty y CRLF pero muere con ANSI (rc=1), y el mismo paso directo y bajo pty pasa. El fail-closed aguanto, pero la razon fue ilegible y el happy path no puede correr | Repro con salida con ANSI ⇒ hoy NO-MERGE (rojo medido); el fix (`--jq` o entorno neutralizado declarado) la atraviesa; mutacion — una que sobreviva en verde es hueco; el mismo barrido para TODAS las llamadas `gh` del script (`pr view`, `run list`, `pr checks`) o se declara cuales quedan | 18.9 | cc:TODO |
| 18.26 | `[Contrato]` `[lane:gate]` `[tdd:required]` **El sello del veredicto es ciego en grok: el write del reviewer llega sin atribucion de rol** (medido en vivo en la 18.9, esc1): el subagente reviewer (spawn_subagent) escribio `.saikit/veredictos/<sha>.json` con el tool `write` (wire de la sesion hija) y el hook lo acredito `implemented: file edit` SIN registrar `veredicto_sha256` (`vd_write_reviewer` exige rol resuelto + tool `write` + path bajo veredictos/). Sin sello en el estado, `saikit-merge.sh` no puede mergear JAMAS en grok — emparentado con el canal ciego conocido (Task 14.2) pero aca no es advisory: el merge lo EXIGE | Payload real de grok del write del hijo (o la forma que SI llega, medida y pegada) ⇒ sello registrado, con rojo medido antes; si ninguna forma del host lo permite, la fila declara el limite y el contrato del autopilot dice que en grok el merge queda manual | 18.9 | cc:TODO |
| 18.27 | `[Gate]` `[lane:gate]` `[tdd:required]` **grok headless (`-p`) puede cerrar SIN recibo y el Stop no lo sostiene** (medido en vivo en la 18.9, esc2): el turno termino tras el reporte del subagente («SUMMONAIKIT HARNESS DELEGATED - awaiting implementer»), 0 de las 6 etiquetas en la salida, RC=0 y el estado del hook limpiado; en esc1 el recibo salio 6/6 en los dos turnos — NO determinista. El merge queda protegido (fail-closed no depende del recibo) pero la ceremonia de cierre en headless es best-effort y nada lo dice | Repro headless del turno que corta tras la delegacion ⇒ se observa y se pega si el Stop bloquea y que hace grok con el bloqueo sin nadie a quien re-preguntar; se decide y declara: limite escrito (headless sin gate de cierre) o mecanismo que lo sostenga | 18.9 | cc:TODO |
| 18.20 | `[Deploy]` `[lane:gate]` `[tdd:required]` **El protocolo de deploy no puede verificar su propio invariante: «vivo == master» es disciplina, no maquina** (cross-review de grok sobre el protocolo, 2026-09-04; confirmado midiendo). **(a) El instalador no consulta git.** `AGENTS.md` afirma que `install-hook.sh` «garantiza vivo == master», pero el script toma `SOURCE` del checkout que lo corre (`:63-64`) y hace `cmp` contra eso (`:1897`); grep de `origin/master`, `rev-parse`, `git diff` y `HEAD` en el instalador: **cero**. Asi que `YA AL DIA` significa «igual a lo que tenga el que lo corrio», no «igual a master». **(b) El protocolo despliega UNA copia y produccion tiene varias.** Desde 7.5, Phase 15 y 18.15, grok (`~/.grok/hooks`), dsh (`~/.dsh/hooks`) y codex (`~/.codex/hooks`) tienen `DEST` propio, y `grok_hook_cmd` interpola ese `$DEST` (`:894-899`); solo zcode reusa la copia de claude. El paso 2 del protocolo es un `install-hook.sh` sin `--host`, asi que **las demas copias quedan atras y nada lo dice**. MEDIDO EN VIVO el 2026-09-04: grok y dsh corrian `4127f79a` del 3 de septiembre — se habian perdido 18.6, 18.7, 18.5, 18.17 y 18.18, o sea CINCO filas de cambios del hook. El lead lo desplegó a mano al detectarlo. Ademas el deploy-log de la 18.13 afirma que grok apunta al hook de `~/.claude/hooks/`: **dejo de ser cierto en la 18.15** y nadie lo corrigio. **(c) La golden empuja a violar el protocolo de entrega.** `golden-harness.sh:60` toma por default el hook VIVO, asi que regrabar exige que el vivo YA sea el hook nuevo, y la via mas corta es desplegar la rama al perfil REAL — que el protocolo de entrega prohibe explicitamente. **Las dos reglas se pisan**: los dos incidentes de deriva de esta sesion no fueron olvidos del implementador, fueron la herramienta empujando. **(d) Medias**: `check-hook-registration.sh` es fail-open y el instalador se traga su rc (`:265`, `:273`, `:281`, `:283`), asi que como UNICO detector post-merge no alcanza; el rollback del hook usa `cp` y no es atomico, lo que rompe el contrato «exit != 0 ⇒ destino intacto» de la Task 2.2; y «un registro por PR» quedo como disciplina que el log no puede exigir | **DoD REESCRITA el 2026-09-04 tras el adversario: la primera se apoyaba en premisas falsas y su guarda habria bloqueado el arreglo correctivo de ese mismo dia.** (a) La guarda NO puede regir por «sin `--dest` = perfil real»: medido: 96 de 103 lineas que invocan `--quitar-*`/`--restore-vendor`/`--refrescar-manifiesto` en `tests/test_install_hook.sh` no llevan `--dest` en la misma linea, y desinstalar o reparar quedarian bloqueadas justo cuando algo esta roto. **Refutacion medida**: el 2026-09-04 el lead corrio `install-hook.sh --host grok` y `--host dsh` DESDE UNA RAMA (`HEAD != origin/master`) y eso fue lo que saco a grok y dsh de cinco filas de atraso; la guarda propuesta lo habria impedido. El defecto no es que se PUEDA desplegar desde una rama: es que nadie se ENTERA. La salida es DECLARAR, no negar: en toda escritura a un perfil real el instalador reporta la procedencia git de lo que instala (rama, sha, arbol sucio, y si coincide con `origin/master`), y un modo `--check` sin escritura lo vuelve verificable por maquina. **Tres cosas que la entrega DEBE definir y hoy no estan** (cross-review kimi): (i) que verifica `--check` — tiene que recorrer TODAS las copias cableadas (claude/zcode, grok, dsh, codex cuando exista), porque un `--check` que solo mire `~/.claude` es conforme e inutil, y ahi NO estuvo la deriva medida; (ii) que es un «perfil real», porque los tests aislan por `HOME` (`tests/run.sh:155`) y por RUTA una escritura de sandbox es indistinguible de una real — el criterio no puede ser la ruta; (iii) que hace cuando el checkout no es git o `git` falta (reportar «procedencia: desconocida», no callar), y si «coincide con `origin/master`» se juzga contra el ref remoto LOCAL (que puede estar viejo) o exige `fetch`. **`--check` cierra en FALLO, no en silencio** (cross-review CodeRabbit): hay que fijar el conjunto AUTORITATIVO de hosts y que el exito exija que TODAS las copias esperadas coincidan con la fuente Y que la procedencia sea conocida. Si el descubrimiento solo mira directorios existentes, una copia configurada y ausente se saltea — que es EXACTAMENTE el modo de falla que esta fila vino a cerrar (grok y dsh cinco filas atras sin que nadie se enterara). Y si un checkout no-git reporta «desconocida» y sale 0, la automatizacion acepta el deploy igual. Casos: un host configurado cuya copia FALTA, y un checkout que no es git. Un resultado por host, no uno agregado. Nota medida: la propia entrada de deploy-log de ese dia reporta el registro verificado solo para `claude` y `grok` — dsh quedo sin verificar. (b) NO citar `AGENTS.md` como fuente de verdad de las copias: `AGENTS.md:84-85` declara UNA sola copia, lo contrario de lo medido — corregir esa seccion es parte de la entrega, no su insumo. (c) NO tocar el trato de identidad de `--check`: `golden-harness.sh:25-31` documenta a proposito que comparar identidad daria rojo justo en el caso para el que `--check` existe. Cambia el default de `--record` a la fuente del repo, MAS un test de CI aparte que ate `# hook_sha256:` al sha de la fuente; declarar que el cambio de default es no-op para el `--check` de CI porque `test_golden_baseline.sh:69` pasa `--hook` explicito. (d) Las dos invariantes del log YA estan violadas: tres inversiones de fecha y 23% de los 78 encabezados fuera de norma. Se decide y declara: normalizar el historico o acotar desde una fecha de corte; la extraccion NO puede ser un `#[0-9]+` ingenuo (falsos positivos sobre prosa). Rojo medido de cada pieza; mutacion por cada guarda | 18.19 | cc:完了 [PR #179, merge `914dc4a`, CI 9/9 en el head `a9bf51e` (run 33934855275); implemento GLM, reviso y cerro el lead en CUATRO rondas] — el instalador DECLARA la procedencia git de lo que instala en toda corrida que pasa validacion (rama, sha, arbol sucio, `coincide_origin_master=si/no/sin-ref`, sha256 de la fuente), sin mover el exit code; `--check` multi-host con conjunto autoritativo (claude/zcode, grok, dsh, codex), una fila por host y cierre en FALLO ante copia ausente, atrasada, checkout no-git o fuente distinta del blob de `origin/master`; `AGENTS.md` declara las cuatro copias y el deploy por host; `--record` de la golden graba la fuente por defecto con test que ata `# hook_sha256:`; `check-deploy-log.sh` con corte declarado 2026-09-04. **El primer `--check` real fue ROJO y con razon**: claude/grok/dsh al dia y `codex: falta` — codex nunca se habia cableado en esta Mac y hasta hoy nada lo decia; tras cablearlo, `veredicto=ok`. **Cuatro rondas de review**: (1) mutaciones solo corridas a mano por el lead, sin bloque committeado; (2) CI rojo por `rev-parse HEAD origin/master` como PAR (rc=128 sin el ref, `sin-commits` falso — tambien pegaba a un `clone --depth 1`) y por la palabra `desconocida` contra Core Rule 2 del caso 12.9 #2; (3) el arreglo cambio una palabra prohibida por otra: `unknown` disparaba «sin backups NO es unknown» de `test_restore_vendor` — reproducido con un `git` envuelto sin `origin/master`; la salida es de diseno: el campo lleva la RAZON (`sin-ref`), no un estado; (4) los flags con valor COLGABAN si faltaba el valor (`shift 2` sin avanzar), medido rc=124 en cinco flags, cuatro heredados de agosto — ahora exit 2 con casos P10/T14, y `juicio-master-unknown` renombrado `no-observable`. **Residuales declarados**: sin `.saikit/decisiones/18.20.tsv`; el registro de codex en `~/.codex/hooks.json` lo reporta el instalador como `unknown` (copia al dia, registro no observable) |
| 18.21 | `[Deploy]` `[lane:gate]` `[tdd:required]` **Lo que la 18.20 dejo afuera de su propia DoD: el registro, la edad del ref y el rollback** (cross-review de grok sobre la 18.20 mergeada, 2026-09-04; los cuatro verificados por el lead en la Mac del operador). **(a) `--check` verde no implica que el gate corra**: para codex la copia esta al dia y el wrap plantado, pero `~/.codex/hooks.json` NO EXISTE — medido — y el registro se reporta `unknown`. Un archivo de registro AUSENTE no es «no pude mirar», es una ausencia observada: el gate de codex esta apagado mientras `--check` dice `veredicto=ok`. Es el hallazgo #4 original de grok (registro fail-open como detector) que la DoD (d) de la 18.20 no pidio. **(b) El ref local de `origin/master` no tiene edad**: el juicio `coincide_origin_master` y `fuente = bytes de origin/master` usan el ref LOCAL sin fetch, y la linea «sin fetch» (`:1978`) sale en TODAS las corridas con ref, asi que no senala frescura. Un master atrasado semanas produce `coincide=si` + `veredicto=ok`. C11 cubre SIN ref (fallo), no ref VIEJO. **(c) El rollback del hook sigue en `cp`** (`:415` para el wrap de codex; el de grok analogo): no es atomico y rompe «exit != 0 ⇒ destino intacto» de la Task 2.2. Era el #5 original y quedo fuera de (d). **(d) La golden, tocada por la 18.20, todavia CUELGA con flag sin valor**: medido rc=124 en `--hook`, `--scenarios`, `--baseline` — el mismo bug que la 18.20 arreglo en cinco flags del instalador y no en la herramienta que toco en la misma fila. **Limites declarados (bajas de grok)**: un symlink a los mismos bytes pasa `--check` como copia (no mira `-L`); `--check --host X` verde no dice nada del conjunto; el preambulo de `docs/deploy-log.md` sigue diciendo «una copia»; «un registro por PR» es unicidad, no cobertura de cada PR mergeado | Rojo medido de cada pieza. (a) `--check` recorre el REGISTRO de cada host del conjunto autoritativo ademas de los bytes: archivo de registro ausente ⇒ `falta-registro` y FALLO (ausencia observada); existe y no parsea ⇒ `no-observable` y FALLO; una fila por host. Caso: codex con copia al dia y sin `hooks.json` ⇒ fallo nombrado. **Mutacion que discrimina**: tratar la ausencia como `no-observable`-sin-fallo ⇒ el caso queda rojo; OJO, la mutacion no puede ser «quitar el fallo» a secas si otra guarda ya falla por otra causa en el mismo caso — el caso tiene que tener las cuatro copias al dia para que la UNICA causa sea el registro. (b) La procedencia imprime la EDAD del ref (`committerdate` de `origin/master` o mtime de `FETCH_HEAD`) y `--check` falla si supera un umbral declarado; caso con ref envejecido via `GIT_COMMITTER_DATE` o costura; mutacion: quitar el umbral ⇒ rojo. No exige red en cada install. (c) Todo rollback del hook va por temporal + `mv -f` (el patron de la ida); caso que verifica que tras un rollback el destino es byte a byte el backup y que NUNCA existio un destino truncado (costura que aborta a mitad); mutacion: volver a `cp` ⇒ rojo. (d) `golden-harness.sh` con flag sin valor sale 2, con caso `timeout` como P10; mutacion: quitar la guarda ⇒ rc=124 ⇒ rojo. Los cuatro limites declarados quedan declarados, no resueltos, salvo que la entrega decida cerrar el symlink con `[ -L ]` (barato) | 18.20 | cc:完了 [PR #182, merge `f9cf872`, CI 9/9 en el head `94d2f7f` (run 33948364332); implemento GLM, reviso y cerro el lead] — (a) `--check` recorre el REGISTRO de cada host: ausente ⇒ `falta-registro` y fallo, ilegible ⇒ `no-observable` y fallo, una fila por host; (b) la procedencia imprime la edad del ref (mtime del ref loose de `origin/master`, o committerdate; NUNCA `FETCH_HEAD`, y C16 ata que un FETCH_HEAD fresco no tape un ref viejo) y `--check` falla sobre umbral 1d; (c) el rollback del hook va por temporal + cmp + mv atomico en `tools/lib/restaurar_desde_backup.sh`, con costura de aborto (R2) y, tras el hallazgo de CodeRabbit verificado por el lead (backup 755 ⇒ destino 600, NO ejecutable: un rollback correcto en bytes que apagaba el gate), preserva el modo (R1 lo afirma); (d) `golden-harness.sh` con flag sin valor sale 2 (medido). Cerro de paso el symlink con `[ -L ]`. Rastro `.saikit/decisiones/18.21.tsv` presente. Siete mutaciones auto-verificadas. **Efecto en produccion, medido tras el deploy**: claude/grok/dsh `registro=ok`; **codex `registro=falta-registro` ⇒ `veredicto=fallo`** — `~/.codex/hooks.json` no existe; el gate de codex esta apagado y desde hoy el protocolo lo grita en vez de callar. Ese rojo es correcto y se queda hasta que codex se registre (limite de la 18.15). **Limites declarados**: `--check --host X` no afirma el conjunto; el preambulo del deploy-log sigue diciendo «una copia»; «un registro por PR» es unicidad, no cobertura |

**Fuera de alcance, declarado:** PRs en paralelo (choque de worktree medido);
Graphite/stacks; excepción por `patch-id`; autopilot en otros hosts; leer
transcripts; que GitHub proteja la rama (no disponible en plan free; hacer
público el repo es decisión del operador, no de este plan); `--delete-branch`.

---

## 事前確認

- 事項: escritura de ACLs sobre `~/.claude/hooks/` y `~/.claude/hooks/state/`
  理由: quitar `Modify` a identidades de sandbox sobre el script que corre sin sandbox
  scope: Phase 0 / Task 0.1
- 事項: lectura de `~/.claude/settings.json` y `~/.claude/settings.local.json`
  理由: verificar que el hook sigue registrado (modo de falla silencioso) y el cableado del heal
  scope: Phase 0 / Task 0.3, Phase 2 / Task 2.4
- 事項: remoción de ACE de cuentas BORRADAS (SID no resoluble) en `~/AppData`,
  `~/AppData/Local` y `%TEMP%` — aprobado en sesión 2026-08-08
  理由: A7-bis — `%TEMP%` re-contamina `~/.claude` por `move`, y los 5 SID
  huérfanos estaban explícitos repartidos en esos tres niveles. NO se toca
  `CodexSandboxUsers`, que ahí sí escribe legítimamente
  scope: Phase 0 / Task 0.2
- 事項: escritura de ACLs sobre `~/.claude` (la RAÍZ del perfil, no solo `hooks/`)
  理由: ahí vive el otorgamiento explícito medido en la 0.1; mientras siga, el hook
  se puede desregistrar desde `settings.json` sin tocar el archivo
  scope: Phase 0 / Task 0.2
- 事項: escritura sobre `~/.claude/hooks/summonaikit-harness.sh` (el archivo que gatea cada turno)
  理由: la adopción por reemplazo es el objetivo de la Phase 2
  scope: Phase 2 / Task 2.4
- 事項: escritura sobre `C:\Users\ehven\quality-kit\saikit-gate-heal.ps1` y su batería
  理由: el skip por marcador debe mergearse antes de instalar, para no tener dos escritores
  scope: Phase 2 / Task 2.3, Phase 4 / Task 4.1
- 事項: creación de repo remoto y `git push` a `gon0801/summonaikit-claude`
  理由: regla de dejar en git + remoto todo lo tocado
  scope: Phase 1 / Task 1.1 en adelante
- 事項: descarga del kit del vendor en directorio desechable (ya ejecutada 2026-08-08)
  理由: verificar la premisa de si conviene actualizar; se hizo fuera del home y se borró
  scope: pre-plan, completado
- 事項: escritura y reemplazo recuperable de `~/.codex/hooks/summonaikit-harness.sh`
  理由: instalar la fuente aprobada de Phase 6 y validar un turno real con backup/restore listo
  scope: Phase 6 / Task 6.6
- 事項: alta y revocación de folder trust para repos descartables de Grok
  理由: ejecutar hooks de proyecto durante captura, probe y staging sin instalar el harness global
  scope: Phase 7 / Tasks 7.1, 7.2 y 7.6
- 事項: escritura/reemplazo recuperable y remoción selectiva bajo `~/.grok/hooks/` y `~/.grok/agents/`
  理由: instalar o retirar sólo artefactos con propiedad `summonaikit-claude`, preservando el verifier ajeno
  scope: Phase 7 / Tasks 7.5 y 7.6
- 事項: escritura vía `tools/install-hook.sh` (deploy) en `~/.claude/hooks/`,
  `~/.claude/agents/`, `~/.zcode/agents/` + user-config de zcode, y
  `~/.agents/agents/` (kimi) — codex y grok ya cubiertos por las entradas de
  Phase 6/7
  理由: 13.9 instala hook y perfiles con las disciplinas existentes (tres/cinco
  estados, backups, atómico); zcode/kimi no tenían entrada propia en este
  ledger (hueco desde Phase 5/12, cerrado acá)
  scope: Phase 13 / Task 13.9
- 事項: sesiones vivas de medición en 13.1 (turnos claude contra `hook_lab.sh`)
  理由: los canales de atribución de rol se miden, no se suponen; preferencia por el lab, no el perfil vivo (lección 9.9)
  scope: Phase 13 / Task 13.1, ya ejecutada
- 事項: escritura vía `tools/install-hook.sh` en `~/.claude/hooks/recetas/` (con `MANIFEST.sha256`) y `~/.claude/skills/{sencillo,saikit-verificar-app,saikit-setup-autopilot}/`
  理由: plantar recetas y skills del kit con marca `saikit_owned` + hash por archivo; ACL auditada como `hooks/`
  scope: Phase 16 / Tasks 16.5, 16.9; Phase 17 / Task 17.6; Phase 18 / Task 18.7
- 事項: staging por override (`tools/stage-override.sh`) y turnos vivos `-saikit` en repos descartables LOCALES
  理由: medir recetas y la lane de app real sin tocar el hook global (D10, 17.5)
  scope: Phase 16 / Task 16.8; Phase 17 / Task 17.5
- 事項: external-send acotado al repo descartable `gon0801/<nombre fijado en 18.1>` (topic `saikit-descartable` + marcador): `gh repo create`, `git push`, `gh pr create`, `gh pr merge --squash --match-head-commit`, `gh api` de hilos; destructive: `gh repo delete` SOLO de ese repo, verificado por topic + marcador, o a mano por el operador (recomendado)
  理由: 18.1/18.9 miden merge, post-merge y revert contra Actions reales; expira al cerrar 18.10
  scope: Phase 18 / Tasks 18.1, 18.9, 18.10
- 事項: merge automático en repos del usuario — NO se preaprueba en este ledger: la autorización en uso real es por repo y por dos llaves (`-saikit:autopilot` + `autopilot.json` leído de `origin/<rama>`), con `saikit-merge.sh` fail-closed y acotado (D15/D18); aquí solo tests con `gh` falso
  理由: un permiso permanente en este ledger sería más amplio que lo necesario; el producto lleva su propia autorización
  scope: Phase 18 / Tasks 18.4, 18.5
- 事項: external-send opcional — `telegram-send` con el `Close:` redactado (≤4096 chars)
  理由: aviso al usuario sin mirar la terminal; solo si lo activa en el setup
  scope: Phase 18 / Task 18.5
