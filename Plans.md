# summonaikit-claude — Plans.md

Creado: 2026-08-08
Alcance: hacerse cargo del hook que gatea cada turno y llevar la misma fuente a
los hosts aprobados — Claude Code, zcode, Codex y Grok Build— con pruebas
propias, instalación controlada y cierre de los agujeros que hoy lo vuelven un
control decorativo. Contrato de producto y premisas medidas:
`docs/spec/00-project-spec.md`.

**Contrato de datos:** `not_observed != absent`. Lo que no se pudo observar se
marca `unknown`, nunca se afirma como ausente.

---

## Phase 0: Higiene urgente (independiente del resto)

Purpose: cerrar lo que ya es un problema hoy y no depende de ninguna decisión de
diseño. Si el plan se cancelara entero, esta fase igual debe correr.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 0.1 | `[Guardrail]` `[lane:gate]` `[tdd:skip:system-acl-not-unit-testable]` Quitar `Modify` de `CodexSandboxUsers` y del SID huérfano `S-1-5-21-…-2159037359` sobre `~/.claude/hooks/` y `~/.claude/hooks/state/`. Hoy una identidad **aislada** puede reescribir el script que corre SIN sandbox en cada turno, o plantar `agents_seen=implementer,verifier,reviewer` y anular el gate | `icacls` sobre ambas rutas ya no lista `Modify` para esas identidades; `Gon\ehven`, `SYSTEM` y `Administrators` conservan su acceso; Claude Code arranca y un turno normal corre sin error tras el cambio | - | cc:完了 [d9057b0] |
| 0.2 | `[Guardrail]` `[lane:gate]` `[tdd:skip:system-acl-not-unit-testable]` Quitar `Modify` de `CodexSandboxUsers` y del SID huérfano sobre **`~/.claude` la raíz**: ahí está el otorgamiento explícito `(OI)(CI)` que midió la 0.1 — en `hooks/` solo se heredaba. Mientras siga, esas identidades escriben `settings.json` y **desregistran** el hook: el gate desaparece sin tocar el archivo, el mismo efecto que la 0.1 cerró por la otra vía | `tools/hook-acl.ps1 -Path "$HOME/.claude"` sale 0 **al terminar la corrección**; `settings.json` y `settings.local.json` dejan de ser escribibles por esas identidades; ningún SID no resoluble conserva ACE en `~/.claude`, `AppData`, `AppData\Local` ni `%TEMP%`; un archivo creado en `%TEMP%` y movido a `~/.claude/state/` NO arrastra cuentas borradas (prueba directa del vector A7-bis); un turno normal corre sin error y una tarea delegada al sandbox (`codex-companion.sh`) sigue funcionando. **No se exige que la auditoría se MANTENGA en 0**: A7-bis lo vuelve imposible de garantizar — ese vector se detecta, no se previene | 0.1 | cc:完了 [e19a739] |
| 0.3 | `[Guardrail]` `[lane:fast]` `[tdd:required]` El heal verifica el **REGISTRO**, no solo el contenido: si `settings.json` deja de apuntar al hook, el archivo puede estar perfecto y el gate no existir. Es el modo de falla más silencioso del sistema y hoy nada lo detecta | Fixture con `settings.json` sin la ruta del harness ⇒ `tools/check-hook-registration.sh` lo reporta fuerte y sale 0 (fail-open); fixture con el registro presente ⇒ silencio; settings ilegible o ausente ⇒ `unknown`, jamás "ausente" (Core Rule 2); mutation-test declarado. **El cableado al heal de `quality-kit` NO va acá**: la escritura sobre `saikit-gate-heal.ps1` está declarada para la Task 2.3, y ahí se conecta | - | cc:完了 [2d72a6d] |
| 0.4 | `[Guardrail]` `[lane:fast]` `[tdd:required]` **Revisión cruzada de la Phase 0 (Codex, 2026-08-10)**: `check-hook-registration.sh` tiene tres defectos, uno de ellos medido en vivo. (a) **Cuelgue**: un flag sin valor (`--settings` al final) hace fallar `shift 2` sin consumir nada y el `while` gira para siempre — medido `rc=124` con `timeout`. Es un script que corre en cada `SessionStart` (vía el heal, Task 2.3), así que un cuelgue ahí se come el arranque, y contradice su propio contrato de salir 0 siempre. (b) **Match por subcadena**: `if hook in command` da por registrado un comando que apenas NOMBRE el hook (`echo summonaikit-harness.sh`). (c) **`unknown` mal mezclado**: con un settings legible incompleto y el otro ilegible, afirma que el gate NO corre en las fases faltantes — que podrían estar justo en el que no se pudo leer (Core Rule 2) | Invocación mal formada ⇒ sale != 0 o consume el argumento, y NUNCA gira; un comando que sólo menciona el nombre del hook sin ejecutarlo NO cuenta como registrado; con un settings ilegible presente, las fases faltantes se reportan como `unknown`, no como ausentes; cada arreglo tiene el caso que lo habría atrapado. **No verificados a fondo, a revisar acá**: los asserts de `tests/test_hook_registration.sh` sólo buscan nombres de fase o la palabra `unknown`, así que un mutante que confunda ausencia con `unknown` pasaría igual; y `tmp` no se comprueba tras `mktemp`. **Los 3 defectos cerrados, con su caso cada uno; 3 mutaciones, 3 atrapadas.** El cuelgue sale `unknown` con exit 0 y no con error: el contrato de este archivo es salir 0 SIEMPRE y comunicar por texto, y sus dos llamadores lo asumen — una invocación que no se pudo atender *es* `unknown`. Los dos no verificados también se cerraron: el `mktemp` del test ahora falla limpio (se comprobó plantándolo), y los asserts pasaron a discriminar ausencia OBSERVADA de `unknown`, que antes se confundían porque sólo se miraba que dijera algo. **Límite declarado**: el filtro de "sólo imprimen" NO parsea shell — lo que se verifica es el REGISTRO, no que el comando vaya a ejecutar el hook. Se agregó el caso que protege la forma REAL del registro (env vars por delante y `bash -c`), porque romperla convertiría el arreglo en una alarma falsa en cada arranque | - | cc:完了 [9108225] |
| 0.5 | `[Guardrail]` `[lane:gate]` `[tdd:skip:system-acl-not-unit-testable]` **Revisión cruzada de la Phase 0 (Codex, 2026-08-10)**: `tools/hook-acl.ps1` borra de más, y por lectura del código. (a) **`Test-SidResolvable` devuelve `false` ante CUALQUIER excepción** — un fallo transitorio de resolución (un DC que no contesta) clasifica una cuenta VIVA como borrada y su ACE se elimina: Core Rule 2 violada dentro del único código destructivo del repo. (b) **`-OrphansOnly` no filtra los hijos**: la raíz filtra por `-not Resolvable -and -not IsInherited` (línea 279) y los descendientes sólo por `-not IsInherited` (línea 304), así que `-OrphansOnly -Fix` sin `-RootOnly` elimina ACE explícitos de principales vivos. (c) **Asimetría**: en la raíz un principal resoluble se DEGRADA a `ReadAndExecute`; en los hijos se ELIMINA entero, que no es lo que el contrato del script promete | Una excepción de resolución NO clasifica como huérfano: lo no resoluble se distingue de lo no resuelto, y ante la duda no se borra; `-OrphansOnly` aplica el MISMO filtro en raíz y descendientes; un principal resoluble se degrada, no se elimina, en cualquier nivel. Como la escritura de ACL no es unit-testable (mismo motivo que 0.1/0.2), la evidencia es la **auditoría sin `-Fix`**: sobre el perfil real muestra qué tocaría en cada modo, y esa salida es lo que se compara. **No verificado a fondo, a revisar acá**: el backup excluye los objetos que `Repair-StaleInherited` modifica después, así que el comando que se anuncia como restauración podría no revertir todo. **Los 3 defectos cerrados; 3 mutaciones, 3 atrapadas.** `Test-SidResolvable` pasa a tres estados y sólo el "se miró y no existe" habilita a borrar; la política de reparación y el filtro quedan en UNA función cada uno, compartidas por raíz y descendientes — las dos copias que había son las que divergieron. **Corrección del `[tdd:skip]` de esta misma tarea**: escribir ACL no es unit-testable, pero *clasificar cuál se borra* sí lo era. Un modo biblioteca (`SAIKIT_HOOKACL_LIB_ONLY=1`, un `return` antes del cuerpo ejecutable) permite cargar las funciones sin tocar una sola ACL, así que los tres defectos tienen caso propio en `tests/test_hook_acl.sh` en vez de sólo la auditoría como evidencia. **Evidencia sobre el perfil real** (auditoría sin `-Fix`): los 7 ACE restantes son de `Gon\CodexSandboxUsers`, una cuenta VIVA, y `-OrphansOnly` no selecciona ninguno — justo la distinción que antes no se hacía en los descendientes. **Corrección tras revisión**: el tri-estado estaba en la decisión pero NO en el reporte — `if ($f.Resolvable)` con `$null` caía en el `else` y un SID que no se pudo evaluar se imprimía como `[SID huerfano]`, justo en la salida que esta tarea declara como su evidencia. No borraba de más (la decisión ya decía `no-tocar`); era de reporte. Ahora tres etiquetas, con su caso. **Sigue sin verificar y no se tocó**: el backup excluye los objetos que `Repair-StaleInherited` modifica después | 0.1, 0.2 | cc:完了 [f21a3c2] |

## Phase 1: Red de seguridad antes de tocar nada

Purpose: hoy el hook tiene CERO pruebas de comportamiento. Construirlas contra el
archivo VIVO, en solo-lectura, es lo que vuelve segura cualquier sustitución
posterior — y tiene valor aunque el resto del plan se cancele.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 1.1 | `[Setup]` `[lane:fast]` `[tdd:skip:tooling-setup]` Esqueleto del repo: runner de tests, `bash -n` sobre el hook, `HOME` y directorio de hooks aislados por test (Core Rule 4). Se copia el molde ya probado de `C:\dev\summonaikit-kimi\tests\` | `bash tests/run.sh` sale 0 con el repo vacío de lógica; el runner falla si `bash -n` falla; ningún test escribe fuera de su tmpdir | - | cc:完了 [b41a44b] |
| 1.2 | `[Test]` `[lane:gate]` `[tdd:required]` **Arnés de salida dorada**: capturar los payloads JSON reales de las 3 fases y registrar stdout + exit code del archivo VIVO para N escenarios (armado/sin armar, evidencia completa/incompleta, ciclos de revisión, presupuesto agotado). Es el paso de mayor valor del plan: sin esto, cualquier reemplazo es a ciegas | Existe una línea base reproducible; re-correrla contra el archivo vivo sin cambios da resultado idéntico; el arnés jamás escribe en `~/.claude/`. **DoD cumplida; alcance parcial declarado**: los payloads son reconstruidos, no capturados de stdin — la captura real es la Task 1.4 | 1.1 | cc:完了 [3421ad0] |
| 1.3 | `[Test]` `[lane:gate]` `[tdd:required]` Suite de comportamiento sobre la semántica ACTUAL del gate: armado por sentinel, evidencia de tools, secuencia de roles, recibo, presupuesto de 2 ciclos, y las salidas por target (`claude` vs `cursor`) | Cada gate tiene al menos un caso que pasa y uno que bloquea (tabla por gate en el spec); mutar la condición de cada gate hace fallar su test — 13 mutaciones, 13 atrapadas, y la declaración la **emite la corrida**, no un reporte a mano. Se agregó una batería más, no pedida por la DoD: las guardias del propio mutation-test, porque "13 de 13" describe igual a un driver incapaz de ponerse en rojo | 1.2 | cc:完了 [1bb8cba] |
| 1.4 | `[Test]` `[lane:fast]` `[tdd:skip:captura-manual-con-persona-adelante]` **Deuda declarada de la 1.2**: la línea base se grabó contra payloads *reconstruidos* — fieles a la forma real y con los campos sacados de transcripts reales, pero no una captura cruda de stdin. Claude Code fotografía los hooks al arrancar, así que la captura de verdad necesita una sesión nueva: un paso manual que no se podía hacer dentro del turno. `tools/capture-payloads.sh` ya tiene el procedimiento | 308 payloads capturados en un turno real. Diferencias de forma corregidas: los 54 fixtures no eran JSON válido, faltaban `prompt_id`/`effort`/`tool_use_id`/`duration_ms` y el `last_assistant_message` del Stop, la herramienta de subagentes se llama `Agent` y no `Task`, y el `tool_response` de `Bash` no trae `exitCode`. **Declaración del diff: ningún veredicto se movió** — cambian 7 escenarios, todos por el escapado de rutas en el log más el `task_hash` del 04. Rinde tres defectos nuevos (A9, A10, A11) que se llevan sus propias tareas. **Corrección de registro (2026-08-10)**: la afirmación "los 54 fixtures no eran JSON válido" quedó cumplida sólo en parte — **5 siguen inválidos**, los encontró la revisión cruzada de la Phase 1, y se los lleva la Task 1.5 | 1.2 | cc:完了 [1f53e5f] |
| 1.5 | `[Test]` `[lane:gate]` `[tdd:required]` **Revisión cruzada de la Phase 1 (Codex, 2026-08-10)**, dos hallazgos que tocan lo que la fase ya dio por cumplido. (a) **5 fixtures no son JSON válido** —los 4 del escenario 16 y una línea del transcript del 14— por `"cwd": "C:\dev\demo"`: `\d` no es un escape válido. Hoy pasan porque el hook parsea con `sed` greedy y trata el payload como texto opaco, pero **las Tasks 3.1 y 3.2 lo reemplazan por un parser JSON real**, y ahí los escenarios que sostienen A2 y A9 dejan de parsear. La 1.4 declaró haber corregido exactamente esto. (b) **`tests/run.sh` reporta `PASS` sobre `unknown`**: sin el hook vivo los tests declaran correctamente `unknown — no se pudo mirar` (Core Rule 2 bien aplicada), pero el runner los publica como `PASS` y cierra con `OK`, así que una máquina sin el archivo bajo prueba queda entera en verde sin haber verificado nada. Tras la Task 2.1 hay arreglo natural: la fuente vive en el repo, así que se puede verificar contra ella en vez de declarar `unknown` | Los 62 fixtures `.json` y los 15 `.jsonl` parsean con un parser JSON real, y existe el test que lo comprueba —el que habría atrapado esto—; el veredicto de cada escenario afectado se re-graba y ese diff es la declaración de qué cambió; `run.sh` distingue `verificado` de `unknown` en su resumen y no cierra `OK` si todo fue `unknown`; con el hook vivo ausente la suite cae a la fuente del repo y sólo reporta `unknown` si tampoco está. **No verificados a fondo, a revisar acá**: el guard de fugas sólo inventaría archivos regulares (ciego a symlinks y directorios vacíos); `mktemp`/`mkdir` sin comprobar en `run.sh`; el driver de mutación no exige las 13 mutaciones únicas, así que la DoD "13 de 13" puede degradarse en silencio; y `capture-payloads.sh` no reserva su nombre de archivo de forma atómica. **Las dos mitades cerradas.** Los 5 fixtures corregidos y con su guardia (**la primera pasada cerro 4 de 5**: el del escenario 14 paso de invalido ruidoso a VALIDO Y FALSO — `
` ya era escape legal, sobrevivio al arreglo y la ruta decodificaba con un salto de linea adentro; lo encontro la revision de esta misma tarea, y el guardia era ciego por construccion porque solo miraba parseabilidad. Ahora exige ademas FIDELIDAD: ningun campo de ruta decodifica a un caracter de control) (`tests/test_fixtures_json.sh`: 62 `.json` + 15 `.jsonl` parsean con parser real, y un caso que falla si el árbol de fixtures desaparece — "0 archivos, 0 malos" no es "todos válidos"). **Diff de la línea base declarado**: cambió UN escenario (`16`), 4 líneas, todas la misma ruta del log — el hook registra el texto crudo y el payload ahora es JSON válido; **ningún veredicto se movió**, porque la línea base anterior describía un payload que el host nunca emite. `run.sh` distingue `PASS` de `UNKNOWN` (exit 3), dice cuántos hubo y NO cierra `OK` si todos lo fueron; un fallo real sigue mandando sobre un `unknown`. Y antes de rendirse se prueba lo que hay: sin hook vivo las baterías caen a la fuente del repo y lo dicen, resuelto en un solo lugar (`tests/lib/hook_bajo_prueba.sh`) para que las 4 copias no vuelvan a divergir. **Error propio, declarado**: el primer intento de escapar los fixtures usó un regex que también duplicó los `\\` válidos y dejó MÁS archivos rotos; lo atrapó el test recién escrito y se rehizo consumiendo el par completo. **Sin verificar y sin tocar**, tal como estaban listados: guard de fugas ciego a symlinks y dirs vacíos, `mktemp`/`mkdir` sin comprobar, el driver de mutación sin exigir las 13 únicas, y `capture-payloads.sh` sin reserva atómica | 1.4 | cc:完了 [a9e9fb0] |

## Phase 2: Adopción

Purpose: mover el archivo a control propio sin ventana en la que el harness quede
roto o sin gate.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 2.1 | `[Feature]` `[lane:gate]` `[tdd:required]` La fuente del repo es el archivo vivo, byte a byte, más un **marcador de identidad** en línea fija (`# SAIKIT-CLAUDE-OWNED <repo> <version>`). Sin el marcador no hay forma de distinguir "nuestro" de "del vendor" salvo por hash del archivo entero, que cambia con cada edición legítima | El arnés de 1.2 da salida idéntica entre la fuente del repo y el archivo vivo en TODOS los escenarios; el marcador está presente y es detectable por `grep` de una línea. **Afirmado además por una vía más fuerte**: `sed '2d'` sobre la fuente devuelve el vivo byte a byte (`cmp`), o sea mismos bytes y ningún margen de comportamiento. **Declarado**: el código muerto medido en la 1.3 se adoptó tal cual — borrarlo acá destruiría la propiedad byte a byte que vuelve verificable la adopción entera; su remoción es tarea propia | 1.3 | cc:完了 [8dfe15c] |
| 2.2 | `[Setup]` `[lane:gate]` `[tdd:required]` Instalador por reemplazo con **tres estados**: *nuestro* ⇒ verificar/reparar; *vendor conocido* (hash en manifiesto) ⇒ archivar y reemplazar; *desconocido* ⇒ NO tocar y reportar fuerte. Escritura atómica: `bash -n` sobre temporal y recién ahí `mv`. Backup fechado | Destino desconocido ⇒ exit ≠ 0 sin escribir; destino vendor ⇒ backup + reemplazo; destino nuestro e idéntico ⇒ no reescribe (mtime intacto); un temporal inválido nunca llega al destino; 2 corridas dejan el archivo byte-idéntico. **Los 5 puntos verificados**, más un sexto estado que la DoD no nombraba y hacía falta: `unknown` ≠ `desconocido` — no haber podido leer el manifiesto no es haber visto que el hash falta, y el reporte no acusa al destino. **Batería de mutación de 9, 7 atrapadas**: las 2 que quedan en pie (el `cmp` del temporal y la atomicidad del `mv`) están declaradas en el spec como afirmaciones sostenidas por lectura, no por medición. Un `--dry-run` contra el destino REAL clasifica el archivo vivo como vendor conocido, así que el manifiesto describe la máquina y no una suposición. **Defecto propio corregido en el camino**: el manifiesto volvía con CRLF acá y una línea de solo hash habría clasificado el vendor como desconocido; el arreglo efectivo es `*.sha256 text eol=lf`, y el `sub(/\r$/)` del awk es portabilidad —su caso pasa con y sin él, declarado—. **`--restore-vendor` NO va acá**: es de la 2.4; lo que esta tarea le deja es el contrato de los backups (`saikit-backups/<nombre>.<vendor\|nuestro>.<fecha>.bak`) | 2.1 | cc:完了 [3675ca2] |
| 2.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` `quality-kit`: saltear cualquier archivo que lleve el marcador de propiedad, **mergeado ANTES** de instalar lo nuestro. Dos escritores en `SessionStart` sobre la misma ruta con modelos distintos (ancla vs reemplazo) pueden dejar un archivo doblemente parcheado o truncado. **Además**: cablear acá `tools/check-hook-registration.sh` (Task 0.3) al heal, que es donde su escritura está declarada | Fixture con marcador ⇒ `saikit-gate-heal.ps1` lo saltea y lo dice; fixture sin marcador (`.codex`/`.cursor`/`.agents`) ⇒ se parchea igual que hoy; la batería existente sigue verde. **Los 3 puntos verificados**: la batería del kit pasa de 416 a 443 asertos, 0 fallos. Dos decisiones que la DoD no nombraba y hacían falta: el criterio del skip es **más ancho** que el del instalador —marcador en CUALQUIER línea, no sólo la 2— porque ante una señal de propiedad ambigua el escritor por anclas se abstiene igual, y los dos convergen en no escribir; y el skip corta **antes** de evaluar anclas, para que un archivo nuestro no quede reportado como `ANCLAS-CAMBIARON` en cada arranque. El cableado del registro corre sólo si hay `settings.json` o `settings.local.json`, e informa `unknown` si no encuentra verificador (Core Rule 2); contra el settings REAL calla, o sea que no agrega ruido al arranque de verdad. **Rojo previo medido: 17 asertos** caídos sin la implementación, con los 416 preexistentes intactos. **2 mutaciones dirigidas, 2 atrapadas** (criterio estrecho, guarda de settings). **Defecto propio corregido en el camino**: el fixture del marcador fuera de la línea 2 lo insertaba adentro del ancla B, así que sobrevivía a su mutación por el motivo equivocado —el ancla rota, no el skip—. **Declarado**: hasta la 2.4 el heal sigue parcheando exactamente como hoy, porque el hook vivo todavía NO lleva el marcador (medido) — que es justo el orden que esta tarea pedía. El cambio de comportamiento vive en `quality-kit` (`446e3b3`); acá vive el test que lo mira desde el lado del marcador. **Revisión cruzada (Codex, 1 ronda): 3 hallazgos, los 3 aceptados y corregidos**, todos con la misma raíz — el cableado del registro fallaba en silencio justo bajo `-Quiet`, que es el modo con el que corre `SessionStart`. La política de "no repetir avisos en cada arranque" vale para el skip (pasa en cada arranque sano) y no para un `unknown` (sólo aparece si algo ya se rompió). Se agregó tope de tiempo y chequeo de exit code; batería 443 → 451. **Límite medido y declarado**: un `sleep` del bash de Git for Windows queda huérfano y ningún barrido por parentesco lo alcanza, así que el tope garantiza que el heal deje de esperar, no que se libere el stdout; el resto lo acota el timeout de 30 s del propio `SessionStart` | 2.2 | cc:完了 [39e51d1] |
| 2.4 | `[Setup]` `[lane:gate]` `[tdd:required]` Puesta en producción en dos pasos: primero staging por **override de proyecto** (`<repo>/.claude/hooks/`, que la registración ya prefiere sobre el global) en un repo descartable; después install global con `--restore-vendor` de un comando | El staging gatea turnos reales sin tocar el archivo global; `--restore-vendor` devuelve el estado previo y se prueba ANTES de instalar; tras el install global, un turno `-saikit` real bloquea y uno pelado no. **La premisa del renglón, medida y corregida**: el override NO es una preferencia del host — lo hace el comando REGISTRADO (`git rev-parse --show-toplevel` primero, `$HOME` como fallback). De ahí salen tres cosas que fijan el diseño: corre UN solo hook por turno, el estado también queda aislado (`STATE_ROOT` sale de `dirname $0`), y hace falta un repo git o el archivo queda puesto sin correr nunca. **El staging no toca el global, medido antes y después** —no "no lo escribimos a propósito"—, y `tools/stage-override.sh` no lee el registro para creerle: lo **ejecuta** con un HOME desechable donde el fallback apunta a un archivo inexistente, así que si algo corrió fue el override. **`--restore-vendor` se probó ANTES de instalar y sobre los archivos REALES**: copia del vivo en el repo descartable ⇒ clasificado vendor conocido ⇒ reemplazado ⇒ restaurado byte a byte (`cmp` contra el vivo). Tras el install global, `sed '2d'` del archivo vivo reproduce el vendor archivado: **el único cambio en el archivo que gatea cada turno es la línea del marcador**. **La costura de la 2.3 entró en efecto y se midió el día que correspondía**: el heal reporta `saltado-propiedad` sobre `.claude` y `ya-parchado` en los otros 3 perfiles. **12 mutaciones dirigidas, 12 atrapadas** (7 sobre `--restore-vendor`, 5 sobre el staging). **Defecto propio corregido en el camino**: la medición comparaba los NOMBRES de los archivos de estado, así que una segunda corrida sobre un staging con estado previo habría concluido que el override no corrió; y el primer caso escrito para taparlo usaba una clave de proyecto inventada y pasaba sin discriminar nada — se rehizo calculando la clave como la calcula el hook. `test_hook_source.sh` (Task 2.1) se puso rojo por haber cumplido su propósito y ahora distingue los dos estados sanos del vivo, sin dejar de gritar ante el único que no lo es. **El turno real se corrió el mismo día (operador adelante) y las dos mitades pasaron**: con `-saikit` armó y dejó estado en el staging, sin sentinel no creó nada, y el hook del perfil siguió byte a byte igual con cero claves nuevas en su `state/`. **Ese turno corrigió una conclusión propia**: al cerrar la tarea se declaró incumplible la cláusula "un turno `-saikit` real bloquea" apoyándose en A10, y el estado que dejó dice `agents_seen=implementer,verifier,reviewer` — los tres roles, en orden, donde A9 afirma que queda vacío. El transcript explica por qué: la herramienta se llama `Agent` (esa mitad de A9 sigue en pie) pero el rol viajó en `subagent_type`, que es el campo que el hook SÍ lee, y `agent_type` no aparece ni una vez. O sea que el turno no bloqueó porque se cumplieron TODOS los gates, no porque el gate esté inerte. Lo que sigue sin resolverse es A10 (este turno satisfizo todo, así que no distingue las dos ramas) y **lo que NO se probó en vivo es un turno real que FALLE** — eso está medido sintéticamente con el comando registrado real, no con una sesión que se porte mal a propósito. **Paso manual pendiente, como en la 1.4**: la sesión nueva de Claude Code en `C:\dev\saikit-staging` — el host fotografía los hooks al arrancar. **Sin verificar, declarado**: la huella del hook global antes/después es un guardia que ningún caso mata, porque ningún camino de prueba escribe el global. **Revisión cruzada (Codex, 1 ronda — kimi se colgó a los 600 s y la cadena siguió): 5 hallazgos, 4 aceptados enteros y 1 a medias.** El más serio no era el marcado como alto: la medición del staging **pisaba el contenido** del estado preexistente, así que el siguiente turno real de ese repo arrancaba armado por una medición — A4 reproducido a mano por la herramienta que venía a ayudar; y el caso propio que lo tapaba comprobaba que el archivo existiera, no que no hubiera cambiado. Ahora el estado se aparta entero antes de medir y se devuelve después. El marcado alto —`--restore-vendor` no comprobaba el backup contra el manifiesto— se aceptó con la severidad matizada: no agrega superficie de escritura, pero era incoherente que la ida se negara a tocar un destino desconocido y la vuelta instalara cualquier cosa que parseara; la consulta al manifiesto quedó en una sola función para las dos direcciones. También: el exit code del instalador se anunciaba siempre como 0 (`$?` dentro de un `if !`), y cualquier fallo del lector del settings distinto de `rc 1` se reportaba como "no está registrado" en vez de `unknown`. **Corregir los hallazgos destapó dos defectos propios más**, que encontró la batería: `limpiar_medicion` no era idempotente —corre explícita y por trap, y la segunda pasada borraba el estado que la primera acababa de devolver— y el borrado necesitaba bandera propia, porque sin ella una salida temprana con el trap ya instalado habría borrado el estado del operador. **17 mutaciones dirigidas, 17 atrapadas**; sin hallazgos residuales | 2.3, 0.3 | cc:完了 [6224485] |

## Phase 3: Cerrar los agujeros

Purpose: hoy el gate es evadible con texto plano. Cada task cierra un defecto
medido, con el test que lo habría atrapado.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 3.1 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **A1, redactado con lo medido en la 1.2** — `json_string_field` es greedy y lee el payload crudo: cualquier `subagent_type` que aparezca como **clave JSON** fuera de `tool_input` se toma como el subagente que corrió, y con varias ocurrencias gana la ÚLTIMA. Medido 2026-08-09: con `tool_input.subagent_type=implementer` y otro `subagent_type` más adelante, el hook anota **reviewer** — no solo inventa un rol, **borra el legítimo**. El vector que decía este renglón (un archivo del repo cuyo CONTENIDO menciona el campo) NO reproduce: el contenido llega con las comillas escapadas. Parsear con parser JSON real (el port de Kimi ya usa `python` con fallback declarado) o leer sólo `tool_input` del primer nivel | `subagent_type` como clave fuera de `tool_input` NO registra rol; `tool_input.subagent_type` legítimo SÍ; con las dos presentes gana la de `tool_input`, no la última; revertir el arreglo hace fallar el test; el escenario 12 de la línea base cambia y ese diff es la declaración de qué cambió. **Los 5 puntos cumplidos.** **Primero se midió dónde viaja el campo de verdad**, sobre los 308 payloads crudos de la captura: `subagent_type` aparece **solo** en `.tool_input.subagent_type` (8 de 8), nunca en primer nivel — esa medición es lo que vuelve seguro el arreglo restrictivo, porque acotar la lectura no toca ninguna vía legítima. (`agent_type` de primer nivel, 281, es A9 y se lo lleva la 3.7; acá no se tocó.) **El arreglo es un escáner, no un parser JSON**: recorre el payload llevando si está dentro de una string y a qué profundidad de llaves, y contesta la única pregunta que hace falta — ¿esta clave está adentro del `tool_input` de primer nivel? **Cero dependencias nuevas, y es decisión declarada**: el renglón autorizaba `python` con fallback o leer sólo `tool_input`; se eligió lo segundo en `awk`, que ya es dependencia dura del hook (`json_escape`, el `task_hash`), porque un primario-con-fallback son dos políticas que divergen en silencio — lo que la 0.5 encontró en `hook-acl.ps1`. Los escapes se dejan crudos a propósito: un valor escapado no mapea a ningún rol, así que el error cae del lado seguro. **Postura de fallo declarada**: si el escáner no devuelve nada no se registra rol, el mismo desenlace que un payload sin el campo — inventar uno para "dejar pasar" *es* A1. **Diff de la línea base**: cambió UN escenario (`12`), 4 líneas; los otros 15 no se movieron, incluidos los cuatro turnos completos (05, 06, 10, 11) que delegan por `tool_input` — la vía legítima quedó intacta, medida y no supuesta. **2 mutaciones dirigidas, 2 atrapadas, cada una por un caso distinto**, y el orden de los dos casos en `CASOS_G3` no es cosmético: la batería corta en el primer rojo, así que invertirlos dejaría una mutación sin caso propio. Total del hook: **15 mutaciones, 15 atrapadas**. **Un test se puso rojo por haber cumplido su propósito y el arreglo es parte de esta tarea**, igual que en la 2.4: `test_hook_source.sh` exigía que el arnés AVISARA del cambio de identidad, cierto mientras la línea base viniera del vendor e imposible desde que la Phase 3 la regraba contra la fuente. Ahora distingue los dos estados sanos —otra identidad ⇒ se exige el aviso; la fuente misma ⇒ se exige el silencio— y sin alguno de los dos shas dice `unknown`. **3 mutaciones sobre ese caso, 3 atrapadas**, una dirigida a la rama nueva (mutar el arnés para que avise siempre), porque las dos primeras caían las dos en la rama vieja. **Se corrió el install**, que es lo que lleva el arreglo al archivo que gatea cada turno: destino *nuestro pero distinto* ⇒ reparado con backup, vivo byte a byte igual a la fuente. **Lo que NO cierra, declarado**: el gate sigue advisory —quien controla el `tool_input` de un evento de delegación puede nombrar el rol que quiera; lo que se cerró es que lo haga desde el *resultado* de una herramienta— y la laxitud de `implemented` sigue igual, medida como inerte | 2.4, 1.5 | cc:完了 [d8cc610] |
| 3.2 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **A2 + A8, misma raíz** — el recibo y la pausa se buscan grepeando el tail del transcript CRUDO. De ahí salen dos fallas opuestas: **A2**, un archivo con `SUMMONAIKIT HARNESS PAUSED` adentro de un resultado de herramienta deja pasar cualquier turno; y **A8** (medido en la 1.2), como el JSONL guarda los saltos escapados, la etiqueta del recibo queda precedida por la `n` de `\n` y `has_receipt_label` —que exige un carácter no alfabético— no la ve: un recibo correcto en texto corrido falla las 6 etiquetas y el turno se bloquea hasta agotar presupuesto. El arreglo es uno solo: decodificar el transcript y evaluar el TEXTO DEL ASISTENTE línea por línea, en vez de grepear el JSON crudo (parser real con fallback declarado, igual que la 3.1) | Pausa y etiquetas que aparecen SOLO dentro de un resultado de herramienta ⇒ el gate sigue exigiendo (A2); recibo escrito por el asistente como texto corrido ⇒ las 6 etiquetas cuentan como presentes (A8); el mismo recibo en viñetas sigue contando; revertir cualquiera de las dos mitades hace fallar su test; en la línea base el escenario 05 pasa de bloquear a cerrar limpio y el 14 pasa de permitir a seguir exigiendo, y ese diff es la declaración de qué cambió. **Los 6 puntos cumplidos.** **El arreglo son dos walkers awk** — `assistant_text_payload` (extrae `last_assistant_message`, depth 1) y `assistant_text_transcript` (extrae `content[].text` con `type:"text"` exacto de mensajes `role:"assistant"`) — ambos char-por-char con el idiom de la 3.1 (`ins`/`esc`/`depth`), pero ésta **decodifica** `\n \" \\ \t` (la 3.1 los dejaba crudos a propósito: un rol escapado no mapea; una etiqueta detrás de `\n` sí debe contar). **Por qué dos funciones y no una con fallback**: awk no "falla" con JSON malo, devuelve vacío — y con `$text` vacío el gate BLOQUEA (reclama las 6 etiquetas), que es lo contrario de fail-open. Cada canal vive en su función; transcript ilegible ⇒ canal 2 vacío ⇒ se evalúa sólo canal 1, sin rama especial. **El `tail -n 160` se conserva** (CORRECCIÓN 2 del plan): leer el transcript entero agrandaría el hueco de un recibo de hace 5 turnos y es caro en Windows. **Un atajo `index($0,"\"assistant\"")` se probó y se sacó**: era un acelerador declarado no-autoridad, pero filtraba líneas de mensajes `user` que el walker necesitaba ver para que la mutación de `role:assistant` fuera atrapable — sin él, el walker es la única autoridad. **Premisa del orden declarada**: `type` antes de `text` y `role` antes de `content` (308 payloads medidos); si se invierte, fallo visible (el gate reclama etiquetas), no silencioso. **Diff de la línea base**: el escenario **05** pasa de exit 2 + 6 etiquetas missing + `cycle=1` a **exit 0, stdout vacío, estado borrado** (cierre limpio, arreglo de A8); el **14** pasa de exit 0 + "estado sin cambios" a **exit 2** con los 4 motivos del gate (sin recibo, sin etiquetas, sin evidencia, sin subagentes) + `cycle=1` — idéntico al 07/paso 03, arreglo de A2. **03, 07 y 08 no se movieron** (CORRECCIÓN 4 verificada): sus textos del asistente no contienen `pytest`/`skipped`, así que el cambio de superficie de `:716` —el tercer consumidor de `$text`— no les alcanza. **19 mutaciones, 19 atrapadas** (15 heredadas + 4 nuevas: canal payload crudo, canal transcript vacío, texto incluye tool_result, texto incluye tool_use), cada una con su caso propio. **Costo medido** (CORRECCIÓN 2): transcript de 530 KB con un `tool_result` de 500 KB — hook vivo 262 ms, hook con walker 276 ms, **delta ~14 ms**, órdenes de magnitud por debajo del tope de ~1 s; no hizo falta ajustar el recorte. **Revisión cruzada (codex, 1 ronda — kimi se colgó a los 300 s y la cadena siguió): 4 hallazgos, 1 aceptado y 3 declarados.** El aceptado (severidad media, verificado empíricamente por codex): dos bloques `type:text` del mismo mensaje se concatenaban sin separador — si el primero termina en letra, la etiqueta del segundo no se reconoce. Arreglado: `\n` al cerrar cada content item, con su caso propio (`caso_g4_recibo_dos_bloques_pasa`) que lo habría atrapado. Los 3 declarados (baja, no se arreglan): la premisa de `role` antes de `content` no estaba explícita (ahora sí, en el comentario del walker); el caso D prueba `thinking` no `tool_use.input` (el walker no rastrea depth 5, así que el contenido de `input.command` es inherentemente ignorado — el escenario 14 de la baseline cubre el vector real del `tool_result`); y la medición de costo faltaba en Plans.md al momento del commit (se declara acá). **Segunda ronda sobre staged (codex): 3 hallazgos, 0 aceptados y 3 declarados.** (1) [media] el walker depende del orden de claves — ya declarado en el comentario del walker como premisa medida (308 payloads) y consistente con cómo el hook entero depende del orden para sus greps de sed; (2) [baja] el caso D prueba `thinking` no `tool_use.input` — ya declarado en el cierre de esta tarea; (3) [baja] `.zcode/plans/` se había colado en el commit como artefacto temporal — **arreglado**: sacado del staging y `.zcode/` agregado al `.gitignore`. Sin severidad alta ⇒ no hay tercera ronda; los residuales se declaran acá. **Se corrió el install**, que es lo que lleva el arreglo al archivo que gatea cada turno: destino *nuestro pero distinto* ⇒ reparado con backup, vivo byte a byte igual a la fuente. **Lo que NO cierra, declarado**: A6 (`transcript_path` arbitrario) es Task 3.6 — la 3.2 no acota la ruta, pero al exigir estructura de transcript un archivo arbitrario deja de producir texto útil, lo que reduce el impacto de A6 sin cerrarlo; el gate sigue **advisory**; un recibo de un turno anterior dentro de las últimas 160 líneas sigue contando — límite conocido, no ampliado por esta tarea | 3.1 | cc:完了 [3fbed93] |
| 3.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **A3** — `TEST_RUNNER_RE` sin fronteras de palabra: `cat pytest.log` cuenta como verificación. Mismo arreglo ya validado en el port de Kimi (Task 2.15) | `cat pytest.log` y `cat tsconfig.json` ⇒ no marcan verificado; `pytest -q` y `./node_modules/.bin/vitest run` ⇒ sí. **Los 4 puntos cumplidos.** **El arreglo es un wrapper** (`TEST_RUNNER_WORD_RE`) que exige que el runner esté flanqueado por start/end o un carácter que NO forme parte de un nombre de archivo `[A-Za-z0-9_.-]`. `TEST_RUNNER_RE` se mantiene intacto como única fuente de la lista de runners — el wrapper lo referencia, no lo duplica. Mismo idiom que el port de Kimi (líneas 229-230 de su hook), calibrado sobre el `TEST_RUNNER_RE` real y no un regex de ejemplo. **El lado derecho tiene tres alternativas**, y la del medio es la novedad sobre el borrador de Kimi: `\.(…)` acepta un punto **no** seguido de alfanumérico/guion/guion-bajo/punto — un punto al final de una frase (`Verify: se corrió pytest.`) cuenta, pero `pytest.log` no. **Por qué importa la rama del punto**: desde la 3.2, `:870` evalúa la **prosa decodificada del asistente**, no comandos. Sin esa alternativa, un recibo que termina con `pytest.` pierde el crédito y el gate exige de más — el mismo daño que A8 cerró. **Verificado contra 8 casos** (cat pytest.log, cat tsconfig.json, cat pytest._cache, pytest -q, ./node_modules/.bin/vitest run, python -m pytest, dos formas de prosa con punto final). **3 casos G2 nuevos** (no 1): `caso_g2_runner_en_path_no_marca` (`cat pytest.log` y `cat tsconfig.json` y `pytest._cache` y `pytest.-old` → `verified=0`), `caso_g2_runner_con_ruta_marca` (`./node_modules/.bin/vitest run` → `verified=1`), `caso_g2_excusa_con_punto_final_no_reclama` (`Verify: se corrió pytest.` no reclama evidencia). **1 fixture nuevo**: `_RECIBO_SIN_RETRO_PYTEST_PUNTO`. **21 mutaciones, 21 atrapadas** (19 heredadas + 2 nuevas: `runner_sin_frontera` y `runner_frontera_sin_punto_de_frase`, cada una con su caso propio). **Dificultad con MSYS2**: el sed para la mutación de punto-de-frase no funcionaba por corrupción de backslashes en el shim de Git for Windows; se reemplazó por `awk '{gsub(/\\\.\(/, "XX("); print}'` que maneja los escapes de forma predecible. **Bug del script cross-review arreglado** (lateral, en `quality-kit/cross-review.ps1`): `WaitAll(5000)` no capturaba stderr/stdout parcial tras `taskkill` porque los pipes quedaban abiertos por procesos huérfanos — cambiado a `Wait` individual por task. Sin este fix, la salida de codex salía en blanco y el cuelgue era indiagnosticable. **Cross-review (codex, 1 ronda sobre staged): 3 hallazgos, 1 aceptado y 2 declarados.** El aceptado (severidad media, verificado empíricamente por codex): la rama `\.([^A-Za-z0-9]|$)` aceptaba `pytest._cache`, `pytest.-old`, `pytest..bak` porque `_`, `-`, `.` no son alfanuméricos. Arreglado: cambiando `[^A-Za-z0-9]` por `[^A-Za-z0-9_.-]` en la rama del punto, y agregando `pytest._cache` y `pytest.-old` al caso `caso_g2_runner_en_path_no_marca`. Los 2 declarados (baja): el plan contradecía 20 vs 21 mutaciones (cosmético); y Plans.md seguía en `cc:TODO` al momento del commit (se cierra acá). **Diff de la línea base**: el escenario **13** (`13-defecto-a3-cat-del-log`) — las entradas `verified: cat pytest.log` y `verified: cat tsconfig.json` del evidence.log desaparecen, y `verified=1` pasa a `verified=0` en ambos pasos. El escenario **no tiene Stop** (sólo prompt + 2 tools), así que el diff es sólo sobre `verified`. **03, 07 y 08 no se movieron** (controles negativos). **Se corrió el install**: destino *nuestro pero distinto* ⇒ reparado con backup. **Lo que NO cierra, declarado**: un runner citado como palabra completa en un comando que no lo ejecuta (`grep vitest config.ts`) sigue contando; el gate sigue **advisory**; A11 (el guardia de fallas busca `exitCode` que el payload no trae) sigue abierto y es la Task 3.8 | 3.1 | cc:完了 [7195a05] |
| 3.4 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **A4** — estado por proyecto y sin revalidar el sentinel: un turno `-saikit` abandonado sigue cobrando recibo a turnos que no lo pidieron (reproducido en vivo 2026-08-08, obligó a un borrado manual). Llavear por sesión y desarmar ante prompt sin sentinel, salvo corrección al vuelo | Dos sesiones del mismo repo no comparten estado; prompt sin sentinel desarma y el Stop siguiente no bloquea; una corrección al vuelo NO desarma; el presupuesto agotado limpia el estado. **Los 4 puntos cumplidos.** **El arreglo son cuatro cambios**: (E1) el estado se llavea por proyecto **Y sesión** — `session_id` viaja en cada payload (medido Task 1.4) y se lee con un escáner nuevo `json_top_level_string` (depth==1, mismo idiom de la 3.1) que **no** es el `json_string_field` greedy (ese es el defecto A1) y que ignora un `session_id` anidado en `session_crons`/`background_tasks`; el valor **se sanea** (`[^A-Za-z0-9_-]`→`_`, truncado), no se hashea, así el directorio queda legible+sin colisión+sin traversal (familia de A6); `RN_PENDING_PATH` se queda **por proyecto** (`PROJECT_DIR`) a propósito, lección del fallo "codex NEW (a)"; (E2) prompt sin sentinel desarma borrando `STATE_PATH/LOG_PATH/RN_ORDER_PATH`, acotado a `PHASE=prompt` para no tocar el armado de cursor en `session`; (E3) la corrección al vuelo es un prompt **con** `-saikit` que re-arma (no desarma); (E4) presupuesto agotado limpia el estado antes de `emit_budget_exhausted`. **Decisión de diseño confirmada con Gon**: la corrección al vuelo = prompt que aún trae `-saikit`; re-armar resetea `cycle` a 0 (no se persigue "preservar presupuesto", era la opción descartada). **La cláusula 3 es vacua bajo ese diseño** (el armado pisa exactamente lo que el desarme borra) — `caso_g1_correccion_al_vuelo_no_desarma` queda como **guardia de regresión**, declarado sin mutación propia (opción A del plan). **5 casos nuevos** (c.1 dos sesiones con **dos mitades** — B no ve estado de A **y** el estado de A sobrevive al turno de B; c.2 desarme; c.3 guardia; c.4 limpieza) + un caso de regresión `session_id` anidado. **4 mutaciones nuevas** (session_sin_llave, desarmar_quita_borrado, session_id_greedy, presupuesto_no_limpia), cada una acreditada a **su** caso; `desarmar_quita_borrado` neutraliza la condición de E2 (no borra el rm: dejaría `if…; then fi` vacío que `bash -n` rechaza). El lab parametriza `session_id` y `lab_hook_swap` **re-descubre** la ruta de estado tras cada swap (sin eso el `LAB_ESTADO_PATH` cacheado apuntaba al hoyo bajo el mutante de keying y la mutación se acreditaba a `caso_g1_arma_con_sentinel`). **Diff de la línea base**: sólo rutas — `state/<PROJECT_KEY>/` → `state/<PROJECT_KEY>/<SESSION_UUID>/` en cada escenario armado + cabecera (988→1079 líneas); **sin cambio de comportamiento** en los 16 escenarios. **Cross-review codex (1 ronda sobre staged)**: 2 hallazgos — [media] session_id anidado competía con el de primer nivel (regresión al lector greedy re-llaveaba la ruta a mitad de turno) → **cerrado** con caso+mutación propios; [baja] cada sesión deja un directorio sin recolección → **declarado** (era la CORRECCIÓN 6 del plan). **Se corrió el install**: destino *nuestro pero distinto* ⇒ reparado con backup, vivo byte a byte igual a la fuente. **Lo que NO cierra, declarado**: cross-host es Task 5.3; la cláusula 3 no tiene mutación propia (vacua, guardia); re-armar resetea `cycle` (un prompt incidental sin sentinel a mitad de ciclo pierde el presupuesto gastado); cursor arma en `session` y el `prompt` siguiente sin `-saikit` lo desarma; el gate sigue **advisory** | 3.1 | cc:完了 [d10c6e1] |
| 3.5 | `[Guardrail]` `[lane:fast]` `[tdd:required]` **A5** — `command_text` crudo persiste en `harness-evidence.log`: un comando con credenciales en la línea queda en claro hasta el cierre limpio, y si el turno no cierra bien, indefinidamente | Un comando con `token=`/`password=`/`://user:pass@` se registra redactado; el log conserva utilidad diagnóstica (nombre del runner detectado). **Los 2 puntos cumplidos.** **El arreglo es `redact_secrets` en `mark_evidence`** — el único punto que escribe texto derivado del payload al log (los otros tres escritores de `$LOG_PATH` emiten valores acotados: `cksum` del prompt, rol canonizado, aviso fijo). Tres patrones best-effort de la DoD en `sed -E`: `(token|password)=valor` con alternativa de entrecomillado simple/doble/pelado (sin ella, `password='hunter two'` filtraba la cola — hallazgo de codex); y `://[^[:space:]@/?#]*@` que preserva el host (la autoridad de una URI termina en `/`, `?` o `#` — RFC 3986; sin excluirlos, `https://host?owner=a@corp.com` se redactaba sin credenciales — hallazgo de codex). `-E` es POSIX Issue 8 (GNU/BSD/busybox); la flag `I` NO se usa: su fallo es silencioso (sed sale con error y la línea del log queda sin detalle). **No se redacta `combined` ni `command_text`**: el grep de señales de falla necesita el texto crudo — esto es `lane:fast`, no toca `verified`/`implemented`. **4 casos G2 nuevos** (11 → 15): credenciales en comando (las 3 formas de la DoD), comando sin credenciales no se altera (+ guardias URL con `@` post-path CORR 6 y `?owner=a@` CORR 12), credenciales en ruta de edición (caller `:811`, el otro `mark_evidence`), y credencial entrecomillada se redacta entera (cola del secreto, hallazgo codex). **1 mutación nueva** (25 → 26): `mut_redaccion_quitada` (reverte el call site), atrapada por `caso_g2_credenciales_en_comando_se_redactan`; fija además la ubicación del arreglo (movida al call site, el sed no matchea y salta la guardia 2 del driver). **Typo atrapado por TDD**: el plan canónico tenía `[Pp][Aa][Ss][Ss][Ww]` pero al transcribirlo al hook se droppeó un `[Ss]` ("password" tiene dos `s`) — `caso_g2_credenciales_en_comando_se_redactan` lo puso en rojo de inmediato y se fixó antes del verde. **Diff de la línea base**: 0 escenarios divergentes en `--check`; la identidad del encabezado cambia como `[nota]` informativa (hook_sha256/bytes/líneas), no como veredicto. **Cross-review ya hecha en el plan** (kimi + codex, 2 rondas sobre el documento staged; Gon pidió la 2ª explícitamente sobre el tope de 1 del quality-kit): 13 correcciones, 5 bloqueantes, todas incorporadas al PLAN CANÓNICO antes de ejecutar. **Cross-review sobre el código** (codex, 1 ronda sobre el feat commit `37047a6`): LGTM, sin hallazgos. **Se corrió el install**: destino *nuestro pero distinto* ⇒ reparado con backup, vivo byte a byte igual a la fuente. **Lo que NO cierra, declarado**: best-effort contra 3 patrones, NO un scanner de secretos (`Bearer`, `.env`, `AWS_SECRET_ACCESS_KEY` sin `token=`/`password=` no se redactan); el gate sigue **advisory**; secretos en el transcript del host siguen ahí (el hook no los controla); una comilla sin cerrar o un espacio escapado con backslash filtrar la cola (frontera de regex-vs-shell-parser); A6 sigue abierto y es la Task 3.6 | 3.1 | cc:完了 [37047a6] |
| 3.6 | `[Guardrail]` `[lane:fast]` `[tdd:required]` **A6** — `transcript_path` sale del payload y se hace `tail` sin acotar: primitiva de lectura de archivo arbitrario controlada por payload | `transcript_path` fuera del directorio de transcripts del host ⇒ se ignora y se reporta `unknown` (fail-open, no bloquea). **La cláusula cumplida.** El arreglo son `PROFILE_DIR` (deriva `dirname "$HOOK_DIR"`) y `transcript_en_perfil`: la ruta del transcript debe resolver —vía `cd "$(dirname …)" && pwd`, que normaliza `..`, symlinks y la forma Windows— a una ruta DENTRO del perfil del host; fuera, o si no se puede afirmar que está dentro, devuelve falso y el gate trata el transcript como no observado (`$tail_text=""`, mismo desenlace que un archivo ilegible) emitiendo `transcript=unknown` por stderr. **Por qué `cd`+`pwd` y no comparar strings, medido y no supuesto**: `json_string_field` NO decodifica escapes, así que en Windows `transcript_path` llega como `C:\\Users\\…` (backslashes dobles literales) mientras `HOOK_DIR` llega como `/c/Users/…`; comparar prefijos crudos daría FUERA siempre y apagaría el canal transcript en TODA la producción Windows — y la línea base no lo vería (sus escenarios sustituyen `__TRANSCRIPT__` por rutas POSIX del sandbox). `cd`+`pwd` lleva las dos formas a la misma canónica (`/c/Users/…`, o `/tmp/…` en el banco vía el mount virtual de MSYS, que reverse-mapea `C:\…\Temp`). **Decisión central**: el "directorio de transcripts del host" no es derivable de forma portable (codificación interna del host, zcode distinto en la Phase 5, banco escribe en `$sb/entrada`), así que se ancla al PERFIL (un superset derivable que igual cierra la lectura de `/etc/passwd`, `~/.ssh/id_rsa`, el repo, `%APPDATA%`). **2 casos G4 nuevos** (11 → 13): `caso_g4_transcript_fuera_de_perfil_se_ignora` (el catch: un recibo plantado en un transcript hermano del banco bajo TMPDIR se ignora ⇒ exit 2 + `transcript=unknown` por stderr + el estado sobrevive) y `caso_g4_transcript_ruta_windows_y_traversal` (la regresión, dos mitades: (a) un transcript in-bounds apuntado en forma Windows vía `cygpath -w` sigue leyéndose ⇒ cierre limpio; (b) una ruta que arranca adentro y sale con `..` se ignora ⇒ exit 2). **2 mutaciones nuevas** (26 → 28, G4 7 → 9): `transcript_sin_containment` (anula la contención → el catch da rojo) y `containment_sin_resolver` (compara el string crudo en vez de resolver → el caso B da rojo), cada una acreditada a **su** caso en la declaración que emite la corrida. **Total del hook: 28 mutaciones, 28 atrapadas.** **Diff de la línea base: 0 escenarios divergentes** en `--check` — medido y no supuestos: `tools/golden-harness.sh:244` pone cada transcript del banco en `$sb/entrada/`, bajo `$sb` = perfil, así que la contención devuelve 0 y la línea de stderr no se emite. **Salvedad declarada**: ningún escenario de la baseline usa forma Windows (todos sustituyen `__TRANSCRIPT__` por POSIX), así que "0 divergentes" NO es evidencia de que el canal transcript siga vivo en producción Windows; esa evidencia la da el caso B(a), no la baseline. **Cross-review no completada**: codex colgó al tope de 300 s dos veces (incluso con el diff sólo-código de 15k chars) y kimi colgó una — patrón documentado (mismo que kimi en 3.2/3.5). Kimi dejó **revisión parcial sin hallazgos**: confirmó que el arnés dorado pone hook y transcripts bajo el mismo sandbox (la contención no cambia su comportamiento) y señaló dos observaciones —`PROFILE_DIR` en el banco y `_tp_dir` sin `local`— ambas ya cubiertas por el diseño del plan (la segunda es decisión explícita: `local` se comería el exit status del `cd` y el fallo pasaría inadvertido). Declarada no completada por cuelgue de los tres revisores; no se persigue otra ronda. **Se corrió el install**: destino *nuestro pero distinto* ⇒ reparado con backup (`…/saikit-backups/summonaikit-harness.sh.nuestro.20260811-213649.bak`), vivo byte a byte igual a la fuente (`cmp`). **Commit lateral separado** (`ab3e4c8`, `chore(test)`): el harness de zcode escribe `.harness-mem/state/*.json` mientras la suite corre, y el guardia de fugas de `run.sh` lo reportaba como LEAK en los 4 tests más largos (con todos sus casos en verde); añadido al `.gitignore` y al prune del `manifiesto()`, mismo criterio que `.claude`/`.zcode`. **Desviaciones del plan canónico, medidas y declaradas**: (1) el caso B(a) usa `cygpath -w` crudo (backslash simple) en vez de doblar backslashes — `sed 's/\\/\\\\/g'` se rompe en MSYS2 (char 8 unterminated) y bash `${//}` tampoco dobla; `json_string_field` es extractor raw y `cd`+`pwd` resuelve la forma cruda igual (medido); (2) el plan afirmaba "hook ASCII puro" pero HEAD ya tenía 27 líneas con em-dash (comentarios de 3.1/3.4); los agregados de esta tarea SÍ son ASCII puro, no es bloqueante. **Lo que NO cierra, declarado**: el gate sigue **advisory** — A6 cierra una primitiva de **lectura** arbitraria, no vuelve al gate un control de seguridad; en **staging** el transcript vive en `~/.claude/projects/` y el hook en `<repo>/.claude/hooks`, fuera del perfil derivado, así que el canal transcript queda desactivado y el gate corre sólo con `last_assistant_message` (el cross-host como defecto de estado es la Task 5.3); **lo de adentro del perfil sigue legible** (`.credentials.json`, `history.jsonl`, transcripts de OTRAS sesiones) — la contención acota la raíz, no es un permiso por archivo, y cerrarlo del todo pide anclar a `projects/` que es justo lo descartado por portabilidad; sigue **symlinks** (uno bajo el perfil que apunte afuera se resuelve a su destino); **case-insensitivity** de Windows no se persigue; `transcript_path` NO se migra a `json_top_level_string` (la contención neutraliza cualquier ruta leída, venga del lector greedy o del escáner) | 3.1 | cc:完了 [81f9768] |
| 3.7 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **A9 + A10, van juntos** — hoy el gate de ceremonia es **inerte** y está medido (Task 1.4, 2026-08-09). **A9**: la herramienta que invoca subagentes se llama `Agent` y el matcher registrado nombra `Task`, así que esos eventos no llegan nunca; los 281 que sí llegan traen el rol en `agent_type` de primer nivel y el hook busca `subagent_type`. **A10**: el hook no ve `SUMMONAIKIT_HOOK_TARGET=claude` pese a estar en el comando registrado — causa `unknown`, efecto medido. Separarlos empeora: arreglar A9 solo no cambia nada mientras el TARGET no llegue, y arreglar A10 solo hace que TODOS los turnos bloqueen porque `agents_seen` sigue vacío | Un evento con `agent_type` de primer nivel registra el rol; el verificador del registro (`tools/check-hook-registration.sh`) reporta fuerte si el matcher de `PostToolUse` no cubre la herramienta de subagentes; se aísla por qué el hook no ve el TARGET (hook que imprima su entorno, sesión nueva) y se corrige, o se declara con la evidencia de por qué no se puede; el escenario 16 de la línea base pasa de bloquear a cerrar limpio y `caso_g3_agent_type_no_cuenta` invierte su expectativa — ese diff es la declaración; revertir cualquiera de las dos mitades hace fallar su test. **Al cerrar se borra `C:\dev\saikit-captura`**: ahí viven los 308 payloads crudos que sostienen A9 y A10 (traen texto de un turno real, por eso no están en el repo) y esta es la última tarea que los necesita. **PRIMER PASO OBLIGATORIO: re-medir, porque la premisa de A9 caducó.** El turno real de la Task 2.4 (2026-08-10, con el hook ya instalado) dejó `agents_seen=implementer,verifier,reviewer` — los tres roles, en orden, donde A9 afirma que queda vacío. En ese transcript la herramienta se llama `Agent` (esa mitad de A9 sigue en pie) pero el rol viaja en **`subagent_type`**, el campo que el hook SÍ lee, y `agent_type` no aparece ni una vez. O sea que el arreglo que este renglón planea —"un evento con `agent_type` de primer nivel registra el rol"— tocaría hoy algo que no está roto, y mal hecho rompe lo que anda. Capturar payloads frescos antes de escribir una línea; si la captura confirma `subagent_type`, esta tarea cambia de forma y A9 se re-redacta con lo medido. **CIERRE (2026-08-12, `a585dfe`)** — re-medición hecha (captura de payloads + env en `C:\dev\saikit-captura`), y **no confirmó `subagent_type`: confirmó `agent_type`**. A9 y A10 cerrados tal como el renglón los planeaba (no cambió de forma). **A9**: los eventos INTERNOS del subagente (los que matchean `Bash|Edit|Write|apply_patch|Task` y llegan al gate) cargan el rol en `agent_type` top-level; el hook lee `agent_type` como **fallback** (`:831`, después de `subagent_type`). **A10 — causa aislada (ya no `unknown`)**: el host no propaga el prefijo `VAR=val` del comando registrado (medición directa: registrando `SUMMONAIKIT_HOOK_TARGET=claude bash …`, la var no llegó al env del hook); `PHASE` sobrevive por fallback al payload (`:1144`) pero `TARGET` no tenía fallback y la rama `claude` nunca corría. Arreglo: el hook detecta Claude por `CLAUDECODE=1` (`:23`, medido; Claude Code lo setea). **La conclusión de 2.4 ("el rol viaja en `subagent_type`") era una atribución errónea del transcript**; no se reconstruyó qué pobló `agents_seen` allí y no hace falta (el arreglo es fallback, `subagent_type` sigue ganando si llega). **DoD verificada**: `agent_type` registra el rol (caso `caso_g3_agent_type_cuenta`, el viejo `caso_g3_agent_type_no_cuenta` invertido y **renombrado** — la DoD lo nombra literalmente); `check-hook-registration.sh` reporta fuerte si el matcher no cubre `Agent` (verificado contra el settings REAL: reporta `Bash|Edit|Write|apply_patch|Task`); el escenario 16 pasa de bloquear a cerrar limpio (diff declarado: `agent:` se acumulan en 02-04, `agents_seen` se puebla, `last_review=3` sólo en 04, 05 limpio — único escenario que mueva). **Tests**: 3 casos G3 (el invertido + `caso_g3_agent_type_generico_no_cuenta` guardia del canal nuevo + `caso_g3_target_por_claudecode_fallback` para A10) y 2 mutaciones (28→30, G3 5→7). `lab_run` y `golden-harness` unsetean `CLAUDECODE` (determinismo: la suite corre adentro de Claude Code sin que el fallbackcontamine los `target=auto`). **Plan revisado** (Claude+codex, 17 correcciones aplicadas al doc antes de ejecutar; 3 bloqueantes: columna falsa de la tabla, mutación `[ false ]` que no mutaba, caso A10 que no discriminaba). **Cross-review del código NO correída** (regla nueva del repo: sólo si el operador la pide). **Suite**: `run.sh OK (17 tests)`, 0 FAIL/LEAK/UNKNOWN (corrida dentro de Claude Code ⇒ invariancia `CLAUDECODE` probada de yapa). **Install**: vivo byte a byte igual a la fuente. **Validación real (operador)**: un turno `-saikit` que delegó **sólo implementer** (omitintiendo verifier/reviewer a propósito) **bloqueó** reclamando *Missing verifier* y *Missing reviewer* — y **NO** reclamó *Missing implementer* (o sea, `agents_seen=implementer` estaba registrado: A9 probado en producción). El bloqueo mismo, con `REVISION BUDGET EXHAUSTED` tras 2 ciclos, prueba A10 (la rama `[ "$TARGET" = "claude" ]` corrió y exigió la secuencia; antes del fix ese turno habría cerrado limpio). Corolario honesto: el camino de presupuesto-agotado **también** limpia el estado (igual que el cierre limpio), por eso los dirs de sesión quedaron vacíos y `agents_seen` no era directamente legible post-hoc — la prueba del párrafo anterior son los mensajes del gate en vivo. **Captura borrada**: el contenido de `C:\dev\saikit-captura` se borró (payloads gone); el dir vacío quedó porque un proceso lo tenía como cwd (se suelta al cerrar esa sesión). **Lo que NO cierra, declarado**: subagente **read-only** (Read/Grep/Glob) no genera eventos para el gate y su rol no se registra — es el hueco que `check-hook-registration` reporta; **cursor/zcode** sin detector (`CLAUDECODE` es Claude-only); la vía "limpia" para TARGET (export dentro del `bash -c` del comando) queda fuera porque toca `settings.json` del operador; el gate sigue **advisory** | 3.1 | cc:完了 [a585dfe] |
| 3.8 | `[Guardrail]` `[lane:fast]` `[tdd:required]` **A11** — el guardia de fallas busca `exitCode` en el payload y el `tool_response` real de `Bash` no trae ese campo (medido 59 de 59). De las cuatro señales que mira, tres son texto; una batería que falla con un assert normal no dice ninguna y queda acreditada como verificación | Un runner que falla NO acredita verificación, y el criterio no depende de un campo que el payload no trae; `command not found` sigue funcionando; un runner que pasa sigue acreditando; `caso_g2_runner_fallido_forma_real` invierte su expectativa y ese diff es la declaración | 3.1 | cc:完了 [87247a0] — **CIERRE (2026-08-12)**: `exitCode[^0-9]*[1-9]` retirado (código muerto, medido 59/59); el hook grepea DOS regex sobre `combined`: `FAILURE_SIGNAL_RE_CI` (`-Eiq`) con las 3 originales + `AssertionError`/`Traceback`/`SyntaxError`/`TypeError`/`error TS[0-9]` + `[1-9]...(failed|failing|failures?|errors?)` (frontera de dígito no-cero) + `(failures?|errors?)[=:]...[1-9]` (phpunit/unittest Python); `FAILURE_SIGNAL_RE_CS` (`-Eq` sin `-i`) con `test result: FAILED` / `FAIL[^a-zA-Z]` / `FAILURES!` / `--- FAIL:`. Dos greps porque `-i` es global y case-sensitive iría mezclado con falsos positivos (`0 failures!`, `failed to connect`). **Cross-review del PLAN con codex (1 ronda, 4 hallazgos aceptados, los 4 verificados con `/usr/bin/grep` GNU 3.0)**: el draft usaba `[[:<:]]FAILED[[:>:]]` (inválido en GNU grep 3.0 — `rc=2`, con `if ! grep` acreditaba siempre) y `\bFAILED\b` con `-i` (matcheaba `0 failed`); la decisión de alcance pasó de Minimal a **Amplio con doble grep** para cubrir los ecosistemas que el hook YA reconoce (cargo/go/phpunit incluidos). **DoD verificada**: `caso_g2_runner_fallido_forma_real` invertido (`verified` 1→0); `command not found` sigue funcionando; runner que pasa sigue acreditando (`caso_g2_runner_marca_verificado` + el nuevo `caso_g2_runner_pasa_0_failed_sigue_acreditado`); el criterio no depende de `exitCode`. **Tests**: G2 15→21 (5 casos nuevos aislados por rama: pytest summary / tsc / phpunit / cargo / go, + 1 caso negativo de frontera). **Mutaciones 30→36 (G2 6→12)**, cada una acreditada a su caso propio (falla_assertion/failed/tsc/phpunit/cs/frontera_aflojada). **Baseline 0 divergentes** (16 escenarios se comportan igual). **Gate final `run.sh OK (17 tests)`** corrido con `SAIKIT_HOOK_VIVO` apuntando a la fuente (run.sh mide el hook INSTALADO por diseño — `tests/run.sh:72` — y el install va post-commit; sin esa variable los 6 casos nuevos daban rojo contra el vivo aún sin actualizar). **Pre-commit PASS**. **Lo que NO cierra, declarado**: postura sigue **fail-open** (no fail-closed) porque **tsc exitoso no imprime nada** y exigir señal positiva rompería la DoD — el arreglo achica el hueco, no lo elimina; iconos sin palabra (vitest `×`/jest `✕`/mocha `✗`) NO cubiertos; `test_FAIL.py` en un comando que pasa sería falso positivo de la rama `FAIL[^a-zA-Z]` (raro, declarado); el gate sigue **advisory**; re-captura de payloads 59/59 NO hecha (la 3.7 borró la captura; medición vigente en spec + README + comentarios del lab); el caso `caso_g2_runner_fallido_go_no_marca` exigió cambiar `^FAIL[^a-zA-Z]` por `FAIL[^a-zA-Z]` (sin ancla `^`) porque `$combined` es una sola línea y el `FAIL` del JSON llega precedido por la comilla de `"stderr":"FAIL\t..."`, no al inicio de línea — descubierto al correr el primer verde (el caso go daba falso verde). **Fix post cross-review del CÓDIGO con codex (1 ronda, 3 hallazgos, `0eb8381`)**: el operador eligió arreglar H2+H3, declarar H1. **H2 (fix, falso positivo)**: las excepciones (`AssertionError`/`SyntaxError`/`TypeError`/etc.) estaban sin `:` en el regex CI; como `$combined` incluye `command_text`, un runner EXITOSO cuyo comando menciona la excepción (`pytest tests/test_typeerror.py`) matcheaba como fracaso → dejaba de acreditar. Fix: exigir `:` (los tracebacks reales lo traen; los nombres de archivo, no). Caso nuevo `caso_g2_runner_pasa_typeerror_en_comando_sigue_acreditado` + mutación nueva `mut_falla_excepciones_sin_dospuntos` + actualización de `mut_falla_assertion_quitada` (lleva los `:` del nuevo patrón). **H3 (fix)**: `mut_falla_cs_quitada` rompía cargo Y go (ambos CS), el driver cortaba en cargo y go quedaba sin mutación propia. Mutación nueva `mut_falla_go_quitada` neutraliza SÓLO `FAIL[^a-zA-Z]` (rama de go), dejando `test result: FAILED` (cargo) intacta — cada caso CS tiene su mutación propia. **H1 (declarado, NO arreglado)**: ctest (`N tests failed`), make (`*** Error`), cargo-compile (`error[E0XXX]`/`error: could not compile`), gradle/mvn sin tests-summary (`BUILD FAILURE`) son runners reconocidos por `TEST_RUNNER_RE` cuyas señales NO matchean las regex — incumplimiento parcial de la DoD universal, declarado como límite best-effort (no parser de runners); dotnet cubierto vía `[FAIL]`, gradle/mvn con tests-summary vía `Failures: N`. **Conteos finales**: G2 21→22, mutaciones 36→38 (G2 12→14). Baseline 0 divergentes. `run.sh OK (17 tests)` (corrido con `SAIKIT_HOOK_VIVO` apuntando a la fuente — run.sh mide el hook INSTALADO y el install va post-commit) |

## Phase 4: Consolidación

Purpose: retirar lo que quedó duplicado y dejar declarado lo que cambió de dueño.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 4.1 | `[Config]` `[lane:gate]` `[tdd:required]` Retirar de `quality-kit` **solo el target `.claude`** de los dos parches, tras días en verde. NO se borra el código del parche: `.codex` tiene rama propia (`-harness-lite`) y el sentinel aplica a `.cursor`/`.agents`; borrar las anclas de `.claude` borraría además su cobertura de tests | Los 3 perfiles restantes se siguen parcheando; la batería de `quality-kit` sigue verde con la cobertura de `.codex` verificada por separado; `.claude` ya no aparece como target | 3.8 | cc:完了 [8c3b270] — **CIERRE (2026-08-12)**: `.claude` fuera de `$targets` del heal (1 línea en `saikit-gate-heal.ps1`); header CONVIVENCIA + bloque "salto=normal" reescritos (el skip ya no es el estado normal). **DoD verificada**: los 3 perfiles restantes se parchean (TEST GROUP 3q: `.codex`/`.cursor`/`.agents` reciben el sentinel; `.claude` NO, queda byte-identical); batería quality-kit **457 PASS / 0 FAIL**; cobertura `.codex` verificada por separado (3m half `.codex` + 3n con marcador sobre `.codex`). **TDD medido**: red 455/2 (sólo 3q, `.claude` aún como target) → green 457/0 con A1; el baseline era 290/18 por un **drift pre-existente** (el `~/.claude` vivo tiene los fixes Task 3.x —incl. 3.2, que cambió cómo se lee el recibo— y el fixture congelado + `~/.cursor`/`~/.codex` no), que la migración de la pasada live a `~/.cursor` (vendor pre-3.2, mismo comportamiento que el fixture) **resolvió de paso** (18 FAIL → 0). **Cross-review del PLAN con codex (1 ronda, 0 altas, tope quality-kit cumplido)**: 4 hallazgos aceptados — (1) `[media]` 3p `run-tests.ps1:1810` quedaba vacuo sin un target marcado → `$regHomeI` suma un `.codex` marcado y el `-notmatch 'saltado, lo maneja'` bajo `-Quiet` prueba de verdad; (2) `[media]` orden TDD de B1 aclarado (rojo medido contra quality-kit actual, no tras modificarlo); (3) `[baja]` Plans.md Status = commit de summonaikit-claude, SHA quality-kit en la nota; (4) `[baja]` "no se stagea a git". **Costura (este repo)**: `test_quality_kit_skip.sh` ahora asserta `.claude` ∉ `$targets` (espejo de 3q desde este lado); su rojo se midió en la batería del kit (3q). **No se borró código del parche**: `.codex` (`-harness-lite`/RN-G), `.cursor`/`.agents` y el skip por marcador quedan (red de seguridad, dead-in-prod pero cubierto por tests). **Cross-review del CÓDIGO con codex (1 ronda, 0 altas)**: 1 hallazgo `[baja]` — dos comments residuales (`saikit-gate-heal.ps1:655` y header de TEST GROUP 3p en `run-tests.ps1`) aún afirmaban que el skip "pasa en cada arranque sano", contradiciendo el nuevo comportamiento (`.claude` no se itera); arreglados (comment-only, parse OK). quality-kit: `928fd1c` (+`7529ed2`). **Límites declarados**: skip por marcador dead-in-prod (conservado por insurance + tests); `.claude` ya no reporta `saltado` (efecto buscado de la consolidación); `Invoke-RegistrationCheck` sigue Claude-only (verifica registro, no parcheo); el gate sigue **advisory** |
| 4.2 | `[Docs]` `[lane:fast]` `[tdd:skip:docs-only]` Declarar el cambio de dueño del contrato: el hook de Kimi extrae su contrato de `$HOME/.claude/hooks/summonaikit-harness.sh`, o sea de este repo. Deja de ser cierto que "Kimi sigue al vendor" — sigue a este repo. Actualizar el spec del port de Kimi y su detector de drift | El spec de `summonaikit-kimi` declara la nueva fuente; su detector de drift apunta a lo correcto; ningún documento sigue afirmando que el contrato se sigue del vendor | 2.4 | cc:完了 [6391d3f] — **CIERRE (2026-08-12)**: el cambio vive en `summonaikit-kimi` @ `90af5a5` (7 archivos, +61/−39: spec/README/Plans/hook/2 tools/1 test); acá vive sólo el plan (`6391d3f`). El código del port ya apuntaba a `$HOME/.claude/hooks/summonaikit-harness.sh` (marcador `# SAIKIT-CLAUDE-OWNED` en línea 2); el cierre es **prosa, comentarios y el echo de AVISO del detector** — sin tocar control-flow ni tests. **DoD verificada**: (1) el spec declara la fuente (Core Rules 1/2, divergencia #3, contrato de drift, Non-Goals, Links); (2) el detector apunta a lo correcto — la ruta ya era correcta por código, los comentarios y el echo de AVISO quedaron alineados; (3) los greps `el vendor` como actor upstream y `parche local de quality-kit` como causa típica de AVISO ⇒ **0 hits**. `tests/run.sh` OK (18/18 PASS); `bash -n` OK; `check_drift.sh` exit 0 sobre el perfil real (reporta **AVISO**, no DRIFT: el pin es stale de 2026-08-09 — el hook avanzó con la Phase 3 de `summonaikit-claude`— pero las regiones copiadas no se movieron). **Cross-review del PLAN con Grok (1 ronda, 0 altas)**: 2 medias + 6 bajas, todas aceptadas; la [media] 2 expandió el alcance al actor del camino AVISO (`quality-kit` → `summonaikit-claude`, pues tras la Task 4.1 `quality-kit` ya no itera `.claude`); la [media] 1 cortó los contrastes redundantes (`no el vendor`, `ya no es el del vendor`) que habrían hecho fallar el grep de la propia DoD. **Límites declarados**: `vendorizada`/`vendorizado` y los símbolos `VENDORED_HARNESS_CONTEXT`/`vendored_contract()`/`refresh_vendored_contract.sh` intactos (string funcional, afirmado por `test_extract_contract.sh:89`); el pin congelado `docs/pinned/*` intacto (snapshot histórico, editarlo falsificaría el pin); **no se re-pinea** (regiones sin mover ⇒ no hay `DRIFT` que aceptar); `summonaikit-claude` no gana marcador `SAIKIT-KIMI-OWNED` (no existe, no lo pide el DoD); el gate sigue **advisory** |
| 4.3 | `[Docs]` `[lane:fast]` `[tdd:skip:docs-only]` README: instalación, staging por override, `--restore-vendor`, y la declaración explícita de que el gate es **advisory** y no un control de seguridad (aun corregidos A1 y A2, quien controla el texto del turno puede influirlo) | Las 4 secciones existen, ningún archivo citado falta, y la limitación del gate está escrita sin eufemismos | 4.1, 4.2 | cc:完了 [377d153] — **CIERRE (2026-08-12)**: el repo no tenía README; ahora `README.md` declara las 4 secciones de la DoD. **DoD verificada**: (1) existen `## Instalación`, `## Staging por override`, `## `--restore-vendor``, `## El gate es advisory — no es un control de seguridad`; (2) los 8 archivos citados existen (`tools/install-hook.sh`, `tools/stage-override.sh`, `tools/check-hook-registration.sh`, `hooks/summonaikit-harness.sh`, `hooks/vendor-manifest.sha256`, `docs/spec/00-project-spec.md`, `Plans.md`, `tests/run.sh`); (3) la limitación está sin eufemismos — "no es un control de seguridad" + "quien controla el texto del turno puede influirlo", con los vectores que A1/A2 no cierran (`tool_input` de delegación, recibo que cita un runner, subagente de solo lectura) y fail-open. `[tdd:skip:docs-only]`. **Límites declarados**: no se tocó el hook ni el instalador; el gate sigue **advisory**; Phase 4 queda cerrada |

## Phase 5: El segundo host — zcode (GLM)

Purpose: `zcode` (CLI propio de Z.ai, paquete `zcode-app-cli`) tiene un sistema
de hooks **con el mismo contrato que Claude Code**, y el operador ya corre ahí
hooks escritos para Claude apuntando a `$HOME/.claude/hooks/`. Esta fase mide si
el hook de este repo sirve tal cual y, si sirve, lo pone a gatear el segundo
host. **Premisas medidas 2026-08-10 en `docs/spec/00-project-spec.md`** (§ El
segundo host); si alguna cae, la fase se re-planifica como port propio al estilo
de `summonaikit-kimi`, que es lo que costó cuando el contrato NO era compatible.

**Orden recomendado, y el costo de no seguirlo:** 5.1 y 5.2 son medición con
persona adelante y se pueden hacer ya. La 5.5 (línea base del target) conviene
grabarla **después de la Phase 3**: cada defecto que la Phase 3 corrige cambia el
comportamiento del hook, así que grabarla antes obliga a regrabarla siete veces.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 5.1 | `[Test]` `[lane:gate]` `[tdd:skip:captura-manual-con-persona-adelante]` **PRIMER PASO OBLIGATORIO: capturar payloads reales de zcode**, mismo criterio que la Task 1.4 — de ahí salieron A9, A10 y A11, ninguno visible en payloads reconstruidos. `tools/capture-payloads.sh` ya existe; lo que cambia es el registro (formato de zcode) y el repo descartable | Payloads crudos de las 3 fases capturados en un turno `-saikit` real de zcode, en un repo descartable; queda escrito el **diff de forma contra los payloads de Claude** (campos presentes/ausentes, anidamiento); se declara **cómo se llama la herramienta de subagentes y en qué campo viaja el rol** (`tool_input.subagent_type` vs `agent_type` de primer nivel); se mide si el alias `Task` ↔ `Agent` del matcher hace que los eventos de delegación SÍ lleguen —la mitad de A9 que en Claude no llega nunca—; si la captura contradice alguna premisa del spec, se re-redacta la fase antes de escribir una línea | 2.4 | cc:完了 [21bed43] — **CIERRE (2026-08-12)**: captura real de un turno `-saikit` en zcode (3.7.5-11), las 3 fases (`UserPromptSubmit`, `PostToolUse`, `Stop`) en `C:/dev/saikit-captura-zcode`. **DoD verificada**: (1) payloads crudos de las 3 fases (5 archivos); (2) diff de forma contra Claude en `docs/task-5.1-captura.md` — snake_case presente y compatible **+** camelCase duplicada; (3) herramienta de subagentes = **`Agent`**, rol en **`tool_input.subagent_type`** (no hay `agent_type` top-level); (4) alias `Task`↔`Agent` **CONFIRMADO**: el matcher `Task` disparó para `tool_name=Agent` ⇒ el matcher del harness (`Bash|Edit|Write|apply_patch|Task`) atrapa la delegación en zcode sin cambiar (la mitad de A9 que falta en Claude acá llega); (5) **ninguna premisa tumbada** ⇒ no se re-redacta la fase. **Hechos nuevos para 5.2–5.5**: `CLAUDECODE` ausente (target necesita `ZCODE_SESSION_ID`/`ZCODE_PROJECT_DIR`); `transcript_path` efímero (revisar A6); `tool_response.exitCode` presente en Bash (inofensivo, A11 lo retiró). `[tdd:skip:captura-manual-con-persona-adelante]`. Commit 1 = `48fd956` (`--host zcode` + tests); commit 3 = `21bed43` (diff + spec). Config real restaurado tras `--quitar` (vecinos intactos). |
| 5.2 | `[Test]` `[lane:gate]` `[tdd:required]` **El contrato de SALIDA, medido y no supuesto.** La guía del propio zcode declara que el stdout se parsea con **esquema ESTRICTO: cualquier clave extra invalida la salida entera** y el efecto se descarta. El hook emite hoy cuatro formas distintas (`hookSpecificOutput.additionalContext`, `decision:block`, `continue:false`+`stopReason`, y `systemMessage` del aviso de revisión), ninguna verificada contra ese validador. Además la guía dice `exit 2` = block **nombrando sólo `PreToolUse`/`PermissionRequest`**, y para `Stop` habla de "request continuation": si el 2 del Stop no bloquea, el gate entero es decorativo en zcode | Cada una de las 4 formas de salida tiene un veredicto MEDIDO en zcode (aceptada / rechazada por el esquema / ignorada), y el rechazo se distingue del silencio; `exit 2` en `Stop` tiene medido si bloquea, si continúa o si cuenta como error; el resultado decide si `TARGET=claude` alcanza o hace falta un `TARGET=zcode` propio, y esa decisión queda declarada con su evidencia. **Se puede medir en la MISMA sesión que la 5.1** | 5.1 | cc:完了 [e265cc1] — **CIERRE (2026-08-12)**: probe `tools/probe-zcode-output.sh` (commits `3343fb6`+`29ef5f4`) registrado en el user-config de zcode (UPS+Stop, `type:command`), 7 modos medidos un turno c/u, operador adelante. **DoD verificada**: las 4 formas + `exit2` con veredicto MEDIDO — `exit2` **aceptada** (4 pasadas del modelo: bloquea), `block0` (JSON `decision:block`+exit0) **aceptada** (4 pasadas: bloquea vía JSON), `context` (`additionalContext`) **aceptada** (nonce en el request del modelo como mensaje `system`), `extra` **aceptada** ⇒ **esquema NO estricto** (clave extra tolerada; tumba el bullet del spec y coincide con la doc oficial), `budget` (`continue:false`) **ignorada** (= empty, indistinguible), `notice` (`systemMessage`) **ignorada** (no se muestra; RN fail-open), `empty` control (1 Stop). **Decisión §E: `TARGET=claude` ALCANZA** — la inyección del contrato llega al modelo y ambas mitades del bloqueo del vivo (`decision:block` y `exit 2`) mueven el Stop; 5.3–5.5 son adaptación, no port. Veredictos y citas redactadas en `docs/task-5.2-salida.md`; spec § segundo host corregido (esquema no estricto + `exit 2` en Stop sí bloquea). **Defecto propio corregido en el camino**: la extracción del evento con `sed` greedy rompía en UTF-8 multibyte del `responseText` de zcode (C.UTF-8 + GNU sed 4.9, `[^"]*` cruzaba comillas) → fix con `grep -o` + test de regresión (`29ef5f4`). `[tdd:required]`: test suelto verde + 3 mutaciones atrapadas; gates `pre-commit` Passed + `run.sh` OK (18 tests). Config vivo restaurado (`--quitar`, 0 entradas 5.2, vecinos intactos). **Lo que NO cierra, declarado**: `budget` ignorada ⇒ el corte por presupuesto depende de `exit 2` (que sí bloquea), lo resuelve 5.4; detección de target vía `ZCODE_SESSION_ID` (`CLAUDECODE` ausente en zcode) es 5.4; aislamiento de estado cross-host es 5.3; el gate sigue **advisory** |
| 5.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Aislar el estado por host — defecto nuevo, medido hoy por lectura de la config real.** Los hooks que el operador ya corre en zcode apuntan a `$HOME/.claude/hooks/...`. Si el harness se registrara igual, `STATE_ROOT` sale de `dirname $0` y los dos hosts escribirían **el mismo `harness-state.env`** del mismo proyecto: un turno de zcode podría cerrar o pisar el turno de Claude. Es A4 en versión cross-host, y ninguna tarea existente lo cubre | Un turno de zcode y uno de Claude sobre el MISMO repo no comparten estado, medido antes y después (no "no lo escribimos a propósito"); el mecanismo queda declarado —copia propia bajo `~/.zcode/hooks/`, que el `dirname $0` ya separa, o llaveado explícito por host— y se elige con su razón; el caso que lo habría atrapado existe | 5.2 | cc:完了 — **CIERRE vivo 2026-08-13**: turno `-saikit` en zcode escribio estado bajo `state/zcode/`; 5.4 cableo el registro; 5.6 hizo correr implementer->verifier->reviewer. Residual G2 (skip EN) se parchea en el mismo cambio. Lab+TDD+baseline+docs hechos (2026-08-12). Mecanismo: **llaveado explícito por host** (no copia). `PROJECT_DIR` pasa a `STATE_ROOT/$HOST/$PROJECT_KEY`, donde `HOST` sale del env medido (5.1): `zcode` si `ZCODE_SESSION_ID`/`ZCODE_PROJECT_DIR`, `claude` si `CLAUDECODE=1`, `other` si ninguno (nunca colapsa a `claude`). `RN_PENDING_PATH` hereda `HOST` vía `PROJECT_DIR`. **A0 re-medido en el lab**: mismo `$0`+sesión, Claude y zcode escribían el mismo `harness-state.env` (y el mismo `RN_PENDING`) antes del cambio — defecto confirmado. **TDD**: `caso_g1_dos_hosts_mismo_repo_no_comparten_estado` (catch, dos mitades + RN, rojo medido contra HEAD pre-5.3) + `caso_g1_host_segun_senal` (control de detección, sin mutación propia; cubre `ZCODE_PROJECT_DIR`-only, hallazgo 5) + `mut_host_sin_llave` (revierte `PROJECT_DIR` sin `HOST`, acreditada al catch). **39 mutaciones, 39 atrapadas**. **Lab**: `lab_run` unsetea `ZCODE_*` siempre y repone desde `LAB_ZCODE_*`. **Regresión propia encontrada y cerrada por la batería**: `caso_g3_target_por_claudecode_fallback` (lab_sembrar + `CLAUDECODE=1`) quedó rojo porque el seed caía en `state/other/` y el hook leía `state/claude/` — el plan no lo anticipaba (asumía que ningún caso viejo seteaba `CLAUDECODE`). Fix localizado al caso (sembrar en el path de `claude/`); NO se acopló el lab al esquema `state/<host>/` porque la batería de mutaciones muta ese esquema a propósito (lo rompe). **Baseline dorada**: `golden-harness.sh` unsetea `ZCODE_*` y su `normalizar` entiende `state/<host>/<key>/` (clase de chars `[a-z]+`, no alternancia — el delimitador `|` choca con `\|`/`|` en el sed de MSYS2). Diff = **solo el header** (sha/bytes/líneas); 0 divergentes, ningún veredicto movido. **A6 en zcode = fail-open por tmp** (`transcript_path` efímero fuera del perfil; no se agranda la allowlist). Detalle en `docs/task-5.3-aislamiento.md`; spec § segundo host actualizado. **STOP vivo (§C del plan) = la cláusula "medido antes y después" sobre el mismo repo con los DOS hosts**: requiere registrar el harness en el user-config de zcode, que es alcance de 5.4; sin eso no se hace (la DoD pide medición viva, el lab no la sustituye). **Gates**: `pre-commit` Passed; `run.sh` todos los test_* en OK (behavior, mutations, golden_baseline, golden_harness…). **No se toca** contrato de salida/`TARGET`/`emit_*`; no se migra el árbol viejo (`state/<key>/` queda huérfano); el gate sigue **advisory** |
| 5.4 | `[Setup]` `[lane:gate]` `[tdd:required]` **Registro e instalación en zcode.** El wrapper difiere del de Claude (`hooks.events.<Evento>[]` con `{enabled,timeoutMs,maxOutputBytes}` alrededor, contra `hooks.<Evento>[]`), y hay tres trampas declaradas por la guía del CLI: los hooks de archivo **están desactivados salvo `hooks.enabled:true`**; el matcher de `UserPromptSubmit` se prueba contra **el texto del prompt** y el de `Stop` contra **la vista previa de la respuesta**, así que un matcher copiado del registro de Claude no matchearía nunca; y `timeout` va en **segundos** | El hook queda registrado en las 3 fases de `~/.zcode/cli/config.json` sin matcher donde el match value no es el nombre de la herramienta; `hooks.enabled` verificado en `true`; `tools/install-hook.sh` y `tools/check-hook-registration.sh` aprenden la SEGUNDA forma de registro sin perder la primera (mismo criterio de tres estados y de `unknown` ≠ ausente); un registro con matcher equivocado se reporta fuerte en vez de darse por bueno | 5.3 | cc:完了 — **vivo 2026-08-13**: harness en las 3 fases; TARGET por `ZCODE_*`; budget `exit 2` (el turno agotó 2 ciclos y cortó). Tipos implementer/verifier/reviewer registrados (5.6) y delegados. |
| 5.5 | `[Test]` `[lane:gate]` `[tdd:required]` **Línea base y casos del target zcode**, y la puesta en producción. Se graba DESPUÉS de la Phase 3 a propósito: cada defecto corregido allá cambia el comportamiento, y grabarla antes obliga a regrabarla siete veces | Los escenarios de zcode tienen su bloque en la línea base y `--check` es reproducible; cada gate tiene al menos un caso que pasa y uno que bloquea **en este target**; la puesta en producción se verifica **ejecutando** un turno zcode (no leyendo `~/.zcode/cli/config.json` ni escribiendo `<repo>/.zcode/config.json` — ese override lo ignora este CLI); un turno `-saikit` real en zcode arma, y uno pelado no | 5.4, 3.8 | cc:完了 [90beabe] |
| 5.6 | `[Setup]` `[lane:gate]` `[tdd:required]` **Perfiles implementer/verifier/reviewer en zcode.** 5.4 cableó el hook; el runtime de zcode solo conoce tipos en `~/.zcode/agents/<name>.md`. Medido en vivo: `Agent type 'implementer' not found. Available agents: general-purpose, Explore`. Sin esos tres archivos el Stop exige una ceremonia que el host no puede ejecutar | `--host zcode` instala los 3 perfiles desde `agents/` del repo a `~/.zcode/agents/` (tres estados: ausente / nuestro / desconocido); `--quitar-zcode` borra solo los que llevan `saikit_owned` (con backup); fuente ausente o sin bash.exe ⇒ exit 2, config y agentes intactos; un agente ajeno no se pisa; marca solo en el frontmatter; sin `--host` no se escribe ese dir | 5.4 | cc:完了 [46027c2] |

---

## Phase 6: El tercer host — Codex

Purpose: Codex CLI corre otra variante del kit, con A1–A11 abiertos y la
ceremonia de roles **inerte** (su rama también pregunta por `TARGET = "claude"`
y su wrapper manda `codex`). Esta fase le da el hook de este repo. Diseño
aprobado en `docs/phase-6-codex-design.md` (`599be48`), con premisas de disco
medidas; los payloads y la salida siguen siendo `unknown` hasta 6.1/6.2. Spec:
`docs/spec/00-project-spec.md`. Si una premisa cae, la fase se re-planifica antes
de tocar el hook de producto.

**Reabre un Non-Goal declarado**, solo para `.codex`. `.cursor` y `.agents`
siguen fuera y su divergencia sigue declarada.

**Orden y el costo de no seguirlo:** 6.1 y 6.2 son medición con persona
adelante y valen aunque el resto se cancele. **6.4 no se implementa antes que
6.1**: prender la ceremonia sin saber si el rol llega convierte el gate de
inerte en *inservible*, que es el error exacto que el spec declara para el orden
A9/A10. El perfil Codex no se instala hasta 6.6. 6.4 deja `codex` antes de
zcode/Claude; 7.3, serializada detrás, insertará Grok arriba. El orden final es
`grok > codex > zcode > claude > other`.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 6.1 | `[Test]` `[lane:gate]` `[tdd:required]` **PRIMER PASO OBLIGATORIO: capturar payloads reales de Codex CLI 0.147.0**, mismo criterio que 1.4 y 5.1 — de ahí salieron A9/A10/A11, ninguno visible en payloads reconstruidos. **Simplificación medida frente a 5.1**: el wrapper `.ps1` ya prefiere `<repo>/.codex/hooks/summonaikit-harness.sh`, así que la captura es **colocación de archivo en el repo descartable, NO mutación de `~/.codex/hooks.json`** — el config del operador no se toca | Payloads crudos de las 3 fases (`UserPromptSubmit`, `PostToolUse`, `Stop`) capturados en un turno `-saikit` real de Codex, en un repo descartable; queda escrito el **diff de forma contra los payloads de Claude**; se declara **cómo se llama la herramienta de subagentes y en qué campo viaja el rol**; se mide si el matcher REAL de `hooks.json` (`…\|Task\|exec\|local_shell_call\|shell_command\|commandExecution`) atrapa la delegación; se mide si `transcript_path` cae adentro de `~/.codex/sessions/` (decide si A6 contiene o queda fail-open); se confirma que `SUMMONAIKIT_HOOK_TARGET=codex` está en el env del hook; `~/.codex/hooks.json` verificado sin cambios (cksum antes/después). Si una premisa del diseño cae, se re-redacta la fase | - | cc:完了 — **CIERRE (2026-08-13)**: 3 turnos reales en `C:\dev\saikit-captura-codex` (2 rondas), Codex CLI 0.147.0 / `gpt-5.6-sol`, 14 payloads de las 3 fases. **DoD verificada**: (1) la herramienta de subagentes es **`spawn_agent`** y el despacho **NO** genera evento (0 payloads con ese `tool_name`), pero los eventos **de adentro** del subagente sí llegan con el rol en **`agent_type` de PRIMER NIVEL** (`agent_type=reviewer` + `agent_id`) — misma forma que Claude post-3.7, y `hooks/summonaikit-harness.sh:927` **ya lo lee sin cambios**; (2) **la DoD se cumple sólo para la vía de los eventos internos, y se acota a eso**: el matcher real los atrapa porque llegan como `Bash`, así que el rol llega al gate sin tocar `~/.codex/hooks.json`. Lo que la captura **NO** puede afirmar es si el matcher atrapa el **despacho** de `spawn_agent`: se observaron 0 eventos, y no se distinguió "no emite" de "lo filtra el matcher" (el `hooks.json` de proyecto sin matcher capturó 0 porque `config.toml` exige `trusted_hash`). Ese punto queda **`unknown`**, no cerrado, y la 6.5 **no puede tomar "el registro está completo" como medido**: alcanza para la ceremonia, que es lo único que esta fase necesita, y no para nada que dependa del evento de despacho; (3) `transcript_path` cae **adentro** de `~/.codex/sessions/` ⇒ la contención de A6 **funciona** con el hook en `~/.codex/hooks/` (mejor que zcode, fail-open allá); (4) `SUMMONAIKIT_HOOK_TARGET=codex` presente y **`CLAUDECODE` AUSENTE** en sesión real — y una prueba de cableado lanzada desde Claude reprodujo `CLAUDECODE=1`, o sea que la colisión que D2 previene está **medida**, no supuesta; (5) forma **snake_case idéntica a Claude** (sin duplicación camelCase, a diferencia de zcode), con `turn_id` nuevo (no se usa: la 3.4 llavea por `session_id`) y `permission_mode=default` (Claude nunca dio `default`). **Ninguna premisa cayó ⇒ D3 SE PRENDE** en la 6.4. Perfil intacto: los 3 cksum de `~/.codex/` idénticos antes/después. Detalle en `docs/task-6.1-captura.md`. **Sin resolver, declarado**: no se distinguió si `spawn_agent` no emite evento o si el matcher lo filtra — el `<repo>/.codex/hooks.json` sin matcher capturó 0 porque `config.toml` exige `trusted_hash` por entrada; no mueve ningún veredicto. **Declarado**: durante la captura el sentinel NO arma, porque el `.ps1` corre UN hook y el shim reemplaza al harness (mismo "un hook, no dos" de `stage-override.sh`) |
| 6.2 | `[Test]` `[lane:gate]` `[tdd:required]` **El contrato de SALIDA, medido y no supuesto.** Se reusa `tools/probe-zcode-output.sh` (5.2). Hipótesis fuerte pero NO medida: el fork de Codex usa nuestra misma forma de bloqueo (`decision:block` + `exit 2` + stderr) y su `emit_allow` no imprime para no-cursor ⇒ el vendor cree que el esquema de Claude sirve ahí | Cada una de las 4 formas de salida tiene veredicto MEDIDO en Codex (aceptada / rechazada / ignorada), y el rechazo se distingue del silencio; `exit 2` en `Stop` tiene medido si bloquea, si continúa o si cuenta como error; el resultado decide si `TARGET=codex` alcanza o hace falta una forma propia, con su evidencia. **Se puede medir en la MISMA sesión que 6.1** | 6.1 | cc:完了 — **CIERRE (2026-08-14)**: **12 turnos headless** (`codex exec --json`) en `C:\dev\saikit-probe-codex`, Codex CLI 0.147.0, sesión nueva por turno. Detalle en `docs/task-6.2-salida.md`. **El veredicto que decide la 6.4: el hook vivo, tal como emite hoy, sería DECORATIVO en Codex.** `emit_gate_failure` manda la forma 2 **más `exit 2`**, y con el mismo JSON: **exit 0 ⇒ 4 Stops, 3 `hook_prompt`, bloquea**; **exit 2 ⇒ 1 Stop, 0 `hook_prompt`, no bloquea (2/2)**. O sea que **Codex descarta el stdout del hook cuando el exit no es 0** — y no es que el 2 no llegue: la cadena `.ps1 → shim → probe` se verificó aparte devolviendo exit 2 con su stderr. ⇒ **`TARGET=codex` necesita forma propia: el JSON de bloqueo va con `exit 0`.** **Veredictos**: `additionalContext` ✅ aceptada (2/2, entra como mensaje de rol `developer`, no `system` como zcode); clave extra ❌ **rechazada (0/3) ⇒ esquema ESTRICTO**; `decision:block`+exit 0 ✅ aceptada (el `reason` llega envuelto en `<hook_prompt hook_run_id=…>` y el modelo lo emitió como su propio mensaje); `continue:false`+`stopReason` ⚪ ignorada; `systemMessage` ⚪ ignorada; `exit 2` pelado ⚪ ignorada. **Dos premisas tumbadas**: (1) la hipótesis del diseño §6.2 cae en sus DOS mitades; (2) «el esquema no es estricto» **no es propiedad del esquema sino del host** — 5.2 lo midió tolerante en zcode y acá invalida la salida entera, así que la corrección F2 del spec no se generaliza. **Fila 8 agregada sobre la marcha** (forma 2 + exit 2 = el vivo): no estaba en el plan y es la que evita que 6.4 prenda un gate que no gatea. **Confusor descartado por medición**: el `Stop`/`UPS` del operador tienen un segundo hook (`harness-mem`) y la inyección nuestra entró igual. **Perfil intacto**: los 3 cksum de `~/.codex/` idénticos antes/después; ninguna config se editó (el registro es colocación de shim). **Límite declarado**: todo se midió **headless**, no en la TUI — el veredicto de `systemMessage` vale para headless y queda `unknown` en interactivo. **Sin resolver**: cuántas pasadas hace Codex antes de cortar solo (nuestro tope de 3 emisiones por ronda llegó primero, a propósito), si hay Stop de cierre, y qué hace con dos hooks de la misma fase saliendo 2. **Gates**: `tests/run.sh` OK (18 tests) con `SAIKIT_HOOK_VIVO` a la fuente; `pre-commit run --all-files` Passed |
| 6.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Absorber `ROLE FALLBACK` — cierra un agujero que Claude YA tiene hoy.** Es la única de las cinco divergencias del sabor Codex que es **código en la condición del gate**, no texto de contrato: permite declarar `ROLE FALLBACK: <ROL> (razón)` en el recibo en lugar del despacho cuando el subagente se cae por infraestructura (429, límite de uso). Nuestro hook no tiene ninguna escotilla ahí. **No toca Codex: es un cambio de Claude** | El gate acepta la declaración en lugar del despacho para los 3 roles; un recibo SIN la declaración y sin despacho sigue bloqueando; `mut_role_fallback_quitada` atrapada por el caso nuevo; el diff de la línea base queda **declarado por adelantado** — ningún veredicto se mueve (ningún escenario existente escribe la etiqueta) y sí cambia el texto del contrato inyectado, así que `02-armado-contrato` diverge; si diverge algo más, es señal de que la escotilla toca de más | - | cc:完了 — **CIERRE (2026-08-13)**: el gate acepta `ROLE FALLBACK: <ROL>` en lugar del despacho en los **tres** roles; un recibo sin la declaración y sin despacho sigue bloqueando. **44 mutaciones, 44 atrapadas**; `mut_role_fallback_quitada` queda acreditada a `caso_g3_role_fallback_implementer_permite`. **La predicción del diff quedó CORTA y se corrige acá**: `docs/phase-6-codex-design.md` predijo que divergiría sólo `02-armado-contrato`; se midieron **22 de 25**. Causa: cambiaron **DOS** textos, no uno — el contrato inyectado **y** el mensaje `Missing X subagent run`. El segundo era **necesario**, no alcance de más: `build_gate_feedback` no reimprime el contrato, sólo `$missing`, así que el mensaje de bloqueo es el único lugar garantizado de llegarle al modelo justo cuando la escotilla hace falta. Auditado por verificador y revisor independientes: **ningún veredicto se movió** en los 25 escenarios (exit codes, `decision:block`, `agents_seen`, ciclos), todo el diff es texto. Línea base regrabada; `test_golden_baseline` OK. **Decisión declarada del lead**: NO se ancló el match a inicio de línea pese a la sugerencia del revisor — anclar más estricto repite **A8** (un recibo legítimo en texto corrido dejaba de contar) y el gate es advisory/fail-open. **Límite escrito al lado del código**: el match es subcadena sin anclar sobre el texto del asistente, así que si el propio agente cita la mecánica mientras un rol falta de verdad, lo perdona — mismo trade-off que el README ya declara para el gate entero. **Gates**: `tests/run.sh` **OK (18 tests)** corrido con `SAIKIT_HOOK_VIVO` apuntando a la fuente (sin esa variable la suite mide el hook INSTALADO y da 4 falsos rojos — pasó dos veces en el camino), `pre-commit` Passed |
| 6.4 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **`HOST=codex` (D2) + la rama de ceremonia (D3).** `HOST` gana una rama por **señal explícita antes de zcode/Claude**: el operador corre Codex desde adentro de Claude (agente `codex:codex-rescue`, `cross-review.ps1`), y ahí el hook de Codex hereda `CLAUDECODE=1` y se creería Claude. Allowlist, no passthrough: solo el literal `codex` | Un turno de Codex y uno de Claude sobre el MISMO repo no comparten estado, medido antes y después; un `SUMMONAIKIT_HOOK_TARGET` con valor no reconocido cae por las demás señales y nunca acredita `codex`; `CLAUDECODE=1` presente **más** `TARGET=codex` resuelve a `codex`; Codex queda antes de zcode/Claude y la DoD deja explícito que 7.3 añadirá Grok por encima; la ceremonia acepta `codex` **solo si 6.1 midió que el rol llega** — si no, la rama NO se prende y queda escrito como límite, no como pendiente; `mut_host_codex_sin_rama` y, si aplica, `mut_ceremonia_sin_codex` atrapadas; **y la forma de salida de codex sale con `exit 0`, no con el `exit 2` del vivo** — 6.2 midió que Codex descarta el stdout cuando el exit no es 0, así que `emit_gate_failure` tal cual es decorativo ahí: un caso tiene que fijar que en target codex el JSON de bloqueo viaja con exit 0, y una mutación que le devuelva el `exit 2` tiene que ponerse roja | 6.1, 6.2 | cc:完了 [17d67a5] — **CIERRE (2026-08-15)**: D2: la rama explícita `SUMMONAIKIT_HOOK_TARGET=codex` va PRIMERO en la detección de HOST (allowlist: solo el literal; valores no reconocidos caen por las señales de abajo hasta `other`, atado por `caso_g1_host_codex_solo_literal`; 7.3 insertará grok encima). D3 **SE PRENDE** (6.1 midió el rol llegando en `agent_type` de primer nivel): la ceremonia pasa de `[ "$TARGET" = "claude" ]` a `case claude\|codex`, y la escotilla D4 (ROLE FALLBACK) vale por la MISMA rama — atado dentro de `caso_g3_ceremonia_se_exige_en_codex`. Forma de salida: el JSON de bloqueo viaja con **exit 0** en target codex (6.2: Codex descarta el stdout con exit != 0); claude/zcode conservan su exit 2 (`caso_g6_bloqueo_por_target` intacto). **ROJO medido pre-fix (4/4)**: el lado codex caía bajo `claude/` heredando CLAUDECODE=1; `target=codex` daba `other/` (sin rama) y `zcode/` (orden); ceremonia inerte (cierre limpio sin bloqueo); ni decision:block ni stderr en el Stop codex. Post-fix: **13/13 verdes** (4 nuevos + 9 regresiones vecinas). **3 mutaciones nuevas atrapadas por SU caso** (`host_codex_sin_rama`→dos_hosts_codex; `ceremonia_sin_codex`→ceremonia_se_exige_en_codex; `bloqueo_codex_exit2`→bloqueo_codex_exit_cero) **más `mut_secuencia_tambien_en_cursor` ACTUALIZADA** al literal nuevo del case (su sed apuntaba a la línea que esta task movió — sin actualizarla, la guardia 2 de la batería reventaba) y re-atrapada. **Límite declarado**: `emit_budget_exhausted` conserva la forma claude en codex — `continue:false` es ignorado ahí (6.2, ⚪): el corte no bloquea en codex pero el estado ya se limpió (A4-c4) y el turno termina; forma propia queda para su fila si la medición de 6.6 la pide. **Línea base: 0 divergencia** (`test_golden_baseline` OK — ningún escenario corre target codex). Gates: `tests/run.sh` OK (18 tests, 62 mutaciones/62) sobre el tip 2d3a80a con `SAIKIT_HOOK_VIVO` a la fuente — UNA corrida cubre 6.4+6.5, declarado acá y en 6.5; `pre-commit` Passed |
| 6.5 | `[Setup]` `[lane:gate]` `[tdd:required]` **Manifiesto + `--host codex` + verificador del registro.** El destino pasa a decidirse por `--host`, y cada host declara su ruta: el mensaje actual de rechazo del `--dest` (*"Una sola copia del hook vive en ~/.claude/hooks"*) queda falso y se reescribe. `~/.zcode` sigue rechazado; `~/.codex` se habilita **solo** con `--host codex` | `hooks/vendor-manifest.sha256` gana la entrada de Codex (`4e6a92fa…7902db0f`, 55609 bytes) con su etiqueta; `--host codex` respeta los tres estados y un `--dry-run` contra el destino real lo clasifica **vendor conocido**; `--restore-vendor` funciona para ese destino con sus mismas negativas (sin backup ⇒ exit 6; desconocido no se pisa ni para deshacer; backup fuera del manifiesto no se restaura); `check-hook-registration.sh` afirma **dos cosas separadas** —el registro nombra al wrapper, y el wrapper existe y nombra al hook— sin colapsarlas, conservando exit 0 siempre y `unknown` ≠ ausente; **chequeo explícito de que ningún aserto de quality-kit dependa de `$skippedOwned` vacío**, ANTES de instalar | 6.4 | cc:完了 [2d3a80a] — **CIERRE (2026-08-15)**: el destino lo decide `--host` y cada host declara su ruta (D1). `--host codex` instala la segunda copia en `~/.codex/hooks` por el flujo NORMAL de DEST (3 estados + escritura atómica + backup fechado), con la ruta del host como default sin `--dest`; `~/.zcode` sigue rechazado SIEMPRE (mensaje reescrito — el viejo *"Una sola copia…"* quedó falso con D1, atado por caso); `~/.codex` se habilita SOLO con `--host codex` (caso propio). Manifiesto: entrada del vivo de Codex (`4e6a92fa…7902db0f`, 55609 bytes, **verificado byte a byte HOY** contra `~/.codex/hooks/`), con su límite fail-closed declarado en la línea. Verificador: TERCERA forma `--codex-hooks-json` con las DOS afirmaciones separadas — (a) el registro nombra al wrapper en las 3 fases (lector python reutilizado: la forma `hooks.<Fase>` de Codex es idéntica a Claude, medida en el registro vivo); (b) el wrapper existe y nombra al hook (`--codex-wrapper`, default = la cadena real) — exit 0 siempre, ausente OBSERVADO ≠ ilegible (unknown), y el advisory de `Agent` NO aplica en codex a propósito (6.1: el rol llega por eventos internos Bash; el despacho quedó `unknown` — advisory sin medición sería alarma falsa, comentado en el código). **ROJO medido en los 3 archivos de test** (14 asserts: `--host solo acepta zcode`, `--dest` bajo `.codex` escribía, mensaje viejo presente, flags codex ignorados); post-fix **los 3 OK**, cero regresión. **Evidencia viva (solo lectura)**: `--dry-run --host codex` contra el destino real clasifica **VENDOR CONOCIDO** con la etiqueta del manifiesto, rc=0 sin escribir; el checker queda en **SILENCIO** contra `~/.codex/hooks.json` real (las dos afirmaciones verdes). **Chequeo `$skippedOwned` (DoD, ANTES de instalar)**: cero ocurrencias en `quality-kit/tests/run-tests.ps1` — ningún aserto depende de que quede vacío (las 4 del heal son de reporte). El instalador NO corrió contra `~/.codex` (eso es 6.6). Gates: `tests/run.sh` OK (18 tests, 62 mutaciones/62) sobre 2d3a80a — la corrida única del lote 6.4+6.5; `pre-commit` Passed |
| 6.6 | `[Test]` `[lane:release]` `[tdd:required]` **Línea base del target codex, instalación y turno real.** Escenarios **26+**, construidos desde los payloads de 6.1 y NO reconstruidos. Puesta en producción **directo al perfil** (decisión del operador), con `--restore-vendor` como red | Los escenarios de Codex tienen su bloque en la línea base y `--check` es reproducible; cada gate tiene al menos un caso que pasa y uno que bloquea **en este target**; los fixtures nuevos pasan `test_fixtures_json.sh` (parseo real **y** fidelidad de rutas); `tests/fixtures/arnes-falso/escenarios/04-codex/` espeja al `03-zcode`; el hook queda instalado en `~/.codex/hooks/` y un turno `-saikit` real en Codex **arma**, uno pelado **no**, y el estado aparece bajo `state/codex/`; `~/.claude/hooks/` y `~/.codex/hooks.json` verificados sin cambios; `pre-commit run --all-files` pasa y, tras merge a `master`, se ejecuta el deploy/check de AGENTS.md más `--host codex` y se registra en `docs/deploy-log.md` | 6.5, 6.3 | cc:完了 [eb57765 + deploy 2e7a181] — **CIERRE (2026-08-15)**. **Instalado y medido en vivo**: `install-hook.sh --host codex` clasificó el destino VENDOR CONOCIDO (etiqueta del manifiesto) → backup fechado → reemplazo byte a byte; verificador codex en SILENCIO; `~/.codex/hooks.json` y `~/.claude/hooks/` con cksum idéntico antes/después. **Turno `-saikit` real ARMÓ** (contrato inyectado; el modelo intentó `spawn_agent`, reintentó una vez como exige el flujo y declaró `SUMMONAIKIT HARNESS DELEGATED - awaiting implementer` — la escotilla viva en Codex; estado bajo `state/codex/<key>/<session>/`, D2 en producción); **el turno pelado NO armó** (1 solo estado tras ambos). Deploy en `docs/deploy-log.md` (PR #23). Nota operativa: `codex exec` corre read-only — el patch del turno armado lo rechazó el sandbox de Codex, no el gate. Los payloads crudos de 6.1 NO se commitearon y su repo descartable fue borrado ⇒ **RECAPTURA headless** (shim en `<repo>/.codex/hooks/`, mecanismo 6.1/6.2; `codex exec`, codex-cli 0.147.0 — el mismo binario que correrá el hook; 14 payloads, 3 con `agent_type=reviewer` de delegación real; los 3 cksum del perfil idénticos antes/después). Forma confirmada + 2 notas nuevas declaradas: `permission_mode=bypassPermissions` en headless (la TUI dio `default`; el hook no ramifica por ese campo) y `tool_response` es **STRING plano** (no objeto como Claude) — los fixtures conservan esa forma. **Escenarios 27-33** (los codex arrancan en 27: el 26 lo ocupó 10.1): cada gate con su mitad que pasa y la que bloquea en este target; los bloqueos salen con **exit 0 y stdout idéntico por sha a sus gemelos claude/zcode** (la diferencia visible ES la forma 6.4); el budget graba `continue:false` que Codex ignora (límite declarado); `estado_host: codex` pinna `state/codex/` en la base (el snapshot normaliza el host — `golden-harness` ganó esa línea para codex). `arnes-falso/04-codex` espeja al 03-zcode con la rama D2 en el hook falso (**ROJO medido**: neutralizada, el caso estado_host se pone rojo — la trampa de vacuidad que 5.5 arregló para zcode). **Auditoría del regrabado: región 01-26 BYTE-IDÉNTICA** (0 veredictos movidos), +475 líneas = solo los bloques nuevos; `--check` reproducible 33/33; fixtures pasan `test_fixtures_json` (132 json). Suite del lote OK (18 tests, 63 mutaciones/63, golden 33/33) sobre eb57765. Pendiente para cerrar: tras merge a master, `install-hook.sh --host codex` (backup + `--restore-vendor` de red), turno `-saikit` real arma / pelado no / estado bajo `state/codex/`, `~/.claude/hooks/` y `~/.codex/hooks.json` sin cambios, deploy-log |

---

## Phase 7: El cuarto host — Grok Build TUI

Purpose: un turno `-saikit` en Grok Build 1.0.3 gatea con la misma fuente,
arreglos A1–A11 y ceremonia implementer → verifier → reviewer. Diseño:
`docs/phase-7-grok-design.md`; spec: `docs/spec/00-project-spec.md`. La
documentación oficial fija hipótesis, no evidencia viva: payloads y resolución
de roles **ya medidos en 7.1** (`docs/task-7.1-captura.md`); el contrato de
salida permanece `unknown` hasta 7.2.

**Orden:** 7.1 y 7.2 son mediciones independientes en un repo descartable y
pueden correr antes de que cierre Phase 6. La implementación se serializa en las
costuras compartidas: 7.3 detrás de 6.4, 7.5 detrás de 6.5 y la numeración/release
7.6 detrás de 6.6. Así no hay dos writers sobre `HOST`, el instalador ni los
escenarios 26+.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 7.1 | `[Test]` `[lane:gate]` `[tdd:required]` **PRIMER PASO OBLIGATORIO: capturar payloads reales de Grok Build 1.0.3**, usando sólo hooks de proyecto en un repo descartable. `tools/capture-payloads.sh --host grok` puede crear el JSON/shim de medición; `/hooks-trust` es el único estado global permitido y se declara/revoca al terminar. No se instala el harness global | Quedan payloads crudos y versión exacta de `UserPromptSubmit`, `PostToolUse` exitoso, `PostToolUseFailure`, `SubagentStart`, `Stop reason=end_turn` y Stop de cierre (`channel_closed`/`shutdown`), más el diff contra Claude; se mide nombre nativo/alias de cada tool, ubicación del rol, resolución de un agente temporal, matcher real, `GROK_HOOK_EVENT`/`GROK_SESSION_ID`/`CLAUDECODE`, entrega del `env` map, `lastAssistantMessage`, transcript y contención A6; `~/.grok/hooks/`, `~/.grok/config.toml` y agentes globales quedan byte a byte intactos. Si cae una premisa, se corrigen diseño, spec y filas 7.2–7.6 antes de código de producto | - | cc:完了 — **CIERRE (2026-08-13)**: 5 turnos reales headless en `C:\dev\saikit-captura-grok` (+4 de diagnóstico), Grok Build 1.0.3 / `grok-4.6`, **37 payloads**. `capture-payloads.sh --host grok` con tests (rojo→verde). **D3 SE PRENDE ENTERO**: el rol llega por tres canales (`SubagentStart.subagentType`, despacho `spawn_subagent` con `toolInput.subagent_type` — que SÍ emite, a diferencia de Codex — y eventos internos con `subagentType`) y el `env` map **sí entrega** `SUMMONAIKIT_HOOK_TARGET=grok`. Envelope camel con literales snake (`user_prompt_submit`, `stop`); `GROK_HOOK_EVENT`/`GROK_SESSION_ID` presentes, `CLAUDECODE` ausente; `transcriptPath` cae en `~/.grok/sessions/…/updates.jsonl` (A6 contiene); `lastAssistantMessage` en el Stop; Stop de turno `reason=end_turn` vs cierre `reason=shutdown` (existe en headless). **Premisas caídas**: (1) D5 — las fallas (`exit 1`, `FileNotFound`, `NoMatchesFound`) llegan como `PostToolUse` con `toolResult` de error, `PostToolUseFailure` no disparó nunca (queda `unknown` qué lo dispara); (2) el shell de hooks en Windows es **powershell.exe** — el comando del JSON canónico de D1 necesita la forma `& "…bash.exe" "…"`; (3) la tool de escritura se llama `write`, no `search_replace` (cubrir ambas); (4) el frontmatter con `skills:` string no parsea en Grok (7.5 necesita traducción). Trust de proyecto: `GROK_FOLDER_TRUST=0` NO alcanza; hubo que agregar la entrada en `trusted_folders.toml`, restaurada byte a byte al final. Perfil `~/.grok` con cksums idénticos antes/después. Detalle: `docs/task-7.1-captura.md` |
| 7.2 | `[Test]` `[lane:gate]` `[tdd:required]` **Contrato de salida medido, no inferido de la guía.** Extender `tools/probe-zcode-output.sh --host grok` para el mismo repo descartable, sin registro global. Hereda de 7.1 (medido): el `command` del JSON va en forma PowerShell (`& "<bash.exe>" "<probe>"`); el trust se hace por `trusted_folders.toml` (backup + restore byte a byte; no existe flag `--trust` y `GROK_FOLDER_TRUST=0` no carga hooks); los turnos son **headless** (`grok -p`), sin operador adelante; el oráculo del nonce es `~/.grok/sessions/<cwd-encoded>/<uuid>/` + `--debug-file` | Las 4 formas (`additionalContext`, `decision:block`, `continue:false`+`stopReason`, `systemMessage`) y `exit 2` en Stop tienen veredicto vivo distinguible (aceptada/rechazada/ignorada); se mide también que el Stop de cierre (`reason=shutdown`, dispara en headless) ignora la decisión sin mutar estado; el resultado fija `TARGET`/budget de Grok. Si ninguna forma bloquea de manera fiable, 7.3–7.6 no arrancan: se re-planifica una salida propia o se cancela el host, sin publicar un gate decorativo | 7.1 | cc:完了 [fab5752] — **CIERRE (2026-08-14)**: 7 rondas headless + 4 variantes oráculo (`docs/task-7.2-salida.md`). **Bloquean**: `decision:block` top-level exit 0 (log `block=true`, 2 `end_turn`, el modelo recibe el `reason`) y `continue:false`+`stopReason` (`prevent_continuation=true`). **Ignoradas**: `exit 2` (PowerShell lo devuelve como 1; Grok fail-open en Stop), `additionalContext` (4 formas medidas, ninguna llega al modelo — el armado se re-planifica en 7.3), `systemMessage` (sin superficie en headless). Esquema NO estricto (clave extra tolerada). Decisión sobre `shutdown`: emitida e ignorada sin mutar estado (DoD 2 ✓). Decisión: `TARGET=grok` con forma propia (bloqueo sin exit 2); el gate NO es decorativo. Perfil `~/.grok` byte a byte intacto (cksums); trust declarado y revocado. Lección: el restore quirúrgico del plan dejaba líneas huérfanas en el TOML — detectado por cksum, restaurado desde backup, plan corregido. |
| 7.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Envelope y señal Grok (D2+D4+D5+D6):** `HOST=grok`, aliases camel con precedencia snake, `user_prompt_submit`, tools nativas (`run_terminal_command`, `search_replace`, **`write`**, `spawn_subagent`), evidencia de fallas **leída del `toolResult` de `post_tool_use`** (7.1 midió que `PostToolUseFailure` no dispara), sentinel `-saikit` buscado **dentro del wrapper `<user_query>…</user_query>`**, y filtro `reason=end_turn`. Se integra sobre la rama Codex ya aterrizada | Un envelope real de 7.1 arma; el orden de identidad es `grok > codex > zcode > claude > other`; Grok y Claude sobre el mismo repo no comparten estado; todos los lectores camel medidos (`sessionId`, `toolName`, `toolInput`, `lastAssistantMessage`, `transcriptPath`) tienen caso de precedencia snake; `run_terminal_command`, `search_replace`, `write` y `spawn_subagent` acreditan sólo lo que corresponde; un `post_tool_use` con `toolResult` de error **no acredita verificación** (runner rojo visible); un Stop Grok distinto de `end_turn` (`shutdown` medido; `channel_closed` sigue `unknown`) sale sin contar ciclo, limpiar ni crear estado; cada rama nueva tiene catch rojo y mutación dirigida, y las suites Claude/zcode/Codex no cambian de veredicto | 7.1, 7.2, 6.4 | cc:完了 — **CIERRE (2026-08-15, sesión zcode)**: D2 con `GROK_HOOK_EVENT` por PRESENCIA (no valor) y ARRIBA de codex — el escenario medido que cierra: Grok lanzado desde adentro de Claude hereda `CLAUDECODE=1` y sin la prioridad ambos colapsan a `state/claude/` (espejo exacto del caso codex). D4: alias camel como FALLBACK con precedencia snake en `sessionId` (+ `GROK_SESSION_ID` del env como último recurso, A4), `toolName`, padre `toolInput` de `json_tool_input_string` (parámetro `$2`, default snake), `transcriptPath`, walker (`last_assistant_text` wrapper) y literales `user_prompt_submit`/`session_start` en el case de PHASE — sin ese literal, un envelope real caía a `PHASE=tool` y NUNCA armaba. D5: el veto del crédito lee `toolResult.exit_code != 0` **con grep acotado al objeto** (`[^}]*` no cruza su cierre; un eco en otro objeto no colase, clase C3) y las variantes `FileNotFound`/`NoMatchesFound`; `search_replace` entra a la señal precisa de edición del review-notice. **Detalle medido en el camino**: `exit_code` viaja como NÚMERO (`"exit_code":1` sin comillas) — los lectores de strings del hook no lo ven (medido: `output_for_prompt` sale, `exit_code` no), por eso el grep y no el lector. D6: el sentinel dentro de `<user_query>` NO necesitó cambio de RE (`>` y `\n` ya son frontera izquierda válida; atado por caso), y el Stop grok distinto de `end_turn` sale inmediato sin tocar estado — `reason` VACÍO no exime (Core Rule 2). **16 casos grok** (armado/cross-host/shutdown, créditos camel, 5 precedencias snake — uno por lector medido, 3 canales de rol vía despacho e interno, turno completo, transcript camel) + **6 mutaciones dirigidas** (host_grok_sin_rama, phase_sin_user_prompt_submit, toolresult_veto_quitada, toolresult_variantes_quitada, alias_padre_camel_quitado, stop_sin_filtro_end_turn), todas atrapadas por SU caso. **Lección del propio cambio**: 3 mutaciones VIEJAS quedaron obsoletas porque sus sed apuntaban a líneas que esta task reescribió (`clave1 == "tool_input"` → `clave1 == padre`; dos call sites `assistant_text_payload` → `last_assistant_text`) — la guardia "la mutacion no cambio nada" las atrapó y se actualizaron a los patrones nuevos con la misma semántica. Línea base `--check`: **33/33 escenarios se comportan igual** (identidad del hook cambia, 0 veredictos movidos). Batería de comportamiento completa OK (Claude/zcode/Codex/cursor sin cambio de veredicto). **Lo que NO hace, por diseño**: ceremonia grok y formas de salida (7.4: el `case claude\|codex` sigue sin grok), instalador/JSON de registro (7.5), línea base grok (7.6); el armado inyecta `additionalContext`, que 7.2 midió IGNORADO en Grok — el canal de armado real se redefine en 7.4, declarado |
| 7.4 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Ceremonia Grok (D3), habilitada por 7.1.** El rol llega por tres canales (medido) y el `env` map entrega `TARGET=grok` (medido): se prende la rama fuerte `claude\|codex\|grok`; 7.2 fija las formas de salida y budget | Implementer→verifier→reviewer queda exigido en target Grok, con caso que pasa, caso que bloquea y mutación `mut_ceremonia_sin_grok`; prueba negativa: sin la señal Grok la rama no aplica; ninguna rama Grok altera la ceremonia de los otros hosts | 7.3 | cc:完了 — **CIERRE (2026-08-15, sesión zcode)**: la ceremonia entra por `case claude|codex|grok` (7.1 midió el rol por TRES canales y TARGET=grok por env map en los 37 dumps — la condición del diseño cumplida por partida doble; los canales ya los leía 7.3). **Formas de salida (7.2 decidido)**: el bloqueo viaja `decision:block` + **exit 0** (exit 2 es IGNORADO en Grok: PowerShell lo devuelve como 1 y el host hace fail-open — misma forma que codex/6.4), y el **armado re-planificado**: `additionalContext` es ignorado en las 4 formas medidas y UPS no está en `blockingEvents`, así que el CONTRATO viaja adosado al `reason` de cada bloqueo — el único canal medido que llega al modelo (7.2: el modelo lo citó textual). Llega al primer Stop bloqueado en vez de al armado: declarado. El **budget usa la forma default** (`continue:false`+`stopReason` con exit 0), que es la aceptada — sin cambio de código. **3 casos** (completa cierra / incompleta bloquea con el contrato en el reason / negativa: cursor no exige) + **mut_ceremonia_sin_grok** acreditada al caso de bloqueo. **Ajuste de 5 casos 7.3**: asertaban bloqueo grok como rc!=0 (exit 2) — con 7.4 el bloqueo grok es exit 0 + `decision:block`, y los cierres limpios grok ahora despachan los 3 roles en orden real (edita ANTES del reviewer: el aviso RN acompañaba el cierre si editabas después). Behavior completo OK (142 casos). Validación full por CI Linux del PR |

| 7.5 | `[Setup]` `[lane:gate]` `[tdd:required]` **`--host grok`, JSON de registro, verificador y perfiles (D1+D7).** Primero preflight de hook+JSON+agentes; sólo después se escribe, para que hook o JSON desconocidos dejen TODO intacto. Un agente desconocido es no fatal y no se pisa. Hereda de 7.1 (medido): el `command` del JSON se genera en forma PowerShell (`& "<bash.exe>" "<hook>"`); `saikit_owned` top-level **es tolerado** por el loader; el frontmatter de los perfiles **requiere traducción** (la clave `skills:` como string no parsea en Grok); `PostToolUseFailure` se registra aunque no dispare (D5) | `--host grok` cubre ausente/nuestro/desconocido para hook y JSON, con publicación atómica y rollback ante fallo; JSON desconocido o hook desconocido ⇒ exit != 0 y cero cambios en cualquier destino; implementer/reviewer se instalan con marca y frontmatter traducido (con su caso de test), el `~/.grok/agents/verifier.md` ajeno sobrevive y se reporta; `--quitar-grok` retira sólo archivos propios con backup; el verificador separa JSON→hook, existencia+marca del hook y matcher de delegación, siempre exit 0 y `unknown` ≠ ausente | 7.4, 6.5 cc:完了 — **CIERRE (2026-08-16, sesión zcode; implementación por subagente supervisado)**: PR #29 (rama apilada sobre 7.4).  publica hook+JSON+perfiles con preflight que no escribe nada ante hook/JSON desconocido y rollback; JSON canónico de 5 eventos con command PowerShell  (la forma POSIX que 7.1 midió funcional),  top-level; perfiles con  OMITIDA (el loader de Grok la rechaza como string — si la fuente pasa a secuencia, la traducción se revisa, declarado); el  ajeno del operador sobrevive y se reporta;  retira solo lo nuestro con backup. Verificador  con las 3 afirmaciones separadas, exit 0 siempre. 26 casos con TDD (rojo medido 26+5) y 5 rondas de bots entre ambos PRs. Desviaciones declaradas en el PR: ruta POSIX del command, SubagentStart fuera de las fases exigidas (el rol llega por PTU, medido),  sin especializar (la vuelta de grok es ). Outage de Actions documentado; validación local completa |

| 7.6 | `[Test]` `[lane:release]` `[tdd:required]` **Línea base, staging, instalación y turno real.** Toma el primer bloque libre DESPUÉS de los escenarios Codex ya mergeados; fixtures sólo desde 7.1. Staging con hook de proyecto confiado (entrada en `trusted_folders.toml`, declarada y revocada — 7.1) antes del perfil global | Cada gate Grok tiene caso que pasa y bloquea; `--check` es reproducible; fixtures parsean y conservan rutas; staging ejecuta el hook de proyecto sin tocar el global; el install deja hook+JSON propios bajo `~/.grok/hooks/`, conserva el verifier ajeno y no toca `~/.claude/hooks/` ni `~/.codex/hooks/`; sesión nueva: `-saikit` arma bajo `state/grok/`, prompt pelado no crea estado y la ceremonia cumple exactamente lo medido en 7.1/7.4; `pre-commit run --all-files` pasa y, tras merge a `master`, se ejecuta el deploy/check de AGENTS.md más `--host grok` y se registra en `docs/deploy-log.md` | 7.5, 6.6 | cc:完了 [7c79194] — **CIERRE (2026-08-16, sesión zcode)**: los 37 payloads de 7.1 ya no existían ⇒ RECAPTURA headless (grok 1.0.3, mismo binario; 22 payloads en 3 turnos, perfil byte a byte, trust declarado/revocado) — forma 1:1 contra 7.1 + UN dato nuevo que resuelve su `unknown`: **PostToolUseFailure SÍ disparó** (1 evento, campo `error` SIN toolResult, sobre despacho spawn_subagent con tipo desconocido) ⇒ PTUF = falla de runtime/despacho del tool; los errores de dominio siguen llegando como post_tool_use con toolResult (D5 intacta). **8 escenarios (35-42)**, mitad pasa/mitad bloquea: las formas 7.2/7.4 grabadas (bloqueo decision:block exit 0 con el contrato adosado al reason; budget con la forma default continue:false que grok SÍ obedece, a diferencia de codex); el 37 pinnar el veto D5 del exit_code NUMERO (37 rojo no acredita / 39 verde sí) y el 42 el Stop `reason=shutdown` que todo turno headless dispara (sale inmediato con el turno armado, estado sin cambios); ceremonia por los DOS canales que tocan la sesión armada (despacho `toolInput.subagent_type` + SubagentStart `subagentType`; el canal interno del hijo viaja con sessionId propio y no acredita al padre — medido, declarado). golden-harness: token grok con las TRES señales reales del runner + unsets GROK_* + estado_host grok; arnes-falso/05-grok con rama por SETNESS (ROJO medido → emite other). **Región 01-34 BYTE-IDENTICA (127211 bytes, 0 veredictos movidos), --check 42/42 ×2, fixtures 169 OK. Staging** con hook de PROYECTO (JSON canónico generado por el instalador 7.5 con overrides): disparó (debug log `hook_name=project/summonaikit:…`, hook_count=10 — con el global vivo AMBOS registros corren, cada uno con su árbol de estado), turno cerró ceremonia completa block=false, global con archivos intactos. **Install**: lo había publicado una SESIÓN GEMELA detectada a mitad de task (wt `summonaikit-claude-wt-p7`, rama `feat/7.6-grok-baseline`, SIN commits; su install 01:52, su turno dejó cycle=1 bloqueado) ⇒ esta task lo VERIFICÓ: YA AL DIA con identidad byte a byte (sha hook == source, JSON NUESTRO_IDENTICO), verifier ajeno intacto (353659411), verificador de registro exit 0 silencioso. **Turno real** en repo nuevo: `-saikit` armó bajo `state/grok/1699990704/`, ceremonia con recibo (verifier vía `loop-verifier`, mapeo por stem — el tipo `verifier` no existe bundled y el slot global lo ocupa el ajeno, declarado), Stop block=false cierre limpio; pelado en sesión nueva no creó NADA; conteo con scope a mi key: 1. `~/.claude/hooks/` y `~/.codex/hooks/` intactos. Residuo de la gemela declarado, no borrado (decisión del lead). Detalle y línea de tiempo del perfil: `docs/task-7.6-release.md`. PENDIENTE tras merge: PR + CI `suite` en verde; deploy de AGENTS.md **más `--host grok`** y entrada en `docs/deploy-log.md` |

---

## Phase 8: Defectos de la auditoría del sentinel (2026-08-13)

Purpose: auditoría multi-agente sobre el working tree (workflow
`saikit-sentinel-audit`, 21 agentes, 5 lentes + batería empírica): 14 hallazgos
CONFIRMADOS con reproducción ejecutada, 1 refutado. Detalle y repros:
`docs/audit-sentinel-2026-08-13.md`. Esta fase cierra los 4 lotes priorizados
por el operador; los hallazgos restantes (C6 dotnet/gradle fallidos acreditan,
C8 mención de runner acredita, C9 fallback `$INPUT`, C10 frontera izquierda
acepta `/`, C11 eco del feedback satisface etiquetas, C12 fuga de valores
top-level al texto del gate, C13 acumulación de `state/`, C14 borrado del aviso
RN por Stop fallido hermano) quedan como deuda declarada en ese doc, no como
tareas fantasma.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 8.1 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C1+C2+C3 — los lectores del camino de armado y de evidencia siguen siendo el sed greedy.** C1: `json_string_field` corta el valor en la primera comilla escapada — `arregla el "bug" -saikit` NO arma, y una corrección CON sentinel pero con comillas antes DESARMA a media ceremonia (borra estado+log+orden RN); un `command` entrecomillado (`bash -c "… pytest …"`) pierde el crédito de verificación. C2: el sentinel se grepea sobre JSON crudo sin decodificar — `\n`/`\t` literales antes de `-saikit` rompen la frontera izquierda: un prompt multilínea con el sentinel en su propia línea no arma, y la corrección multilínea desarma. C3: `command`/`tool_name`/`file_path`/`transcript_path` se leen con el greedy que toma la ÚLTIMA ocurrencia del payload — un `"command":"pytest -q"` DENTRO de `tool_response` acredita `verified=1` por un comando que nunca corrió (misma clase A1 que la 3.1 ya cerró para `subagent_type`) | `prompt` se lee con un lector top-level escape-aware y DECODIFICADO (`\n`/`\t`/`\"`/`\\`) antes del sentinel; `command`/`file_path` se leen acotados a `tool_input` (escáner de la 3.1); `tool_name`/`transcript_path` con el lector top-level de la 3.4; casos rojos medidos contra el hook pre-fix: comillas antes del sentinel arma, corrección con comillas no desarma, sentinel tras `\n` arma, comando entrecomillado acredita, eco de `command` en `tool_response` NO acredita; mutaciones que revierten cada lector al greedy atrapadas; la línea base se regraba solo si algún escenario mueve texto y ningún veredicto se mueve sin declararlo | - | cc:完了 — **CIERRE (2026-08-13)**: `json_top_level_decoded` (gemelo parametrizado de `assistant_text_payload`) lee `prompt`; `command`/`file_path` van por `json_tool_input_string`, `tool_name`/`transcript_path` por `json_top_level_string`. 6 casos nuevos (5 con ROJO medido contra el hook pre-fix vía driver suelto; el de eco de tool_name con rojo vía mutante, patrón aceptado del repo) + 3 mutaciones (`prompt_greedy`, `command_desacotado`, `tool_name_desacotado`) atrapadas. **Límite declarado**: revertir `transcript_path` al greedy no tiene caso que lo distinga hoy (la contención A6 acota el daño); queda sin mutación propia, no se finge cobertura. Corpus 81+ casos verde; suite completa OK en mi alcance (los 2 FAIL de la corrida son trabajo grok en vuelo de otra sesión, verdes en HEAD — medido en worktree limpio) |
| 8.2 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C4 — texto viejo del tail del transcript subvierte las escotillas del Stop.** `$text` concatena `last_assistant_message` con TODO el texto asistente de las últimas 160 líneas del transcript (turnos anteriores incluidos): un recibo VIEJO en el tail invierte la escotilla DELEGATED (bloquea un turno de delegación legítimo), y un PAUSED VIEJO en el tail deja pasar el gate entero de un turno que no pausó | Las escotillas PAUSED y DELEGATED (incluida su cláusula de recibo-ausente) se evalúan sobre el texto del turno ACTUAL (`last_assistant_message`; fallback al texto completo SOLO si el payload no trae el campo, para no romper hosts sin él); caso rojo medido: PAUSED solo-en-tail ya no salta el gate, DELEGATED actual con recibo viejo en tail permite; las etiquetas del recibo siguen mirando ambos canales (sin re-abrir A2/A8); mutación que devuelve las escotillas al texto completo atrapada; veredictos de la línea base sin movimientos no declarados | 8.1 | cc:完了 — **CIERRE (2026-08-13)**: `text_hatch` = `assistant_text_payload` con fallback a `$text` SOLO si el payload no trae el campo. 2 casos nuevos (rojo medido en ambas direcciones) + `mut_escotillas_leen_tail_viejo` atrapada. **Efecto colateral cerrado en el mismo cambio**: los catches de `mut_texto_incluye_tool_result`/`tool_use` dependían de que la escotilla leyera el tail; los 2 casos del walker migran a `lab_payload_stop_sin_mensaje` (builder nuevo — ojo: `lab_payload_stop ''` NO sirve, su `${1:-Listo.}` trata vacío como ausente) y las 2 mutaciones siguen atrapadas — verificado en batería acotada. Etiquetas intactas (`caso_g4_recibo_solo_en_transcript_pasa` sigue verde) |
| 8.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C7 — `has_receipt_label` rechaza `**Understand**:`.** El regex exige solo espacios entre etiqueta y `:`; el markdown bold (la forma MÁS natural en que el modelo escribe el recibo) rompe las 6 etiquetas y un recibo honesto y completo se bloquea con "todo falta" — ciclos de revisión de más, que es la lentitud reportada | `**Label**:` y `__Label__:` cuentan como etiqueta; el recibo plano sigue contando (sin regresión A8); caso rojo medido con el recibo bold completo; mutación que quita la alternativa bold atrapada | 8.1 | cc:完了 — **CIERRE (2026-08-13)**: `($label|$alt)(\*\*|__)?[[:space:]]*:` — rojo medido (las 6 etiquetas fallaban a la vez con `**Label**:`), `caso_g4_recibo_bold_pasa` verde, `mut_etiqueta_sin_bold` atrapada, `mut_etiqueta_sin_frontera` sigue atrapada (sin regresión A8: vinetas/corrido/pegada verdes) |
| 8.4 | `[Docs]` `[lane:fast]` `[tdd:skip:texto-de-contrato]` **C5 — el contrato inyectado promete un resume que el gate ya no respalda.** Tras PAUSED/DELEGATED el texto dice "Run the full cycle once they reply" / "resume the cycle", pero la respuesta natural del usuario (sin `-saikit`) desarma por diseño (A4-c2, y es la spec del operador): la promesa quedó falsa | El texto del contrato dice la verdad: la respuesta sin `-saikit` libera el gate y el ciclo prometido corre por disciplina del lead (o el usuario re-arma con `-saikit`); el diff de la línea base es solo texto (`02-armado-contrato` y hermanos), declarado por adelantado; ningún veredicto se mueve | 8.2 | cc:完了 — **CIERRE (2026-08-13)**: los bullets PAUSED y DELEGATED dicen la verdad (un prompt sin `-saikit` desarma por diseño; el ciclo prometido corre por disciplina del lead o se re-arma con `-saikit`). Diff de línea base auditado línea por línea ANTES de regrabar: 22 escenarios divergentes, 44 líneas `stdout (sha …)` + 2 dumps del contrato, **0 exit codes / 0 decision movidos**. Regrabada; `--check` 25/25 OK |

---

## Phase 9: Deuda del gate

Purpose: cerrar la deuda confirmada de la auditoría 2026-08-13 (C6, C8–C14 +
advisory del matcher) con el mismo patrón de la Phase 8 — caso ROJO medido → fix
mínimo → mutación dirigida → línea base auditada. Casos, código del fix y
mutaciones YA DISEÑADOS y cross-reviewados (codex r1, 5 hallazgos incorporados)
en `docs/phase-9-10-plan.md`; esa es la fuente de detalle de cada fila.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 9.1 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C6 — dotnet y gradle fallando ya no acreditan `verified`.** El banner de dotnet (`Failed!  - Failed:     1, …`) no trae dígito ANTES de la palabra (vía A) ni `failures:` (vía B); `FAILURE: Build failed` / `BUILD FAILED` de gradle tampoco matchean: el runner fallido acreditaba `verified=1` | 2 casos rojos medidos (`verified=0` con dotnet/gradle fallidos); `FAILURE_SIGNAL_RE_CI` vía B gana `failed` (`(failures?\|errors?\|failed)[=:]…[1-9]`, el `[1-9]` protege `Failed: 0`); `FAILURE_SIGNAL_RE_CS` gana `\|FAILURE: Build failed\|BUILD FAILED`; control negativo dotnet EXITOSO (`Passed!  - Failed:     0`) sigue acreditando; 2 mutaciones (`falla_dotnet_quitada`, `falla_gradle_quitada`) atrapadas; suite OK | - | cc:完了 — **CIERRE (2026-08-15)**: `failed` entra a la **via B** del `FAILURE_SIGNAL_RE_CI` (`(failures?\|errors?\|failed)[=:]…[1-9]`) y los dos literales de gradle (`FAILURE: Build failed`, `BUILD FAILED`) al `_CS`. **Por que ninguna via los agarraba**: dotnet dice `Failed:     1` y la via A pide el digito ANTES de la palabra; gradle dice `FAILURE`/`FAILED` y el `FAIL[^a-zA-Z]` del CS exige un NO-letra despues, y ahi siguen `U` y `E`. **ROJO MEDIDO** en los 3 escenarios (dotnet, `FAILURE: Build failed`, `BUILD FAILED`): acreditaban `verified=1` con el runner reventado. **Dos controles positivos** protegen el `[1-9]`: `0 failed` de pytest y `Failed:     0` de un dotnet EXITOSO siguen acreditando. Mutaciones `falla_dotnet_quitada` y `falla_gradle_quitada`, una por via, cada una atrapada por SU caso |
| 9.2 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C8 — el runner debe estar en POSICIÓN de comando, no de mención.** Mencionar un runner en un `echo` o un directorio que se llama como el runner acreditaba verificación: el crédito grepea `TEST_RUNNER_WORD_RE` sobre `tool_name + command` sin exigir posición | Nueva `TEST_RUNNER_CMD_RE` (posición: inicio, separador de shell, `\n`/`\t` literales escapados, prefijos `VAR=val`/`time`/`env`/`nice`/`xargs`/ruta) + guardia `ECHO_LEAD_RE` (primer token `echo`/`printf` jamás acredita); el crédito pasa a `$command_text` SOLO (sin concatenar `tool_name`); la PROSA del recibo sigue con `TEST_RUNNER_WORD_RE` (declarar != correr); 2 casos rojos (`runner en echo`, `runner como argumento`) + 2 controles verdes (`&&`, `VAR=val`) + 3 r1 (`multilinea marca`, `echo "pytest" no marca`, `echo empiezo && pytest no acredita — límite del lado estricto ATADO por test`); mutación `credito_por_mencion` atrapada; riesgo declarado: regex más delicado del plan, controles verdes desde el Step 1 y línea base completa | - | cc:完了 — **CIERRE (2026-08-15)**: se completo la mitad que faltaba. `TEST_RUNNER_CMD_RE` ya habia llegado con el PR #20 (sesion paralela), pero **`ECHO_LEAD_RE` no existia** y `echo pytest` acreditaba: `echo` ES un comando, asi que el runner quedaba en posicion legitima. Ahora, si el PRIMER token es `echo`/`printf`, el comando **jamas** acredita; y el credito **deja de concatenar `tool_name`** (el ancla y el separador son del COMANDO). **Detalle que importa**: la guarda mira SOLO la primera linea (`head -n 1`) — con `grep -E ^` sobre el comando entero, un comando multilinea legitimo con un `echo` en cualquier linea perdia el credito. **ROJO MEDIDO** en `echo pytest` y `printf`. **Controles verdes**: `pytest -q && echo listo` y `CI=1 pytest -q` siguen acreditando. **LIMITE del lado estricto, ATADO por caso**: `echo hola && pytest` tampoco acredita — distinguirlo exigiria parsear el shell y el gate es advisory, asi que se elige perder un credito legitimo antes que regalar uno falso. Mutacion `credito_por_mencion` atrapada |
| 9.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C11 — citar el feedback del gate ya no satisface etiquetas.** El feedback contiene la forma `Label:` de las 6 etiquetas; un mensaje que lo CITA (con `'` antes de la etiqueta) pasaba la frontera `[^[:alpha:]]` y cerraba sin recibo real | Fix en DOS mitades: `has_receipt_label` frontera izquierda `(^|[^\[:alpha:+'"])` (excluye comillas) + `build_gate_feedback` reescrito sin forma `Label:` matchable en prosa (el template multilínea se queda — límite declarado: citar el template completo sigue contando, indistinguible por diseño advisory); caso rojo medido; mutación `frontera_acepta_comillas` atrapada; línea base diverge SOLO en escenarios con gate fallido (07, 11, 19, 20, 22 y hermanos), auditada a 0 veredictos antes de regrabar | - | cc:完了 [502368d] — **CIERRE (2026-08-15)**: mitad 1: la frontera izquierda de `has_receipt_label` pasa a `(^\|[^[:alpha:]'\"])` — citar la mecánica (`'Understand:'`… las seis más el marcador nombrado en la misma frase, `_TEXTO_CITA_FEEDBACK`) ya no cierra; **ROJO medido** (exit 0 pre-fix → exit 2 post-fix). **Mitad 2, deriva del plan DECLARADA**: `build_gate_feedback` HOY no contiene ninguna forma `Label:` citada en prosa (el texto evolucionó vía 6.3/8.x desde la auditoría C11: la ayuda dice *"one line that BEGINS with its label and a colon"* sin regalar la forma) — no hubo nada que reescribir y esta fila lo registra en vez de inventar un cambio; el template literal se conserva (límite declarado: citarlo COMPLETO con saltos reales sigue contando — indistinguible de un recibo, advisory por diseño, atado en el comentario del caso). Límite mitad 1: etiqueta legítima tras apóstrofo deja de contar (recuperable). Regresión G4 completa verde (bold, corrido, dos bloques, pegada, viñetas, falta-una, sin-recibo). Mutaciones: `frontera_acepta_comillas` NUEVA atrapada por `caso_g4_cita_del_feedback_no_satisface`; `etiqueta_sin_frontera` ACTUALIZADA al literal nuevo (su sed apuntaba a la frontera que esta task movió — sin eso la guardia 2 reventaba) y re-atrapada por su caso de siempre. **Línea base: 0 divergencia CONFIRMADA byte a byte** (la predicción del plan — divergencia en 07/11/19/20/22 — asumía la reescritura de prosa que ya no hizo falta; la región 01-26 quedó byte-idéntica en el regrabado de 6.6). Gates: `tests/run.sh` OK (18 tests, 63 mutaciones/63) sobre eb57765 — corrida única del lote 9.3+6.6-parcial; `pre-commit` Passed |
| 9.4 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C9 — sin campo `prompt` no se arma (salvo `session` de cursor).** El fallback `prompt_text="$INPUT"` armaba con `-saikit` en CUALQUIER campo del payload (un `resumen_previo` de resume armaba la ceremonia) | Builder `lab_payload_prompt_sin_campo` + caso rojo medido (no arma, no crea estado); fallback acotado a `[ "$PHASE" = "session" ]` (cursor arma ahí con `-saikit` en el texto, `caso_g6_armado_por_target` intacto); mutación `fallback_sin_acotar` atrapada | - | cc:完了 — **CIERRE (2026-08-15)**: el fallback `prompt_text="$INPUT"` queda acotado a `PHASE=session`. Sin acotar, un payload sin campo `prompt` hacia que el sentinel se buscara en el **payload entero**: la forma real es un resume, donde el texto viejo viaja en un campo de resumen, y **armaba la ceremonia entera sin que nadie la pidiera**. **ROJO MEDIDO**: `caso_g1_prompt_sin_campo_no_arma` armaba e inyectaba el contrato completo. Builder nuevo `lab_payload_prompt_sin_campo`. Se **conserva** en `session` porque ahi cursor si arma con el sentinel en el texto y no hay campo `prompt` que leer — `caso_g6_armado_por_target` intacto, como pedia la DoD. Mutacion `fallback_sin_acotar` atrapada. **Residual declarado**: dentro de `session`, un `summary` que cite un `-saikit` viejo sigue armando (sale el contrato en vez de las reglas de 10.6); atado por `caso_g1_session_con_sentinel_en_summary_arma_y_no_da_reglas` |
| 9.5 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C10 — `docs/-saikit.md` y `--saikit` ya no arman.** La frontera izquierda del sentinel acepta `/` y `-`: REFERENCIAR un archivo o flag que contiene el token armaba la ceremonia entera | `SAIKIT_SENTINEL_RE` frontera izquierda `[^A-Za-z0-9_/-]` (excluye `/` y `-`); caso rojo medido (`docs/-saikit.md` y `--saikit` no arman); `caso_g1_sentinel_con_frontera` extendido; controles que siguen armando: inicio, espacio, `\n` decodificado, `(`/`,`/comillas; mutación `frontera_izquierda_floja` atrapada | - | cc:完了 — **CIERRE (2026-08-15)**: la frontera izquierda del `SAIKIT_SENTINEL_RE` excluye tambien `/` y `-`. Referenciar un archivo (`docs/-saikit.md`) o citar un flag (`--saikit`) armaba la ceremonia: el mismo dano que `x-saikit`, por dos caracteres que faltaban en la clase. **ROJO MEDIDO** en los dos. **Tres controles positivos en el MISMO caso** (inicio de texto, `(`, `,`) para que apretar la frontera no repita **A8** — un recibo legitimo que deja de contar. Se **alinea con la deteccion del carril `:fast`** (10.1), que ya usaba esta frontera: antes habia dos criterios distintos para el mismo token. Mutacion `frontera_izquierda_floja` atrapada (reemplaza la LINEA entera para no mutar de paso el regex de `:fast`, que comparte la clase) |
| 9.6 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C12 — el walker del transcript deja de filtrar valores top-level.** Sin reset de `en_text`/`en_assistant` al cerrar llaves, un valor top-level POSTERIOR a `message` (un requestId artificial con `Retro: …` adentro) se concatena al texto del asistente y aporta etiquetas que el turno no escribió | Fixture `lab_transcript_fuga_top_level` + caso rojo medido (fuga que aporta justo Close/Retro NO cierra el gate); fix: resets `if (depth < 4) en_text = 0` / `if (depth < 2) {en_assistant…}` en la rama de cierre del awk; regresión walker completa verde; mutación `walker_sin_resets` atrapada | - | cc:完了 — **CIERRE (2026-08-15)**: el walker prendia `en_text`/`en_assistant` al ENTRAR y **no los apagaba nunca** al cerrar llaves, y su condicion de emision **no mira `depth`**. Consecuencia: un valor top-level POSTERIOR a `message` (un `requestId`) se concatenaba al texto del asistente y aportaba etiquetas que el turno no escribio. **ROJO MEDIDO**: con un recibo sin `Retro` y una fuga que traia justo `Retro: none.`, **el gate CERRABA (exit 0) por texto ajeno** — el modo de falla mas feo del gate. Fix: `if (depth < 4) en_text = 0` y `if (depth < 2) en_assistant = 0` en la rama de cierre del awk, **despues** del `depth--`. Fixture nuevo `lab_transcript_fuga_top_level`; el Stop va sin `last_assistant_message` para que decida el canal transcript. Mutacion `walker_sin_resets` atrapada; regresiones del walker (`role:assistant`, `type:text`) verdes |
| 9.7 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C13 — `state/` deja de acumular sesiones para siempre.** Dos mitades declaradas: (a) toda limpieza remata con `rmdir` best-effort del dir de sesión (jamás `rm -rf` ahí); (b) barrido TTL al ARMAR de hermanas del MISMO proyecto+host con `harness-state.env` > 14 días (`rm -rf` acotado al dir de sesión bajo `$PROJECT_DIR`); fail-open sin `find` | 2 casos rojos medidos: cierre limpio borra el DIRECTORIO; el barrido discrimina muerta (15d, se barre) de FRESCA (sobrevive, A4) en el MISMO caso, y no toca el estado del turno que arma; regresión `caso_g1_dos_sesiones_no_comparten_estado` verde; mutación `estado_inmortal` atrapada; si `touch -d` falla en MSYS2, medirlo y cambiar a `-mmin` con TTL por env solo-para-tests (decisión al implementar, documentada en el caso) | 9.4 | cc:完了 — **CIERRE (2026-08-15)**: dos mitades, y una sin la otra no arregla nada. **(a)** `podar_dir_sesion`: `rmdir` best-effort al final de CADA limpieza (desarme, cierre limpio, presupuesto agotado) — es `rmdir`, **jamas `rm -rf`**: si quedo algo adentro, el dir sobrevive y se ve. **(b)** `barrer_estado_viejo`: al ARMAR se lleva las hermanas del mismo proyecto+host con `harness-state.env` > 14 dias, que cubre las sesiones que nunca cerraron limpio. **MEDIDO ANTES DE DISENAR**: `touch -d`, `touch -t` y `find -mmin` funcionan en MSYS2, asi que **no hizo falta** el TTL-por-env que preveia el plan como contingencia. Tres acotamientos: sin `find` no se barre nada (fail-open); solo debajo de `$PROJECT_DIR`; nunca el dir del turno que dispara. **ROJO MEDIDO en las dos mitades.** La hermana **FRESCA sobrevive** — eso separa este barrido de A4, y es justo lo que ataca `barrido_sin_ttl`. Mutaciones `estado_inmortal` y `barrido_sin_ttl`, una por mitad. **DESVIACION**: el plan ubicaba `estado_inmortal` en G5; quedo en **G1** porque el caso es de armado y estado y el driver corre los casos DEL GATE en que se registra — en G5 no lo habria atrapado nadie. **ALCANCE**: obligo a tocar `stop_gate` (dos lineas pegadas a `rm -f` ya existentes), declarado en la nota de coordinacion |
| 9.8 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **C14 — solo un Stop LIMPIO borra el aviso RN pendiente ajeno.** El `elif` del bloque REVIEW-NOTICE corre en todo Stop: un Stop que BLOQUEA borraba el aviso pendiente de una sesión hermana | Caso rojo medido (Stop fallido NO borra `review-notice-pending.log` ajeno); el `rm` del `elif` se mueve al camino limpio (`if [ -z "$missing" ]`, junto al `rm -f "$RN_ORDER_PATH"`); el disparo del aviso no se toca; regresión RN (`caso_g1_dos_hosts_mismo_repo_no_comparten_estado`, mitad 3) y flujo aviso→entrega verdes; mutación `aviso_se_borra_en_fallo` atrapada | - | cc:完了 [3a572ce] — **CIERRE (2026-08-15, por orden del operador sin esperar 9.7)**: el elif del bloque REVIEW-NOTICE ahora solo ANOTA `rn_pendiente_borrable` (la secuencia se observó limpia) y el `rm` del aviso vive dentro de `[ -z "$missing" ]`, junto al del RN_ORDER — el borde declarado ("el Stop de una secuencia limpia puede borrar el aviso") es el CIERRE limpio, no cualquier Stop. El disparo del aviso (la mitad `-gt`) no se tocó; si el aviso disparó en ese mismo Stop, el flag no se setea y el pendiente queda para el turno siguiente. **ROJO medido**: con contadores observados y secuencia limpia (`last_review >= last_code_edit` — la condición real del elif; el caso del plan sin contadores hubiera nacido verde y se corrigió al implementar, deriva declarada), el Stop fallido borraba el `review-notice-pending.log` ajeno. El caso ata **las DOS mitades del borde** (fallido no borra / cierre limpio SÍ se lleva el desactualizado). Regresión verde: cross-host mitad 3, G5 completo, G4 cita. `mut_aviso_se_borra_en_fallo` atrapada por su caso (el sed devuelve el rm al elif). Línea base: **0 divergencia confirmada** (`test_golden_baseline` OK en la suite del cierre). **Gates**: `tests/run.sh` OK (19 tests, **70 mutaciones/70**) sobre el MERGE e3050c7 — la corrida midió por primera vez los dos lotes juntos (este fix + 9.1/9.2/9.6/9.7 de Phase 9; composición del bloque compartido verificada: el `rn_pendiente_borrable` convive con los `podar_dir_sesion` de 9.7a); `pre-commit` Passed. El aviso de coordinación quedó saldado al revés: 9.7 aterrizó primero y este fix se montó encima por merge |
| 9.9 | `[Test]` `[lane:fast]` `[tdd:skip:accion-de-operador]` **Advisory del matcher — `Agent` entra al matcher de PostToolUse.** Corregida por cross-review r1 (hallazgo 5): `tools/check-hook-registration.sh` YA prueba el matcher contra `Agent` — no hay cambio versionable de código | Acción de operador: editar el matcher en `~/.claude/settings.json` (`Bash\|Edit\|Write\|apply_patch\|Task` → `…\|Task\|Agent`), `bash tools/check-hook-registration.sh` sale 0 sin advisory; turno `-saikit` vivo verifica en `harness-evidence.log` que el despacho `Agent` registra el rol; anotado en `docs/deploy-log.md` (sin commit de código, A10: el repo no genera ese archivo) | - | cc:完了 — **CIERRE (2026-08-15)**: acción de operador, sin código. **Verificado**: el matcher es `Agent\|Bash\|Edit\|Write\|apply_patch\|Task\|SendMessage` y `check-hook-registration.sh` sale 0 **en silencio** (sin el advisory de A9 que aparecía en todos los deploys previos). Anotado en `docs/deploy-log.md`. **NO se capturó** la evidencia viva del despacho `Agent` en `harness-evidence.log`, y la razón es estructural: ese log se borra al cerrar el turno (las tres limpiezas lo incluyen desde siempre), así que post-hoc no existe. Capturarlo a mitad de turno exige correr contra el perfil VIVO — se intentó y **consumió un aviso RN pendiente real** de este proyecto (`rn_take_pending` lo lee y lo borra a propósito). Eso se prueba en `hook_lab.sh`, no contra el hook vivo. **Lo que esa evidencia probaría —que el rol viaja en el payload de `Agent`— ya está cubierto por los casos G3**; lo único no medido en vivo es la ENTREGA del evento, que es justo lo que el matcher acaba de habilitar y el verificador confirma |
| 9.10 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **P0 — el runner bash propio no acredita verificación.** `bash tests/run.sh` (el gate FINAL de este repo) no matchea ninguna rama de `TEST_RUNNER_RE`: en hosts sin transcript legible el gate de verificación queda insatisfible — el comando CORRECTO no acredita y el Stop sigue exigiendo la batería que ya corrió | `TEST_RUNNER_RE` (constante compartida con `TEST_RUNNER_WORD_RE`, misma superficie para evento y prosa) gana dos ramas: verbo shell `(ba\|z\|da\|k)?sh[[:space:]]+[^[:space:]]*tests?/run\.sh` (cubre `bash tests/run.sh`, ruta absoluta, cadena con `&&`) e invocación directa anclada `^(\./)?tests?/run\.sh`; A3 NO se reabre: `cat tests/run.sh` y `grep run.sh tests/run.sh` siguen sin contar (el `.` antes del `sh` de `run.sh` no puede iniciar el match con la frontera izquierda del wrapper); casos rojos medidos; corpus A3 existente intacto; mutación `runner_bash_quitada` atrapada; línea base sin divergencia (ningún escenario usa el runner) | - | cc:完了 [50ab7dd] — **CIERRE (2026-08-14)**: diseño validado empíricamente contra matriz de 25 inputs ANTES de tocar el hook (los 7 casos requeridos + corpus A3 + bordes). **ROJO medido pre-fix (4/5 casos, los 2 negativos son controles desde el día 1, patrón 9.2)**: `bash_run_sh`, `bash_ruta_absoluta`, `bash_tras_and`, `run_sh_directo` daban `verified=0`. Post-fix: 6/6 verdes + **G2 entero (32 casos) en verde** (corpus A3 intacto: cat pytest.log / cat tsconfig.json / cat pytest._cache / cat pytest.-old / eco en tool_response). Mutación `runner_bash_quitada` (sed rompe el sufijo `tests?/run\.sh` de ambas ramas) atrapada por `caso_g2_runner_bash_run_sh_marca`. **Línea base: 0 divergencia** (`--check` 26/26 OK sin regrab — ningún escenario ejercita el runner). **Límites declarados del lado estricto** (sin crédito el gate pide la razón, recuperable): `bash -e tests/run.sh` (espacio entre flag y ruta) y `./otros-tests/run.sh` (frontera izquierda `-`) NO cuentan. **Lección del camino (corregida en el mismo cambio)**: la primerísima redacción de la línea del catálogo de mutaciones llevaba `` `bash tests/run.sh` `` con backticks — el catálogo vive en una asignación con comillas dobles y la sustitución de comando EJECUTÓ la suite recursivamente (diagnosticado con `bash -x`; el catálogo ya tiene una línea preexistente con `` `:` `` que es un no-op inofensivo de la misma clase); la línea se reescribió sin backticks. **Cross-review codex r1 (2026-08-15), hallazgo ALTA — decoys en la rama run.sh [fix 19b39f6]**: el wrapper WORD_RE solo sabe de fronteras de palabra, no de posición — `bash contest/run.sh` ("contest" termina en "test"), `bash tests/run.sh/typo` (`/` pasa por frontera derecha) y `grep/printf/echo bash tests/run.sh` (espacio o comilla antes del verbo pasa la frontera izquierda) acreditaban `verified=1` sin correr la suite (confirmado con ejecución propia; medidos 5/5 en rojo). Fix: las ramas run.sh salen de TEST_RUNNER_RE (vuelve a su forma pre-9.10) y viven en `TEST_RUNNER_CMD_RE` nueva, SIN wrapper, con posición de comando (`^` o separador `; & | && ||`), segmentos de path estrictos (`([^/[:space:]]*/)*tests?/run\.sh`) y terminador `([[:space:]]|$)`; aplicada en los DOS call sites (crédito del evento sobre `$command_text` solo — sin concatenar tool_name, el ancla `^` es del comando — y fallback de prosa del stop_gate). Matriz de aceptación completa en verde (7 positivos + 6 negativos), G2+G4 enteros en verde, 2 mutaciones atrapadas por SU caso (`runner_bash_quitada`, `cmdpos_no_se_aplica` — la primera redacción de ésta vaciaba la variable y un patrón vacío matchea TODO: el catch era el caso equivocado; corregida prefijando un literal imposible), línea base 26/26 sin regrab. Límites declarados: `bash -e tests/run.sh`, `bash tests/run.sh&&otro` y el runner entre comillas no cuentan; en prosa solo cuentan runners clásicos (el crédito del carril run.sh vive en el evento) |

---

## Phase 10: Velocidad de la ceremonia

Purpose: quitar la lentitud de DISEÑO de la ceremonia (línea base medida
2026-08-13: una task ≈ 4 h, 436 llamadas Bash por implementer, suite de ~18 min
re-corrida por fase): carril `fast` explícito, re-review dirigido al delta y
verificación en batch — la disciplina vive en los TEXTOS (contrato + role files),
no en código nuevo del gate. DoD de la fase (10.4): re-medir UNA task real
comparable, total ≤ 1 h sin perder ningún gate. Diseño completo en
`docs/phase-9-10-plan.md`.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 10.1 | `[Feature]` `[lane:gate]` `[tdd:required]` **Carril `fast` explícito — `-saikit:fast`.** El carril NO se infiere de regex de trivialidad (los clasificadores muertos `is_engineering_task`/`is_trivial_task`/`TRIVIAL_RE`/`SUBSTANTIVE_RE` se RETIRAN): el usuario lo pide explícito. Con `lane=fast` el Stop gate exige recibo completo + evidencia de verificación (o skip) pero NO la ceremonia de 3 subagentes; `-saikit` pelado = ceremonia completa, sin cambios; el desarme sin sentinel aplica igual a los dos carriles. OJO (r1 hallazgo 4): `-saikit:fast` YA ARMA hoy (`:` pasa la frontera derecha) — el RE del sentinel NO cambia, lo nuevo es la detección del carril | `start_harness` detecta `lane` con grep EXACTO del sufijo `:fast` con frontera derecha (sufijo desconocido arma FULL — typo cae al lado seguro, ATADO por caso propio); `write_state` persiste `lane=` (parámetro 6, TODOS los call sites); `stop_gate` envuelve la ceremonia de subagentes en `if [ "$(read_state_value lane)" != "fast" ]`; el contrato gana ~3 líneas que explican el carril (línea base diverge SOLO por ese texto, declarado); 5 casos (4 del plan + sufijo desconocido) con ROJO medido (el del campo `lane`, no el armado); 2 mutaciones (`fast_no_se_detecta`, `fast_no_exime_ceremonia`) atrapadas; cero call sites verificados antes de borrar los clasificadores; escenario nuevo en la línea base (armado fast + cierre sin ceremonia) regrabado con diff auditado; suite OK | 9.5 (frontera) — **desviación declarada**: no bloqueante, la detección usa su propio grep con fronteras propias | cc:完了 [e1f6582] — **CIERRE (2026-08-14)**: grep exacto del sufijo `:fast` (frontera derecha propia) en `start_harness`; `lane=` persistido por `write_state` (parámetro 6) y preservado por `mark_evidence`/`record_agent`/`stop_gate`; estado sin el campo (sembrado o pre-10.1) lee `lane=""` ≠ fast ⇒ ceremonia completa (default ausente = lado seguro). **ROJO medido pre-fix (5/5)**: `fast_arma_con_lane` y `pelado_arma_lane_full` por campo ausente (`[]` ≠ fast/full), `sufijo_desconocido` por ídem (`:fasst` y `:rapido`), `fast_cierra_sin_subagentes` exit 2→0, `fast_sin_recibo` por el motivo de ceremonia presente en el bloqueo. 2 mutaciones atrapadas por SU caso (`fast_no_se_detecta`→`caso_g1_fast_arma_con_lane`; `fast_no_exime_ceremonia`→`caso_g3_fast_cierra_sin_subagentes`). Clasificadores retirados tras grep de cero call sites (solo quedan comentarios que documentan la remoción). **Línea base auditada ANTES de regrabar**: 23 escenarios divergentes + escenario `26-fast-cierre-sin-ceremonia` nuevo; divergencia clasificada línea por línea: 81 líneas `lane=full` en snapshots de estado, 44 líneas sha/bytes del contrato, 4 líneas de dump del contrato (2 claude + 2 cursor), **0 exit codes / 0 decisions movidos**; regrabada y reproducible (2 corridas byte-idénticas), `--check` 26/26 OK. **Desviación declarada**: 9.5 NO ejecutada (queda cc:TODO) — el sentinel RE no cambia y la detección del carril usa su propio grep con fronteras propias, como dice el plan en 10.1 Step 2 |
| 10.2 | `[Docs]` `[lane:fast]` `[tdd:skip:texto-de-contrato]` **Re-review dirigido tras hallazgos (contrato + aviso RN).** La 2ª ronda completa (implement→verify→review de nuevo) es comportamiento del LEAD inducido por el texto, no exigencia del gate: se corrige el texto, no el código de decisión | Bloque "Revision after findings (do this the CHEAP way)" en `harness_context` tras la "Delegation rule" (fix exacto de los findings + verify dirigido + una sola batería full por task + evidencia en batch); el aviso RN (`SAIKIT REVIEW NOTICE: … those edits were not reviewed`) gana la coletilla `Re-review the new diff only; do not restart the ceremony.`; auditoría de línea base: SOLO texto, 0 veredictos movidos, antes de regrabar; sin caso nuevo de gate (no cambia ninguna condición) | - | cc:完了 [bfa6dce] — **CIERRE (2026-08-14)**: bloque "Revision after findings (do this the CHEAP way)" con el texto EXACTO del plan, tras la Delegation rule (entre ella y el bloque Fast lane de 10.1); coletilla en el aviso RN PENDIENTE (el citado por el plan — el systemMessage inmediato conserva su texto propio: la coletilla vive en el aviso que lee el turno siguiente, donde se decide la re-review). **Línea base auditada ANTES de regrabar**: 23 escenarios divergentes, divergencia 100% texto (46 líneas sha/bytes + 6 líneas de dump de contrato/aviso RN, 0 líneas `lane=`), **0 exit codes / 0 decisions movidos**; regrabada, `--check` 26/26 OK. Sin caso nuevo de gate ni mutación (no cambió ninguna condición — el gate ya solo exige que los 3 roles corran UNA vez en orden) |
| 10.3 | `[Docs]` `[lane:fast]` `[tdd:skip:texto-de-perfil]` **Role files — verifier no duplica al reviewer, y ambos en batch.** `agents/reviewer.md` no re-corre la suite (ese es el trabajo del verifier); `agents/verifier.md` corre la suite enfocada y la full a lo sumo una vez por task; `agents/implementer.md` deja la batería full al verifier; los tres agrupan evidencia en batch (una llamada por checkpoint, no una por comando) | Los 3 archivos con los cambios de texto EXACTOS del plan (Steps 1-3); **Step 4 (reinstalar perfiles al host, flujo 5.6) DIFERIDO al deploy post-merge del lead** — declarado en la fila y el PR; la validación es la medición de 10.4 | - | cc:完了 [6cc82fb] — **CIERRE (2026-08-14)**: reviewer.md "Verification" abre con el texto EXACTO del plan (no re-corre la suite; re-corre SOLO lo que diste por desconfiable, con el por qué; su trabajo es el diff) y conserva la frase de migraciones (no nombrada para remover); verifier.md bullet Tests con el texto EXACTO del plan + sección nueva "Batch your evidence"; implementer.md misma sección + "run focused checks while editing; leave the full battery to the verifier". **Step 4 DIFERIDO al deploy post-merge del lead** (flujo 5.6: los perfiles vivos del host siguen con el texto viejo hasta que el lead reinstale — declarado también en el PR). Sin gate ni línea base tocados (los role files no son superficie del hook); la validación de velocidad es la medición de 10.4 |
| 10.4 | `[Test]` `[lane:fast]` `[tdd:skip:medicion-viva]` **Medición de cierre de la fase.** Cronometrar UNA task real comparable con `-saikit` (ceremonia completa) y UNA con `-saikit:fast`, con el método del transcript (gaps por evento, como la medición 2026-08-13) | DoD de la fase: task full ≤ 1 h (antes ~4 h), task fast ≤ 15 min, cero gates perdidos (recibo + verify presentes en ambas); si no se cumple, el residuo se mide y se decide (no se itera a ciegas); resultado registrado en `docs/` y filas cerradas | 10.1 | cc:完了 — **CIERRE (2026-08-15)**: las tres mediciones hechas y escritas en `docs/task-10.4-datapoint-2026-08-15.md`. Carril `fast` (Task 10.10): 92.4 min, modelo pensando 4%, **68% del tiempo de herramientas fue espera bloqueada** sobre UNA corrida de la bateria local. Carril `full` (Task 10.8): 154.2 min, herramientas del lead 23%, y lo que domina es el **tiempo de pared de los subagentes** (52.5 min el implementer + 23.7 el reviewer = 76 min). **Los dos carriles fallan su objetivo por motivos opuestos**: el `fast` espera computo (lo arregla el CI de la 10.5, ~1m32s) y el `full` espera razonamiento de otro agente (ninguna infraestructura lo arregla). **Decision tomada**, que es lo que el DoD exigia: los objetivos se fijan medidos en **30 min para `fast`** (condicional a correr la bateria en CI; sin CI el piso es ~90) y **2 h para `full`**, con la unica palanca real de acotar el alcance de cada delegacion. Se declara explicitamente que **la ceremonia NO sobra**: en la 10.8 el implementer encontro un bug de no-determinismo que el lead no previo (el `task_hash` de la fase `session` es un cksum del input entero, asi que el token `__TRANSCRIPT__` rompia `--check`) y el reviewer refuto con evidencia una objecion equivocada del lead |
| 10.5 | `[Setup]` `[lane:fast]` `[tdd:skip:la-validacion-es-el-ci]` **CI Linux para la suite.** La suite tarda ~18 min en Git Bash/MSYS2 por el costo del fork (~28 ms c/u, documentado en AGENTS.md); en Linux el mismo fork es ~0.1 ms. Job `suite` en ubuntu-latest que corre `tests/run.sh` completo con `SAIKIT_HOOK_VIVO` apuntando a la fuente del repo | El job corre en cada push/PR y sale verde con las salvedades DECLARADAS: los tests Windows-bound se saltan SOLO con `SAIKIT_CI_LINUX=1` (lo setea el job) CON LISTADO en el output del runner y el resumen los nombra (`OK (N tests, M SKIP linux-ci declarados)`); sin la variable los tests corren normal (en Linux fallan honestamente, en Windows siempre corren); "not_observed != absent" aplica | - | cc:完了 [44191c1 + c9d7650] — **CIERRE (2026-08-15)**: job `suite` (ubuntu-latest, timeout 30 min) corriendo `tests/run.sh` con `SAIKIT_HOOK_VIVO=$GITHUB_WORKSPACE/hooks/...` (lección 3.5: sin eso mide el hook instalado, inexistente en el runner). **Validación = el propio CI, 2 iteraciones**: (1) job pelido → 13 PASS / **4 FAIL**, todos Windows-bound: `test_capture_payloads`, `test_install_hook`, `test_probe_zcode_output` (los tres tools exigen un `bash.exe` de Windows para armar el `command` del registro) y `test_hook_acl` (clasifica SIDs de Windows vía PowerShell; el `pwsh` de ubuntu existe pero no resuelve `S-1-5-18`) — **ninguno prueba el gate hook**; (2) mecanismo `SAIKIT_CI_LINUX=1` en `run.sh` → **`suite` PASS en 1m32s** (motivación entregada: ~18 min → ~1.5 min) con `SKIP (linux-ci): <test> — <razón>` por cada uno y resumen `OK (14 tests, 4 SKIP linux-ci declarados — ver arriba)`; verificado además que SIN la variable los 4 corren (fallback honesto, probado en repo sintético) y `test_runner_guards` verde con el runner modificado. **Cobertura verde-por-ausencia en Linux, declarada caso por caso**: `caso_g4_transcript_ruta_windows_y_traversal` mitad (a) (ruta Windows vía `cygpath`, guard propio `command -v cygpath`) no corre en Linux; su mitad (b) (traversal `..`) sí. Los `cygpath` de `sandbox.sh`/`golden-harness.sh`/`run.sh` son mecánica de env/normalización, no cobertura. `test_quality_kit_skip` trae su propio skip interno preexistente (sin quality-kit en el runner) y da PASS por su lógica, no por la nuestra. **Lo que el CI Linux SÍ verifica completo**: las 2 baterías de gate (comportamiento + 51 mutaciones), los 2 golden (línea base de 26 escenarios reproduce en Linux — el comportamiento es payload-driven), fixtures, sandbox, runner guards, hook_source/registration, restore_vendor, stage_override, check_syntax. **Cross-review codex r1 (2026-08-15), 2 hallazgos sobre esta fila**: [baja, hallazgo 3 — ARREGLADO en 7836aef] con UNKNOWN y SKIP simultáneos, la rama de UNKNOWN salía antes del resumen de SKIP y los omitidos no se nombraban nunca, contradiciendo la garantía declarada; fix: el resumen de skips imprime SIEMPRE que haya skips, antes de ramificar (también en el exit 3 y el FAIL), atado por el caso nuevo de `test_runner_guards` (unknown+skip sintéticos ⇒ exit 3 Y línea de resumen, medido en rojo contra el runner pre-fix). [media, hallazgo 2 — DECLARADO, no arreglado] el skip linux-ci es por ARCHIVO entero; los casos portables de los 4 archivos saltados (p. ej. los casos portables de `test_probe_zcode_output.sh`) quedan sin cobertura CI — residual declarado, la granularidad por caso queda como mejora futura |
| 10.6 | `[Feature]` `[lane:gate]` `[tdd:required]` **Reglas permanentes en `SessionStart` — el canal que el contrato NO cubre.** Medido en el transcript `e86ddb2c` (2026-08-14): una task se fue **2.6 h bloqueado esperando** (51 % del tiempo de herramientas, 14 esperas de exactamente 10 min), de las cuales ~1.75 h eran evitables — 70 min por correr la batería completa 3 veces y 35 min esperando una re-review que ya había terminado. Y **la regla ya existía**: `harness_context()` la dice desde la 10.2 (*"one full battery run per task, at the end"*). Se violó igual, y la causa no es que falte texto: `harness_context()` se llama dentro de `start_harness`, o sea **sólo en el camino armado**, y esa sesión nunca armó (se trabajó con `/goal`, sin `-saikit`). Agregar más al contrato no arregla nada. Falta un canal para invariantes que valga **arme o no**, y `SessionStart` es ese canal: no gatea, no arma, no cuenta ciclos. **No toca el invariante del sentinel** | Un `SessionStart` sin sentinel emite las reglas y sale 0, **sin crear estado** y **sin desarmar** un turno en curso; el gate del sentinel y `harness_context()` quedan intactos; regresiones verdes: `caso_g1_no_arma_sin_sentinel` (un `prompt` sin sentinel sigue mudo — es la que atrapa una rama mal acotada, por eso no se agrega un caso propio para eso) y `caso_g6_armado_por_target` (cursor sigue armando en `session` con sentinel); `mut_session_sin_reglas` y `mut_session_pisa_gate` atrapadas, cada una por SU caso; **alcance acotado a `TARGET=claude` a propósito** — es el único host donde se MIDIÓ que `SessionStart` acepta `hookSpecificOutput.additionalContext` y que el texto llega al modelo (repo descartable + `claude -p` headless, ver `docs/task-10.6-plan.md`); zcode, Codex y Grok quedan `unknown` (≠ "no lo tienen") y entran por su propia fila con su medición delante, porque registrar a ciegas es el error que la 6.2 evitó por un pelo; **el registro es acción de operador, no del instalador** — `install-hook.sh` sólo instala el ARCHIVO y su única rama de registro es `--host zcode`, así que la entrada `SessionStart` de `~/.claude/settings.json` la agrega el operador (mismo patrón que la 9.9); `check-hook-registration.sh` la afirma **separada** de las otras tres y como **advisory, no como fase requerida**: el gate funciona sin ella, y meterla en `ESPERADAS` haría que todo install existente se reporte roto; sólo entra al texto una regla que tenga un número medido detrás; la línea base da OK con **cero divergencia**, y se declara POR QUÉ: **ningún escenario de la línea base ejercita `SessionStart`**, así que la línea base no cubre el camino nuevo — los casos de `gate_cases.sh` y sus dos mutaciones son su única red, y el escenario de línea base queda como hueco declarado para su propia fila | - | cc:完了 [3f4f076] — **CERRADA (2026-08-15): desplegada y verificada en produccion.** Medición previa: `SessionStart` en Claude acepta `hookSpecificOutput.additionalContext` y el texto llega al modelo **textual** (el modelo lo citó y lo identificó como *"contexto adicional de un hook de SessionStart"*), medido con `claude -p` headless en repo descartable con `.claude/settings.json` de proyecto — el perfil NO se tocó. **Implementado**: `standing_rules()` + `emit_standing_rules()` y una rama en `start_harness` acotada a `PHASE=session` **y** `TARGET=claude`, colocada DESPUÉS del desarme y ANTES del `emit_allow`. **ROJO medido** en `caso_g1_session_inyecta_reglas` (3 asertos) antes del fix. **Honestidad sobre el otro caso**: `caso_g1_session_no_desarma` nació VERDE — el acotamiento a `PHASE=prompt` ya existía, así que es guarda de regresión, no comportamiento nuevo, y se declara como tal. **Mutaciones**: `mut_session_sin_reglas` → `caso_g1_session_inyecta_reglas`; `mut_session_pisa_gate` → `caso_g1_no_arma_sin_sentinel` (esa es la prueba de que el caso viejo ata el acotamiento, y por eso no se agregó uno nuevo). Batería completa de mutaciones OK. **Corrección al plan hecha al implementar**: el plan decía que el instalador ganaba la 4.ª fase — **es falso**, `install-hook.sh` sólo instala el ARCHIVO y su única rama de registro es `--host zcode`; el registro en Claude es acción de operador. **Verificador**: afirma la fase por separado y como **advisory**, nunca como fase faltante (meterla en `ESPERADAS` habría reportado TODO install existente como "el gate NO corre" — la alarma falsa que prohíbe la 0.4); 2 casos nuevos y `SessionStart` agregado a los fixtures de registro **completo** (sin eso, "completo ⇒ SILENCIO" dejaba de significar completo — rompió 6 casos y se arregló ahí, no bajando el advisory). **Gates**: `tests/run.sh` OK (18 tests) con `SAIKIT_HOOK_VIVO` a la fuente, corrida **UNA sola vez al final**; `pre-commit run --all-files` Passed. **VERIFICADO EN VIVO (2026-08-14)**: staging por override de proyecto (`tools/stage-override.sh`, exit 0 — midió que el registro prefiere el override) en repo descartable, y un turno headless `claude -p` **sin `-saikit`** devolvió las 3 reglas **textuales**. **No creó estado**: inyecta sin armar, que es exactamente el diseño. El hook GLOBAL quedó byte a byte intacto (cksum idéntico a `origin/master`) — el staging no lo tocó. **Registro hecho**: la entrada `SessionStart` (sin matcher, `PHASE=session`) quedó en `~/.claude/settings.json` con backup (`settings.json.bak-10.6-20260814-215110`), las 7 entradas ajenas intactas, JSON validado antes de escribir, y el verificador pasó a **silencio** — o sea que afirma la fase en los dos sentidos. **LÍMITE hallado por CodeRabbit (PR #19, Major) y ATADO por caso**: el payload de `SessionStart` no trae `prompt` y sí `summary`, y el fallback al payload crudo hace que un `summary` que cite un `-saikit` viejo (típico de sesión reanudada) **arme** — sale el contrato y las reglas NO. **No se arregla acá a propósito**: ese fallback es C9 / Task 9.4, cuya DoD decide **explícitamente conservarlo** dejando `caso_g6_armado_por_target` intacto; se probó acotarlo a `claude`+`session` y **puso rojo ese caso**, o sea que pisaría el diseño de una tarea ajena. Queda `caso_g1_session_con_sentinel_en_summary_arma_y_no_da_reglas` fijando lo que hoy pasa, y se pondrá rojo si 9.4 lo cambia. **Pérdida de cobertura declarada**: en sesiones reanudadas con `-saikit` en el summary, esta tarea no entrega nada. **DEPLOY HECHO (2026-08-15)**: `install-hook.sh` REPARADO con backup, vivo == master (`1303783510 88474`), verificador en silencio; y un turno `claude -p` en repo nuevo **sin `-saikit` y sin staging** devolvio las 3 reglas textuales — o sea que esto ya corre en todos los repos de la maquina. **Queda como hueco declarado** el escenario de `SessionStart` en la linea base |
| 10.7 | `[Docs]` `[lane:fast]` `[tdd:skip:texto-de-contrato]` **Una ceremonia por tarea, no por edición.** Feedback del operador (Gon, 2026-08-15, post-10.2): dentro de una tarea FULL, el lead re-delegaba la ceremonia completa por CADA edición propia — renombra un doc → cadena completa; lo mueve → cadena completa otra vez. El bloque CHEAP-way de la 10.2 cubre la revisión tras HALLAZGOS, pero no la re-delegación por edición trivial del propio lead. Falta la regla de granularidad | Bloque "One ceremony per task, not per edit" en `harness_context`, inmediatamente después del bloque CHEAP-way: hacer TODAS las ediciones primero y correr la secuencia implement→verify→review UNA vez sobre el diff final estable; las ediciones triviales in-task (rename, move, typo) NO re-disparan la delegación — el lead las verifica con un chequeo enfocado y lo declara en el recibo. SIN cambio de código del gate (la secuencia ya se exige UNA vez por turno: esto corrige la conducta del lead, no la condición); sin caso nuevo de gate; auditoría de línea base: SOLO texto, 0 exit codes / 0 decisions, diff declarado por adelantado → regrabada y reproducible | - | cc:完了 [7724656] — **CIERRE (2026-08-15)**: bloque agregado tras el CHEAP-way con el texto propuesto (sin ajustes — ya era la voz del contrato). **Auditoría ANTES de regrabar**: 23 escenarios divergentes (los que inyectan el contrato), divergencia 100% texto — 46 líneas sha/bytes + 4 líneas de dump de contrato (2 claude + 2 cursor), 0 líneas de estado, **0 exit codes / 0 decisions movidos**; regrabada y reproducible (2 corridas byte-idénticas), `--check` 26/26 OK. Sin caso nuevo de gate ni mutación (no cambió ninguna condición). **Desviación de numeración declarada**: la task llegó numerada 10.6, pero master ya había mergeado el PR #19 con esa fila (standing-rules SessionStart) después del punto de referencia del brief (82d6132); esta task toma el siguiente número libre, 10.7, con el commit alineado |
| 10.8 | `[Test]` `[lane:gate]` `[tdd:required]` **La línea base dorada no cubre `SessionStart`.** La Task 10.6 agregó una salida nueva en esa fase y `test_golden_baseline` dio OK con **cero divergencia** — no porque el cambio no toque nada, sino porque **ningún escenario de la línea base ejercita `SessionStart`**. Hoy esa rama está atada sólo por sus casos de `gate_cases.sh` y dos mutaciones | Un escenario nuevo cubre la fase `session`: una sesión sin sentinel emite las reglas permanentes y **no** crea estado; `--check` reproducible; el diff de regrabado se audita antes y queda declarado (cero veredictos movidos en los escenarios existentes); si la sesión de Grok está regrabando la línea base, se serializa — **la regraba UNA sola sesión** | 10.6 | cc:完了 — **CIERRE (2026-08-15)**: escenario `34-sesion-reglas-permanentes`, dos pasos, fase `session` + `TARGET=claude`. **Diagnostico corregido durante la task**: no era que "ningun escenario ejercita SessionStart" — los escenarios 03 y 04 SI usan la fase `session`, pero con target `cursor`, y las reglas permanentes solo se emiten con target `claude`. Esa combinacion era la que faltaba. **Prueba negativa medida** (lo que hace que el escenario sea red y no decoracion): con una copia del hook donde se desactiva la emision, `golden-harness --check --hook <copia-rota>` da **exit 1** y senala EXACTAMENTE `34-sesion-reglas-permanentes` y ningun otro; control con el hook sano, exit 0. **Auditoria del regrabado**: los 33 escenarios previos quedan byte-identicos, verificado DOS veces (contra el hook propio y, tras mergear el PR #27 de la otra sesion, contra el hook de GLM — su cambio de 131 lineas para grok movio cero veredictos). Hook vivo == hook del repo (`3441408864 104755`) antes y despues de grabar. Bateria completa `tests/run.sh: OK (19 tests)`, 0 FAIL 0 SKIP. **Hallazgo colateral**: la linea base venia describiendo un hook de DOS generaciones atras (92052 -> 98088 -> 104755 bytes) y `--check` seguia verde — eso es el diseno (decision 2: la identidad queda fuera de la comparacion) y a la vez la prueba del hueco que esta fila cierra. **Desvio declarado, no corregido**: los dos payloads ordenan las claves JSON distinto que los hermanos (`cwd` antes de `hook_event_name`); es cosmetico y cambiarlo mueve el `task_hash`, o sea obliga a regrabar y a re-correr la bateria — no se justifica. `transcript_path` se omite a proposito y esta declarado en el README: en fase `session` el `task_hash` es un cksum del input ENTERO, asi que el token `__TRANSCRIPT__` (ruta de sandbox distinta por corrida) rompia la reproducibilidad de `--check`; ademas es la convencion ya asentada (03, 04 y `hook_lab.sh:232` tampoco lo llevan) y el campo solo se lee en `stop_gate`, inerte para esta rama |
| 10.9 | `[Test]` `[lane:gate]` `[tdd:required]` **Reglas permanentes en los otros tres hosts.** La 10.6 acotó la emisión a `TARGET=claude` a propósito: es el único host donde se MIDIÓ que `SessionStart` acepta `additionalContext` y que el texto llega al modelo. zcode, Codex y Grok quedan `unknown`, **no** "no lo tienen" | **Medición primero, una por host**, con el operador o headless en repo descartable y el perfil intacto (cksum antes/después): si el host tiene fase de arranque, qué forma de salida acepta ahí y si el texto llega al modelo. Recién con eso se habilita la emisión, host por host. Registrar a ciegas es el error que la 6.2 evitó por un pelo — ahí Codex resultó **descartar el stdout cuando el exit no es 0**, algo que ninguna otra fase sugería | 10.6 | cc:完了 [PR #37] — **DoD cumplida, y el resultado NO fue uniforme**, que es justo por lo que la fila mide en vez de extrapolar. **Q1 medido en los TRES** (el `.ok` del probe, no lectura de docs): la fase de arranque existe y dispara en zcode, Codex y Grok; el literal del evento sigue la tabla de 7.1 (snake `session_start` en Grok, CamelCase en los otros dos). **Veredictos: zcode ACEPTADA, Codex ACEPTADA, Grok IGNORADA.** Los dos que aceptan lo hacen con los DOS oráculos coincidiendo en el mismo archivo de transcript — en zcode el texto entra como `role="system"` con el prefijo `SessionStart hook additional context:` y `#1` (la MISMA forma que 5.2 midió para UPS), en Codex como `role="developer"` (la que midió 6.2) — y en los dos el modelo devolvió el token literal que el texto le pedía. Una sola señal habría dejado el veredicto en `unknown`. **Grok se descartó con el oráculo AGOTADO**: las 3 formas (envoltorio camel, snake, top-level) emitieron y ninguna llegó, ni a la respuesta ni a los archivos de sesión — hallazgo 5 del cross-review de 7.2 aplicado. **Cambios que salieron de eso**: el hook emite en `claude`+`codex` (zcode NO necesitó rama: resuelve a `TARGET=claude` por el fallback de 5.4) y `install-hook.sh --host zcode` pasa de 3 a 4 fases. **Polizón cerrado**: el guard de la 10.6 decía «sólo Claude» y el código ya cubría zcode; el caso que lo ata nació exigiendo lo contrario y se invirtió el mismo día con el veredicto en la mano, para que el diff fuera la declaración de que la medición existió. **Hallazgo propio, de Codex y no de esta fila**: registrar un hook ahí NO es editar `hooks.json` — cada handler exige `[hooks.state.'<archivo>:<evento>:<grupo>:<hook>']` con `trusted_hash` en `config.toml`, y sin él se saltea EN SILENCIO; se llegó descartando cwd, matcher, comillas y posición de a un turno por hipótesis; es la familia del hallazgo de 6.1, medido allá para un hooks.json de proyecto y confirmado acá para el GLOBAL. De ahí sale una regla operativa: los índices son parte de la clave, así que NO se reordena el array de una fase. **Línea base**: el `suite` daba cero divergencia y leerlo como "no rompe nada" habría sido el error — el escenario 34 cubre `session` sólo con target claude. Se agregó el 44 (codex emite / grok mudo, mismo payload, sólo cambia el target); el paso de codex sale marcado `identico a 34/paso 01`, confirmación independiente de que recibe la forma de Claude, que es la única que su esquema estricto acepta. Regrabada y **reproducible: `--check` OK sobre 44 escenarios**. **TDD**: rojo medido en cada tramo (5 casos/12 asserts del probe; 3 asserts del caso de codex; 3 casos del instalador). **5 mutaciones dirigidas, 5 atrapadas cada una por SU caso**; `mut_session_pisa_gate` ACTUALIZADA porque apuntaba al literal que esta fila movió y habría quedado inerte — mismo tropiezo que 6.4 y 9.3. **Integridad**: 7 de 8 archivos de perfil vuelven byte a byte a su cksum previo; el 8.º (`~/.zcode/cli/config.json`) cambió **por acción del operador** —su `/login` escribe ahí la API key— y se verificó por ESTRUCTURA, no por cksum, sin leer el contenido. Un paso del diagnóstico de Codex reordenó el array y se revirtió en el acto, verificado con turno real además del cksum. **Declarado y NO perseguido**: los veredictos de la 7.2 son de Grok **1.0.3** y la versión instalada es **1.0.4** — revalidarla es fila propia; el registro de `SessionStart` en Codex sigue siendo acción de operador (exige que el host acuñe el `trusted_hash`) y el instalador no escribe ahí; y la guarda de metacaracteres dentro de `codex_arranque_registrar` es inalcanzable por su cuenta, se sostiene por lectura (precedente 2.2) |
| 10.10 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **El lector acotado de `tool_name` se quedó sin caso.** La 9.2 sacó `tool_name` del crédito de verificación (cerraba un agujero real: una tool MCP llamada como un runner acreditaba sin correr nada). Efecto lateral: `mut_tool_name_desacotado` perdió su detector y **se retiró**. Se repuso la mitad que importaba con `credito_por_tool_name`, pero la otra —que el LECTOR esté acotado— sólo es observable en la detección de edición del aviso RN (`^(edit\|write\|...)$`) | Un caso ata que un `tool_name` leído del payload ENTERO (un eco en `tool_response`) no dispara ni suprime el aviso RN; la mutación que devuelve el lector greedy se pone roja con ese caso; no se toca el crédito de verificación, que ya no mira `tool_name` | - | cc:完了 — **CIERRE (2026-08-15, carril `fast`)**: caso `caso_g5_tool_name_eco_no_marca_edicion` + fixture `lab_payload_read_con_eco_tool_name` (un `Read` legitimo con `file_path`, y un `tool_name: Edit` ecoado adentro de `tool_response`). Mutacion `tool_name_desacotado` **repuesta** apuntando a ese caso: antes se habia retirado porque al sacar `tool_name` del credito (9.2) se quedo sin detector. **VERDE con el hook sano y ROJO con la mutacion**, las dos direcciones medidas. No se toco el credito de verificacion. **Dos correcciones que hizo falta medir, no suponer**: (1) el archivo de orden del aviso RN se crea en CADA evento con las tres claves y `last_code_edit` queda VACIA salvo edicion real — el primer aserto miraba la presencia de la clave y daba rojo contra el hook sano; ahora mira el VALOR. (2) el eco tiene que ser una clave ANIDADA REAL, no un JSON escapado adentro de un string: escapado, el lector greedy no lo encuentra y la mutacion no se atrapaba — la primera version quedaba verde en las dos direcciones, que es el peor caso |
| 10.11 | `[Feature]` `[lane:gate]` `[tdd:required]` **Candado duro para las reglas de velocidad — DECISIÓN antes que código.** Las reglas de la 10.6 son TEXTO: llegan a toda sesión pero un modelo puede ignorarlas, y eso ya pasó (la regla "una corrida de la batería por tarea" vivía en el contrato desde la 10.2 y se violó igual). Un candado real exigiría que el kit registre `PreToolUse`, fase que **hoy no registra** en ningún host | **Primero se decide, con número:** medir en 2 o 3 tasks reales cuántas veces se viola cada regla ahora que llegan por `SessionStart`. Si la violación persiste, se diseña el candado (fase nueva ⇒ instalador, verificador y los 4 hosts, del tamaño de 6.5/7.5); si no persiste, **se cierra la fila declarando que el texto alcanzó** y no se construye la máquina. No se implementa un candado sin el dato que lo justifique | 10.6 | cc:完了 — **CIERRE (2026-08-15)**: **NO se construye el candado.** Cumplimiento medido sobre el transcript en las dos tareas que recibieron las reglas por `SessionStart`: regla 1 (una bateria por tarea) cumplida en ambas; regla 2 (no quedarse bloqueado) **violada en la 10.10** (60.1 min, 6 sondeos) y **cumplida en la 10.8** (0 min); regla 3 nunca ejercitada => `unknown`, no "se cumple". Correccion de metodo declarada: el primer conteo dio 3 corridas de `tests/run.sh` en la 10.8, pero dos eran `grep` sobre el archivo — la cifra real es UNA. Razones de no construir: (1) la unica regla violada dejo de violarse sin candado; (2) el candado exige registrar `PreToolUse`, fase que hoy no existe en ningun host — instalador, verificador y los 4 hosts, tamano de una 6.5/7.5; (3) lo que hacia tentador violar la regla 2 (la bateria local de ~10 min) ya tiene arreglo mas barato en el CI de la 10.5 (~1m32s). **Limitacion declarada**: n=2 y las dos tareas las corrio el MISMO agente en la MISMA sesion, que ademas escribio en el medio el datapoint que cuantificaba esa perdida — la mejora **no se puede atribuir limpiamente al texto**. **Disparador de reapertura**: si la regla 2 se viola de nuevo en cualquier tarea armada posterior, la fila se reabre y se disena el candado |
| 10.12 | `[Feature]` `[lane:gate]` `[tdd:required]` **El contrato inyectado no muestra la FORMA del recibo; el gate la exige igual.** El texto de `UserPromptSubmit` enumera las seis compuertas como prosa numerada ("1. Understand - ..."), lo que invita a escribirlas decoradas. El lector del Stop gate, en cambio, exige que cada linea EMPIECE con la etiqueta y dos puntos. Medido en vivo (2026-08-15, turno de la 10.10): un recibo con las seis compuertas presentes y correctas pero decoradas (`Understand -- ...`) fue rechazado entero, y costo un ciclo completo de revision de los dos del presupuesto | El contrato inyectado incluye la forma exacta y minima del recibo (las seis lineas `Etiqueta: ...`) donde hoy solo describe las etapas, con una linea que diga que la etiqueta va al inicio de linea sin adorno; un caso ata que el texto inyectado contiene esa forma; no se toca el lector del gate, que ya es correcto -- el defecto es que el contrato no dice lo que el gate mide | 10.1 | cc:完了 — **CIERRE (2026-08-16)**: el contrato inyectado ahora muestra la forma del recibo. **El diagnostico original estaba MAL y lo corrigio la revision cruzada (codex)**: el problema no era la decoracion. `has_receipt_label` acepta vineta y negrita desde la Task 8.3 (`- **Understand**: ...` cuenta); lo que rompio el recibo medido en vivo fue reemplazar los DOS PUNTOS por un guion. Y el diagnostico correcto hace la fila mas valiosa: la lista numerada del propio contrato dice "1. Understand - ...", o sea el contrato venia **modelando la forma que falla**. El texto nuevo dice que lo que importa es el colon, que vineta y negrita estan bien, y advierte sobre ese guion. Se corrigio ademas el mensaje de rechazo del gate, que describia el contrato de otra forma (hallazgo de coderabbit, Major). Atado por `caso_g1_contrato_muestra_forma_recibo`. NO se toco el lector |
| 10.13 | `[Bug]` `[lane:gate]` `[tdd:required]` **MEDIR PRIMERO: el credito de verificacion no se acredito en un turno donde la bateria SI corrio.** Observado en vivo (2026-08-15, turno armado de la 10.10, `task_hash=1075619084`): el `harness-evidence.log` del turno entero quedo con trece lineas `implemented:` y NINGUNA `verified:`, con `verified=0` en el estado, pese a que `tests/run.sh` corrio y dio OK (19 tests). Recien acredito cuando se corrio `bash tests/run.sh` como llamada en primer plano. **Hipotesis, NO medida: los jobs lanzados en background no llegan al PostToolUse con el texto del comando.** Si se confirma, el gate castiga exactamente la conducta que la regla 2 de la 10.6 ordena ("no te quedes bloqueado esperando un job") | **Primero se aisla la causa en `hook_lab.sh`**, no contra el hook vivo: un payload de PostToolUse con la forma que deja un job en background vs uno en primer plano, y se mira cual acredita. Recien con la causa medida se decide el arreglo (puede ser el lector del comando, puede ser que el host no mande el evento -- y si es lo segundo, el resultado es `unknown` declarado, no un parche a ciegas). Un caso ata la forma que hoy NO acredita | 9.2 | cc:完了 — **CIERRE (2026-08-16)**: **hipotesis REFUTADA, no habia defecto.** Dos evidencias independientes: (1) en `hook_lab`, un `PostToolUse` de un runner lanzado en background (con `tool_response` en la forma real, una cadena "Command running in background with ID: ...") acredita `verified=1` EXACTAMENTE igual que uno en primer plano; (2) en todo el transcript de la sesion hay solo DOS lanzamientos reales de `tests/run.sh` y los dos son POSTERIORES al cierre de la ventana armada donde crei ver el defecto — adentro del turno armado no corrio ningun runner, asi que el `verified=0` era el gate funcionando bien. El credito depende SOLO del comando lanzado, nunca de la forma del `tool_response`. Queda `caso_g2_runner_en_background_marca_verificado` fijando la conducta para que no se rompa en silencio. **Lo que si aparecio midiendo** es un defecto distinto y real, que quedo en la fila 10.15: el credito se otorga al DESPACHAR el comando, no al terminarlo |
| 10.14 | `[Bug]` `[lane:gate]` `[tdd:required]` **La regla 2 del kit desarma el gate del kit.** MEDIDO en vivo (2026-08-15, turno armado de la 10.8): una notificacion de tarea en background llega al harness como `UserPromptSubmit` (viene con su contexto de hook adjunto, se observo en el turno), no trae el sentinel, y `start_harness` la trata como turno nuevo sin armar => ejecuta el desarme A4 (`rm -f` del estado, del log y de la orden RN, mas `podar_dir_sesion`). Resultado: el turno queda SIN gate a mitad de camino y el recibo deja de exigirse, en silencio. El desarme A4 es correcto para un prompt humano; el defecto es que una notificacion del sistema no es un prompt humano. Agrava que la regla permanente 2 de la 10.6 ("no te quedes bloqueado esperando un job, lanzalo y segui") EMPUJA a generar justo esos eventos: cumplir la regla apaga el candado | **Medir primero cual es la senal distinguible**: capturar el payload real de una notificacion de background y compararlo contra el de un prompt humano (candidatos a mirar: ausencia del campo `prompt`, un `source`/tipo propio). Si hay una senal fiable, el desarme A4 se acota a prompts humanos y un caso ata que una notificacion NO desarma; si NO la hay, se declara `unknown` y se documenta la limitacion en vez de parchear a ciegas. **No se toca el desarme A4 sin esa medicion**: aflojarlo de mas revive A4, que es el defecto que ese borrado cierra | 10.6 | cc:完了 — **CIERRE (2026-08-16)**: la guarda contra notificaciones va ANTES del gate del sentinel y usa una marca ESTRICTA. Recorrido completo, porque las tres versiones fueron rechazadas por una razon distinta y cada una enseno algo: **(v1)** chequeo solo en la rama del desarme — el reviewer mostro que una notificacion cuyo texto contiene `-saikit` (lo normal aca: incluye el resumen del subagente) tomaba la rama de ARMADO, donde `write_state` resetea cycle/implemented/verified a cero y el log se sobrescribe, borrando en silencio evidencia ya acreditada. **(v2)** guarda adelantada con marca laxa — greptile (P1) y coderabbit (Major) encontraron INDEPENDIENTEMENTE el mismo agujero: un prompt HUMANO que pide `-saikit` y ademas MENCIONA la marca (hablar de este defecto ya la menciona) salia por la guarda y se quedaba SIN gate. Perder el candado justo cuando se pidio es peor que el defecto que la guarda cierra. **(v3, la que quedo)** marca ESTRICTA anclada al inicio del texto (medido: las 5 notificaciones reales EMPIEZAN con la marca; una mencion humana la lleva en medio), y la laxa se conserva SOLO para no desarmar, jamas para saltear el armado — asimetria deliberada. **Medicion base**: `<task-notification>` aparece en 5/5 notificaciones y 0/41 turnos humanos; el cartel `[SYSTEM NOTIFICATION...]` NO sirve (0/5, lo agrega el harness al mostrar el evento). **Costo residual DECLARADO**: un mensaje humano sin texto (solo adjunto) ya no desarma; se acepta citando el precedente medido de la Task 9.4/C9. Casos: `caso_g1_notificacion_tarea_no_desarma`, `caso_g1_notificacion_con_sentinel_no_rearma`, `caso_g1_prompt_vacio_no_desarma`, `caso_g1_mencion_humana_de_la_marca_sigue_armando`; mutaciones `mut_notificacion_no_se_reconoce`, `mut_marca_notificacion_laxa`, `mut_desarma_con_prompt_vacio`, mas la REPARACION de la preexistente `mut_desarmar_quita_borrado`. **El CI atrapo una mutacion mia INERTE** (el `sed` de `mut_desarma_con_prompt_vacio` dejo de anclar al cambiar la condicion): re-anclada y verificada en las dos direcciones. Verificado: PR #30 con los 5 checks en verde, bateria completa incluida |
| 10.15 | `[Bug]` `[lane:gate]` `[tdd:required]` **El credito de verificacion se otorga al DESPACHAR el comando, no al terminarlo.** Hallazgo lateral de la medicion de la 10.13 (2026-08-15), medido: se lanzo `bash tests/run.sh` y el `harness-evidence.log` anoto `verified:` **mientras el job seguia corriendo**; esa misma bateria termino en `tests/run.sh: FAIL`. O sea el gate dio por verificado un turno cuya verificacion fallo. La causa es estructural, no un descuido: el guard de senales de falla (`FAILURE_SIGNAL_RE_*`) mira el `tool_response`, y en el evento de despacho de un job en background ese campo solo dice "Command running in background with ID: ..." — no hay falla que ver TODAVIA. Distinto de la hipotesis de la 10.13, que quedo refutada: el credito SI se otorga a los jobs en background (medido en `hook_lab`, las dos formas dan `verified=1`) | **Decidir primero que se puede observar**, porque el host puede no mandar un segundo evento al terminar el job: si NO lo manda, el credito por comando en background es estructuralmente ciego al resultado y lo honesto es DECLARARLO (el recibo ya exige evidencia en prosa, que es donde el resultado real aparece) en vez de fingir un chequeo que no existe. Si SI lo manda, el credito se mueve a ese evento. Medir en `hook_lab` con las dos formas antes de tocar nada; un caso ata la conducta que se elija | 9.2 | cc:完了 — **CIERRE (2026-08-16)**: un DESPACHO en background ya no acredita verificacion. **Es A11 volviendo por otra puerta**: la Task 3.8 cerro A11 grepeando senales de fracaso en la salida del runner, y eso funciona cuando la salida EXISTE; en el evento de despacho de un job en background el `tool_response` solo dice "Command running in background with ID: ..." y el guard no tiene que mirar. **Medicion que el DoD exigia**: NO existe un segundo evento donde mirar el resultado — cuando el job termina, el aviso llega por la via del PROMPT como notificacion de tarea, no como `PostToolUse` (medido al implementar la 10.14), y desde la 10.14 el hook ignora esas notificaciones a proposito. **Decision, siguiendo el precedente del spec** (limites declarados en vez de chequeos fingidos, fila A11): si no hay resultado observable no se acredita; denegar falla del lado seguro, otorgar en falso ES A11. El resultado real sigue viajando por la prosa del recibo, que el gate ya exige. **Limite declarado**: el grep va sobre el payload entero, asi que un comando que mencione literalmente esas cadenas tampoco acredita (falso negativo, lado seguro). **Inversion deliberada**: el caso que dejo la 10.13 fijaba la conducta contraria, y la revision cruzada (codex, PR #30) PREDIJO esta friccion — "ese caso convierte en regresion esperada un defecto conocido, dificultando implementar la correccion de 10.15". La conducta que la 10.13 si probo (que el credito no depende de la FORMA del `tool_response`) la sigue cubriendo `caso_g2_runner_marca_verificado`. Atado por `caso_g2_runner_en_background_no_acredita` con `mut_despacho_bg_acredita`; verificado en las dos direcciones, control de primer plano verde en ambas. La linea base NO se movio (`--check` exit 0): ningun escenario ejercita un despacho en background |
| 10.16 | `[Feature]` `[lane:gate]` `[tdd:required]` **La regla permanente dice CUANTAS veces correr la bateria, pero no DONDE.** El texto de la 10.6 dice "corre la bateria completa UNA vez por tarea, al final". Se cumplio al pie de la letra en las tres tareas siguientes (10.10, 10.8 y el paquete A) y las tres la corrieron **en local**, a ~10 min, cuando el CI de la 10.5 corre la misma bateria en **1m32s**. El diagnostico estaba ESCRITO en `docs/task-10.4-datapoint-2026-08-15.md` desde la primera y aun asi se repitio dos veces mas: el agente cumplia la regla literalmente y se quedaba tranquilo. Causa medida del costo: crear un proceso cuesta **56 ms** en esta maquina Windows y una invocacion del hook **658 ms**; un `--check` del golden lanza el hook 129 veces y la bateria de mutaciones son 78 mutaciones por su grupo de casos. En Linux el mismo trabajo cuesta ~1/6 | La regla permanente nombra el LUGAR ademas de la frecuencia ("una vez por tarea, **en el CI**; en local solo el archivo que estas tocando"), y menciona el flujo que lo hace posible: rama + push + leer el CI, en vez de commit directo a master. Un caso ata que el texto inyectado contiene esa mencion. **Es el contraejemplo exacto de la 10.11**: alli se decidio NO construir un candado porque el texto alcanzaba; aca el texto FALLO tres veces seguidas, y la correccion sigue siendo texto — porque lo que fallo fue que estaba incompleto, no que se ignorara | 10.6 | cc:完了 — **CIERRE (2026-08-16)**: la regla permanente ahora nombra el LUGAR ademas de la frecuencia, y lo hace CONDICIONADO ("si el repo tiene CI") porque el kit corre en repos que pueden no tenerlo. Dos rondas de revision la endurecieron, las dos con hallazgos que el texto original habilitaba: **(1) greptile P1** — decir "empuja la rama y lee el CI" manda a esperar un resultado que NUNCA LLEGA, porque muchas configuraciones corren CI en `pull_request` y no en un push de rama suelta; medido en este mismo repo el 2026-08-16 (se empujo `fix/paquete-A-gate-v2` y no se creo ninguna corrida hasta abrir el PR). O sea la regla escrita para evitar una perdida de tiempo tenia adentro otra de la misma familia. **(2) greptile P1, segunda ronda** — si el CI del repo NO corre la bateria completa, seguir la regla al pie de la letra la dejaria sin correr en NINGUN lado; se agrego la invariante explicita: la bateria completa corre una vez en ALGUN lado, consultar el CI no es el objetivo. **Coderabbit pidio exigir Linux y se DECLINO con motivo** (repetido en dos rondas, misma respuesta): exigirlo dejaria la regla inaplicable en repos con CI de Windows o macOS, donde sacar la bateria de la maquina propia sigue conviniendo; se nombra de donde sale el ~6x sin convertirlo en requisito. **Medicion de base**: crear un proceso cuesta 56 ms en la maquina afectada y una invocacion del hook 658 ms; la misma bateria son ~10 min en local y 2m58s en el CI de Linux. **Contraste deliberado con la 10.11**, que decidio NO construir un candado porque el texto alcanzaba: aca el texto FALLO tres tareas seguidas y la correccion sigue siendo texto, porque lo que fallo fue que estaba INCOMPLETO, no que se ignorara. Atado por `caso_g1_reglas_nombran_donde_correr_la_bateria` con `mut_reglas_no_dicen_donde`; verificado en tres direcciones (sano verde, y rojo al podar tanto la mencion al PR como la invariante) |
| 10.17 | `[Test]` `[lane:gate]` `[tdd:required]` **El arnes dorado no cubre la rama del desarme A4.** MEDIDO durante el paquete A (verificacion independiente, 2026-08-15): con una copia del hook donde el desarme nunca borra el estado, `golden-harness --check` sigue dando **exit 0** — ningun escenario ejercita esa rama. Causa estructural: ningun escenario manda un SEGUNDO `UserPromptSubmit` despues de armar, que es la unica forma de llegar al desarme. **Matiz que NO se debe perder**: la rama SI esta cubierta por las otras dos capas (`test_gate_mutations.sh` + `test_gate_behavior.sh` la atrapan, comprobado de frente), asi que no es una ausencia total — es una ausencia en la capa que graba COMPORTAMIENTO, que es la que detecta cambios no intencionales al reemplazar o refactorizar el hook | Un escenario dorado con dos pasos de fase `prompt`: el primero arma con sentinel, el segundo llega sin el y debe desarmar. Se coordina con quien tenga la regrabacion en curso (regla: la regraba UNA sola sesion) y el diff se audita declarando cero veredictos movidos en los escenarios previos | 10.14 | cc:完了 — **CIERRE (2026-08-16)**: escenario `43-desarme-a4`, dos pasos de fase `prompt` en la misma sesion: el primero arma con sentinel, el segundo llega sin el y desarma. Era la unica forma de alcanzar esa rama, y por eso ningun escenario la tocaba. **Prueba negativa medida, que es lo que prueba que el hueco se cerro**: con una copia del hook donde el desarme nunca borra el estado, `golden-harness --check` da ahora **exit 1** y senala EXACTAMENTE `43-desarme-a4` — la MISMA rotura daba exit 0 antes de esta fila (medido en la verificacion del paquete A). Control con el hook sano: exit 0. **Auditoria del regrabado**: los 42 escenarios previos quedan identicos salvo la linea vacia separadora antes del bloque nuevo; cero veredictos movidos. Coordinacion: la regrabo UNA sola sesion y no habia ninguna otra en curso (la fase 7 ya habia cerrado). El matiz declarado en la fila se mantiene: la rama ya estaba cubierta por `test_gate_mutations.sh` y `test_gate_behavior.sh`; lo que faltaba era la capa que graba COMPORTAMIENTO, que es la que detecta cambios no intencionales al refactorizar o reemplazar el hook |

---

## Phase 11 — Hallazgos del run de campo con Kimi (2026-08-16)

**Propósito:** un run real bajo el host Kimi devolvió 4 hallazgos; los 2
accionables se cierran acá (los otros 2 quedaron declarados como trade-offs
aceptados en `docs/task-11-plan-hallazgos-kimi.md`). Ejecuta GLM; revisa el
lead. **Ampliación (2026-08-16):** el cierre de la 11.3 desde el host zcode
produjo los datapoints post-11.3 (`docs/task-11-plan-hallazgos-kimi.md`
§Post-11.3) — un turno COMPLETO agotó el presupuesto porque los dos canales de
texto del gate estaban ciegos. De ahí salen las 11.4–11.6, con la decisión del
lead: el "unknown honesto" (camino 2) es el piso obligatorio; extender A6 al
tmpdir de zcode (camino 1) queda gateado por la medición de la 11.6, porque el
precedente que cita el datapoint (alias camel de Grok, 7.3/D4) caía DENTRO del
perfil y no ensanchó la raíz de contención, mientras que `%TEMP%` es el vector
A7-bis declarado por este mismo repo (Task 0.2).

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 11.1 | `[Guardrail]` `[lane:fast]` `[tdd:required]` **Higiene de base de rama.** Kimi ramificó desde un master local con un commit no pusheado y el commit se coló al PR. Las reglas permanentes ganan el bullet "rama desde origin/<default> con fetch previo; antes del PR, `git log origin/<default>..HEAD` contiene SOLO los commits de esta task", y la línea Close del recibo pide declararlo. Detalle: `docs/task-11-plan-hallazgos-kimi.md` | Las reglas emitidas en SessionStart contienen la regla nueva (`caso_g1_reglas_exigen_base_de_rama_limpia`); 1 mutación nueva atrapada; escenarios SessionStart de la baseline regrabados con diff auditado y cero veredictos movidos en el resto; install corrido | 10.9 | cc:完了 [5a7df06] — **CIERRE (2026-08-16, PR #38 mergeado en `544bc51`)**: CI 5/5 con `suite` verde (la batería corrió UNA vez, en CI). Bullet de higiene de base en las reglas permanentes + línea `Close:` del recibo; `caso_g1_reglas_exigen_base_de_rama_limpia` en verde y mutación `reglas_sin_base_de_rama_limpia` atrapada (28→29). **La expectativa del plan sobre la golden era EQUIVOCADA y GLM lo midió**: divergieron 42/44 escenarios, no solo los `SessionStart`, porque la línea `Close:` vive en el contrato que inyecta CADA escenario armado. El diff se auditó DOS veces por caminos independientes (GLM con lectura; el lead revisor por normalización mecánica de ambos lados): 0 exit codes, 0 veredictos `decision`, 0 líneas de estado movidas — solo texto de contrato y sus shas/bytes. La rama cumplió la regla que implementa: `git log origin/master..HEAD` = exactamente los 2 commits de la task. **Deploy post-merge a los cuatro perfiles** (claude ya al día por GLM; codex y grok REPARADO con backup; zcode sin copia propia): los tres con copia en `1627467692 120958` == master byte a byte, registro exit 0 en las tres formas, `test_golden_baseline.sh` OK contra el instalado — entrada en `docs/deploy-log.md`. Declarado: `summonaikit-kimi` hereda el texto vía extracción y su `check_drift.sh` queda en AVISO hasta el re-pin de la 11.3 |
| 11.2 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **La escotilla PAUSED gana la guardia `!recibo` (paridad con DELEGATED).** Recibo completo + PAUSED hoy se salta la validación y no limpia estado; recibo ROTO + PAUSED cierra en silencio — la clase de bug ya arreglada en la escotilla hermana. Con la guardia, recibo presente cae al gate normal; el contrato aclara que PAUSED es solo para cuando NO se puede avanzar. Resuelve la ambigüedad reportada por Kimi sin sentinel nuevo. Detalle: `docs/task-11-plan-hallazgos-kimi.md` | `caso_g4_recibo_completo_mas_paused_cierra_limpio` (exit 0 + estado borrado) y `caso_g4_recibo_roto_mas_paused_sigue_exigiendo` (exit 2) en verde; 2 mutaciones nuevas atrapadas (29→31, la 11.1 ya sumó una); escenario `09-pausa-declarada` sin moverse en `--check`; install corrido; límite de subcadena declarado | - | cc:完了 [3e404d3] — **CIERRE (2026-08-16, PR #39 mergeado)**: CI 5/5 con `suite` verde (batería UNA vez, en CI). Escotilla PAUSED con la cláusula `!recibo` idéntica a DELEGATED; 2 casos G4 en verde con rojo medido antes del fix; 2 mutaciones nuevas (29→31) — `paused_sin_guardia_de_recibo` la atan AMBOS casos (probado empíricamente) y `paused_exige_recibo` la atrapa `caso_g4_pausa_permite`. Golden: divergencia 38/44 por el bullet del contrato (precedente 11.1), auditada por dos vías — 0 veredictos movidos; hallazgo medio de review (backslash espurio en el sha de la cabecera, artefacto de regrabación manual) corregido regenerando con `--record` oficial (diff de 3 líneas de cabecera). Límite de subcadena sin anclar declarado en el comentario de la escotilla y en el PR. **Deploy post-merge**: claude YA AL DÍA (install desde la rama, idéntico a master); codex y grok REPARADO con backup; registro exit 0 en las tres formas; `test_golden_baseline.sh` OK contra el instalado — entrada en `docs/deploy-log.md` |
| 11.3 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Espejo de 11.2 en `summonaikit-kimi` + re-pin del drift.** El port reproduce la asimetría (`summonaikit-harness-kimi.sh:1152` sin guardia); misma cláusula, tests espejo, y el texto de contrato de 11.1/11.2 entra por su mecanismo de extracción (no a mano) con re-pin de `check_drift.sh`. El commit vive en ese repo; acá queda el puntero (precedente Task 4.2) | Los dos casos espejo en verde en la suite de ese repo; `check_drift.sh` exit 0 tras el re-pin; batería de ese repo corrida una vez; fila cerrada con el hash del commit de `summonaikit-kimi` | 11.1, 11.2 | cc:完了 [1509821] — **CIERRE (2026-08-16, PR gon0801/summonaikit-kimi#3 mergeado)**: commit `1509821` en `main` de summonaikit-kimi. Casos espejo 15a/15b (recibo completo+PAUSED ⇒ exit 0 + estado borrado; roto+PAUSED ⇒ exit 2 con feedback) con rojo medido; mutación quirúrgica (quitar la cláusula `!recibo`) atrapada por AMBOS casos, medida por el verifier. Refresh del bloque vendorizado con diff auditado y re-pin (`20ecee4f…`, `check_drift.sh` exit 0). Batería UNA vez en el CI de ese repo (job `suite`, run 31999376273, verde). **El re-pin destapó absorciones pendientes del port** (declaradas en la fila 11.3 de Plans.md de allá): `FAILURE_SIGNAL_RE_CI/CS` (nuestras 9.1+9.2, `1081390`), el ancla de apertura del heredoc con sustitución (nuestra 7.4 r2, `90df14b`) y el literal `$TOOL_HINT` del contrato, que la inyección de kimi resuelve por "the Agent tool" (su CLI `--extract-contract` queda crudo para el refresh). Deploy allá: `install.sh` + `check_deploy` AL DIA. Acá no hay cambio de hook: deploy no-op registrado en `docs/deploy-log.md`. Review sin hallazgos altos/medios; cosméticos declarados (fecha del pin un día adelantada por reloj; backslash literal en un mensaje de FAIL de test) |
| 11.4 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Unknown honesto en el Stop gate (camino 2 del datapoint post-11.3).** Un turno zcode COMPLETO agotó los 2 ciclos con "Missing SUMMONAIKIT HARNESS RECEIPT": los DOS canales de texto estaban ciegos — `last_assistant_message` real sin el recibo (contradice el fixture 21, reconstrucción Phase 5 vieja) y transcript rechazado por A6 (`%TEMP%\zcode-claude-hook-*`). Exigir evidencia no observable afirma ausencia desde la no-observación: Core Rule 2 violada dentro de `stop_gate`, y cada bloqueo consume ciclo (línea ~2052). Con AMBOS canales no observados (campo `last_assistant_message`/`lastAssistantMessage` AUSENTE del payload + transcript ilegible o fuera del perfil), el gate emite diagnóstico fuerte (stderr + log) y cierra sin consumir ciclo, limpiando estado como ya hace el presupuesto agotado (A4). Distinción clave: campo PRESENTE sin recibo = ausencia OBSERVADA ⇒ sigue bloqueando como hoy — claude/codex/grok (canal medido 1.4/7.1) no pierden dientes. NO toca A6: el camino 1 queda gateado por la 11.6. Detalle: `docs/task-11-plan-hallazgos-kimi.md` §Post-11.3 | Caso "ambos canales no observados" ⇒ diagnóstico + exit 0 + `cycle` intacto + estado limpio; caso "`last_assistant_message` presente sin recibo" ⇒ exit 2 como hoy; mutaciones nuevas atrapadas (una por rama); cero regresiones A6 (`transcript_path` ajeno sigue ignorado y logueado); la postura queda declarada en `docs/spec/00-project-spec.md` (Spec delta aprobado); golden re-grabada con diff auditado y cero veredictos movidos; install corrido | - | cc:完了 [7b7889c] — **CIERRE (2026-08-17, PR #42 mergeado, fix de review en `8d3dadd`)**: CI 5/5 con `suite` verde (batería UNA vez, en CI, tras el fix también). 2 casos G4 nuevos con rojo medido antes del fix; mutaciones 31→33 (`unknown_honesto_quitado`/`unknown_ciega_al_payload`), cada una atrapada (la segunda la atrapa temprano `caso_g4_delegado_sin_rol_bloquea`, acreditación ampliada en el comentario). **Golden: mejor que la expectativa del plan — `--check` 44/44 SIN divergencias** (la baseline no cubre ambos-ciegos; esa cobertura vive en los casos nuevos, mismo precedente que 11.2 declaró); no se regraba. Verifier subagente: suite de comportamiento completa OK + mutación manual de sensibilidad en rojo. Review: un hallazgo MEDIO (el log del unknown se autodestruía una línea después de escribirse — el cierre borra el log) corregido en `8d3dadd`: el diagnóstico vive SOLO en stderr, igual que el presupuesto agotado, y el spec ajustado con la nota de cursor (único host sin Stop medido, mismo gate que la 11.6). **Deploy post-merge a los tres perfiles con copia** (claude/codex/grok REPARADO con backup `…005026/005059/005103`), registro exit 0 en las tres formas, `golden-harness.sh --check` OK contra el instalado — entrada en `docs/deploy-log.md`. zcode sin copia propia (registro apunta a la de claude) |
| 11.5 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **`check_drift.sh` como CI de `summonaikit-kimi`** (el commit vive allá; acá queda el puntero — precedente 4.2/11.3). El re-pin de la 11.3 destapó tres absorciones silenciosas acumuladas entre pins. Son DOS señales, no una: (a) job de push/PR contra la copia pineada (`docs/pinned/`) — hermético, determinista, sin credenciales cross-repo; atrapa que las regiones vendorizadas de kimi se muevan respecto de lo absorbido; (b) job PROGRAMADO (cron) contra `summonaikit-claude@master` (checkout del repo hermano; token si privado, declarado) — atrapa la deuda upstream entre pins SIN poner cada PR de kimi en rojo por velocidad ajena al autor. DRIFT accionable (exit 1) rompe su job; AVISO (texto fuera de regiones) no. El caso "hook/checkout inaccesible en el runner" se declara como unknown RUIDOSO y el job no queda verde por haber comparado contra nada (precedente 1.5: PASS sobre unknown) | En el repo kimi: una rama/fixture de prueba que mueva una región copiada pone el job (a) en rojo; el flujo normal queda verde; el job (b) existe con su schedule y su caso inaccesible declarado como unknown, no como verde; fila cerrada con el hash del commit de `summonaikit-kimi` | 11.3 | cc:完了 [74cbb98] — **CIERRE (2026-08-17, PRs gon0801/summonaikit-kimi#4 y #5 mergeados; feature `74cbb98`, fixes `690807b`/`2d41ba9`, docs `613d4c4`)**: señal (a) `tools/check_absorbed.sh` (sha del pin + regiones copiadas + bloque VENDORED contra el heredoc pineado, oráculo awk independiente) cableada al job `drift-absorbed` y al final de `tests/run.sh`; 5 casos en `test_check_absorbed.sh` con el caso del DoD (región movida ⇒ rojo) medido también por el verifier en tmpdir. Señal (b) job `drift-upstream` (diario + manual) con checkout de `summonaikit-claude@master`: DRIFT rompe, AVISO no, y sin checkout el UNKNOWN es ruidoso (grep anclado al prefijo del mensaje, fix del hallazgo bajo-3). Suite de kimi UNA vez en CI; el primer push la rompió por el sandbox de `test_runner_ci_skips.sh` (stub nuevo en `690807b`) — el CI cazó lo que el dev local no midió, que es justo para lo que existe. Deploy allá: `install.sh` + `check_deploy` AL DIA. **Pendiente del operador**: el secret `SAIKIT_CLAUDE_REPO_TOKEN` (PAT lectura sobre gon0801/summonaikit-claude) — sin él, cada corrida programada del job (b) declara UNKNOWN y rompe, por diseño. Residuales declarados (review, bajos): fragilidad del awk a un segundo heredoc con la misma ancla (peor caso falso-rojo ruidoso) y doble corrida inofensiva de drift-absorbed en push+PR. Acá no hay cambio de hook: deploy no-op registrado en `docs/deploy-log.md` |
| 11.6 | `[Test]` `[lane:fast]` `[tdd:skip:captura-manual-con-operador-adelante]` **Medición zcode que decide el camino 1 (condicional, patrón Phase 5: la medición decide el alcance).** Captura real (procedimiento tipo `capture-payloads.sh`/`tools/probe-zcode-output.sh`, sesión nueva con operador adelante — misma clase que 1.4/2.4) que responda TRES preguntas: (a) **la presencia YA está medida y la premisa original de esta fila estaba mal** (revisión del plan, 2026-08-17): la captura real de la Task 5.1 dice que el Stop de zcode SÍ trae `last_assistant_message` (`docs/task-5.1-captura.md:35`, "sí (+ `responseText`/`responsePreview`)"), así que lo abierto NO es si el campo llega sino QUÉ trae y por qué un turno con recibo válido no lo halló — candidatos a distinguir con la captura: valor truncado tipo preview, escape de `\n` que el walker no extrae, o recibo que no quedó en el ÚLTIMO mensaje. **Esta es la pregunta que decide si la rama nueva de la 11.4 hace algo en zcode**: esa rama exige el campo AUSENTE (`hooks/summonaikit-harness.sh:1907`, subcadena de la clave sobre el payload crudo), y con el campo presente el turno vuelve a caer en "ausencia OBSERVADA" ⇒ bloquea y consume ciclo igual que antes, que es el fallo de campo que originó la fila. Si la captura lo confirma, el fallo de zcode sigue ABIERTO y su cierre real es el camino 1 o un arreglo del canal, no la 11.4; (b) ¿el `transcript.jsonl` del tmpdir parsea con `assistant_text_transcript` (forma `message.content[].text` con role:assistant)? — hoy NO hay fixture `.transcript.jsonl` de zcode: esquema jamás medido; (c) ¿los despachos reales de subagentes escriben `agents_seen`? Si (b) mide que sí y el path es estable, se abre la task de extender A6 ACOTADA a HOST=zcode (señal de env, misma confianza que el split de STATE_ROOT en 5.3) y ANCLADA a `$TEMP/zcode-claude-hook-*/transcript.jsonl` — nunca un permiso genérico de tmp: `%TEMP%` es el vector A7-bis (Task 0.2) y el precedente Grok (7.3/D4) NO ensanchó la raíz. Si (b) mide que no, el camino 1 se rechaza y el cierre queda en la 11.4 | Captura registrada en `docs/` con las tres respuestas (o `unknown` con su motivo, Core Rule 2); la decisión del camino 1 escrita con la evidencia adelante; el veredicto sobre si la rama de la 11.4 alcanza a zcode, escrito explícitamente (sí / no / `unknown` con motivo). **La corrección de fixtures y la regrabación de la golden NO van acá**: viven en la 11.8, que es otro carril — esta fila es captura manual (`lane:fast`, `tdd:skip`) y tocar fixtures + baseline es `lane:gate` `tdd:required` en todo el resto del plan (10.8, 10.17, 11.1) | 11.4 | cc:TODO |
| 11.7 | `[Guardrail]` `[lane:gate]` `[tdd:required]` **Espejo de 11.4 en `summonaikit-kimi`: un turno con wire ciego sigue bloqueando y quemando ciclo.** El datapoint post-11.3 dio por hecho que el branch `wire_found=0` del port "ya lo loguea como diagnóstico, no como fallo" y por eso la 11.4 no abrió espejo. **Medido por lectura del port (2026-08-17): NO es equivalente.** `hooks/summonaikit-harness-kimi.sh:1226` suprime SOLO la exigencia del recibo cuando `wire_found=0`; `:1214` sigue agregando "Missing verification evidence or explicit skipped-check reason" (el `wire_found=0` corto-circuita el OR a verdadero) y el bloque de roles (`:1255+`) sigue agregando su "Missing \<rol\> subagent run" — y la única salida de ese bloque, la declaración `ROLE FALLBACK`, matchea sobre `wire_text`, que con wire ciego es la cadena VACÍA: el escape depende del mismo canal ciego que ya falló, exactamente como en claude. Resultado: un turno completo y honesto sin wire acumula `missing`, bloquea y consume ciclo — el fallo que la 11.4 cierra acá sigue vivo allá. El commit vive en ese repo; acá queda el puntero (precedente 4.2/11.3/11.5) | Caso espejo "wire ausente" ⇒ diagnóstico + exit 0 + `cycle` intacto + estado limpio, con rojo medido antes del fix; caso "wire hallado sin recibo" ⇒ sigue bloqueando (el gate no pierde dientes); mutación quirúrgica nueva atrapada; batería de kimi UNA vez en su CI; `check_absorbed.sh` (11.5) en verde tras el cambio; fila cerrada con el hash del commit de `summonaikit-kimi` | 11.4 | cc:完了 [a0134c1] — **CIERRE (2026-08-17, PRs gon0801/summonaikit-kimi#6 y #7 mergeados; feature `a0134c1`, fixes `d11984d`/`8eefd54`, cierre `8c2224c`)**: caso 4c (turno incompleto sin wire ⇒ exit 0 + stderr `unknown honesto` + estado limpio) con rojo medido; 4b/7c re-pinneados (el reporte vivible es stderr; la pausa sin wire ya no se castiga); mutación quirúrgica medida por el verifier (invertir el `wire_found=0` del cierre; atrapada por los casos del wire ciego); DoD "wire hallado sin recibo sigue bloqueando" cubierto por el caso 2 y trampas 3/3b/3c existentes. Batería UNA vez en el CI de kimi — el primer push la rompió en 3 suites que ejercían el bloqueo sin wire: el CI cazó lo que el dev local no midió (regla agregada a la retro: si un cambio altera qué hace cerrar el Stop, correr TODAS las suites que llaman Stop, no solo las tocadas). Review APPROVE sin altos/medios; dos bajos corregidos in-PR. Deploy allá: `install.sh` + `check_deploy` AL DIA. Acá no hay cambio de hook: deploy no-op registrado en `docs/deploy-log.md` |
| 11.8 | `[Test]` `[lane:gate]` `[tdd:required]` **(Condicional — el alcance lo fija la 11.6.) Fidelidad de los fixtures zcode + la rama de canales ciegos en la capa que graba comportamiento.** Dos huecos ya medidos, independientes de lo que responda la captura: (1) **los fixtures zcode no reproducen la forma medida** — la 5.1 midió que zcode emite CADA clave en snake_case **y** camelCase y que el Stop trae además `responseText`/`responsePreview`, y los `*.stop.zcode.json` de `escenarios/17-25` traen solo la snake_case (son reconstrucción Phase 5, no captura), así que hoy la suite no puede distinguir un canal que llega de uno que llega truncado; (2) **la rama nueva de la 11.4 no tiene escenario dorado** — vive solo en `gate_cases` + mutaciones y ningún escenario ejercita un Stop con los DOS canales ciegos: es la lección de la 10.17 (cubierto por mutaciones ≠ cubierto por la capa que graba COMPORTAMIENTO, la que detecta cambios no intencionales al refactorizar o reemplazar el hook) | Fixtures zcode alineados a lo que mida la 11.6, o declarado por escrito por qué se conservan como están; escenario dorado nuevo con ambos canales ciegos **con prueba negativa medida** (una copia del hook sin la rama debe poner `--check` en exit 1 señalando ESE escenario, como se hizo en la 10.17); regrabación coordinada por UNA sola sesión, con diff auditado y veredictos movidos **DECLARADOS** — acá NO se promete "cero veredictos movidos": cambiar la forma de los fixtures zcode va a mover veredictos y ocultarlo sería el defecto | 11.6 | cc:TODO |

---

## Clasificación del alcance

**Required** — Phases 0 a 3. El 0 es urgente e independiente; el 1 tiene valor
aunque todo lo demás se cancele; el 2 y el 3 son el objetivo.

**Condicional (Phase 5)** — el alcance de la fase lo decide su propia primera
tarea. 5.1 y 5.2 son medición y valen aunque el resto se cancele: dicen si el
hook sirve tal cual en zcode. Si dicen que sí, 5.3-5.5 son trabajo chico. Si
dicen que no, la fase se reemplaza por un port propio al estilo de
`summonaikit-kimi` —un repo entero, no cinco tareas— y esa decisión se toma con
la medición adelante, no antes.

**Required — ampliación aprobada de hosts (Phases 6 y 7).** En ambas, las dos
primeras tareas son medición y conservan valor aunque se cancele el port. Ningún
host se publica si su contrato de salida resulta decorativo. Phase 7 sólo es
independiente de Phase 6 durante 7.1/7.2; las costuras de código, instalador y
baseline están serializadas por `Depends`.

**Recommended (fuera de este plan, independientes):**
- Prueba real de Kimi con un turno `-saikit` en vivo: 24 tareas hechas y **cero
  turnos reales**. Responde además dos hallazgos que quedaron como preguntas.
- ~~CI + pre-commit con detección de secretos en este repo~~ **hecho [534ef72]**:
  `tools/check-secrets.sh` (gitleaks si está disponible, fallback grep con la
  familia de `redact_secrets` si no) como hook local de pre-commit, más el job
  `secrets` del CI (gitleaks 8.30.1 sobre el historial completo, binario
  verificado por sha256 pineado). Calibrado midiendo: 0 falsos positivos sobre
  los archivos escaneados (el fallback excluye a propósito `tests/`, `docs/`,
  `Plans.md` y el comentario-documental del hook; `.gitleaks.toml` allowlista
  `tests/` porque carga trampas por diseño) y trampas reales detectadas. Test
  con 12 casos y 2 mutaciones acreditadas; revisión de bots atendida (PR #24):
  el P1 real era que `gitleaks dir` con 2+ fuentes escanea el directorio
  hermano — ahora se invoca una vez por archivo, atado por caso.

**Optional:**
- Portar el aviso post-revisión a Kimi (canal distinto: Kimi no tiene el mensaje
  de sistema de Claude).
- Adoptar los otros 3 perfiles.

**Reject:**
- **Actualizar al kit v5.** Medido 2026-08-08: mismo regex de armado sin
  fronteras (se arma con "cualquier"), mismo `json_string_field` greedy byte a
  byte, mismas 71 líneas de contrato. No arregla nada y pierde los parches.
- **Seguir el contrato del vendor "en vivo".** Su versión actual trae el mismo
  texto: seguirlo no sigue ninguna mejora. Se reemplaza por importación
  explícita bajo demanda.

## Validación del plan

- `team_validation_mode`: `subagent` — se corrieron dos perspectivas
  independientes (Arquitectura, Seguridad). **Sus hallazgos no se tomaron al pie
  de la letra:** la conclusión central de la perspectiva de Arquitectura ("el
  vendor abandonó el bash y se mudó a Python") se verificó y resultó **mal
  atribuida** — ese launcher era el runtime propio del usuario. La verificación
  independiente cambió el encuadre del plan.
- Perspectiva Skeptic: cubierta por esa misma verificación, que descartó la
  premisa original del proyecto.
- Sin lint/formatter configurado para bash en este repo ⇒ Task 1.1 incluye
  `bash -n` como piso mínimo; `shellcheck` queda como Recommended.

## 事前確認

- 事項: escritura de ACLs sobre `~/.claude/hooks/` y `~/.claude/hooks/state/`
  理由: quitar `Modify` a identidades de sandbox sobre el script que corre sin sandbox
  scope: Phase 0 / Task 0.1
- 事項: lectura de `~/.claude/settings.json` y `~/.claude/settings.local.json`
  理由: verificar que el hook sigue registrado (modo de falla silencioso) y el cableado del heal
  scope: Phase 0 / Task 0.3, Phase 2 / Task 2.4
- 事項: remoción de ACE de cuentas BORRADAS (SID no resoluble) en `~/AppData`,
  `~/AppData/Local` y `%TEMP%` — aprobado en sesión 2026-08-08
  理由: A7-bis — `%TEMP%` re-contamina `~/.claude` por `move`, y los 5 SID
  huérfanos estaban explícitos repartidos en esos tres niveles. NO se toca
  `CodexSandboxUsers`, que ahí sí escribe legítimamente
  scope: Phase 0 / Task 0.2
- 事項: escritura de ACLs sobre `~/.claude` (la RAÍZ del perfil, no solo `hooks/`)
  理由: ahí vive el otorgamiento explícito medido en la 0.1; mientras siga, el hook
  se puede desregistrar desde `settings.json` sin tocar el archivo
  scope: Phase 0 / Task 0.2
- 事項: escritura sobre `~/.claude/hooks/summonaikit-harness.sh` (el archivo que gatea cada turno)
  理由: la adopción por reemplazo es el objetivo de la Phase 2
  scope: Phase 2 / Task 2.4
- 事項: escritura sobre `C:\Users\ehven\quality-kit\saikit-gate-heal.ps1` y su batería
  理由: el skip por marcador debe mergearse antes de instalar, para no tener dos escritores
  scope: Phase 2 / Task 2.3, Phase 4 / Task 4.1
- 事項: creación de repo remoto y `git push` a `gon0801/summonaikit-claude`
  理由: regla de dejar en git + remoto todo lo tocado
  scope: Phase 1 / Task 1.1 en adelante
- 事項: descarga del kit del vendor en directorio desechable (ya ejecutada 2026-08-08)
  理由: verificar la premisa de si conviene actualizar; se hizo fuera del home y se borró
  scope: pre-plan, completado
- 事項: escritura y reemplazo recuperable de `~/.codex/hooks/summonaikit-harness.sh`
  理由: instalar la fuente aprobada de Phase 6 y validar un turno real con backup/restore listo
  scope: Phase 6 / Task 6.6
- 事項: alta y revocación de folder trust para repos descartables de Grok
  理由: ejecutar hooks de proyecto durante captura, probe y staging sin instalar el harness global
  scope: Phase 7 / Tasks 7.1, 7.2 y 7.6
- 事項: escritura/reemplazo recuperable y remoción selectiva bajo `~/.grok/hooks/` y `~/.grok/agents/`
  理由: instalar o retirar sólo artefactos con propiedad `summonaikit-claude`, preservando el verifier ajeno
  scope: Phase 7 / Tasks 7.5 y 7.6
