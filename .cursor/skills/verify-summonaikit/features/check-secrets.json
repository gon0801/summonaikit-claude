{
  "schema_version": 1,
  "id": "check-secrets",
  "card": "check-secrets.md",
  "executor": {
    "kind": "driver",
    "path": "scripts/drivers/check-secrets.sh"
  },
  "execution_mode": "sandbox",
  "surfaces": ["tools/check-secrets.sh"],
  "requirements": ["doctor"],
  "cases": [
    {
      "id": "secrets-strong",
      "required_assertions": ["strong_clean", "strong_trap"]
    },
    {
      "id": "secrets-fallback",
      "required_assertions": ["fallback_warning", "fallback_limit"]
    },
    {
      "id": "secrets-redaction",
      "required_assertions": ["report_no_secret", "evidence_no_secret"]
    }
  ]
}
